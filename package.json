{
  "name": "greppa",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "check": "vp check",
    "ci:publish": "pnpm publish -r --access public --no-git-checks",
    "dev": "pnpm run env:check && pnpm -r --parallel run dev",
    "env:check": "test -f .env.local || pnpm run env:setup",
    "env:setup": "tsx scripts/worktreeEnv.ts",
    "format": "vp fmt",
    "format:check": "vp fmt --check",
    "lint": "vp lint",
    "lint:rules": "ast-grep scan",
    "prepare": "vp config",
    "test": "vp test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@ast-grep/cli": "0.42.0",
    "@changesets/changelog-github": "0.6.0",
    "@changesets/cli": "2.30.0",
    "@types/node": "25.5.0",
    "oxfmt": "0.42.0",
    "oxlint": "1.57.0",
    "tsx": "4.21.0",
    "vite-plus": "0.1.14",
    "vitest": "4.1.1"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0"
}

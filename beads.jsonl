{"id":"greppa-1du","title":"Git pre-commit hooks with Vite+ staged linting","description":"## Context\n\nDeveloper tooling epic (greppa-3mf) set up lint/format configuration. Now codify those checks as git pre-commit hooks so violations are caught before they reach the repo. Uses Vite+'s built-in hook system (`vp config` + `vp staged`), which is already bundled in `vite-plus@0.1.14` — no new dependencies needed.\n\n## Requirements\n\n- [ ] `vp config` runs automatically after `pnpm install` via `prepare` lifecycle script\n- [ ] Pre-commit hook runs `vp staged` to format and lint staged files only (no typecheck)\n- [ ] `staged` config in `vite.config.ts` maps TS/TSX files to `vp lint --fix` + `vp fmt --write`, and other files to `vp fmt --write` only\n- [ ] `.vite-hooks/pre-commit` is version-controlled; `.vite-hooks/_/` (dispatch scripts) is gitignored\n- [ ] Hooks work across grove-managed worktrees (grove already runs `pnpm install` on `add`)\n- [ ] Hooks can be bypassed with `VITE_GIT_HOOKS=0`\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Hook manager | Vite+ built-in (`vp config`) | Already bundled; creates dispatch scripts + sets `core.hooksPath`; no extra deps |\n| Pre-commit scope | Staged files only via `vp staged` | Fast; lint-staged semantics bundled in vite-plus |\n| Checks | Format + lint, no typecheck | tsc is whole-project; too slow for pre-commit; better in CI |\n| Commit-msg hook | None | User preference; conventional format enforced by convention not tooling |\n| Auto-fix | Enabled (`--fix`, `--write`) | Auto-fix formatting and lint issues on commit for zero-friction DX |\n\n## Success criteria\n\n1. `git config core.hooksPath` returns `.vite-hooks/_` after `pnpm install`\n2. Committing a file with a lint error fails the pre-commit hook\n3. Committing a file with a fixable format issue auto-fixes and succeeds\n4. `VITE_GIT_HOOKS=0 git commit` bypasses hooks\n5. New grove worktree gets hooks via `pnpm install` -> `prepare` -> `vp config`","design":"test","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-25T20:40:09.300943996Z","created_by":"sqve","updated_at":"2026-04-16T19:49:35.360662959Z","closed_at":"2026-03-26T07:18:29.479418261Z","close_reason":"All success criteria verified: hooks path set, lint errors block commits, format auto-fixes on commit, VITE_GIT_HOOKS=0 bypass works, grove worktrees inherit hooks via prepare script.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"greppa-1du.1","title":"Configure vp staged hooks and prepare script","description":"Wire up Vite+ hook infrastructure and staged linting config.\n\n## Steps\n\n1. Add `\"prepare\": \"vp config\"` script to root `package.json`\n2. Add `staged` config to `vite.config.ts`:\n   - `*.{ts,tsx}` -> `['vp lint --fix', 'vp fmt --write']`\n   - `*.{json,md,yaml,css}` -> `'vp fmt --write'`\n3. Run `pnpm install` to trigger `prepare` -> `vp config`\n4. Verify `.vite-hooks/pre-commit` exists with `vp staged` content\n5. Verify `.vite-hooks/_/` dispatch scripts are generated and gitignored\n\n## Files to modify\n\n- `/home/sqve/code/personal/greppa/main/package.json` — add `prepare` script\n- `/home/sqve/code/personal/greppa/main/vite.config.ts` — add `staged` config\n\n## Files created by `vp config` (not manually)\n\n- `.vite-hooks/pre-commit` — user hook script (version-controlled)\n- `.vite-hooks/_/*` — dispatch scripts (gitignored via `_/.gitignore`)\n\n## Verification\n\n- `git config core.hooksPath` returns `.vite-hooks/_`\n- `ls .vite-hooks/pre-commit` exists\n- Stage a TS file, commit — hook runs fmt + lint\n- `VITE_GIT_HOOKS=0 git commit --allow-empty -m \"test: bypass\"` succeeds","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-25T20:40:30.527180224Z","created_by":"sqve","updated_at":"2026-04-16T19:53:58.523671595Z","closed_at":"2026-04-16T19:53:58.521658516Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-1du.1","depends_on_id":"greppa-1du","type":"parent-child","created_at":"2026-03-25T20:40:30.527180224Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1fd","title":"Text selection in side-by-side diff spans both columns","description":"Selecting text in the right side of the side-by-side diff also highlights text in the left column. The table layout allows cross-cell selection — need user-select isolation between the two sides so each half can be copied independently.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-03-26T18:24:21.417356490Z","created_by":"sqve","updated_at":"2026-03-26T20:51:34.421606625Z","closed_at":"2026-03-26T20:51:34.416432831Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["diff-viewer","ui"]}
{"id":"greppa-1h7","title":"Component playground","description":"## Context\n\nGreppa's `packages/ui` has 4 components (Badge, EmptyState, Tabs, Tree) with more planned. There is no way to visually iterate on components in isolation. STACK.md originally specified Ladle, but Ladle is effectively unmaintained (no commits since Jan 2025, untriaged issues piling up). A lightweight Vite playground app inside `packages/ui` replaces the need for any external component isolation tool.\n\n## Design summary\n\nA plain Vite React app at `packages/ui/playground/` that imports components, renders their variants, and supports theme switching. Co-located `*.preview.tsx` files next to each component define what to render. Auto-discovered via `import.meta.glob`.\n\n## Requirements\n\n1. **Playground Vite app** — `packages/ui/playground/` with `index.html`, `main.tsx`, `App.tsx`, `App.module.css`. Runs via `pnpm --filter @greppa/ui dev`.\n2. **Preview files** — co-located `*.preview.tsx` next to each component. Each exports a default component rendering all meaningful variants/states. Auto-discovered via `import.meta.glob('../src/**/*.preview.tsx')`.\n3. **Theme switching** — toolbar dropdown that sets `document.documentElement.dataset.theme`. Lists all available themes (catppuccin-latte, catppuccin-mocha, plus any added later). Tokens update instantly via CSS custom properties.\n4. **Sidebar navigation** — lists all discovered components by name (derived from file path). Click to view in preview pane.\n5. **Preview files for existing components** — Badge, EmptyState, Tabs, Tree each get a `*.preview.tsx` showing all variants and states.\n6. **Update STACK.md** — replace Ladle reference with the playground approach.\n\n## Anti-patterns\n\n- Do not add external component isolation tools (Storybook, Ladle, Histoire).\n- Do not add interactive prop controls, documentation generation, or visual regression testing — these are future concerns.\n- Do not introduce new dependencies — use existing React, Vite, CSS Modules.\n- Do not put playground code in `src/` — it must not ship in the library bundle.\n\n## Success criteria\n\n- `pnpm --filter @greppa/ui dev` starts the playground and opens in a browser.\n- Sidebar lists all components with preview files.\n- Clicking a component shows its preview with all variants rendered.\n- Theme dropdown switches between catppuccin-latte and catppuccin-mocha; all components re-theme instantly.\n- All four existing components (Badge, EmptyState, Tabs, Tree) have preview files.\n- STACK.md no longer references Ladle.\n- Playground files are excluded from the library build/exports.\n- All existing tests pass.","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-03-28T10:48:57.314790373Z","created_by":"sqve","updated_at":"2026-03-29T19:30:22.069098061Z","closed_at":"2026-03-29T19:30:22.060965447Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"]}
{"id":"greppa-1h7.1","title":"Scaffold playground Vite app with theme switching","description":"## What\n\nCreate the playground Vite app shell at `packages/ui/playground/` with:\n- `index.html` entry point\n- `main.tsx` React root (imports `tokens.css` and theme CSS)\n- `App.tsx` layout: toolbar (theme picker) + sidebar + preview pane\n- `App.module.css` for playground-specific styles\n- `dev` script in `packages/ui/package.json` pointing to `vite playground/`\n- A single preview file (`Badge.preview.tsx`) to validate the auto-discovery pattern\n\n## Acceptance criteria\n\n- `pnpm --filter @greppa/ui dev` starts Vite and serves the playground.\n- Theme dropdown lists catppuccin-latte and catppuccin-mocha; switching updates all tokens instantly.\n- Sidebar shows \"Badge\" (from the auto-discovered preview file).\n- Clicking \"Badge\" renders all Badge variants in the preview pane.\n- Playground files do not affect `packages/ui` exports or build output.\n\n## Expanded plan\n\n### 1. Add `@vitejs/plugin-react` devDep + `dev` script to `packages/ui/package.json`\n**File:** `packages/ui/package.json`\n- Add `\"@vitejs/plugin-react\": \"6.0.1\"` to devDependencies (same version as web)\n- Add `\"scripts\": { \"dev\": \"vp dev playground/\" }`\n\n### 2. Create `packages/ui/playground/index.html`\n**File:** `packages/ui/playground/index.html`\n- Minimal HTML, same FOUC-prevention theme script as web's `index.html`\n- `<div id=\"root\">` + `<script type=\"module\" src=\"./main.tsx\">`\n\n### 3. Create `packages/ui/playground/main.tsx`\n**File:** `packages/ui/playground/main.tsx`\n- Import `../src/tokens.css` (brings in all token + theme CSS)\n- Import `./App`\n- `createRoot(document.getElementById('root')!).render(<App />)`\n\n### 4. Create `packages/ui/playground/App.tsx`\n**File:** `packages/ui/playground/App.tsx`\n- Auto-discover previews: `import.meta.glob('../src/**/*.preview.tsx', { eager: true })`\n- Extract component names from file paths (e.g., `Badge` from `../src/components/Badge/Badge.preview.tsx`)\n- State: `selectedComponent` (string | null), defaults to first\n- Layout:\n  - **Toolbar**: theme `<select>` with catppuccin-latte/catppuccin-mocha options. onChange sets `document.documentElement.dataset.theme` and persists to localStorage key `gr-theme`. Read initial from `document.documentElement.dataset.theme`.\n  - **Sidebar**: `<nav>` listing component names as buttons. Active state on selected.\n  - **Preview pane**: renders the selected preview's default export.\n- Use CSS Modules (`App.module.css`) for layout.\n\n### 5. Create `packages/ui/playground/App.module.css`\n**File:** `packages/ui/playground/App.module.css`\n- Three-area grid: toolbar (top full-width), sidebar (left), preview (right)\n- Full viewport height, no scrollbar on body\n- Sidebar: fixed width, vertical scroll\n- Preview: overflow auto, padding\n- Use existing `--gr-*` tokens for colors\n\n### 6. Create `packages/ui/src/components/Badge/Badge.preview.tsx`\n**File:** `packages/ui/src/components/Badge/Badge.preview.tsx`\n- Default export component rendering all 4 Badge variants: added, deleted, modified, renamed\n- Each with visible label text\n\n### 7. Create `packages/ui/vite.config.ts`\n**File:** `packages/ui/vite.config.ts`\n- Minimal config: `import react from '@vitejs/plugin-react'; import { defineConfig } from 'vite-plus'; export default defineConfig({ plugins: [react()] })`\n- No build config needed (playground only, not library)\n\n### 8. Exclude playground from tsconfig\n**File:** `packages/ui/tsconfig.json`\n- `include` already only has `[\"src\"]` — playground is outside src, so it's already excluded from declaration emit. No change needed.\n\n### 9. Run `pnpm install` to install new devDep\n\n### 10. Verify\n- `pnpm --filter @greppa/ui dev` starts without errors\n- Existing tests still pass: `pnpm test`","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-28T10:49:09.089500992Z","created_by":"sqve","updated_at":"2026-03-28T20:19:17.405768689Z","closed_at":"2026-03-28T20:19:17.395478714Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-1h7.1","depends_on_id":"greppa-1h7","type":"parent-child","created_at":"2026-03-28T10:49:09.089500992Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1h7.2","title":"Add preview files for EmptyState, Tabs, and Tree","description":"## What\n\nCreate `*.preview.tsx` files for the remaining three components so the playground sidebar lists all four.\n\n## Files to create\n\n1. `packages/ui/src/components/EmptyState/EmptyState.preview.tsx` — render with title, description, and action slot\n2. `packages/ui/src/components/Tabs/Tabs.preview.tsx` — render with 2–3 tabs showing content switching\n3. `packages/ui/src/components/Tree/Tree.preview.tsx` — render with nested items, expandable folders\n\n## Acceptance criteria\n\n- Sidebar lists Badge, EmptyState, Tabs, Tree.\n- Each preview renders all meaningful variants/states of the component.\n- No changes to component source files.\n\n## Notes from task 1\n\n- Preview files must `export default` a component.\n- Auto-discovery glob: `../src/**/*.preview.tsx` — the filename before `.preview.tsx` becomes the sidebar label.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-28T20:19:39.421565030Z","created_by":"sqve","updated_at":"2026-03-28T20:21:25.787280986Z","closed_at":"2026-03-28T20:21:25.775947028Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-1h7.2","depends_on_id":"greppa-1h7","type":"parent-child","created_at":"2026-03-28T20:19:39.421565030Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1h7.3","title":"Update STACK.md to replace Ladle reference","description":"## What\n\nReplace Ladle reference in STACK.md with playground approach.\n\n## Status\n\nAlready done as part of greppa-1h7.2 implementation.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-28T20:21:27.533872248Z","created_by":"sqve","updated_at":"2026-03-28T20:21:33.592004460Z","closed_at":"2026-03-28T20:21:33.580413875Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-1h7.3","depends_on_id":"greppa-1h7","type":"parent-child","created_at":"2026-03-28T20:21:27.533872248Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1j3","title":"Per-worktree isolated development environments","description":"## Problem\n\nMultiple agents (and humans) work in git worktrees simultaneously. Each worktree needs its own server, web dev server, and database — but ports are hardcoded (API: 4400, Vite proxy: 4400), so only one worktree can run at a time.\n\n## Requirements\n\n1. Running `pnpm dev` in any worktree starts both server and web dev server on non-conflicting ports\n2. Two worktrees can run `pnpm dev` simultaneously without port conflicts\n3. The \"main\" worktree uses API port 4400 and Vite port 5173\n4. `grove add <branch>` creates a worktree with `.env.local` already configured\n5. Server accepts `--port` flag override, falls back to `API_PORT` env var, falls back to 4400\n6. `.env.local` and `.data/` are gitignored\n7. Same worktree directory name always produces the same ports (deterministic)\n\n## Success criteria\n\n1. `pnpm dev` in any worktree starts server + web dev server on unique, non-conflicting ports\n2. Two worktrees running `pnpm dev` simultaneously have no port conflicts\n3. \"main\" worktree preserves API port 4400 and Vite port 5173\n4. `grove add <branch>` produces a worktree with `.env.local` pre-configured\n5. `--port` flag > `API_PORT` env var > 4400 default (priority chain works)\n6. `.env.local` and `.data/` are in `.gitignore`\n7. Running `scripts/worktree-env.ts` twice on same worktree produces identical output\n\n## Solution\n\nA TypeScript script that deterministically assigns ports per worktree using FNV-1a hashing. Generates `.env.local` per worktree; all tools read from env vars.\n\n## Architecture\n\n### Port resolver script (`scripts/worktree-env.ts`)\n- Detects worktree root via `git rev-parse --show-toplevel`\n- Hashes the worktree directory basename using FNV-1a\n- API port range: 3100–3999, Vite dev port range: 5100–5999\n- Special case: directory named \"main\" → API 4400, Vite 5173 (preserves current convention)\n- Writes `.env.local` at worktree root with: `API_PORT`, `DEV_PORT`, `DB_PATH`\n- `DB_PATH` = `<worktreeRoot>/.data/app.db`\n- Run via `pnpm run env:setup` (uses tsx)\n\n### Grove integration (`.grove.toml`)\n- Add hook runs `pnpm run env:setup` after `pnpm install`\n- Preserve `.env.local` so it survives worktree operations\n\n### Vite config (`packages/web/vite.config.ts`)\n- Use `loadEnv()` to read `API_PORT` and `DEV_PORT` from `.env.local`\n- Dynamic proxy: `http://localhost:${API_PORT}`\n- Dynamic dev server port from `DEV_PORT`\n\n### Server CLI (`packages/cli/src/commands/serve.ts`)\n- Read `API_PORT` env var as default when no `--port` flag given\n- Fall back to 4400 if neither env var nor flag present\n\n### SQLite isolation\n- DB path from `DB_PATH` env var (not yet wired — DB layer doesn't exist yet)\n- Each worktree gets its own `.data/app.db`\n\n### Dev command (`pnpm dev` at root)\n- Runs `env:setup` first (if `.env.local` missing)\n- Starts server + Vite dev server in parallel\n\n### Gitignore\n- Add `.env.local` and `.data/` entries\n\n## Key decisions\n- FNV-1a hash: deterministic, same worktree name always gets same ports\n- TypeScript via tsx: no bash scripts in a TS project\n- Env vars via loadEnv(): avoids Vite configLoader workspace import issue (vitejs/vite#5370)\n- 900-port ranges: ample space for 6+ concurrent worktrees with negligible collision risk\n- \"main\" special case: preserves existing port 4400 habit","design":"## Problem\n\nMultiple agents (and humans) work in git worktrees simultaneously. Each worktree needs its own server, web dev server, and database — but ports are hardcoded (API: 4400, Vite proxy: 4400), so only one worktree can run at a time.\n\n## Requirements\n\n1. Running `pnpm dev` in any worktree starts both server and web dev server on non-conflicting ports\n2. Two worktrees can run `pnpm dev` simultaneously without port conflicts\n3. The \"main\" worktree uses API port 4400 and Vite port 5173\n4. `grove add <branch>` creates a worktree with `.env.local` already configured\n5. Server accepts `--port` flag override, falls back to `API_PORT` env var, falls back to 4400\n6. `.env.local` and `.data/` are gitignored\n7. Same worktree directory name always produces the same ports (deterministic)\n\n## Success criteria\n\n1. `pnpm dev` in any worktree starts server + web dev server on unique, non-conflicting ports\n2. Two worktrees running `pnpm dev` simultaneously have no port conflicts\n3. \"main\" worktree preserves API port 4400 and Vite port 5173\n4. `grove add <branch>` produces a worktree with `.env.local` pre-configured\n5. `--port` flag > `API_PORT` env var > 4400 default (priority chain works)\n6. `.env.local` and `.data/` are in `.gitignore`\n7. Running `scripts/worktree-env.ts` twice on same worktree produces identical output\n\n## Solution\n\nA TypeScript script that deterministically assigns ports per worktree using FNV-1a hashing. Generates `.env.local` per worktree; all tools read from env vars.\n\n## Architecture\n\n### Port resolver script (`scripts/worktree-env.ts`)\n- Detects worktree root via `git rev-parse --show-toplevel`\n- Hashes the worktree directory basename using FNV-1a\n- API port range: 3100–3999, Vite dev port range: 5100–5999\n- Special case: directory named \"main\" → API 4400, Vite 5173 (preserves current convention)\n- Writes `.env.local` at worktree root with: `API_PORT`, `DEV_PORT`, `DB_PATH`\n- `DB_PATH` = `<worktreeRoot>/.data/app.db`\n- Run via `pnpm run env:setup` (uses tsx)\n\n### Grove integration (`.grove.toml`)\n- Add hook runs `pnpm run env:setup` after `pnpm install`\n- Preserve `.env.local` so it survives worktree operations\n\n### Vite config (`packages/web/vite.config.ts`)\n- Use `loadEnv()` to read `API_PORT` and `DEV_PORT` from `.env.local`\n- Dynamic proxy: `http://localhost:${API_PORT}`\n- Dynamic dev server port from `DEV_PORT`\n\n### Server CLI (`packages/cli/src/commands/serve.ts`)\n- Read `API_PORT` env var as default when no `--port` flag given\n- Fall back to 4400 if neither env var nor flag present\n\n### SQLite isolation\n- DB path from `DB_PATH` env var (not yet wired — DB layer doesn't exist yet)\n- Each worktree gets its own `.data/app.db`\n\n### Dev command (`pnpm dev` at root)\n- Runs `env:setup` first (if `.env.local` missing)\n- Starts server + Vite dev server in parallel\n\n### Gitignore\n- Add `.env.local` and `.data/` entries\n\n## Key decisions\n- FNV-1a hash: deterministic, same worktree name always gets same ports\n- TypeScript via tsx: no bash scripts in a TS project\n- Env vars via loadEnv(): avoids Vite configLoader workspace import issue (vitejs/vite#5370)\n- 900-port ranges: ample space for 6+ concurrent worktrees with negligible collision risk\n- \"main\" special case: preserves existing port 4400 habit\n\n## Outcome\n\n**Completed:** 2026-03-26\n**Tasks:** 3 tasks completed\n**Summary:** Built a deterministic port resolver (`scripts/worktreeEnv.ts`) using FNV-1a hashing that generates `.env.local` per worktree. Wired server CLI and Vite config to read ports from env vars with proper fallback chains. Root `pnpm dev` runs env:check then starts both processes in parallel via `pnpm -r --parallel`. Added `strictPort: true` to Vite config so port conflicts fail loudly rather than silently incrementing.\n**Verification:** All tests passing (13/13), linter clean, typecheck clean, all 7 success criteria met with evidence.","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-03-26T07:41:17.378291310Z","created_by":"sqve","updated_at":"2026-03-26T08:19:12.076072030Z","closed_at":"2026-03-26T08:19:12.074458338Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","infra"]}
{"id":"greppa-1j3.1","title":"Create worktree-env.ts port resolver script","description":"## Goal\n\nCreate `scripts/worktree-env.ts` — the core port resolver that generates `.env.local` per worktree with deterministic ports and DB path.\n\n## Behaviors\n\n1. FNV-1a hash of a string returns a consistent 32-bit unsigned integer\n2. Hash mapped to API port range 3100–3999 and Vite port range 5100–5999\n3. Basename \"main\" bypasses hash and returns API 4400, Vite 5173\n4. Worktree root detected via `git rev-parse --show-toplevel`\n5. DB path resolves to `<worktreeRoot>/.data/app.db`\n6. `.env.local` written at worktree root with `API_PORT`, `DEV_PORT`, `DB_PATH`\n7. Running the script twice on the same worktree produces identical `.env.local`\n\n## Success criteria\n\n- `pnpm run env:setup` generates a valid `.env.local` with API_PORT, DEV_PORT, DB_PATH\n- Running it twice produces identical output (deterministic)\n- \"main\" directory gets ports 4400/5173\n- Other directory names get deterministic ports in their respective ranges\n- `.env.local` and `.data/` added to `.gitignore`\n\n## Details\n\n- Implement FNV-1a hash function (pure TS, no dependencies)\n- Map hash to port ranges using modulo\n- Add `env:setup` script to root `package.json`: `\"env:setup\": \"tsx scripts/worktree-env.ts\"`","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T07:41:47.460564444Z","created_by":"sqve","updated_at":"2026-03-26T07:47:43.212990656Z","closed_at":"2026-03-26T07:47:43.210272101Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","infra"],"dependencies":[{"issue_id":"greppa-1j3.1","depends_on_id":"greppa-1j3","type":"parent-child","created_at":"2026-03-26T07:41:47.460564444Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1j3.2","title":"Wire server and Vite to read ports from env vars","description":"## Goal\n\nMake server CLI and Vite dev server read ports from environment variables so they use worktree-specific values from `.env.local`.\n\n## Behaviors\n\n1. Server CLI: `--port` flag > `API_PORT` env var > 4400 default\n2. Vite config: proxy target reads `API_PORT`, dev server port reads `DEV_PORT`\n3. Vite uses `loadEnv()` to load `.env.local` values\n4. Grove hook runs `pnpm run env:setup` after `pnpm install` on worktree add\n\n## Success criteria\n\n- `serve --port 9999` uses 9999\n- `serve` without flag uses `API_PORT` env var if set, else 4400\n- Vite dev server starts on `DEV_PORT` and proxies to `API_PORT`\n- `grove add <branch>` runs env:setup as part of its hook chain\n\n## Details\n\n- `packages/cli/src/commands/serve.ts`: change `Flag.withDefault(4400)` to read `API_PORT` env var\n- `packages/web/vite.config.ts`: use `loadEnv()` for `API_PORT`/`DEV_PORT`\n- `.grove.toml`: append `pnpm run env:setup` to add hook\n- `.env.local` must be in `.grove.toml` preserve patterns","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T07:48:15.752217913Z","created_by":"sqve","updated_at":"2026-03-26T07:55:19.724182101Z","closed_at":"2026-03-26T07:55:19.722614143Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","infra"],"dependencies":[{"issue_id":"greppa-1j3.2","depends_on_id":"greppa-1j3","type":"parent-child","created_at":"2026-03-26T07:48:15.752217913Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1j3.3","title":"Wire pnpm dev to run env:setup then start server and Vite in parallel","description":"## Goal\n\nReplace the placeholder `pnpm dev` script with a command that runs `env:setup` (if `.env.local` missing), then starts server + Vite dev server in parallel.\n\n## Behaviors\n\n1. `pnpm dev` runs `env:setup` first if `.env.local` doesn't exist\n2. Then starts server and Vite dev server in parallel\n3. Both read ports from env vars set up by `env:setup`\n\n## Success criteria\n\n- `pnpm dev` starts both server and Vite dev server\n- Each process uses ports from `.env.local`\n- Ctrl+C stops both processes\n\n## Details\n\n- Root `package.json` `dev` script: run env:setup then start both processes\n- Options: `npm-run-all`, `concurrently`, or turbo/vp built-in\n- Check what vp (vite-plus) offers for parallel dev","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T07:55:39.850564965Z","created_by":"sqve","updated_at":"2026-03-26T08:13:14.356467847Z","closed_at":"2026-03-26T08:13:14.354958359Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","infra"],"dependencies":[{"issue_id":"greppa-1j3.3","depends_on_id":"greppa-1j3","type":"parent-child","created_at":"2026-03-26T07:55:39.850564965Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1jo","title":"Add hello world backend endpoint","description":"## Context\n\nAdd a `/api/hello` endpoint to the server package that returns a greeting message. Follows the same Effect HttpApi pattern established by the health endpoint.\n\n## Requirements\n\n- [ ] GET `/api/hello` returns `{ message: \"Hello, World!\" }` with 200 status\n- [ ] Endpoint defined as its own `HttpApiGroup` in a dedicated file\n- [ ] Registered in the top-level `Api` composition\n- [ ] Handlers wired in `Http.ts`\n- [ ] Integration test passing via `HttpRouter.toWebHandler` (same pattern as health test)\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Separate file | `HelloApi.ts` | Matches `HealthApi.ts` pattern — one file per API group |\n| Response shape | `{ message: string }` | Simple, demonstrates string response vs health's literal |\n\n## Acceptance criteria\n\n1. `vp test` passes with new hello endpoint test\n2. `curl http://localhost:4400/api/hello` returns `{\"message\":\"Hello, World!\"}` with 200\n3. No regressions on existing health endpoint","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-03-25T20:30:52.194721836Z","created_by":"sqve","updated_at":"2026-03-25T20:47:24.295550493Z","closed_at":"2026-03-25T20:47:24.294036699Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"greppa-1jo.1","title":"Implement /api/hello endpoint with test","description":"## What\n\nAdd a hello world GET endpoint at `/api/hello` returning `{ message: \"Hello, World!\" }`.\n\n## Files to modify/create\n\n- **Create** `packages/server/src/HelloApi.ts` — endpoint declaration\n- **Edit** `packages/server/src/Api.ts` — add `HelloApi` to composition\n- **Edit** `packages/server/src/Http.ts` — add `HelloHandlers` layer + provide to `ApiRoutes`\n- **Edit** `packages/server/src/Http.test.ts` — add integration test\n\n## Implementation notes\n\nFollow exact patterns from `HealthApi.ts` / `Http.ts`:\n- `HttpApiGroup.make(\"hello\").add(HttpApiEndpoint.get(\"getHello\", \"/api/hello\", { success: ... }))`\n- `HttpApiBuilder.group(Api, \"hello\", (handlers) => ...)`\n- `Layer.provide` both `HealthHandlers` and `HelloHandlers` to `ApiRoutes`\n- Test with `handler(new Request(\"http://localhost/api/hello\"))` asserting status 200 and JSON body\n\n## Acceptance criteria\n\n- `vp test` passes\n- Manual: `vp dev` then `curl http://localhost:4400/api/hello` returns `{\"message\":\"Hello, World!\"}`","status":"closed","priority":3,"issue_type":"task","created_at":"2026-03-25T20:31:02.422623047Z","created_by":"sqve","updated_at":"2026-03-25T20:47:31.448665408Z","closed_at":"2026-03-25T20:47:31.447098654Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-1jo.1","depends_on_id":"greppa-1jo","type":"parent-child","created_at":"2026-03-25T20:31:02.422623047Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-1ox","title":"Add hello world backend endpoint","description":"Add a GET /api/hello endpoint that returns { message: \"Hello, world!\" }.\n\n## Success criteria\n\n- GET /api/hello returns 200 with JSON body { message: \"Hello, world!\" }\n- Endpoint follows existing patterns: HttpApiGroup in its own file, added to Api, handler in Http.ts\n- Tests pass: endpoint returns expected response, unknown routes still 404\n- vp check passes (lint, format, typecheck)\n\n## Constraints\n\n- Follow the HealthApi pattern exactly (separate ApiGroup file, Schema-typed response)\n- No new dependencies\n- Single task — this is trivial scope","status":"closed","priority":3,"issue_type":"epic","created_at":"2026-03-25T21:36:40.817918279Z","created_by":"sqve","updated_at":"2026-03-25T21:38:47.750278563Z","closed_at":"2026-03-25T21:38:47.746601164Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"greppa-1ox.1","title":"Add GET /api/hello endpoint with HelloApi group","description":"Add a hello world endpoint following the HealthApi pattern.\n\n## Implementation plan\n\n1. Create packages/server/src/HelloApi.ts\n   - HttpApiGroup named \"hello\"\n   - Single GET endpoint \"getHello\" at /api/hello\n   - Success schema: Schema.Struct({ message: Schema.String })\n\n2. Update packages/server/src/Api.ts\n   - Import HelloApi\n   - Chain .add(HelloApi) after .add(HealthApi)\n\n3. Update packages/server/src/Http.ts\n   - Add HelloHandlers: HttpApiBuilder.group(Api, \"hello\", ...) returning { message: \"Hello, world!\" }\n   - Provide HelloHandlers to ApiRoutes layer\n\n4. Update packages/server/src/Http.test.ts\n   - Add test: GET /api/hello returns 200 with { message: \"Hello, world!\" }\n\n## Verification\n\n- pnpm test passes\n- vp check passes","status":"closed","priority":3,"issue_type":"task","created_at":"2026-03-25T21:36:50.893748100Z","created_by":"sqve","updated_at":"2026-03-25T21:38:43.483484359Z","closed_at":"2026-03-25T21:38:43.481421295Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-1ox.1","depends_on_id":"greppa-1ox","type":"parent-child","created_at":"2026-03-25T21:36:50.893748100Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-22m","title":"greppa-3gt","description":"Sync selectedFilePath with URL search params (?file=path). Read initial file from URL on mount. Update URL on file selection. Handle browser back/forward via popstate. No routing library needed — use native URL API + history.pushState.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-30T06:28:07.820951511Z","created_by":"sqve","updated_at":"2026-03-30T06:43:34.739827420Z","closed_at":"2026-03-30T06:43:34.722097168Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["navigation","ui"]}
{"id":"greppa-2h0","title":"Diff API — server content delivery and client-side VSCode-quality diffing","description":"## Requirements (IMMUTABLE)\n- Shared types defined as Effect Schemas in @greppa/core (ChangeType, FileEntry, DiffResponse); TS types derived via Schema.Type; both server and web import from @greppa/core\n- GET /api/files endpoint accepts oldRef and newRef query params; returns array of { path, changeType, oldPath? } computed via git diff --name-status oldRef newRef\n- GET /api/diff/:oldRef/:newRef/*path endpoint returns { path, oldPath?, changeType, oldContent, newContent } computed via git show oldRef:path and git show newRef:path; empty string for added files oldContent and deleted files newContent\n- Server resolves git repo from CLI --repo arg, falling back to CWD; validates the path is a git repository on startup\n- Git spawning via Effect ChildProcess from effect/unstable/process; non-zero exit codes produce typed errors\n- Client fetches via TanStack Query — useFileList(oldRef, newRef) and useDiffContent(oldRef, newRef, path)\n- Client computes diffs in Web Worker using vscode-diff DefaultLinesDiffComputer; returns DetailedLineRangeMapping[] with innerChanges\n- Two-tier diff rendering — light background on changed lines, darker highlight on exact character ranges from innerChanges\n- Syntax highlighting runs on full file content (not isolated diff lines) for correct parser state\n\n## Success criteria\n- [ ] GET /api/files?oldRef=HEAD~1&newRef=HEAD returns file list matching git diff --name-status output\n- [ ] GET /api/diff/HEAD~1/HEAD/src/some-file.ts returns oldContent and newContent matching git show output\n- [ ] Server rejects invalid refs with typed error response\n- [ ] Client renders diff with two-tier highlighting: light background on changed lines, darker highlight on exact character ranges\n- [ ] Syntax highlighting works on full file content\n- [ ] Diff computation runs in Web Worker, never blocks main thread\n- [ ] Selecting an uncached file shows its diff (loading state → rendered) without error\n- [ ] Fixture mode still works for development when no server is running\n- [ ] All tests passing\n- [ ] Pre-commit hooks passing\n\n## Anti-patterns (FORBIDDEN)\n- NO parsing of git diff unified output (reason: raw content + VSCode DefaultLinesDiffComputer produces better line pairing and character-level diffs than any post-hoc refinement of git Myers output)\n- NO diff computation on main thread (reason: DefaultLinesDiffComputer can block on large files; Web Worker keeps UI responsive)\n- NO eager fetch of all file contents on session start (reason: fetch on file selection only; greppa-30u layers pre-fetching and caching on top)\n- NO removal of fixture infrastructure (reason: keep fixtures working for dev/testing alongside the real API until proven stable)\n- NO custom diff algorithm (reason: vscode-diff is the exact VSCode algorithm, zero-dependency, maintained)\n- NO highlighting individual diff lines in isolation (reason: with full file content available, highlight the complete file for correct parser state)\n\n## Durable decisions\n- Diff library: vscode-diff DefaultLinesDiffComputer — zero deps, exact VSCode algorithm, native two-tier innerChanges output\n- API shape: /api/files?oldRef&newRef for file list, /api/diff/:oldRef/:newRef/*path for content\n- Data flow: server delivers raw content → two parallel Web Workers (Shiki for syntax tokens, vscode-diff for diff ranges) → renderer composites both\n- Schema location: @greppa/core as Effect Schemas, TS types derived via Schema.Type\n- Data fetching: TanStack Query with hooks per resource (useFileList, useDiffContent)\n\n## Approach\nServer delivers raw file contents from git; client computes VSCode-quality diffs using vscode-diff (DefaultLinesDiffComputer) in a Web Worker. This architecture was chosen because VSCode's diff algorithm produces superior line pairing and character-level diffs compared to parsing git diff output. The algorithm uses Myers + DP with size-based selection, heuristic post-processing, and holistic block-level character diffing — the same engine codediff.nvim ported to C.\n\nThe server is intentionally thin: spawn git commands, return content. Two endpoints serve file list (git diff --name-status) and file content (git show). Effect ChildProcess handles process spawning with typed errors. The client owns diff computation and rendering, with TanStack Query managing data fetching state.\n\nSyntax highlighting improves under this architecture — full file content gives Shiki complete parser context instead of isolated diff lines. The Shiki and diff workers run in parallel since they are independent.\n\n## Architecture\n- @greppa/core: Effect Schemas for ChangeType, FileEntry, DiffResponse; shared between server and web\n- @greppa/server: GitService (Effect service wrapping ChildProcess for git commands), DiffApi HttpApiGroup (two endpoints), wired into existing HttpApiBuilder alongside HealthApi\n- @greppa/cli: --repo flag on serve command, provides repo path to server via Effect Layer\n- @greppa/web: TanStack Query hooks (useFileList, useDiffContent), diff Web Worker using vscode-diff, updated DiffViewer consuming computed diff results with two-tier highlighting, updated Shiki worker highlighting full file content\n\n## Design rationale\n\n### Problem\nThe app loads all diff data from bundled JSON fixtures. It cannot serve real git repositories. This is the foundational prerequisite for all production use and for the caching/pipeline epic (greppa-30u).\n\n### Research findings\n\n**Codebase:**\n- packages/server/src/Http.ts — HttpApiBuilder pattern with HealthApi group, Effect Layer composition\n- packages/server/src/HealthApi.ts — HttpApiEndpoint/HttpApiGroup pattern to follow\n- packages/web/src/fixtures/types.ts — current DiffFile, DiffHunk, DiffLine, FileNode, ChangeType types\n- packages/web/src/fixtures/index.ts — bundled JSON imports, Map-based lookup by path\n- packages/web/src/App.tsx — imports fixtures directly, passes to useFileSelection\n- packages/web/src/workers/highlightEngine.ts — Shiki Web Worker with line-by-line highlighting\n- packages/core/src/index.ts — empty placeholder, ready for shared types\n\n**External:**\n- vscode-diff npm (v3.0.1, ~33K weekly DL, zero deps) — standalone extraction of VSCode diff algorithm\n- VSCode algorithm: Myers + DP with size-based selection, heuristic post-processing, holistic block-level char diffing\n- Effect v4 beta: ChildProcess from effect/unstable/process, ChildProcessSpawner for spawner.string(cmd)\n- codediff.nvim: C port of same VSCode algorithm, confirms approach produces high-quality two-tier diffs\n\n### Approaches considered\n\n#### 1. Raw content + client-side VSCode diffing (selected)\n\n**What:** Server sends old/new file content via git show. Client computes line + character diffs using vscode-diff DefaultLinesDiffComputer in a Web Worker.\n**Pros:** Best diff quality (VSCode algorithm), simplest server, natural Web Worker fit, full file content improves syntax highlighting.\n**Cons:** Client does more work (mitigated by Web Worker).\n**Chosen because:** VSCode's algorithm produces superior line pairing and character-level diffs. codediff.nvim validated this approach by porting the same algorithm.\n\n#### 2. Parse git diff on server (rejected)\n\n**What:** Server spawns git diff, parses unified output into DiffFile with pre-computed hunks.\n**Why explored:** Simpler initial architecture, less client computation.\n**Why rejected:** Caps diff quality at git's Myers algorithm. No native character-level pairing — would need post-hoc refinement that produces inferior results.\n**DO NOT REVISIT UNLESS:** VSCode-quality diffs prove unnecessary and performance requires server-side computation.\n\n#### 3. Monaco DiffEditor widget (rejected)\n\n**What:** Embed Monaco's DiffEditor for both computation and rendering.\n**Why explored:** Monaco includes the diff algorithm and a complete diff UI.\n**Why rejected:** Pulls in entire Monaco editor bundle (~4MB) for just the diff algorithm. Replaces custom DiffViewer, losing control over rendering.\n**DO NOT REVISIT UNLESS:** We need Monaco's full editor features (editing, intellisense) in the diff view.\n\n### Scope boundaries\n\n**In scope:** File list endpoint, diff content endpoint, client-side VSCode-quality diffing, two-tier highlighting, TanStack Query integration, full-file syntax highlighting\n**Out of scope:** Comments API (separate concern), file info API (separate concern), IndexedDB caching (greppa-30u), WebSocket push (greppa-30u), pre-fetching (greppa-30u)\n\n### Open questions\n- Web Worker message protocol for diff computation (design during task expansion)\n- Binary file handling in API response (detect and return flag, no content)\n- Language detection from file extension — likely client-side from path, matching current fixture pattern\n\n## Design discovery\n\n### Key decisions made\n\n| Question | Answer | Implication |\n|----------|--------|-------------|\n| Types: plain interfaces vs Effect Schemas? | Effect Schemas in @greppa/core | Single source of truth; server gets runtime validation; client imports derived types |\n| Diff computation: server or client? | Client via vscode-diff in Web Worker | Server just delivers raw content; client gets VSCode-quality two-tier diffs |\n| Diff library | vscode-diff DefaultLinesDiffComputer | Zero deps, exact VSCode algorithm, native innerChanges for char-level |\n| API shape | REST with path params for refs, splat for file path | Cacheable URLs, clean REST semantics |\n| Repo path | CLI --repo arg, CWD fallback | Natural ergonomics: cd my-repo && greppa serve |\n| Data fetching | TanStack Query | Loading/error states, caching, integrates with existing TanStack Router |\n| Scope | File list + diffs only | Comments/file info stay as fixtures for now |\n| Syntax highlighting | Full file content, not isolated lines | Better parser state, parallel with diff worker |\n\n### Research deep-dives\n\n#### VSCode diff algorithm availability\n**Question:** Can VSCode's diff algorithm be used standalone without Monaco?\n**Sources:** vscode-diff npm, Monaco source, codediff.nvim ATTRIBUTION.md\n**Conclusion:** vscode-diff extracts the exact algorithm as a zero-dep package. DefaultLinesDiffComputer.computeDiff() takes string arrays and returns DetailedLineRangeMapping[] with innerChanges.\n\n#### Character-level diff approach\n**Question:** How does VSCode compute character-level diffs within changed blocks?\n**Sources:** codediff.nvim C port analysis, VSCode source at src/vs/editor/common/diff/\n**Conclusion:** Entire removed + added blocks are concatenated as flat character streams and diffed holistically. No explicit line pairing. innerChanges contain (line, col) ranges mapped back via offset arrays. This produces better results than line-by-line pairing + per-pair char diff.\n\n#### Effect v4 child process API\n**Question:** How to spawn git commands in Effect v4 beta?\n**Sources:** effect/unstable/process/ChildProcess, @effect/platform-node\n**Conclusion:** ChildProcess.make(\"git\", [...args]) + ChildProcessSpawner.string(cmd) for stdout. Non-zero exit does not auto-fail — must check explicitly. Requires NodeServices.layer.\n\n### Dead-end paths\n\n#### gitdiff-parser / parse-git-diff\n**Why explored:** Initially planned to parse git diff unified output on the server\n**What found:** Libraries work fine for parsing, but the approach itself was wrong\n**Why abandoned:** Parsing git diff caps quality at git's algorithm. VSCode's algorithm on raw content produces better results.\n\n#### Monaco headless diff\n**Why explored:** Monaco includes the diff algorithm internally\n**What found:** No public API for headless use. Requires full service infrastructure and DOM.\n**Why abandoned:** vscode-diff provides the same algorithm without Monaco's bundle.\n\n#### diff-match-patch for character diffs\n**Why explored:** Google's library with excellent semantic cleanup\n**What found:** Character-level only, no line-level structure. Would require DIY line splitting and pairing.\n**Why abandoned:** vscode-diff handles both tiers natively.\n\n### Open concerns raised\n- Binary files → detect via git, return flag with no content, render placeholder in UI\n- Language detection → derive from file extension client-side (same as current fixture pattern)\n- Worker protocol → design during first task expansion, similar to existing Shiki worker pattern","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-03-31T10:02:09.383580251Z","created_by":"sqve","updated_at":"2026-03-31T12:11:08.629993471Z","closed_at":"2026-03-31T12:11:08.629377083Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"]}
{"id":"greppa-2h0.1","title":"File list endpoint and client fetch","description":"## Goal\nServe the file list from a real git repository and render it in the client via TanStack Query. This proves the full vertical slice: shared schemas in core, git spawning on the server, HTTP endpoint, and client data fetching — replacing the fixtures-based file tree with live data.\n\n## Execution mode\nAFK\n\n## Behaviors\n1. @greppa/core exports a ChangeType Effect Schema (literal union: added, modified, deleted, renamed) and a FileEntry Schema ({ path, changeType, oldPath? })\n2. @greppa/server GitService spawns git diff --name-status oldRef newRef and parses the tabular output into FileEntry[]\n3. Server rejects invalid git refs with a typed error (non-zero exit from git)\n4. GET /api/files?oldRef=X&newRef=Y returns FileEntry[] via HttpApiEndpoint wired into existing Api\n5. @greppa/web useFileList(oldRef, newRef) hook fetches via TanStack Query and returns the file list\n6. App renders the file tree from API data when server is available, falling back to fixtures when not\n\n## References\n- packages/server/src/HealthApi.ts:1-9 — HttpApiEndpoint + HttpApiGroup pattern\n- packages/server/src/Http.ts:1-17 — HttpApiBuilder.group handler pattern, Layer composition\n- packages/server/src/Http.test.ts:1-43 — WebHandler test pattern (no real server needed)\n- packages/web/src/fixtures/types.ts:1-12 — current FileNode and ChangeType types\n- packages/web/src/fixtures/index.ts:1-36 — current fixture loading pattern to eventually replace\n\n## Success criteria\n- [ ] @greppa/core exports ChangeType and FileEntry Effect Schemas with derived TS types\n- [ ] GET /api/files?oldRef=HEAD~1&newRef=HEAD returns correct file list from a real git repo\n- [ ] Invalid refs return typed error response\n- [ ] Server tests pass using WebHandler pattern (no real HTTP server)\n- [ ] Client useFileList hook fetches and returns file list via TanStack Query\n- [ ] File tree renders from API data\n- [ ] Fixture fallback works when server is unavailable\n- [ ] All tests passing\n- [ ] Pre-commit hooks passing","design":"# Expanded implementation plan\n\n## Codebase findings\n\n### Patterns to follow\n- `packages/server/src/HealthApi.ts:4-8` — `HttpApiGroup.make(name).add(HttpApiEndpoint.get(name, path, { success, query, error }))` pattern\n- `packages/server/src/Api.ts:4` — `HttpApi.make('greppa').add(Group)` composition\n- `packages/server/src/Http.ts:10-11` — `HttpApiBuilder.group(Api, 'name', handlers => Effect.succeed(handlers.handle(...)))` pattern\n- `packages/server/src/Http.ts:14` — `ApiRoutes = HttpApiBuilder.layer(Api).pipe(Layer.provide(...handlers))` layer composition\n- `packages/server/src/Http.test.ts:9-18` — WebHandler test: build TestAppLayer, call HttpRouter.toWebHandler, use handler(new Request(...))\n\n### Key gaps\n- **@greppa/core is empty** — only exports name constant. All domain types live in packages/web/src/fixtures/types.ts\n- **No TanStack Query** — @tanstack/react-query not installed in web package\n- **Web doesn't depend on @greppa/core** — only depends on @greppa/ui\n- **No git-related code** exists anywhere in the server package\n- **FileTree expects tree structure** — FileNode[] with nested children, not flat FileEntry[]. Need a buildFileTree converter\n\n### Effect ChildProcess API (effect@4.0.0-beta.40)\n- Import: import { ChildProcess } from 'effect/unstable/process'\n- Array form: ChildProcess.make(\"git\", [\"diff\", \"--name-status\", oldRef, newRef], { cwd: repoPath })\n- Handle: yield* command -> handle.stdout (Stream<Uint8Array>), handle.exitCode (Effect<ExitCode>)\n- Requires: Effect.scoped, NodeServices.layer provides ChildProcessSpawner\n\n### git diff --name-status output format\n```\nM\tpath/to/modified.ts\nA\tpath/to/added.ts\nD\tpath/to/deleted.ts\nR100\told/path.ts\tnew/path.ts\n```\nStatus letters: A->added, M->modified, D->deleted, R(nnn)->renamed (with optional similarity %)\n\n---\n\n## TDD cycles\n\n### Cycle 1: Core schemas in @greppa/core\n\n**RED** — packages/core/src/index.test.ts (new file)\n```ts\nimport { Schema } from 'effect';\nimport { describe, expect, it } from 'vitest';\nimport { ChangeType, FileEntry } from './index';\n\ndescribe('ChangeType', () => {\n  it('decodes valid change types', () => {\n    for (const value of ['added', 'modified', 'deleted', 'renamed']) {\n      expect(Schema.decodeUnknownSync(ChangeType)(value)).toBe(value);\n    }\n  });\n\n  it('rejects invalid values', () => {\n    expect(() => Schema.decodeUnknownSync(ChangeType)('invalid')).toThrow();\n  });\n});\n\ndescribe('FileEntry', () => {\n  it('decodes entry without oldPath', () => {\n    const result = Schema.decodeUnknownSync(FileEntry)({ path: 'a.ts', changeType: 'modified' });\n    expect(result).toEqual({ path: 'a.ts', changeType: 'modified' });\n  });\n\n  it('decodes entry with oldPath', () => {\n    const result = Schema.decodeUnknownSync(FileEntry)({ path: 'b.ts', changeType: 'renamed', oldPath: 'a.ts' });\n    expect(result).toEqual({ path: 'b.ts', changeType: 'renamed', oldPath: 'a.ts' });\n  });\n});\n```\n\n**GREEN** — packages/core/src/index.ts\nReplace contents with:\n```ts\nimport { Schema } from 'effect';\n\nexport const ChangeType = Schema.Literal('added', 'modified', 'deleted', 'renamed');\nexport type ChangeType = typeof ChangeType.Type;\n\nexport const FileEntry = Schema.Struct({\n  path: Schema.String,\n  changeType: ChangeType,\n  oldPath: Schema.optional(Schema.String),\n});\nexport type FileEntry = typeof FileEntry.Type;\n```\n\n**Verify:** vp test packages/core\n\n---\n\n### Cycle 2: GitService — parse git diff --name-status output\n\n**RED** — packages/server/src/GitService.test.ts (new file)\nTest parseNameStatus (pure function, no git spawning):\n```ts\ndescribe('parseNameStatus', () => {\n  it('parses modified file', () => {\n    expect(parseNameStatus('M\\tsrc/index.ts')).toEqual([{ path: 'src/index.ts', changeType: 'modified' }]);\n  });\n  it('parses added file', () => { ... });\n  it('parses deleted file', () => { ... });\n  it('parses renamed file with similarity', () => {\n    expect(parseNameStatus('R100\\told.ts\\tnew.ts')).toEqual([{ path: 'new.ts', changeType: 'renamed', oldPath: 'old.ts' }]);\n  });\n  it('parses multiple lines', () => { ... });\n  it('skips empty lines', () => { ... });\n});\n```\n\n**GREEN** — packages/server/src/GitService.ts (new file)\n- Export parseNameStatus(output: string): FileEntry[] — pure parsing function\n- Status letter mapping: A->added, M->modified, D->deleted, R(nnn)->renamed\n- Renamed lines have two tab-separated paths: old then new\n\n**Verify:** vp test packages/server\n\n---\n\n### Cycle 3: GitService — Effect service with ChildProcess spawning\n\n**RED** — extend packages/server/src/GitService.test.ts\nIntegration test using the greppa monorepo itself:\n```ts\ndescribe('GitService', () => {\n  it('lists changed files between two refs', async () => {\n    const program = Effect.gen(function* () {\n      const git = yield* GitService;\n      return yield* git.listFiles('HEAD~1', 'HEAD');\n    });\n    const result = await Effect.runPromise(\n      program.pipe(\n        Effect.provide(GitServiceLive),\n        Effect.provide(RepoPathLayer(process.cwd())),\n        Effect.provide(NodeServices.layer),\n        Effect.scoped,\n      )\n    );\n    expect(result.length).toBeGreaterThan(0);\n    for (const entry of result) {\n      expect(['added', 'modified', 'deleted', 'renamed']).toContain(entry.changeType);\n      expect(entry.path).toBeTruthy();\n    }\n  });\n\n  it('fails with invalid ref', async () => {\n    const program = Effect.gen(function* () {\n      const git = yield* GitService;\n      return yield* git.listFiles('invalid-ref-xxx', 'HEAD');\n    });\n    await expect(Effect.runPromise(\n      program.pipe(\n        Effect.provide(GitServiceLive),\n        Effect.provide(RepoPathLayer(process.cwd())),\n        Effect.provide(NodeServices.layer),\n        Effect.scoped,\n      )\n    )).rejects.toThrow();\n  });\n});\n```\n\n**GREEN** — extend packages/server/src/GitService.ts\n- RepoPath — Effect Context.Tag<string> for the repository path\n- GitService — Effect Context.Tag with listFiles(oldRef: string, newRef: string): Effect<FileEntry[], GitError>\n- GitServiceLive — Layer implementation:\n  - Spawn git diff --name-status oldRef newRef via ChildProcess.make(\"git\", [...], { cwd: repoPath })\n  - Collect stdout stream, decode to string\n  - Check exitCode — if non-zero, fail with GitError\n  - Parse output via parseNameStatus\n- GitError — tagged class: { _tag: 'GitError', message: string }\n\n**Verify:** vp test packages/server\n\n---\n\n### Cycle 4: FilesApi endpoint + handler wiring\n\n**RED** — extend packages/server/src/Http.test.ts\n```ts\ndescribe('GET /api/files', () => {\n  it('returns file list for valid refs', async () => {\n    const response = await handler(new Request('http://localhost/api/files?oldRef=HEAD~1&newRef=HEAD'));\n    expect(response.status).toBe(200);\n    const body = await response.json();\n    expect(Array.isArray(body)).toBe(true);\n    expect(body.length).toBeGreaterThan(0);\n    expect(body[0]).toHaveProperty('path');\n    expect(body[0]).toHaveProperty('changeType');\n  });\n\n  it('returns error for invalid refs', async () => {\n    const response = await handler(new Request('http://localhost/api/files?oldRef=invalid-xxx&newRef=HEAD'));\n    expect(response.status).toBe(500);\n  });\n});\n```\nUpdate TestAppLayer in Http.test.ts to include GitServiceLive, RepoPathLayer(process.cwd()), and NodeServices.layer.\n\n**GREEN** — 3 files:\n\n1. packages/server/src/FilesApi.ts (new file)\n```ts\nimport { Schema } from 'effect';\nimport { HttpApiEndpoint, HttpApiGroup } from 'effect/unstable/httpapi';\nimport { FileEntry } from '@greppa/core';\n\nexport class FilesApi extends HttpApiGroup.make('files').add(\n  HttpApiEndpoint.get('getFiles', '/api/files', {\n    query: { oldRef: Schema.String, newRef: Schema.String },\n    success: Schema.Array(FileEntry),\n    error: Schema.Struct({ _tag: Schema.Literal('GitError'), message: Schema.String }),\n  }),\n) {}\n```\n\n2. packages/server/src/Api.ts — add .add(FilesApi)\n\n3. packages/server/src/Http.ts — add FilesHandlers:\n```ts\nconst FilesHandlers = HttpApiBuilder.group(Api, 'files', (handlers) =>\n  Effect.gen(function* () {\n    const git = yield* GitService;\n    return handlers.handle('getFiles', ({ query }) =>\n      git.listFiles(query.oldRef, query.newRef)\n    );\n  }),\n);\n```\nUpdate ApiRoutes to provide both handler layers plus GitServiceLive and RepoPath.\n\n**Verify:** vp test packages/server\n\n---\n\n### Cycle 5: Install TanStack Query + add @greppa/core dep to web\n\nNo tests — dependency setup only.\n\n- pnpm --filter @greppa/web add @tanstack/react-query\n- pnpm --filter @greppa/web add @greppa/core@workspace:*\n- Add QueryClientProvider to the app entry point (check packages/web/src/main.tsx)\n\n**Verify:** pnpm install succeeds, vp build succeeds\n\n---\n\n### Cycle 6: useFileList hook + buildFileTree\n\n**RED** — packages/web/src/hooks/useFileList.test.ts (new file)\nTest buildFileTree (pure function):\n```ts\ndescribe('buildFileTree', () => {\n  it('builds nested tree from flat entries', () => {\n    const entries = [\n      { path: 'src/index.ts', changeType: 'modified' as const },\n      { path: 'src/utils/helpers.ts', changeType: 'added' as const },\n    ];\n    const tree = buildFileTree(entries);\n    expect(tree).toHaveLength(1); // src/\n    expect(tree[0].children).toHaveLength(2); // index.ts, utils/\n  });\n\n  it('assigns changeType to file nodes', () => { ... });\n  it('handles renamed files with oldPath', () => { ... });\n});\n```\n\n**GREEN** — packages/web/src/hooks/useFileList.ts (new file)\n- buildFileTree(entries: FileEntry[]): FileNode[] — converts flat API entries to nested FileNode tree\n  - Split each path by /, build directory nodes, assign changeType to leaf file nodes\n  - Set name to last segment, type to 'file'/'directory', propagate oldPath for renamed\n- useFileList(oldRef: string, newRef: string) — TanStack Query hook:\n  - useQuery({ queryKey: ['files', oldRef, newRef], queryFn: () => fetch(...).then(r => r.json()) })\n  - Transform response through buildFileTree\n  - Return { files: FileNode[], isLoading, isError }\n\n**Verify:** vp test packages/web\n\n---\n\n### Cycle 7: App integration with fixture fallback\n\n**RED** — App test verifying:\n- When API data available, file tree renders from API data\n- When API unavailable, fixture data is used\n\n**GREEN** — packages/web/src/App.tsx\n- Import useFileList hook\n- Call useFileList('HEAD~1', 'HEAD') (hardcoded refs for now)\n- When isError or files is empty, fall back to fixture files\n- Pass resolved files to useFileSelection and FileTree\n- Keep all other fixture usage (diffs, comments, fileInfo) unchanged\n\n**Verify:** vp test (all tests), pre-commit hooks pass\n\n---\n\n## Files touched (summary)\n\n| File | Action |\n|------|--------|\n| packages/core/src/index.ts | Replace — add ChangeType + FileEntry schemas |\n| packages/core/src/index.test.ts | New — schema tests |\n| packages/server/src/GitService.ts | New — GitService + parseNameStatus + RepoPath + GitError |\n| packages/server/src/GitService.test.ts | New — parsing + integration tests |\n| packages/server/src/FilesApi.ts | New — HttpApiGroup with getFiles endpoint |\n| packages/server/src/Api.ts | Edit line 5 — add .add(FilesApi) |\n| packages/server/src/Http.ts | Edit — add FilesHandlers, update ApiRoutes |\n| packages/server/src/Http.test.ts | Edit — add /api/files tests, update TestAppLayer |\n| packages/web/package.json | Edit — add @tanstack/react-query + @greppa/core deps |\n| packages/web/src/hooks/useFileList.ts | New — hook + buildFileTree |\n| packages/web/src/hooks/useFileList.test.ts | New — buildFileTree tests |\n| packages/web/src/App.tsx | Edit — use useFileList with fixture fallback |\n| App entry point (main.tsx or similar) | Edit — wrap with QueryClientProvider |\n\n## Risks and mitigations\n\n1. **Effect v4 beta ChildProcess API** — tagged template API is unusual. Mitigation: use array form ChildProcess.make(\"git\", [args]) which is more conventional and well-documented in the .d.ts\n2. **HttpApiEndpoint error schema** — need to verify how typed errors propagate through the API layer. Mitigation: test with WebHandler first; BadRequest handler is built-in\n3. **git diff --name-status on shallow clones** — CI might use shallow clones. Mitigation: tests use HEAD~1 vs HEAD which should always work with depth >= 2\n4. **QueryClientProvider placement** — need to find the actual app entry point to wrap. Check packages/web/src/main.tsx or router setup","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-31T10:02:30.205795871Z","created_by":"sqve","updated_at":"2026-03-31T10:30:35.327533119Z","closed_at":"2026-03-31T10:30:35.326792829Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"],"dependencies":[{"issue_id":"greppa-2h0.1","depends_on_id":"greppa-2h0","type":"parent-child","created_at":"2026-03-31T10:02:30.205795871Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2h0.2","title":"Diff content endpoint and client hook","description":"## Goal\n\nServe raw old/new file content from git for a given file path and refs. Client fetches via TanStack Query hook. This enables the client-side diff computation that follows.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. @greppa/core exports a DiffResponse Effect Schema ({ path, oldPath?, changeType, oldContent, newContent })\n2. @greppa/server GitService gains getFileContent(ref, path) method that spawns git show ref:path and returns string content; returns empty string for non-existent paths (added/deleted files)\n3. GET /api/diff/:oldRef/:newRef/*path returns DiffResponse by looking up changeType from git diff --name-status, then fetching old and new content via git show\n4. Server rejects invalid refs or paths with typed error\n5. @greppa/web useDiffContent(oldRef, newRef, path) hook fetches via TanStack Query, enabled only when path is truthy\n6. Fixture fallback: when API is unavailable, existing fixture data is used for diff content\n\n## References\n\n- packages/core/src/index.ts — existing ChangeType and FileEntry schemas\n- packages/server/src/GitService.ts — existing GitService with listFiles\n- packages/server/src/FilesApi.ts — existing HttpApiGroup pattern\n- packages/server/src/Http.ts — existing handler wiring pattern\n- packages/web/src/hooks/useFileList.ts — existing TanStack Query hook pattern\n\n## Success criteria\n\n- [ ] @greppa/core exports DiffResponse Effect Schema with derived TS type\n- [ ] GitService.getFileContent returns file content for valid ref:path\n- [ ] GitService.getFileContent returns empty string for non-existent paths\n- [ ] GET /api/diff/HEAD~1/HEAD/path returns correct oldContent and newContent\n- [ ] Invalid refs return typed error response\n- [ ] Server tests pass using WebHandler pattern\n- [ ] Client useDiffContent hook fetches and returns diff content via TanStack Query\n- [ ] Fixture fallback works when server is unavailable\n- [ ] All tests passing\n- [ ] Pre-commit hooks passing","design":"# Expanded implementation plan\n\n## Codebase findings\n\n### Patterns to follow\n- `packages/core/src/index.ts:5-18` — Effect Schema pattern: `Schema.Struct`, `Schema.Union`, `Schema.optional`, derive type via `typeof X.Type`\n- `packages/server/src/GitService.ts:48-69` — `runGit(args)` helper spawns git, collects stdout/stderr, checks exit code\n- `packages/server/src/GitService.ts:71-87` — `GitService` is `ServiceMap.Service` with method signatures; `GitServiceLive` is `Layer.succeed`\n- `packages/server/src/FilesApi.ts:6-12` — `HttpApiGroup.make(name).add(HttpApiEndpoint.get(...))` pattern\n- `packages/server/src/Api.ts:6` — `HttpApi.make('greppa').add(Group1).add(Group2)` chain\n- `packages/server/src/Http.ts:15-22` — handler group: `HttpApiBuilder.group(Api, name, handlers => Effect.gen(...))`\n- `packages/server/src/Http.test.ts:10-34` — WebHandler test setup with `PlatformLayer`, `TestAppLayer`, `handler`, `dispose`\n- `packages/web/src/hooks/useFileList.ts:77-100` — TanStack Query hook pattern: `fetchFn` + `useQuery`\n\n### Key observations\n- `runGit` already exists as a reusable helper — can call it for `git show ref:path`\n- `GitService` currently has only `listFiles` — needs `getFileContent` added\n- DiffViewer consumes `DiffFile` (with hunks) — this task adds the raw content fetch; the diff Worker task will transform raw content to DiffFile\n- For this task, we don't need to change DiffViewer — it still consumes fixture DiffFile. The hook just fetches raw content.\n\n## TDD cycles\n\n### Cycle 1: DiffResponse schema in @greppa/core\n\n**RED** — packages/core/src/index.test.ts — add DiffResponse describe block\n**GREEN** — packages/core/src/index.ts — add DiffResponse Schema.Struct with path, changeType, oldPath optional, oldContent, newContent\n**Verify:** pnpm --filter @greppa/core test\n\n### Cycle 2: GitService.getFileContent — spawns git show ref:path\n\n**RED** — packages/server/src/GitService.test.ts — add `getFileContent` tests:\n  - returns content for valid ref:path (use HEAD:package.json which always exists)\n  - fails with GitError for invalid ref\n\n**GREEN** — packages/server/src/GitService.ts:\n  - Add `getFileContent` to GitService type: `(ref: string, path: string) => Effect<string, GitError, ...>`\n  - Add to GitServiceLive: calls `runGit(['show', \\`${ref}:${path}\\`])`\n\n**Verify:** pnpm --filter @greppa/server test\n\n### Cycle 3: DiffApi endpoint + handler wiring\n\n**RED** — packages/server/src/Http.test.ts — add `GET /api/diff/:oldRef/:newRef/*path` tests:\n  - returns 200 with oldContent/newContent for valid file (use a file from HEAD~1..HEAD)\n  - returns error for invalid refs\n\n**GREEN** — 3 files:\n1. packages/server/src/DiffApi.ts — new HttpApiGroup with getDiff endpoint, path params for oldRef/newRef, splat for file path\n2. packages/server/src/Api.ts — add `.add(DiffApi)`\n3. packages/server/src/Http.ts — add DiffHandlers group:\n   - Extract changeType from listFiles result for the given path\n   - Fetch oldContent via getFileContent(oldRef, path) — empty string for 'added'\n   - Fetch newContent via getFileContent(newRef, path) — empty string for 'deleted'\n   - Return DiffResponse shape\n\n**Verify:** pnpm --filter @greppa/server test\n\n### Cycle 4: useDiffContent client hook\n\n**RED** — packages/web/src/hooks/useDiffContent.test.ts — test the fetch function (mock fetch)\n**GREEN** — packages/web/src/hooks/useDiffContent.ts:\n  - `fetchDiffContent(oldRef, newRef, path)` — fetches `/api/diff/${oldRef}/${newRef}/${path}`\n  - `useDiffContent(oldRef, newRef, path)` — useQuery with enabled: path != null\n\n**Verify:** pnpm --filter @greppa/web test\n\n### Cycle 5: Integrate hook in App with fixture fallback\n\nNo test needed — integration wiring. App.tsx calls useDiffContent for selectedFilePath, falls back to fixture diffs when API unavailable.\n\n**Verify:** pnpm test (all), pre-commit hooks\n\n## Files touched\n\n| File | Action |\n|------|--------|\n| packages/core/src/index.ts | Edit — add DiffResponse schema |\n| packages/core/src/index.test.ts | Edit — add DiffResponse tests |\n| packages/server/src/GitService.ts | Edit — add getFileContent to service |\n| packages/server/src/GitService.test.ts | Edit — add getFileContent tests |\n| packages/server/src/DiffApi.ts | New — HttpApiGroup with getDiff endpoint |\n| packages/server/src/Api.ts | Edit — add DiffApi |\n| packages/server/src/Http.ts | Edit — add DiffHandlers |\n| packages/server/src/Http.test.ts | Edit — add /api/diff tests |\n| packages/web/src/hooks/useDiffContent.ts | New — TanStack Query hook |\n| packages/web/src/hooks/useDiffContent.test.ts | New — hook tests |\n| packages/web/src/App.tsx | Edit — integrate useDiffContent with fallback |","status":"closed","priority":1,"issue_type":"task","owner":"sqve","created_at":"2026-03-31T10:56:13.484387132Z","created_by":"sqve","updated_at":"2026-03-31T11:07:08.228729508Z","closed_at":"2026-03-31T11:07:08.228487988Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"],"dependencies":[{"issue_id":"greppa-2h0.2","depends_on_id":"greppa-2h0","type":"parent-child","created_at":"2026-03-31T10:56:13.484387132Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2h0.3","title":"Diff computation Web Worker with vscode-diff","description":"## Goal\n\nCompute line and character-level diffs client-side using vscode-diff DefaultLinesDiffComputer in a Web Worker. Takes raw old/new file content from the API (useDiffContent) and produces DetailedLineRangeMapping[] with innerChanges. This replaces the current fixture-based DiffFile hunks as the source of diff data.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. Install vscode-diff package in @greppa/web\n2. Create diffProtocol.ts with request/response types mirroring highlightProtocol pattern\n3. Create diffWorker.ts that receives old/new content strings and runs DefaultLinesDiffComputer.computeDiff()\n4. Create useDiffComputation hook that sends old/new content to worker and returns computed diff mappings\n5. Wire App.tsx to use useDiffContent → useDiffComputation pipeline instead of fixture hunks when API data is available\n6. Fixture fallback remains working when API is unavailable\n7. Diff computation never runs on main thread\n\n## References\n\n- packages/web/src/workers/highlightWorker.ts — existing worker pattern to follow\n- packages/web/src/workers/highlightProtocol.ts — existing protocol pattern\n- packages/web/src/hooks/useDiffContent.ts — source of old/new content\n- packages/web/src/components/DiffViewer/DiffViewer.tsx — consumer of diff data\n- packages/web/src/components/DiffViewer/buildRows.ts — current row building from DiffHunk\n\n## Success criteria\n\n- [ ] vscode-diff installed as dependency\n- [ ] Diff worker computes diffs without blocking main thread\n- [ ] useDiffComputation returns DetailedLineRangeMapping[] with innerChanges\n- [ ] DiffViewer can consume both fixture data and computed diff data\n- [ ] Tests cover worker protocol and hook behavior","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-31T11:33:40.497924863Z","created_by":"sqve","updated_at":"2026-03-31T11:42:48.572132196Z","closed_at":"2026-03-31T11:42:48.571972119Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["frontend"],"dependencies":[{"issue_id":"greppa-2h0.3","depends_on_id":"greppa-2h0","type":"parent-child","created_at":"2026-03-31T11:33:40.497924863Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2h0.4","title":"Two-tier diff highlighting with innerChanges","description":"## Goal\n\nRender two-tier diff highlighting: light background on changed lines (existing), darker highlight on exact character ranges from vscode-diff innerChanges. Update RowSideCell and TokenSpans to overlay character-level diff ranges on top of syntax tokens.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. RowSideCell accepts innerChanges character ranges for the current line\n2. Character-level ranges render with darker background color overlaid on syntax tokens\n3. CSS variables for character-level highlight colors (removed-inline-bg, added-inline-bg)\n4. Works with both fixture data (no innerChanges → line-level only) and computed diffs (with innerChanges → two-tier)\n\n## References\n\n- packages/web/src/components/DiffViewer/RowSideCell.tsx — render target\n- packages/web/src/components/DiffViewer/TokenSpans.tsx — syntax token renderer\n- packages/web/src/components/DiffViewer/DiffViewer.module.css — existing line-level styles\n- Depends on greppa-2h0.3 output format\n\n## Success criteria\n\n- [ ] Changed lines show light background (existing behavior preserved)\n- [ ] Character ranges from innerChanges show darker highlight overlay\n- [ ] Fixture mode renders without innerChanges (graceful degradation)\n- [ ] Tests cover character-range rendering","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-31T11:33:56.357519649Z","created_by":"sqve","updated_at":"2026-03-31T12:02:28.106144369Z","closed_at":"2026-03-31T12:02:28.105991568Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["frontend"],"dependencies":[{"issue_id":"greppa-2h0.4","depends_on_id":"greppa-2h0","type":"parent-child","created_at":"2026-03-31T11:33:56.357519649Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2h0.5","title":"Full-file syntax highlighting","description":"## Goal\n\nUpdate syntax highlighting to use full file content (oldContent/newContent from API) instead of isolated diff lines. This gives Shiki complete parser context for correct token state across the entire file.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. Highlight worker accepts full file content string and returns tokens indexed by line number\n2. useSyntaxHighlighting sends full old/new content when available from API\n3. DiffViewer maps syntax tokens to diff rows by line number (old line numbers for left side, new line numbers for right side)\n4. Falls back to current diff-line highlighting when using fixtures (no full content available)\n\n## References\n\n- packages/web/src/workers/highlightWorker.ts — current worker\n- packages/web/src/workers/highlightEngine.ts — current engine\n- packages/web/src/components/DiffViewer/useSyntaxHighlighting.ts — current hook\n- Depends on useDiffContent providing oldContent/newContent\n\n## Success criteria\n\n- [ ] Syntax highlighting uses full file content for correct parser state\n- [ ] Tokens mapped to diff rows by line number\n- [ ] Fixture fallback still works with diff-line-only highlighting\n- [ ] Tests cover full-file highlighting path","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-31T11:33:57.213816486Z","created_by":"sqve","updated_at":"2026-03-31T12:07:33.868964639Z","closed_at":"2026-03-31T12:07:33.868403540Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["frontend"],"dependencies":[{"issue_id":"greppa-2h0.5","depends_on_id":"greppa-2h0","type":"parent-child","created_at":"2026-03-31T11:33:57.213816486Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2h5","title":"Add unified/split diff toggle","description":"## Goal\n\nAdd a toggle in the diff header area to switch between side-by-side (split) and unified diff views.\n\n## Why\n\nBoth views have tradeoffs — split is better for comparing changes, unified is better for reading context. The stitch mockup shows this as a segmented control in the header. Resolved the open question in VISION.md.\n\n## Behaviors\n\n- A toggle control is visible when a diff file is selected.\n- Clicking \"Unified\" renders the diff as a single-column unified view.\n- Clicking \"Split\" renders the diff as the existing side-by-side view.\n- The selected mode persists across file selection within a session.\n\n## Success criteria\n\n- Toggle renders in the header/toolbar area.\n- Unified view shows additions and deletions interleaved in one column with +/- gutter indicators.\n- Split view matches current behavior.\n- Selected mode survives file switching.\n- All existing DiffViewer tests pass.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-26T18:58:43.839369283Z","created_by":"sqve","updated_at":"2026-03-29T19:36:59.841264312Z","closed_at":"2026-03-29T19:36:59.832837833Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["diff","ui"]}
{"id":"greppa-2na","title":"Behaviors","description":"1. @greppa/core exports a DiffResponse Effect Schema ({ path, oldPath?, changeType, oldContent, newContent })","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-31T10:55:52.343452084Z","updated_at":"2026-03-31T10:56:02.032654235Z","closed_at":"2026-03-31T10:56:02.032496649Z","close_reason":"wontdo","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"]}
{"id":"greppa-2oq","title":"Shiki syntax highlighting in Web Worker","description":"## Context\n\nSyntax highlighting via Shiki currently runs on the main thread inside a useMemo. For large files, codeToTokens blocks rendering — the entire file is joined into one string and tokenized synchronously. Moving Shiki to a Web Worker lets the first paint show unstyled text immediately, with tokens painting in progressively.\n\n## Requirements\n\n- [ ] Shiki highlighter instance runs entirely in a dedicated Web Worker\n- [ ] First paint shows unstyled monospace text immediately (no blank/loading state)\n- [ ] Worker returns token arrays per line; client patches them into the already-rendered rows\n- [ ] Highlighter instance is reused across file switches (warm grammar cache in worker)\n- [ ] Language grammars are loaded on demand, not all at once\n- [ ] Progressive enhancement: if the worker fails or is slow, plain text remains usable\n- [ ] Works correctly with row-level virtualization (tokens only needed for visible rows, but pre-computed for scroll performance)\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Worker scope | Dedicated Worker (not SharedWorker) | Simpler lifecycle; one worker per tab is fine for local-first |\n| Communication | postMessage with transferable ArrayBuffers where possible | Avoid structured clone overhead for large token arrays |\n| Caching | Worker-side Map keyed by {filePath, lineKey} | Avoids re-highlighting already-processed lines on scroll |\n| Grammar loading | Lazy per-language | Keeps initial worker boot fast; most PRs touch 1-3 languages |\n| Fallback | Plain text render, no spinner | Unstyled code is readable; a loading spinner is worse than plain text |\n\n## Anti-patterns\n\n- Do NOT block first paint on highlighting — the whole point is progressive enhancement\n- Do NOT send the entire file to the worker on every render — cache and send only new/changed content\n- Do NOT load all Shiki grammars at worker init — lazy load per language\n- Do NOT use synchronous worker communication (Atomics/SharedArrayBuffer) — postMessage is sufficient\n\n## Success criteria\n\n1. First paint of a 5,000-line diff shows text in < 50ms (unstyled)\n2. Syntax tokens appear within 500ms of first paint for typical files\n3. Main thread frame budget is never exceeded by highlighting work\n4. Switching between files reuses the worker and grammar cache (no re-init)\n5. Plain text fallback works if worker fails to load","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-03-29T20:42:16.111749635Z","created_by":"sqve","updated_at":"2026-03-31T07:54:50.282235755Z","closed_at":"2026-03-31T07:54:50.278211935Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"]}
{"id":"greppa-2sj","title":"Diff viewer row-level virtualization","description":"## Context\n\nThe diff viewer currently renders every row of every hunk into the DOM at once. A 10,000-line diff creates 20,000+ DOM nodes (both sides). This is the single biggest performance bottleneck and the most impactful fix. The VISION.md promises \"DOM size is constant regardless of diff size.\"\n\n## Requirements\n\n- [x] Row-level virtualization using @tanstack/virtual (or equivalent) — only visible rows exist in the DOM\n- [x] Side-by-side panels share a single virtual scroller; left/right row indices stay aligned\n- [x] Hunk headers are rows in the virtual list, not separate elements outside it\n- [x] Scrolling performance stays smooth (60fps) on a 50,000-line diff\n- [x] File size tiers determine rendering strategy: small (< 500 lines) renders normally, medium (500–5,000) relies on virtualization, large (5,000–50,000) defers syntax highlighting (plain text first), huge (50,000+) collapses by default with summary and expand-on-demand\n- [x] Keyboard navigation (j/k between hunks, n/p between changes) works with virtualized rows\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Virtualization library | @tanstack/virtual | Framework-agnostic, headless, proven at scale, small bundle |\n| Scroll unit | Row | Hunk-level loses on single-hunk monster diffs; row-level guarantees constant DOM |\n| Side sync | Single virtualizer, both sides read same measurements | Avoids dual-scroll sync bugs; rows are always the same height on both sides |\n| Hunk headers | Virtual rows with a different renderer | Keeps everything in one list; no special positioning logic |\n| File size tiers | Graduated strategy | Huge files collapsed by default avoids accidental DOM explosion |\n\n## Anti-patterns\n\n- Do NOT render all rows and hide with CSS — defeats the purpose\n- Do NOT use intersection observer per-row — too many observers, worse than virtualization\n- Do NOT break the side-by-side grid contract — left and right must always align row-for-row\n- Do NOT add scroll event throttling — @tanstack/virtual handles this; extra throttling causes visual lag\n\n## Success criteria\n\n1. DOM node count stays under 200 rows regardless of diff size\n2. Opening a 10,000-line diff takes < 100ms to first paint\n3. Scrolling a 50,000-line diff maintains 60fps\n4. All existing diff viewer tests pass or are updated to work with virtualized rendering\n5. Keyboard navigation works identically to pre-virtualization behavior","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-03-29T20:41:33.746654021Z","created_by":"sqve","updated_at":"2026-03-30T11:43:28.647043906Z","closed_at":"2026-03-30T11:43:28.634559302Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"]}
{"id":"greppa-2sj.1","title":"Replace DiffViewer with virtualized row renderer","description":"## Goal\n\nReplace the current DiffViewer that renders all rows into a CSS grid with a @tanstack/virtual-based implementation that only mounts visible rows. This is the single highest-impact performance change — DOM size becomes constant regardless of diff size.\n\n## Behaviors\n\n1. Virtualizer renders only visible rows — given a diff with 1,000 rows, fewer than 100 DOM row elements exist at any time\n2. Side-by-side alignment preserved — left row N always aligns vertically with right row N in the virtualized list\n3. Hunk headers render as virtual rows — hunk header strings appear at correct positions within the scrollable list using a distinct row renderer\n4. Syntax highlighting tokens apply to visible rows — token spans render correctly on mounted rows via the existing tokenMap\n5. Scroll position stability — scrolling up and down produces no layout jumps or blank flashes on the fixture diffs\n\n## Success criteria\n\n- [ ] DOM row count stays under 100 regardless of diff size\n- [ ] Side-by-side alignment is pixel-correct (left and right rows share the same vertical offset)\n- [ ] Hunk headers render in correct positions within the virtual list\n- [ ] All existing DiffViewer tests pass or are updated for the virtualized structure\n- [ ] TokenSpans still render on visible rows with correct highlighting","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-29T20:42:44.381757141Z","created_by":"sqve","updated_at":"2026-03-30T09:17:28.201933476Z","closed_at":"2026-03-30T09:17:28.189252153Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-2sj.1","depends_on_id":"greppa-2sj","type":"parent-child","created_at":"2026-03-29T20:42:44.381757141Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2sj.2","title":"Add file size tiers for graduated rendering strategy","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-30T09:19:18.694913647Z","created_by":"sqve","updated_at":"2026-03-30T09:24:56.726151689Z","closed_at":"2026-03-30T09:24:56.713973540Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-2sj.2","depends_on_id":"greppa-2sj","type":"parent-child","created_at":"2026-03-30T09:19:18.694913647Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2sj.3","title":"Add keyboard navigation for virtualized diff viewer","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-30T09:25:23.783498970Z","created_by":"sqve","updated_at":"2026-03-30T09:27:25.723024566Z","closed_at":"2026-03-30T09:27:25.700386673Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-2sj.3","depends_on_id":"greppa-2sj","type":"parent-child","created_at":"2026-03-30T09:25:23.783498970Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7","title":"App shell: design tokens, layout, and core UI components","description":"## Problem\n\nThe web package is a placeholder displaying a health check response. No visual foundation exists for the code review tool — no design tokens, no layout system, no components. Nothing to see or interact with.\n\n## Requirements\n\n1. Full `--gr-` token system in `packages/ui`: primitives, semantic mappings, diff tokens, spacing scale (4px base), typography (ui + mono stacks)\n2. Light and dark themes with `[data-theme]` attribute switching and `prefers-color-scheme` detection\n3. Three-panel resizable layout: file tree | diff viewer | detail panel — using react-resizable-panels v4 (`Group`/`Panel`/`Separator` API) with localStorage persistence via `useDefaultLayout`\n4. File tree using React Aria `Tree`/`TreeItem`/`TreeItemContent` from `react-aria-components` with keyboard navigation and built-in `Virtualizer`\n5. Side-by-side diff viewer with Shiki syntax highlighting, line numbers, and added/removed/modified line coloring\n6. Tabbed detail panel (Comments | File Info) using Radix Tabs — Comments shows mock thread UI, File Info shows mock git metadata\n7. Progress-only status bar at the bottom showing file count and review progress (e.g. \"4/12 files reviewed\")\n8. Realistic JSON fixture files with TypeScript diffs, multiple files, adds/deletes/renames\n9. All mock data — no server wiring, no API calls. `pnpm dev` opens browser and shows the full review UI\n10. CSS Modules with `--gr-` custom properties throughout. No runtime CSS-in-JS\n\n## Anti-patterns\n\n- No Mantine or other opinionated component library — Radix primitives only for what they provide (Tabs, Dialog, etc.)\n- No react-virtuoso for the file tree — React Aria's built-in Virtualizer is the only supported path\n- No runtime CSS-in-JS (styled-components, emotion, etc.) — CSS Modules only\n- No unified diff mode in this epic — side-by-side only, toggle deferred\n- No server endpoints or API wiring — everything runs from mock fixtures\n- No file verdict tracking (approved/needs changes) — status bar shows progress count only\n\n## Success criteria\n\n1. `pnpm dev` opens the browser showing a three-panel review UI with realistic mock data\n2. File tree renders a mock file hierarchy with expand/collapse, keyboard navigation (arrows, Home/End), and typeahead\n3. Clicking a file in the tree shows its side-by-side diff with Shiki syntax highlighting in the center panel\n4. Detail panel has Comments and File Info tabs — Comments shows mock threads, File Info shows mock metadata\n5. All three panels are resizable via drag handles and keyboard; sizes persist across page reloads\n6. Light/dark theme toggle works; respects OS preference on first load\n7. Status bar shows \"N/M files reviewed\" and updates when navigating files\n8. Design tokens are defined in `packages/ui` and consumed by `packages/web` via CSS Modules\n9. All existing checks pass: format, lint, typecheck, test, build\n\n## Design\n\n### Token architecture (packages/ui)\n\nThree-layer --gr- namespace as specified in STACK.md:\n- tokens/primitives.css — raw color palette, raw sizes\n- tokens/semantic.css — role-based tokens (bg-default, text-primary, border-default, bg-interactive, etc.)\n- tokens/diff.css — diff-specific tokens (added-bg, removed-bg, added-char-bg, gutter colors)\n- tokens/spacing.css — 4px base scale (--gr-space-1 through --gr-space-16)\n- tokens/typography.css — font stacks (ui + mono), code-specific sizes\n- themes/light.css — :root defaults\n- themes/dark.css — [data-theme=\"dark\"] overrides + prefers-color-scheme media query\n\n### Layout (packages/web)\n\nThree-panel horizontal layout:\n- Header: Greppa logo + theme toggle\n- Body: FileTree (React Aria Tree) | DiffViewer (side-by-side, Shiki) | DetailPanel (Radix Tabs: Comments | File Info)\n- Footer: StatusBar with review progress\n\nreact-resizable-panels v4: Group with orientation=\"horizontal\", three Panels with Separators. Sidebar collapsible, detail panel collapsible. useDefaultLayout for localStorage persistence.\n\n### Key libraries\n\n- react-resizable-panels v4 — layout panels\n- react-aria-components — Tree + Virtualizer for file tree\n- @radix-ui/react-tabs — detail panel tabs\n- shiki — syntax highlighting singleton\n\n### Mock data\n\nJSON fixtures in packages/web/src/fixtures/:\n- files.json — file tree structure (directories, files with change types)\n- diffs/*.json — per-file diff data with hunks, line numbers, content\n- comments.json — mock comment threads with authors, timestamps, replies\n- fileInfo.json — mock git metadata (change frequency, authors, related PRs)\n\n### Package boundaries\n\n- packages/ui — token CSS files, theme CSS, shared primitive components (if any emerge)\n- packages/web — app shell, FileTree, DiffViewer, DetailPanel, StatusBar, Header, mock fixtures","design":"### Token architecture (packages/ui) — theme-driven\n\nColors derive from curated syntax color themes. The theme IS the mode (light/dark). No separate dark mode toggle.\n\n**Two-layer architecture:**\n- Theme CSS file sets all --gr-color-* semantic tokens via [data-theme='theme-name']\n- Components consume --gr-color-* only — never raw colors\n\n**Theme contract (18 colors + 2 metadata):**\nEvery theme must provide:\n- Surfaces (6): base, mantle, crust, surface0, surface1, surface2\n- Text & borders (6): text, subtext1, subtext0, overlay2, overlay1, overlay0\n- Accent (2): accent, accentText\n- Status (4): green, red, yellow, blue\n- Metadata (2): name, type ('light' | 'dark')\n\nDiff tokens derived via alpha transparency from status colors (green/red/yellow at 15%/30%/20%).\n\n**Theme switching:**\n- data-theme attribute on <html> — e.g., data-theme=\"catppuccin-mocha\"\n- OS preference picks initial default (Mocha if dark, Latte if light)\n- User selection stored in localStorage\n- Inline <script> in index.html prevents FOUC\n\n**Theme-independent tokens (stay as-is):**\n- spacing.css — 9-step non-linear scale with --gr-scaling multiplier\n- typography.css — font stacks, weights, sizes, leading\n\n**Shiki integration:**\n- Shiki loads the matching VS Code theme file for syntax highlighting\n- Same palette, different format — guaranteed visual harmony between syntax and UI chrome\n\n**First themes:**\n- Catppuccin Mocha (dark) — default dark\n- Catppuccin Latte (light) — default light\n\n**Escape hatch (deferred):**\nCustom theme JSON format matching the 18-color contract. Validated at load time.\n\n### File structure (packages/ui/src/)\n\n- tokens/spacing.css — 9-step non-linear scale with --gr-scaling\n- tokens/typography.css — font stacks, weights, sizes\n- themes/catppuccin-mocha.css — [data-theme='catppuccin-mocha'] { ... }\n- themes/catppuccin-latte.css — [data-theme='catppuccin-latte'] { ... }\n- tokens.css — barrel import of spacing + typography + all themes\n\n### Layout (packages/web) — unchanged\n\nThree-panel horizontal layout:\n- Header: Greppa logo + theme picker\n- Body: FileTree | DiffViewer | DetailPanel\n- Footer: StatusBar with review progress\n\nreact-resizable-panels v4 with localStorage persistence.\n\n### Key libraries — unchanged\n\n- react-resizable-panels v4 — layout panels\n- react-aria-components — Tree + Virtualizer for file tree\n- @radix-ui/react-tabs — detail panel tabs\n- shiki — syntax highlighting singleton (themes shared with UI)","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-03-26T14:15:09.627486671Z","created_by":"sqve","updated_at":"2026-03-26T17:02:28.841455295Z","closed_at":"2026-03-26T17:02:28.837389986Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"]}
{"id":"greppa-2v7.1","title":"Create --gr- design token system with light and dark themes","description":"## Goal\n\nCreate the full --gr- design token system in packages/ui with light and dark themes.\n\n## Behaviors\n\n1. Primitive tokens define the raw color palette (neutral grays, blue/green/red/yellow accent scales) and raw size values — never referenced by components directly\n2. Semantic tokens map primitives to roles: bg-default, bg-subtle, bg-inset, text-primary, text-muted, border-default, bg-interactive, bg-interactive-hover\n3. Diff tokens follow VS Code's diffEditor structure: added-bg, added-char-bg, gutter-added-bg, added-border — same pattern for removed and modified\n4. Spacing scale: --gr-space-1 (4px) through --gr-space-16 (64px)\n5. Typography: --gr-font-ui (system-ui stack), --gr-font-mono (ui-monospace stack), --gr-text-code (0.8125rem), --gr-leading-code (1.5rem)\n6. Light theme is the :root default\n7. Dark theme activates via [data-theme=\"dark\"] attribute on html\n8. OS preference respected via @media (prefers-color-scheme: dark) when no explicit data-theme is set\n9. All token CSS files are imported and re-exported from packages/ui/src/index.ts (or a tokens entry point)\n10. packages/web can consume tokens by importing the CSS from @greppa/ui\n\n## File structure\n\n- packages/ui/src/tokens/primitives.css\n- packages/ui/src/tokens/semantic.css\n- packages/ui/src/tokens/diff.css\n- packages/ui/src/tokens/spacing.css\n- packages/ui/src/tokens/typography.css\n- packages/ui/src/themes/light.css\n- packages/ui/src/themes/dark.css\n- packages/ui/src/tokens.css (barrel import of all token + theme files)\n\n## Success criteria\n\n- All token files exist with the categories above\n- Light and dark themes both render correctly\n- Tokens are consumable from packages/web via @greppa/ui import\n- pnpm typecheck, pnpm lint, pnpm format:check, pnpm build all pass","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T14:15:30.817085552Z","created_by":"sqve","updated_at":"2026-03-26T14:25:30.891577069Z","closed_at":"2026-03-26T14:25:30.888848448Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.1","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T14:15:30.817085552Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.2","title":"Restructure tokens into theme-driven system with Catppuccin Mocha and Latte","description":"## Goal\n\nReplace the fixed color primitive/semantic/diff/theme CSS files with a theme-driven system. Each theme is a single CSS file that sets all --gr-color-* semantic tokens. Ship Catppuccin Mocha (dark) and Catppuccin Latte (light) as the first two themes.\n\n## Behaviors\n\n1. Remove primitives.css, semantic.css, diff.css, themes/light.css, themes/dark.css\n2. Create themes/catppuccin-mocha.css — [data-theme='catppuccin-mocha'] selector setting all ~40 --gr-color-* tokens using Catppuccin Mocha palette values\n3. Create themes/catppuccin-latte.css — same structure using Catppuccin Latte palette values\n4. Diff tokens derived via alpha: diff-added-bg = green at 15% alpha, diff-added-char-bg = green at 30%, diff-gutter-added-bg = green at 20%, diff-added-border = green at 40%. Same pattern for red (removed) and yellow (modified)\n5. Each theme sets color-scheme: dark|light for native browser element styling\n6. spacing.css stays unchanged (theme-independent, 9-step non-linear with --gr-scaling)\n7. typography.css stays unchanged (theme-independent, font stacks + weights + sizes)\n8. Update tokens.css barrel to import spacing + typography + all theme files\n9. Update index.ts if needed\n10. Add inline script to packages/web/index.html that reads localStorage for theme preference, falls back to OS prefers-color-scheme (Mocha if dark, Latte if light), and sets data-theme on <html> before first paint\n11. All semantic token names stay the same (--gr-color-bg-default, etc.) — only the source of values changes\n\n## Theme contract\n\nEach theme CSS file must set these semantic tokens:\n\nSurfaces:\n- --gr-color-bg-default (from base)\n- --gr-color-bg-subtle (from mantle)\n- --gr-color-bg-inset (from crust)\n- --gr-color-bg-emphasis (from surface2)\n- --gr-color-bg-surface (from surface0)\n- --gr-color-bg-surface-hover (from surface1)\n- --gr-color-bg-surface-active (from surface2)\n\nText:\n- --gr-color-text-primary (from text)\n- --gr-color-text-secondary (from subtext1)\n- --gr-color-text-muted (from subtext0)\n- --gr-color-text-on-emphasis (derive from theme type)\n\nBorders:\n- --gr-color-border-default (from overlay1)\n- --gr-color-border-muted (from overlay0)\n- --gr-color-border-emphasis (from overlay2)\n\nInteractive:\n- --gr-color-bg-interactive (from accent)\n- --gr-color-bg-interactive-hover (from accent, lightened/darkened 10%)\n- --gr-color-text-interactive (from accent)\n\nStatus:\n- --gr-color-bg-success (from green at 15% alpha)\n- --gr-color-text-success (from green)\n- --gr-color-bg-danger (from red at 15% alpha)\n- --gr-color-text-danger (from red)\n- --gr-color-bg-warning (from yellow at 15% alpha)\n- --gr-color-text-warning (from yellow)\n\nDiff:\n- --gr-color-diff-added-bg (green at 15% alpha)\n- --gr-color-diff-added-char-bg (green at 30% alpha)\n- --gr-color-diff-gutter-added-bg (green at 20% alpha)\n- --gr-color-diff-added-border (green at 40% alpha)\n- Same pattern for removed (red) and modified (yellow)\n\n## File structure\n\nRemove:\n- packages/ui/src/tokens/primitives.css\n- packages/ui/src/tokens/semantic.css\n- packages/ui/src/tokens/diff.css\n- packages/ui/src/themes/light.css\n- packages/ui/src/themes/dark.css\n\nKeep:\n- packages/ui/src/tokens/spacing.css\n- packages/ui/src/tokens/typography.css\n\nCreate:\n- packages/ui/src/themes/catppuccin-mocha.css\n- packages/ui/src/themes/catppuccin-latte.css\n\nUpdate:\n- packages/ui/src/tokens.css (barrel)\n- packages/web/index.html (inline theme script)\n\n## Catppuccin palette reference\n\nMocha (dark): base=#1e1e2e, mantle=#181825, crust=#11111b, surface0=#313244, surface1=#45475a, surface2=#585b70, overlay0=#6c7086, overlay1=#7f849c, overlay2=#9399b2, subtext0=#a6adc8, subtext1=#bac2de, text=#cdd6f4, blue=#89b4fa, green=#a6e3a1, red=#f38ba8, yellow=#f9e2af, lavender=#b4befe\n\nLatte (light): base=#eff1f5, mantle=#e6e9ef, crust=#dce0e8, surface0=#ccd0da, surface1=#bcc0cc, surface2=#acb0be, overlay0=#9ca0b0, overlay1=#8c8fa1, overlay2=#7c7f93, subtext0=#6c6f85, subtext1=#5c5f77, text=#4c4f69, blue=#1e66f5, green=#40a02b, red=#d20f39, yellow=#df8e1d, lavender=#7287fd\n\n## Success criteria\n\n- All existing --gr-color-* semantic token names still work\n- Catppuccin Mocha renders correctly as dark theme\n- Catppuccin Latte renders correctly as light theme\n- data-theme attribute switches between themes\n- First load respects OS preference (no FOUC)\n- spacing and typography tokens unchanged\n- All checks pass: typecheck, lint, format:check, build, test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T14:26:08.805996694Z","created_by":"sqve","updated_at":"2026-03-26T15:28:35.429190109Z","closed_at":"2026-03-26T15:28:35.426101154Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.2","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T14:26:08.805996694Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.3","title":"Three-panel resizable layout with header and status bar","description":"## Goal\n\nCreate the app shell layout: header, three-panel resizable body (file tree | diff viewer | detail panel), and status bar footer. Uses react-resizable-panels v4 with localStorage persistence.\n\n## Behaviors\n\n1. Header component with \"Greppa\" text and a theme picker (dropdown listing available themes)\n2. Three horizontal panels using react-resizable-panels v4 Group/Panel/PanelResizeHandle API\n3. Left panel (file tree placeholder): default 20% width, min 150px, collapsible\n4. Center panel (diff viewer placeholder): fills remaining space, min 300px\n5. Right panel (detail panel placeholder): default 25% width, min 200px, collapsible\n6. Panel sizes persist to localStorage via autoSaveId prop on PanelGroup\n7. PanelResizeHandle styled with --gr-color-* tokens: 1px border-default, hover highlight\n8. StatusBar footer showing static \"0/0 files reviewed\" placeholder text\n9. App uses --gr-color-bg-default for body background, --gr-font-ui for base font\n10. Theme picker sets data-theme on html and stores choice in localStorage\n11. All styling via CSS Modules referencing --gr-color-* tokens\n\n## File structure\n\n- packages/web/src/App.tsx — replace current health check with layout shell\n- packages/web/src/App.module.css — layout styles\n- packages/web/src/components/Header.tsx + Header.module.css\n- packages/web/src/components/StatusBar.tsx + StatusBar.module.css\n- packages/web/src/hooks/useTheme.ts — theme read/write logic\n\n## Success criteria\n\n- pnpm dev shows the three-panel layout with header and status bar\n- Panels are resizable via drag handles\n- Panel sizes persist across page reloads\n- Theme picker switches between Catppuccin Mocha and Latte\n- All checks pass: typecheck, lint, format:check, build, test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T15:24:27.962516525Z","created_by":"sqve","updated_at":"2026-03-26T15:40:06.015072313Z","closed_at":"2026-03-26T15:40:06.011861945Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.3","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T15:24:27.962516525Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.4","title":"Create mock data fixtures for file tree, diffs, comments, and file info","description":"## Goal\n\nCreate realistic JSON fixture data that all three panels will consume. This is the foundation for the file tree, diff viewer, and detail panel.\n\n## Behaviors\n\n1. files.json — tree structure with directories and files, each file has a path, change type (added/modified/deleted/renamed), and status (reviewed/unreviewed)\n2. diffs/*.json — per-file diff data with hunks containing line numbers, content, and change type per line. Use realistic TypeScript code snippets\n3. comments.json — mock comment threads with author name, timestamp, body text, file path, line number, and replies\n4. fileInfo.json — mock git metadata per file: last modified date, change frequency, authors, related PR numbers\n5. TypeScript types for all fixture shapes exported from a types.ts file\n6. At least 5 files in the tree across 2-3 directories, including one add, one delete, one rename, and two modifications\n7. Fixture data importable via standard ES imports from packages/web/src/fixtures/\n\n## File structure\n\n- packages/web/src/fixtures/types.ts — shared types\n- packages/web/src/fixtures/files.json — file tree data\n- packages/web/src/fixtures/diffs/ — per-file diff JSON files\n- packages/web/src/fixtures/comments.json — comment threads\n- packages/web/src/fixtures/fileInfo.json — git metadata\n\n## Success criteria\n\n- All fixture files parse without errors\n- Types match the JSON structure\n- Realistic TypeScript diff content (not lorem ipsum)\n- All checks pass: typecheck, lint, format:check, build, test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T15:55:16.137437706Z","created_by":"sqve","updated_at":"2026-03-26T15:59:21.214191892Z","closed_at":"2026-03-26T15:59:21.210513839Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.4","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T15:55:16.137437706Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.5","title":"File tree component with React Aria Tree and file selection state","description":"## Goal\n\nBuild the FileTree component using React Aria Tree and wire it into App.tsx with file selection state. This makes the left panel functional and provides the selection state that DiffViewer and DetailPanel will consume in later tasks.\n\n## Behaviors\n\n1. Install react-aria-components in packages/web\n2. FileTree component renders fixture files.json as a nested tree with directories and files\n3. Each file shows its name and a change-type indicator (added/modified/deleted/renamed)\n4. Directories expand/collapse; all expanded by default\n5. Clicking a file sets it as the selected file in app state\n6. Keyboard navigation works: arrow keys, Home/End, typeahead (provided by React Aria)\n7. App.tsx lifts selectedFilePath state and passes it down\n8. StatusBar reads fixture data to show \"N/M files reviewed\" based on the status field in files.json\n9. CSS Modules with --gr- tokens for all styling\n\n## File structure\n\n- packages/web/src/components/FileTree.tsx — component\n- packages/web/src/components/FileTree.module.css — styles\n- packages/web/src/components/FileTree.test.tsx — tests\n- packages/web/src/App.tsx — wire in selection state\n\n## Success criteria\n\n- File tree renders all 5 files across 3 directories from fixtures\n- Clicking a file updates selection (visible highlight)\n- Keyboard navigation works (arrows, expand/collapse, typeahead)\n- StatusBar shows \"2/5 files reviewed\" (from fixture data)\n- All checks pass: typecheck, lint, format:check, build, test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T16:05:06.935036165Z","created_by":"sqve","updated_at":"2026-03-26T16:09:23.289095776Z","closed_at":"2026-03-26T16:09:23.285471363Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.5","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T16:05:06.935036165Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.6","title":"Side-by-side diff viewer with Shiki syntax highlighting","description":"## Goal\n\nBuild the DiffViewer component that renders side-by-side diffs with Shiki syntax highlighting when a file is selected in the tree. Wire it into the center panel of App.tsx.\n\n## Behaviors\n\n1. Install shiki in packages/web\n2. Export diff fixtures from fixtures/index.ts as a map from file path to DiffFile\n3. DiffViewer component receives a DiffFile and renders side-by-side: left (before) and right (after)\n4. Each hunk renders with a hunk header showing the range info\n5. Line numbers shown in gutters on both sides\n6. Added lines highlighted with --gr-color-diff-added-bg, removed with --gr-color-diff-removed-bg, modified with --gr-color-diff-modified-bg\n7. Character-level highlights use --gr-color-diff-added-char-bg and --gr-color-diff-removed-char-bg\n8. Shiki highlights syntax using a Catppuccin-compatible theme — load theme once via singleton/lazy init\n9. Empty state when no file is selected: centered \"Select a file to view diff\" message\n10. CSS Modules with --gr- tokens for all styling\n11. App.tsx looks up the selected file path in the diff map and passes the DiffFile to DiffViewer\n\n## File structure\n\n- packages/web/src/components/DiffViewer.tsx — component\n- packages/web/src/components/DiffViewer.module.css — styles\n- packages/web/src/components/DiffViewer.test.tsx — tests\n- packages/web/src/fixtures/index.ts — update to export diffs map\n- packages/web/src/App.tsx — wire DiffViewer into center panel\n\n## Success criteria\n\n- Clicking a file in the tree shows its side-by-side diff in the center panel\n- Syntax highlighting renders via Shiki\n- Line numbers appear in gutters\n- Added/removed/modified lines use correct diff token colors\n- Empty state shows when no file selected\n- All checks pass: typecheck, lint, format:check, build, test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T16:12:19.724904019Z","created_by":"sqve","updated_at":"2026-03-26T16:21:53.413527554Z","closed_at":"2026-03-26T16:21:53.409753823Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.6","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T16:12:19.724904019Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.7","title":"Detail panel with Comments and File Info tabs using Radix Tabs","description":"## Goal\n\nBuild the DetailPanel component with two tabs (Comments and File Info) using @radix-ui/react-tabs. Wire it into the right panel of App.tsx, driven by the selected file.\n\n## Behaviors\n\n1. Install @radix-ui/react-tabs in packages/web\n2. Export comments and fileInfo fixtures from fixtures/index.ts\n3. DetailPanel receives the selected file path and looks up data from fixtures\n4. Comments tab shows mock thread UI: each thread has author, timestamp, body text, and replies indented\n5. File Info tab shows mock git metadata: last modified, change frequency, authors, related PRs\n6. Empty state when no file is selected: centered message\n7. Tab styling uses --gr- tokens via CSS Modules\n8. App.tsx passes selectedFilePath to DetailPanel\n\n## File structure\n\n- packages/web/src/components/DetailPanel.tsx — component\n- packages/web/src/components/DetailPanel.module.css — styles\n- packages/web/src/components/DetailPanel.test.tsx — tests\n- packages/web/src/fixtures/index.ts — update to export comments and fileInfo\n- packages/web/src/App.tsx — wire DetailPanel into right panel\n\n## Success criteria\n\n- Comments tab shows mock threads with replies for selected file\n- File Info tab shows mock metadata for selected file\n- Empty state shows when no file selected\n- Tabs switch between Comments and File Info\n- All checks pass: typecheck, lint, format:check, build, test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T16:22:10.227224266Z","created_by":"sqve","updated_at":"2026-03-26T16:24:31.187620933Z","closed_at":"2026-03-26T16:24:31.183497744Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.7","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T16:22:10.227224266Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2v7.8","title":"Status bar updates reviewed count when navigating files","description":"## Goal\n\nMake the status bar reviewed count update when the user selects a file in the tree. Selecting an unreviewed file marks it as reviewed, incrementing the count.\n\n## Behaviors\n\n1. App.tsx tracks a Set of reviewed file paths as state, initialized from fixture data\n2. When a file is selected (onSelectFile), add it to the reviewed set\n3. StatusBar receives the updated count\n4. Count only increases — no way to un-review\n\n## File structure\n\n- packages/web/src/App.tsx — mutable reviewed state\n- packages/web/src/App.test.tsx — test the update behavior\n\n## Success criteria\n\n- Selecting an unreviewed file increments the status bar count\n- Count starts at fixture value (2/5)\n- All checks pass","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T16:25:10.284285729Z","created_by":"sqve","updated_at":"2026-03-26T16:26:24.126020689Z","closed_at":"2026-03-26T16:26:24.122025268Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ui"],"dependencies":[{"issue_id":"greppa-2v7.8","depends_on_id":"greppa-2v7","type":"parent-child","created_at":"2026-03-26T16:25:10.284285729Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-2vg","title":"Add longer fixture diffs for realistic testing","description":"## Goal\n\nAdd at least 2 new fixture diff files with ~200+ lines of code content each to stress-test the UI at realistic scale.\n\n## Why\n\nCurrent largest fixture (rateLimiter) is only 38 lines of code content. Real-world diffs are much longer. Need realistic data to test scrolling performance, layout stability, and identify virtualization needs.\n\n## Behaviors\n\n- New fixture files are loadable from the file tree.\n- Each fixture contains multiple hunks spanning 200+ lines of code.\n- Fixtures include a mix of additions, deletions, and context lines.\n\n## Success criteria\n\n- At least 2 new fixture JSON files in packages/web/src/fixtures/diffs/.\n- Each has 200+ lines of code content across multiple hunks.\n- Files appear in the file tree and render correctly in the DiffViewer.\n- Fixtures represent realistic TypeScript code, not lorem ipsum.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-26T18:58:53.916418642Z","created_by":"sqve","updated_at":"2026-03-29T19:42:06.361739135Z","closed_at":"2026-03-29T19:42:06.353265259Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["fixtures","testing"]}
{"id":"greppa-2vj","title":"Hello world backend endpoint","description":"## Goal\n\nAdd a `/api/hello` GET endpoint that returns `{ message: \"Hello, world!\" }`.\n\n## Success criteria\n\n- [ ] `GET /api/hello` returns 200 with `{ message: \"Hello, world!\" }`\n- [ ] Response schema is type-safe via `HttpApiEndpoint` + `Schema`\n- [ ] Endpoint is registered in the top-level `Api` class\n- [ ] Integration test verifies status code and response body\n- [ ] Follows existing HealthApi pattern (group file, handler, test)\n\n## Constraints\n\n- No new packages or dependencies\n- Follow existing three-layer pattern: *Api.ts → Api.ts → Http.ts","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-25T20:49:54.947950202Z","created_by":"sqve","updated_at":"2026-03-25T21:14:39.088226535Z","closed_at":"2026-03-25T21:14:39.086208938Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"greppa-2vj.1","title":"Add /api/hello endpoint with test","description":"## What\n\nAdd a hello world GET endpoint at `/api/hello` following the existing HealthApi pattern.\n\n## Steps\n\n1. Create `packages/server/src/HelloApi.ts`:\n   - `HttpApiGroup.make('hello')` with one endpoint\n   - `HttpApiEndpoint.get('getHello', '/api/hello', { success: Schema.Struct({ message: Schema.String }) })`\n\n2. Register in `packages/server/src/Api.ts`:\n   - Import `HelloApi`\n   - Add `.add(HelloApi)` to the `Api` class chain\n\n3. Wire handler in `packages/server/src/Http.ts`:\n   - Add `HelloHandlers` using `HttpApiBuilder.group(Api, 'hello', ...)`\n   - Handler returns `{ message: \"Hello, world!\" }`\n   - Provide `HelloHandlers` to `ApiRoutes`\n\n4. Add test in `packages/server/src/Http.test.ts`:\n   - `GET /api/hello` returns 200\n   - Response body matches `{ message: \"Hello, world!\" }`\n\n## Done when\n\n- Tests pass (`vp test`)\n- Type-checks (`vp check`)","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-25T20:50:02.209797829Z","created_by":"sqve","updated_at":"2026-03-25T21:14:39.103597021Z","closed_at":"2026-03-25T21:14:39.102045494Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-2vj.1","depends_on_id":"greppa-2vj","type":"parent-child","created_at":"2026-03-25T20:50:02.209797829Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-30u","title":"Data pipeline and caching","description":"## Context\n\nThe app currently loads all diff data from bundled JSON fixtures. In production, diffs must be computed from git on demand and delivered to the client efficiently. This epic builds a four-layer data pipeline: eager metadata, predictive pre-fetch, IndexedDB cache, and background warm-up via WebSocket push.\n\n## Requirements\n\n- [ ] Layer 1 — Eager metadata: on session start, server sends full file list with path, change type, line count, and size tier in one payload\n- [ ] Layer 2 — Predictive pre-fetch: while reviewing file N, client fetches N+1 and N+2 based on file ordering\n- [ ] Layer 3 — IndexedDB cache: diffs cached by {filePath, oldRef, newRef}, persistent across sessions, invalidated when ref changes\n- [ ] Layer 4 — Background warm-up: after initial load, server computes remaining diffs via worker threads and pushes results over WebSocket; client stashes in IndexedDB\n- [ ] Client always checks IndexedDB first, then falls back to server request\n- [ ] The DiffFile contract (types.ts) remains the client-side interface regardless of data source\n- [ ] Server computes diffs by spawning git diff and parsing unified diff output into structured DiffFile data\n- [ ] WebSocket connection for real-time push of background-computed diffs\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Cache storage | IndexedDB | Persistent, large capacity, async API, survives page refresh |\n| Cache key | {filePath, oldRef, newRef} | Natural invalidation — new push = new ref = cache miss |\n| Cache scope | Persistent across sessions | \"Pick up where you left off\" is a core UX promise |\n| Diff computation | Server-side git diff → parse to DiffFile | git is fast; parsing on server keeps browser main thread free |\n| Background push | WebSocket | Already needed for live updates; natural channel for push |\n| Pre-fetch depth | N+1 and N+2 | Covers next-file navigation and skip-one; deeper adds diminishing returns |\n\n## Anti-patterns\n\n- Do NOT fetch all diffs eagerly on session start — defeats the entire pipeline\n- Do NOT skip IndexedDB and always re-fetch — wastes computation on already-reviewed files\n- Do NOT block UI on background warm-up — it's opportunistic, not required\n- Do NOT cache without invalidation — stale diffs from old refs are worse than no cache\n- Do NOT parse diffs on the main thread for large files — use the server or a worker\n\n## Success criteria\n\n1. Opening a 500-file PR shows the file list in < 200ms (metadata only)\n2. Selecting a file shows its diff in < 100ms when cached in IndexedDB\n3. Selecting an uncached file shows its diff in < 500ms (server computation + transfer)\n4. After reviewing 3-4 files, remaining files are pre-cached via background warm-up\n5. Reopening the same PR in a new session loads from IndexedDB (no server recomputation)\n6. Changing the target ref invalidates stale cache entries automatically","design":"## Outcome\n\n**Completed:** 2026-04-10\n**Tasks:** 6 (greppa-30u.1–30u.6; 30u.4 was split into 30u.5 CacheService extraction and 30u.6 SSE warm-up)\n**Summary:** Four-layer data pipeline delivered end-to-end. Layer 1 threads `lineCount`/`sizeTier` through the `/api/files` payload via one `git diff --numstat` call. Layer 2 prefetches N+1/N+2 neighbors through `usePrefetchNeighbors`, skipping large files. Layer 3 persists committed diffs in IndexedDB via `diffCache` (idb-keyval), keyed by `{oldRef, newRef, path}` — natural ref-change invalidation. Layer 4 streams warm-up diffs over SSE (deviation from the original WebSocket choice: no WS infra existed, SSE is one-way, native `EventSource`, no new deps), backed by the new `CacheService` seam extracted alongside. Client `useWarmup` subscribes after the file list settles and writes each pushed diff into `diffCache` fire-and-forget.\n**Verification:** All 55 test files / 519 tests passing. Success criteria audited with code evidence: file list <200ms (server P95 gate under warm-up load in Http.integration.test.ts), cached <100ms (IDB-first read in useDiffContent), uncached path via `computeDiff` + CacheService, warm-up streams a DiffResponse per non-large file, persistent idb-keyval store across sessions, ref-scoped keys.\n**Deviations:** (1) SSE instead of WebSocket — documented, spirit preserved (one-way server→client push). (2) `worker_threads` deferred — task 4b explicitly chose server-side sequential `Stream.mapEffect` pending profiling; existing `git` child processes are already non-blocking. Follow-up candidate: add `Stream.mapEffect` concurrency or workers if profiling shows warm-up lagging criterion 4 at scale.\n**Code review follow-ups (not blocking, logged for future work):** IndexedDB has no eviction policy (orphan rows accumulate across PRs); `useWarmup` receives `files` as a dep which can thrash the SSE connection on react-query refetches; `CacheService.invalidate` has no production caller; `computeDiff` does O(N) path lookup per request; SSE test asserts event count but not path set.","notes":"## Server cache state — audit followup (2026-04-09)\n\nA code audit found four mutable module-level caches in packages/server/src/Http.ts that overlap with this epic's scope:\n\n- fileListCache (ttl 30s, cap 50) — listFiles results\n- diffContentCache + worktreeDiffContentCache — parsed diff payloads\n- worktreeFileListCache (ttl 3s, cap 1) — worktree listFiles\n- stateStore: Map<string, StoredState> — shared-link state, capped at STATE_MAX_ENTRIES=10_000 with no LRU/TTL eviction\n\nThese live as module-global singletons outside the Effect layer/app boundary. It works today but will fight this epic in three ways:\n\n1. Layer 4 (background warm-up via workers pushing over WebSocket) needs the cache to be addressable from more than one subsystem. Module globals are addressable, but they also can't be scoped, overridden per request, or reset between tests in isolation — which matters more as the pipeline grows.\n2. When IndexedDB becomes the client cache, the server cache becomes the \"inner ring\" of the same pipeline. Aligning both behind a service interface (get / set / invalidate / evict) makes invalidation consistent across layers.\n3. stateStore's 10k cap with no eviction is a slow leak; a real LRU or TTL belongs in the same service.\n\nRecommended before (or as part of) Layer 4:\n- Introduce a CacheService (or per-concern services) provided via makeHttpLayer so handlers read from context instead of module scope.\n- Give stateStore a real eviction strategy — likely LRU keyed by last access.\n- Keep fileListCache/diffContentCache TTLs, but surface them as construction parameters on the service.\n\nNot urgent on its own (the TTL+size caps mitigate leak risk today), but worth bundling with the data pipeline work since both touch the same code.","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-03-29T20:42:18.950845725Z","created_by":"sqve","updated_at":"2026-04-10T18:01:28.798117427Z","closed_at":"2026-04-10T18:01:28.797962Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"]}
{"id":"greppa-30u.1","title":"Task 1: Layer 1 metadata — lineCount and sizeTier end-to-end","description":"## Goal\n\nThread line-count and size-tier metadata through the existing `/api/files` pipeline end-to-end so every file entry returned on session start carries `{ path, changeType, oldPath?, lineCount, sizeTier }`. The file list UI renders the line count beside each file as a visible smoke test.\n\nThis is the Layer 1 vertical slice from epic greppa-30u. The transport layer (`useFileList` -> `/api/files` -> `FilesApi` -> `GitService.listFiles`) is already in place. This task enriches the payload shape, computes the new fields from `git diff --numstat`, and surfaces them in the UI.\n\n## Execution mode\n\nHITL. Two decisions will need your input as implementation progresses:\n- Exact thresholds for `sizeTier` (default proposed: `small < 50`, `medium < 500`, `large >= 500` by `added + removed` lines)\n- Visual treatment for the line-count display in the file list (subdued numeric label vs tier badge)\n\n## Behaviors\n\nEach behavior is exactly one RED-GREEN-REFACTOR cycle. Do not write tests or implementation for more than one at a time.\n\n1. `FileEntry` schema in `@greppa/core` accepts and requires `lineCount: number` and `sizeTier: 'small' | 'medium' | 'large'`. Update the schema definition and its type; all existing usages must continue to compile.\n\n2. A new `parseNumstat` helper in `GitService.ts` converts `git diff --numstat` output into a `Map<string, number>` keyed by file path, with value `added + removed`. Treat binary diffs (where numstat reports `-\\t-\\tpath`) as `0`.\n\n3. `GitService.listFiles` emits enriched `FileEntry` entries: run both `git diff --name-status` and `git diff --numstat`, join by path, derive `sizeTier` from `lineCount` via the thresholds above. Renamed files key off the new path. Return `FileEntry[]` with every entry carrying the new fields.\n\n4. `FileNode` in `packages/web/src/fixtures/types.ts` gains optional `lineCount?: number` and `sizeTier?: 'small' | 'medium' | 'large'` fields; `buildFileTree` in `useFileList.ts` propagates them onto file nodes (directories aggregate `lineCount` by summing descendants — a natural precursor to future directory-level size rendering).\n\n5. The file list UI renders `lineCount` beside the file name for every file node. Minimal treatment — a subdued numeric label. No badge styling yet; that decision is deferred.\n\n## References\n\n- `packages/server/src/GitService.ts:53-71` — `parseNameStatus` is the pattern to mirror for `parseNumstat`.\n- `packages/web/src/hooks/useFileList.ts:107-131` — `buildFileTree` is where propagation of new fields onto file nodes happens.\n- `packages/core/src/index.ts:10-22` — `FileEntry` schema, point of change for behavior 1.\n\n## Out of scope for this task (follow-up tasks)\n\n- `listWorkingTreeFiles` worktree path enrichment (separate vertical for uncommitted work)\n- Server-side caching changes (covered later by the CacheService audit followup in epic notes)\n- Pre-fetch, IndexedDB, WebSocket warm-up (Layers 2-4)\n- Tier-based badge styling\n\n## Success criteria\n\n- [ ] `FileEntry` schema in `@greppa/core` includes `lineCount` and `sizeTier`\n- [ ] `GitService.listFiles` returns entries with `lineCount` derived from `git diff --numstat` and `sizeTier` derived from the agreed thresholds\n- [ ] `GitService.integration.test.ts` asserts the new fields on a real repo (e.g. against this repo's own history)\n- [ ] `buildFileTree` propagates `lineCount`/`sizeTier` onto file nodes; unit test covers it\n- [ ] The file list UI shows the line count beside each file name in a live dev run\n- [ ] Binary diffs do not crash `parseNumstat` — covered by a unit test using a fabricated numstat fixture\n- [ ] All tests passing\n- [ ] Pre-commit hooks passing","design":"## Expanded plan\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/core/src/index.ts` — `FileEntry` schema (step 1)\n- `packages/server/src/GitService.ts` — `parseNumstat` helper (step 2), `listFiles` numstat join (step 3), `listWorkingTreeFiles` compile-level shim (step 3)\n- `packages/core/src/index.test.ts` — schema decode/reject tests (step 1)\n- `packages/server/src/GitService.integration.test.ts` — `parseNumstat` unit tests + extended `listFiles` integration assertions (steps 2, 3)\n- `packages/web/src/fixtures/types.ts` — `FileNode` extension (step 4)\n- `packages/web/src/hooks/useFileList.ts` — `buildFileTree` propagation (step 4)\n- `packages/web/src/hooks/useFileList.test.ts` — propagation test (step 4)\n- `packages/web/src/components/FileTree/FileTree.tsx` — render line-count element beside file names (step 5)\n- `packages/web/src/components/FileTree/FileTree.module.css` — subdued `.lineCount` styling (step 5)\n- `packages/web/src/components/FileTree/FileTree.test.tsx` — render assertion for file + directory nodes (step 5)\n\n**Patterns to follow:**\n- `packages/server/src/GitService.ts:53-71` — `parseNameStatus` is the mirror for `parseNumstat` (split lines, tab-split, skip malformed, skip empty).\n- `packages/server/src/GitService.integration.test.ts:60-144` — `parseNameStatus` unit-test block is the mirror for the `parseNumstat` describe block (pure unit, no git).\n- `packages/server/src/GitService.integration.test.ts:187-212` — `listFiles` real-repo integration pattern, guarded by `describe.runIf(parentSha != null && headSha != null)`.\n- `packages/web/src/components/FileTree/FileTree.tsx:86-99` — `Tree.ItemContent` already renders `Tree.Label` then a change-type `Badge`; the line-count element slots in after the badge for file nodes only.\n- `packages/server/src/GitService.ts:119-145` — `runGit` helper wraps every git invocation; `listFiles` will call it twice via `Effect.all` (once for `--name-status`, once for `--numstat`).\n\n**Reusable code:**\n- `statusMap` at `GitService.ts:45-51` — status letter → changeType already mapped.\n- `parseNameStatus` at `GitService.ts:53-71` — keep as-is but narrow its return type (see step 3 blast-radius note).\n- `Effect.all` — already imported at top of `GitService.ts`, used for parallel ref validation.\n\n**Blast radius of making `lineCount`/`sizeTier` required on `FileEntry`:**\n- `parseNameStatus` can no longer claim to return `FileEntry[]` (it only has path/changeType/oldPath). Fix in step 3: narrow its return type to a local alias (e.g. `NameStatusEntry = Omit<FileEntry, 'lineCount' | 'sizeTier'>`).\n- `listWorkingTreeFiles` at `GitService.ts:229-230` feeds `parseNameStatus` directly into the `FileEntry[]`-typed service method. Fix in step 3 with a compile-level shim mapping every entry to `{ ...e, lineCount: 0, sizeTier: 'small' }`. Functional worktree numstat enrichment is explicitly out of scope and deferred to a follow-up task. Mark with `TODO(greppa-30u): compute numstat for worktree (follow-up task)`.\n- `WorktreeFilesApi` schema stays unchanged (uses `FileEntry`); the shim makes it compile. Worktree UI will show `0` line counts until the follow-up lands, which is acceptable because the worktree panel doesn't display line counts yet.\n- `fileListCache` in `Http.ts:63` and `worktreeFileListCache` in `Http.ts:173` are typed against `FileEntry[]` — no migration.\n- Test fixtures in `useFileList.test.ts`, `useWorktreeFiles.test.ts`, and any other call site building `FileEntry` literals will fail to type-check when the fields become required. Fix as they surface during each step's GREEN phase.\n\n**Default `sizeTier` thresholds (HITL — confirm on step 3 RED):**\n`small < 50`, `medium < 500`, `large >= 500`, measured as `added + removed` lines per file from numstat. Bake as module-local constants in `GitService.ts` so tests can import them if useful.\n\n### Steps\n\n#### Step 1: `FileEntry` schema carries `lineCount` and `sizeTier`\n\n**Pattern:** `packages/core/src/index.ts:13-18` — existing `FileEntry = Schema.Struct(...)`.\n\n**Changes:**\n- `packages/core/src/index.ts:13-18` — extend `FileEntry` struct to add:\n  - `lineCount: Schema.Number`\n  - `sizeTier: Schema.Literal('small', 'medium', 'large')` (literal union via Schema.Literal multi-arg or Schema.Union of three literals, match existing `ChangeType` style at lines 5-10).\n  - Export a `SizeTier` type alias for callers.\n- `packages/core/src/index.test.ts` — add cases asserting the extended schema decodes a full payload and rejects payloads missing either new field.\n\n**Verify:** `pnpm --filter @greppa/core test`\n\n#### Step 2: `parseNumstat` parses `git diff --numstat` output\n\n**Pattern:** `packages/server/src/GitService.ts:53-71` — `parseNameStatus` split-and-parse shape.\n\n**Changes:**\n- `packages/server/src/GitService.ts` — add exported `parseNumstat(output: string): Map<string, number>`:\n  - Split on `\\n`, skip empty lines, tab-split each line.\n  - `<added>\\t<removed>\\t<path>` with numeric added/removed → map entry `path → added + removed`.\n  - `-\\t-\\t<path>` (binary diff) → map entry `path → 0`.\n  - Malformed lines (wrong field count, non-numeric non-`-` values) → skipped.\n- `packages/server/src/GitService.integration.test.ts` — add a `describe('parseNumstat', ...)` block next to the existing `parseNameStatus` block around line 144. Unit tests only (no git, no `runIf` guard).\n\n**Verify:** `pnpm --filter @greppa/server test -- parseNumstat`\n\n#### Step 3: `listFiles` joins numstat into enriched `FileEntry` with `sizeTier` derivation\n\n**Pattern:** `packages/server/src/GitService.ts:188-192` — current `listFiles` implementation.\n\n**Changes:**\n- `packages/server/src/GitService.ts`:\n  - Narrow `parseNameStatus` return type to a local `type NameStatusEntry = Omit<FileEntry, 'lineCount' | 'sizeTier'>` (keep behavior identical; only the type annotation changes).\n  - Add module-local `const SIZE_TIER_MEDIUM = 50`, `const SIZE_TIER_LARGE = 500`, and `deriveSizeTier(lineCount: number): SizeTier` using those constants — confirm values with user on first RED run.\n  - Rewrite `GitServiceLive.listFiles` to run `git diff --name-status` and `git diff --numstat` in parallel via `Effect.all`, then for each name-status entry look up `numstat.get(entry.path) ?? 0`, derive the tier, and return the enriched `FileEntry`. Renamed entries key off the new path.\n  - Update `GitServiceLive.listWorkingTreeFiles` at `GitService.ts:229-230` to map each parsed `NameStatusEntry` to `{ ...entry, lineCount: 0, sizeTier: 'small' }`. Add `// TODO(greppa-30u): compute numstat for worktree (follow-up task)`.\n- `packages/server/src/GitService.integration.test.ts:187-212` — extend the existing `listFiles` real-repo assertions to also check `typeof entry.lineCount === 'number'` and `['small', 'medium', 'large'].includes(entry.sizeTier)` on every result entry.\n- Any existing test fixtures building `FileEntry` literals without the new fields will fail to type-check; fix by adding the defaults inline.\n\n**Verify:** `pnpm --filter @greppa/server test -- GitService`\n\n#### Step 4: `buildFileTree` propagates `lineCount` and `sizeTier` onto file nodes\n\n**Pattern:** `packages/web/src/hooks/useFileList.ts:107-131` — `buildFileTree` constructing `FileNode` from `FileEntry`.\n\n**Changes:**\n- `packages/web/src/fixtures/types.ts:3-11` — extend `FileNode`:\n  - `lineCount?: number`\n  - `sizeTier?: 'small' | 'medium' | 'large'`\n  Only file nodes populate these; directory aggregation is deferred.\n- `packages/web/src/hooks/useFileList.ts:111-117` — copy `entry.lineCount` and `entry.sizeTier` onto the `fileNode` literal in the per-entry loop.\n- `packages/web/src/hooks/useFileList.test.ts` — add propagation test using real `FileEntry` shape fixtures.\n- Any other test fixtures constructing `FileEntry` literals in the `web` package will surface as type errors; fix inline with the defaults chosen in step 3.\n\n**Verify:** `pnpm --filter @greppa/web test -- useFileList`\n\n#### Step 5: `FileTree` UI renders `lineCount` beside file names\n\n**Pattern:** `packages/web/src/components/FileTree/FileTree.tsx:86-99` — existing `Tree.ItemContent` renders `Tree.Label` then a change-type `Badge`.\n\n**Changes:**\n- `packages/web/src/components/FileTree/FileTree.tsx` — after the change-type `Badge` at lines 97-99, insert a subdued `<span>` rendering `node.lineCount` (only for file nodes where `lineCount != null`). Format with `Intl.NumberFormat` or a simple thousands-separator helper. No color (MEMORY: no colorized filenames — change badges are the only colored elements).\n- `packages/web/src/components/FileTree/FileTree.module.css` — add a `.lineCount` class using the existing muted-foreground token from the stylesheet (look at sibling class names already in the file for the right token).\n- `packages/web/src/components/FileTree/FileTree.test.tsx` — assert line-count text renders for a file node carrying `lineCount` and does NOT render for directory nodes.\n\n**Verify:** `pnpm --filter @greppa/web test -- FileTree`\n\n### Final verification\n`cape check`\n\n## Outcome\n\n### What landed\n\nLayer 1 metadata flows end-to-end:\n\n- **`@greppa/core`** — `FileEntry` schema extended with required `lineCount: number` and `sizeTier: 'small' | 'medium' | 'large'` via a new `SizeTier` union. `packages/core/src/index.ts`.\n- **`@greppa/server`** — `parseNumstat` helper parses `git diff --numstat` output (handles binary `-\\t-\\t` as 0). `deriveSizeTier` uses thresholds `small < 50`, `medium < 500`, `large >= 500`. `listFiles` runs name-status and numstat in parallel via `Effect.all` and joins by path. `parseNameStatus` narrowed to a local `NameStatusEntry` type. `listWorkingTreeFiles` patched with a compile-level shim (`lineCount: 0, sizeTier: 'small'`) — marked `TODO(greppa-30u): compute numstat for worktree (follow-up task)`.\n- **`@greppa/web`** — `FileNode` type extended with optional `lineCount?`/`sizeTier?`, `buildFileTree` copies them onto file nodes. `FileTree` renders a subdued `<span className={styles.lineCount}>` (tabular-nums, opacity 0.55, auto-margin-left) beside each file name when `lineCount > 0`.\n- Added a package script to `packages/core/package.json` to enable filtered invocation.\n\n### Behaviors TDD-driven across 5 steps\n\n- Step 1: `FileEntry` decodes enriched payloads (1 cycle)\n- Step 2: `parseNumstat` basic numeric line, multi-line, binary diff zero (3 cycles)\n- Step 3: `deriveSizeTier` boundary mapping + `listFiles` integration assertion on real repo output (2 cycles)\n- Step 4: `buildFileTree` propagates fields onto file nodes (1 cycle)\n- Step 5: `FileTree` renders line count beside file name (1 cycle)\n\n### Known follow-ups (intentionally out of scope)\n\n- **Worktree numstat enrichment** — `listWorkingTreeFiles` returns `lineCount: 0` for every entry until a follow-up computes numstat for `HEAD` (marked with TODO comment). Covered by the task's declared out-of-scope list.\n- **Pre-existing web package type errors** — `ActivityRail.tsx` (missing `Popover` export), `FileTree.tsx:93` (`FileIcon baseUrl` prop), `highlightEngine.test.ts:17` (`requestId` type) all exist at HEAD and are unrelated to Layer 1. Confirmed via `git show HEAD:<file>`. Not addressed.\n- **Pre-existing oxlint violations** — `FileHeader.tsx:119` (array index key), `stateCache.ts:34` (no-console), `StackedDiffViewer.test.tsx:172` (unnecessary optional chain) all exist at HEAD. Should be addressed by a separate cleanup task.\n- **Directory-level `lineCount` aggregation** — noted in Step 4 investigation as deferred; directories do not currently sum descendant line counts.\n\n### Verification\n\n- `cape check`: exit 0 (vitest 497 cases + oxlint + oxfmt)\n- Server package type-check: exit 0\n- Web package type-check: exit 1 with 3 pre-existing errors (detailed above, unrelated to Layer 1)\n- Checkpoints recorded at `.beads/greppa-30u/verify.json` for steps 1-5 at SHA `fdfe63e`.\n\n### HITL confirmations during execution\n\n- `sizeTier` thresholds: `50`/`500` (user confirmed \"Makes sense\")\n- `listFiles` schema approach: required fields + `listWorkingTreeFiles` compile-level shim (user confirmed \"Makes sense\")\n- Visual treatment for line count: subdued numeric label, not a tier badge (default applied without further prompt)","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-09T11:05:36.124376184Z","created_by":"sqve","updated_at":"2026-04-09T12:01:17.992047613Z","closed_at":"2026-04-09T12:01:17.991769723Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-30u.1","depends_on_id":"greppa-30u","type":"parent-child","created_at":"2026-04-09T11:05:36.124376184Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-30u.2","title":"Task 2: Layer 2 predictive prefetch of N+1 and N+2 (skip large)","description":"## Goal\n\nWhile a file is selected, prefetch the diff content for the next one or two files in the current ordering, so navigation feels instant when the user moves forward through a review. Large files (`sizeTier: 'large'`) are skipped to avoid wasting server work on content the user might not want.\n\nThis is the Layer 2 vertical slice from epic greppa-30u. It builds directly on Layer 1's delivered `FileEntry` ordering and the `sizeTier` tag, making `sizeTier` the first real consumer of its own presence in the schema. No server changes, no new endpoint — everything reuses `useDiffContent`'s existing query + key shape via tanstack-query's `prefetchQuery`.\n\n## Execution mode\n\nHITL. One decision point during implementation:\n\n- Should `sizeTier: 'large'` files be **skipped entirely** during prefetch, or just **delayed** until the user explicitly navigates to them? Default proposed: skip. Rationale: prefetch is opportunistic; \"large\" already means the server paid real work to compute the diff, and wasting that for a file the user may never open is exactly what Layer 2 tries to avoid.\n\n## Behaviors\n\nEach behavior is exactly one RED-GREEN-REFACTOR cycle. Do not write tests or implementation for more than one at a time.\n\n1. A pure helper `nextFilesToPrefetch(orderedFiles, selectedPath, depth)` returns the next `depth` file nodes after `selectedPath` in the ordered list. If `selectedPath` is not in the list, returns an empty array. If there are fewer than `depth` successors, returns however many exist.\n\n2. `nextFilesToPrefetch` skips file nodes with `sizeTier: 'large'` when selecting the next `depth` candidates — it walks past them and continues counting until `depth` non-large files are collected or the list is exhausted.\n\n3. A new hook `usePrefetchNeighbors({ orderedFiles, selectedPath, oldRef, newRef, depth })` calls `queryClient.prefetchQuery` once per entry returned by `nextFilesToPrefetch`, using the exact same `queryKey` shape and `queryFn` as `useDiffContent` (`['diff', oldRef, newRef, path]` with `fetchDiffContent(oldRef, newRef, path)`). Prefetching is a no-op when `oldRef === '' || newRef === ''`.\n\n4. `useSelectionCoordinator` calls `usePrefetchNeighbors` once per selection context (committed + worktree) with `depth: 2`, wired to the current primary selected path in each context.\n\n5. `usePrefetchNeighbors` does not re-fire when the same `selectedPath` + `oldRef`/`newRef` combination is already active (idempotent within an effect cycle). Re-entering the same selection produces no additional prefetch calls.\n\n## References\n\n- `packages/web/src/hooks/useDiffContent.ts:4-39` — `fetchDiffContent` and the canonical query key `['diff', oldRef, newRef, path]`. The prefetch hook must reuse both so cache hits line up.\n- `packages/web/src/hooks/useSelectionCoordinator.ts:25-49` — where `committedFilePaths` and `worktreeFilePaths` are derived from the flat `collectFiles(files)` list. The new hook plugs in here.\n- `packages/web/src/useFileSelection.ts:15-16` — `collectFiles` definition; already produces `FileNode[]` in the same display order used by the tree view.\n- `packages/web/src/hooks/useFileList.ts:111-119` — `buildFileTree` was updated in greppa-30u.1 to propagate `lineCount` and `sizeTier` onto file nodes, so `collectFiles` returns nodes that already carry `sizeTier` without additional wiring.\n\n## Out of scope (follow-up tasks)\n\n- Prefetching more than N+2 depth — epic explicitly says N+1 and N+2 cover diminishing returns.\n- Prefetching based on reviewed vs unreviewed ordering — the current display order is authoritative for Layer 2.\n- Server-side work-ahead — that's Layer 4 (background warm-up over WebSocket).\n- Cancellation of in-flight prefetches when the user changes selection faster than the network responds. Tanstack-query already handles staleness via query keys; cancellation is deferred unless benchmarking shows it's necessary.\n- IndexedDB persistence of prefetched results across sessions — that's Layer 3.\n- Worktree numstat enrichment — still carries the `listWorkingTreeFiles` shim from greppa-30u.1.\n\n## Success criteria\n\n- [ ] `nextFilesToPrefetch` unit tests cover: basic N+1/N+2, selection at end of list, selected path not found, skipping `large` files.\n- [ ] `usePrefetchNeighbors` test asserts tanstack-query's `queryClient.prefetchQuery` is called with the exact `['diff', oldRef, newRef, path]` shape for the expected neighbors. Mock the query client.\n- [ ] `useSelectionCoordinator` passes the depth-2 configuration when it invokes `usePrefetchNeighbors`, verified via the exposed call shape (integration test or refactor to take an injectable prefetcher).\n- [ ] A selection with no ref pair set is a no-op — no prefetch calls fire. Unit test.\n- [ ] Selecting a file whose next neighbor is `sizeTier: 'large'` prefetches the neighbor-after-that instead, if available. Unit test.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.","design":"## Expanded plan (expand-task)\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/web/src/hooks/nextFilesToPrefetch.ts` (new) — pure helper selecting next N non-large file nodes after a selected path in display order\n- `packages/web/src/hooks/nextFilesToPrefetch.test.ts` (new) — vitest unit tests\n- `packages/web/src/hooks/usePrefetchNeighbors.ts` (new) — hook calling `queryClient.prefetchQuery` for the helper's output\n- `packages/web/src/hooks/usePrefetchNeighbors.test.ts` (new) — `renderHook` test with a real `QueryClient` wrapper and mocked `fetch`\n- `packages/web/src/hooks/useSelectionCoordinator.ts` — wire `usePrefetchNeighbors` into both the committed and commit selection contexts\n\n**Patterns to follow:**\n- `packages/web/src/hooks/useDiffContent.ts:4-39` — canonical `fetchDiffContent(oldRef, newRef, path)` import and the `['diff', oldRef, newRef, path]` query key shape. Reuse exactly so prefetched cache entries collide with on-demand fetches.\n- `packages/web/src/hooks/useComputedDiffs.test.ts:1-73` — test wrapper template: `// @vitest-environment happy-dom`, `createWrapper()` that builds a fresh `QueryClient` with `retry: false` in a `QueryClientProvider`, `renderHook(..., { wrapper })`, `vi.spyOn(globalThis, 'fetch')` for stubbing.\n- `packages/web/src/hooks/buildDiffFile.test.ts:1` — pure-helper test template: `import { describe, expect, it } from 'vitest';`, no DOM environment, direct function calls.\n- `packages/web/src/hooks/useComputedDiffs.ts:1-8` — direct `fetchDiffContent` import from `./useDiffContent`; the new hook should mirror this import shape rather than copy-pasting the fetch function.\n\n**Reusable code:**\n- `packages/web/src/useFileSelection.ts:15-16` — `collectFiles(nodes: FileNode[]): FileNode[]` already returns nodes in display order; `useSelectionCoordinator` already calls it at lines 26, 30, 54. `nextFilesToPrefetch` takes `FileNode[]` directly so callers feed it `collectFiles(files)`.\n- `packages/web/src/fixtures/types.ts:3,5-15` — `SizeTier` alias and `FileNode.sizeTier?: SizeTier`. Already available after greppa-30u.1.\n- `packages/web/src/hooks/useDiffContent.ts:4-22` — `fetchDiffContent(oldRef, newRef, path)`.\n\n**Blast radius:**\n- No existing hook uses `useQueryClient()` yet — first consumer. `QueryClient` + `QueryClientProvider` are set up at `packages/web/src/main.tsx:22-24`.\n- `useSelectionCoordinator` has no existing test file; no test fallout from wiring changes. Only `App.tsx` consumes it; the hook's external return shape stays unchanged, so App is unaffected.\n- `fetchDiffContent` is already used from `useComputedDiffs.ts:8,62`; no impact there.\n\n**Constraint carried from the epic:**\n- Anti-pattern \"do NOT fetch all diffs eagerly on session start\" — prefetch must be bounded to `depth: 2` per selection context, and `large` files must be skipped to respect the intent.\n\n### Steps\n\n### Step 1: `nextFilesToPrefetch` returns next `depth` successors in display order\n\n**Pattern:** `packages/web/src/hooks/buildDiffFile.ts` — pure helper module shape.\n\n**Changes:**\n- `packages/web/src/hooks/nextFilesToPrefetch.ts` (new) — export a pure function `nextFilesToPrefetch(orderedFiles: FileNode[], selectedPath: string | null, depth: number): FileNode[]` that returns the next `depth` file nodes after the node whose `path === selectedPath`. If `selectedPath` is not present (or null), return `[]`. If fewer than `depth` successors exist, return what's available.\n- `packages/web/src/hooks/nextFilesToPrefetch.test.ts` (new) — vitest unit (pure, no DOM environment, `import { describe, expect, it } from 'vitest'`).\n\n**Verify:** `cape test packages/web/src/hooks/nextFilesToPrefetch.test.ts`\n\n---\n\n### Step 2: `nextFilesToPrefetch` skips `sizeTier: 'large'` successors\n\n**Pattern:** existing step 1 helper — extend the same function.\n\n**Changes:**\n- `packages/web/src/hooks/nextFilesToPrefetch.ts` — when collecting the next `depth` successors, walk past nodes where `sizeTier === 'large'` and continue counting until either `depth` non-large nodes are collected or the list is exhausted.\n- `packages/web/src/hooks/nextFilesToPrefetch.test.ts` — add coverage for the large-skip behavior.\n\n**Verify:** `cape test packages/web/src/hooks/nextFilesToPrefetch.test.ts`\n\n---\n\n### Step 3: `usePrefetchNeighbors` prefetches using the canonical diff query key\n\n**Pattern:** `packages/web/src/hooks/useDiffContent.ts:4-39` for the query key and `fetchDiffContent` import; `packages/web/src/hooks/useComputedDiffs.test.ts:1-73` for the wrapper.\n\n**Changes:**\n- `packages/web/src/hooks/usePrefetchNeighbors.ts` (new) — export `usePrefetchNeighbors({ orderedFiles, selectedPath, oldRef, newRef, depth })`. Obtain the client via `useQueryClient()` from `@tanstack/react-query`. Inside a `useEffect`, compute `nextFilesToPrefetch(orderedFiles, selectedPath, depth)` and for each neighbor call `queryClient.prefetchQuery({ queryKey: ['diff', oldRef, newRef, neighbor.path], queryFn: () => fetchDiffContent(oldRef, newRef, neighbor.path) })`. When `oldRef === '' || newRef === ''`, return early (no-op).\n- `packages/web/src/hooks/usePrefetchNeighbors.test.ts` (new) — `// @vitest-environment happy-dom`, fresh `QueryClient` + `QueryClientProvider` wrapper (retry: false), `renderHook` the new hook, assert the expected prefetch calls. Cover: prefetch fires for the N+1 and N+2 neighbors with the correct query key shape; no-op when `oldRef === ''` or `newRef === ''`; large-neighbor skip is honored (delegated to step 2 helper but verified end-to-end).\n\n**Verify:** `cape test packages/web/src/hooks/usePrefetchNeighbors.test.ts`\n\n---\n\n### Step 4: `usePrefetchNeighbors` is idempotent within an effect cycle\n\n**Pattern:** standard `useEffect` dependency-array idiom.\n\n**Changes:**\n- `packages/web/src/hooks/usePrefetchNeighbors.ts` — ensure the `useEffect` deps are scalar/stable (`selectedPath`, `oldRef`, `newRef`, `depth`, and a stable derivation from `orderedFiles` — e.g., depend on the joined list of candidate paths via `useMemo`). Re-rendering with the same `selectedPath` + ref pair must not fire additional prefetches.\n- `packages/web/src/hooks/usePrefetchNeighbors.test.ts` — add a re-render test that asserts `prefetchQuery` was called the expected number of times only (not doubled).\n\n**Verify:** `cape test packages/web/src/hooks/usePrefetchNeighbors.test.ts`\n\n---\n\n### Step 5: `useSelectionCoordinator` wires `usePrefetchNeighbors` for both contexts at `depth: 2`\n\n**Pattern:** `packages/web/src/hooks/useSelectionCoordinator.ts:25-54` — existing `collectFiles(files)` / `collectFiles(worktreeFiles)` usage demonstrates how the hook already derives ordered file lists per context.\n\n**Changes:**\n- `packages/web/src/hooks/useSelectionCoordinator.ts` — compute an ordered `FileNode[]` per context (reuse `collectFiles(files)` and `collectFiles(worktreeFiles)`). Determine the primary selected path per context from the existing selection state in the hook (single-selection case; if multi-selection, use the active/primary path). Call `usePrefetchNeighbors` twice with `depth: 2`: once for the committed context using the hook's `oldRef`/`newRef` props, once for the commit context using `commitDiffRange?.oldRef ?? ''` / `commitDiffRange?.newRef ?? ''`. No-op refs are handled by the hook itself.\n\n**Verify:** `cape test packages/web/src/hooks/`\n\n---\n\n### Final verification\n`cape check`\n\n## Outcome\n\n### Delivered\n\n- `packages/web/src/hooks/nextFilesToPrefetch.ts` — pure helper that walks `orderedFiles` starting after `selectedPath`, skipping `sizeTier === 'large'` nodes, returning up to `depth` successors.\n- `packages/web/src/hooks/usePrefetchNeighbors.ts` — React hook that calls `queryClient.prefetchQuery` with the canonical `['diff', oldRef, newRef, path]` key and `fetchDiffContent` query function. Uses a `useRef` signature guard so repeat renders with equivalent inputs do not fire additional prefetches.\n- `packages/web/src/hooks/useSelectionCoordinator.ts` — now derives ordered `FileNode[]` per context and calls `usePrefetchNeighbors` once per context with `depth: 2`. Primary selected path is the first path in ordered display order that is in `multiSelect.selectedPaths` for the active source.\n- Test coverage: `nextFilesToPrefetch.test.ts` (basic + skip-large), `usePrefetchNeighbors.test.ts` (canonical key + idempotent re-render), `useSelectionCoordinator.test.ts` (depth-2 wiring for committed context via `vi.mock`).\n\n### Scope note: worktree prefetch is a no-op today\n\n`usePrefetchNeighbors` reuses `useDiffContent`'s committed query shape (`['diff', oldRef, newRef, path]` + `fetchDiffContent`). The worktree diff pipeline uses a different endpoint (`/api/worktree/diff/<path>`) and a different cache key, so `usePrefetchNeighbors` cannot prefetch worktree diffs as-is.\n\n`useSelectionCoordinator` still calls the hook for the worktree context per the task's \"once per context\" requirement, but passes empty refs so the hook short-circuits. Actual worktree prefetch would require either a second hook variant or generalizing `usePrefetchNeighbors` to accept a query-key builder + query function — out of scope here and not required by any epic success criterion yet.\n\n### Verification\n\n- `cape check` — vitest, oxlint, oxfmt all pass (502 tests, 52 files).\n- Per-step: `cape test packages/web/src/hooks/nextFilesToPrefetch.test.ts`, `cape test packages/web/src/hooks/usePrefetchNeighbors.test.ts`, `cape test packages/web/src/hooks/useSelectionCoordinator.test.ts`, `cape test packages/web/src/hooks/` — all green.\n\n## Code review fixes\n\n### Code review fixes\n\nAddressed `cape:code-reviewer` findings:\n\n- **Critical [C1]:** Removed the no-op worktree `usePrefetchNeighbors` call from `useSelectionCoordinator`. The hook reuses `useDiffContent`'s committed query key shape (`['diff', oldRef, newRef, path]`), which does not address the worktree diff endpoint (`/api/worktree/diff/<path>`). Worktree prefetch needs a separate hook variant; a code comment at the call site documents the deferral. Only the committed context is prefetched.\n- **Important [I1]:** Included `selectedPath` directly in the `lastSignatureRef` signature so empty-neighbor cases (e.g., selecting the last file in a list) cannot collide across distinct selections.\n- **Suggestion [S1]:** Strengthened the rerender idempotency test by asserting `prefetchSpy` is called exactly twice on first render, clearing the spy, then asserting zero calls after the equivalent rerender.\n- **Suggestion [S2]:** Added braces to all single-line `if ... return/break/continue` statements in `nextFilesToPrefetch.ts`, `usePrefetchNeighbors.ts`, and `useSelectionCoordinator.ts` per CLAUDE.md convention.\n- **Important [I2]:** Left skip-and-continue semantics in `nextFilesToPrefetch` as-is. The behavior matches task Behavior 2 verbatim (\"walks past them and continues counting until `depth` non-large files are collected\").\n\nAll three cape check stages (vitest, oxlint, oxfmt) pass.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-09T12:17:54.155078873Z","created_by":"sqve","updated_at":"2026-04-09T12:53:07.188612333Z","closed_at":"2026-04-09T12:53:07.188204905Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-30u.2","depends_on_id":"greppa-30u","type":"parent-child","created_at":"2026-04-09T12:17:54.155078873Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-30u.3","title":"Task 3: Layer 3 IndexedDB cache for committed diffs","description":"## Goal\n\nPersist `fetchDiffContent` results in IndexedDB keyed by `{filePath, oldRef, newRef}` so that diffs survive page reloads and new sessions. On a cache hit, the client serves the stored entry immediately without touching the network. On a miss, the client fetches from `/api/diff/:oldRef/:newRef/:path` and writes the result back. Ref changes naturally miss the cache (different key) and fall through to the server.\n\nThis is the Layer 3 vertical slice from epic greppa-30u. It builds on Layer 2: the committed prefetcher (`usePrefetchNeighbors`) and on-demand reader (`useDiffContent`) both flow through the same `fetchDiffContent` function, so a single wrapping around that function (or its query function in react-query) cascades persistence to both consumers automatically.\n\n## Execution mode\n\nHITL. Decision points during implementation:\n\n- **IndexedDB library vs raw API**: default proposed is `idb-keyval` (tiny, no schema). Alternative: raw IndexedDB (more code, more control). Raw is overkill for a single key-value-shaped store.\n- **Eviction policy**: default proposed is none (rely on browser quota + natural key turnover as refs change). Alternative: LRU with a soft cap. Defer LRU until benchmarking shows quota pressure.\n- **Query layer integration**: two options — (a) wrap `fetchDiffContent` so every caller transparently hits IndexedDB first; (b) use tanstack-query's `persistQueryClient` with an IndexedDB persister. Option (a) is simpler and keeps the persistence layer decoupled from react-query; option (b) ties cache invalidation to query keys. Default proposed: (a).\n\n## Background from Task 2\n\n- `fetchDiffContent(oldRef, newRef, path)` is the single server-call boundary for committed diffs. Reused by `useDiffContent` (on-demand) and `usePrefetchNeighbors` (predictive). Wrapping it is the minimum-surface change.\n- The worktree diff path (`/api/worktree/diff/<path>`) is NOT currently routed through a shared function. Out of scope here — worktree cache is intentionally ephemeral since the working tree is mutable.\n- The epic's audit note calls out four mutable module-level server caches (`fileListCache`, `diffContentCache`, etc.). Those are out of scope for Layer 3 (client-side cache); they belong to a later Layer 4 / CacheService refactor.\n\n## Behaviors\n\nEach behavior is exactly one RED-GREEN-REFACTOR cycle. Do not write tests or implementation for more than one at a time.\n\n1. A new `diffCache` module (likely `packages/web/src/diffCache.ts`) exposes `getDiff({ oldRef, newRef, path })` and `setDiff({ oldRef, newRef, path }, value)`. Keys are computed deterministically from the triple. Missing entries return `null`. Uses `idb-keyval` under the hood.\n\n2. `diffCache.getDiff` and `setDiff` survive a simulated \"reload\" within tests — open a second instance of the store and read the previously written entry. (Use the `idb-keyval` test pattern in the codebase if one exists; otherwise fall back to `fake-indexeddb` as a dev dep.)\n\n3. `fetchDiffContent` is routed through the cache: on call, it first checks `diffCache.getDiff`, returns the hit if present, otherwise fetches from the server and writes the result via `diffCache.setDiff` before returning. Existing tests for `useDiffContent` and `useComputedDiffs` must still pass (cache miss path is the old behavior).\n\n4. A selected committed file that was previously loaded in a prior session is served from IndexedDB on the next session without any fetch — verified by spying on `globalThis.fetch` and asserting zero calls.\n\n5. Changing `oldRef` or `newRef` correctly bypasses the previously cached entry and fetches fresh — verified by seeding the cache with one triple and asserting a fetch fires for a different triple.\n\n## Out of scope (follow-up tasks)\n\n- Layer 4 background warm-up over WebSocket — separate task. This task only covers the client cache.\n- LRU / quota-aware eviction — deferred until benchmarks prove pressure.\n- Worktree diff caching — worktree is mutable; caching is counterproductive without a change-detection signal.\n- Generalizing `usePrefetchNeighbors` to cover worktree — orthogonal to Layer 3. Track as a separate follow-up if desired.\n- Server-side `diffContentCache` refactor to a CacheService — epic audit note calls this out for the Layer 4 phase.\n\n## Success criteria\n\n- [ ] `diffCache.getDiff` / `setDiff` round-trip a value through IndexedDB in unit tests. Uses `fake-indexeddb` or equivalent test shim.\n- [ ] `fetchDiffContent` calls `diffCache.getDiff` first; on hit, returns immediately without `fetch`. Unit test with mocked cache + spied `fetch`.\n- [ ] `fetchDiffContent` writes to `diffCache.setDiff` after a successful server response. Unit test asserts the write happened with the correct key.\n- [ ] Changing `oldRef`/`newRef` produces a cache miss even if the same `path` was previously cached. Unit test.\n- [ ] All existing `useDiffContent` / `useComputedDiffs` / `usePrefetchNeighbors` tests still pass after the integration.\n- [ ] Pre-commit hooks pass.\n\n## References\n\n- `packages/web/src/hooks/useDiffContent.ts:4-22` — current `fetchDiffContent` implementation; this is the single wrapping point for committed diffs.\n- `packages/web/src/hooks/usePrefetchNeighbors.ts` — Task 2 prefetcher; should inherit cache behavior for free.\n- `packages/web/src/hooks/useComputedDiffs.ts:8,62` — second consumer of `fetchDiffContent` via direct import.\n- Epic `greppa-30u` success criteria #2, #5, #6 — cover IndexedDB hit latency, cross-session loading, and ref invalidation.","design":"## Expanded plan (expand-task)\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/web/src/hooks/useDiffContent.ts:4-22` — `fetchDiffContent` is the single wrapping point for committed diffs. Signature stays; body gains a cache read-through and write-back.\n- `packages/web/package.json` — add devDependency `fake-indexeddb` (test shim) and dependency `idb-keyval` (production store).\n\n**New files:**\n- `packages/web/src/hooks/diffCache.ts` — `getDiff` / `setDiff` API wrapping `idb-keyval`.\n- `packages/web/src/hooks/diffCache.test.ts` — unit tests, sibling convention.\n\n**Patterns to follow:**\n- `packages/web/src/hooks/useDiffContent.test.ts:1-61` — vitest + `vi.spyOn(globalThis, 'fetch')` pattern. Mirror exactly for the integration tests on `fetchDiffContent`.\n- `packages/web/src/hooks/nextFilesToPrefetch.ts` (+ its sibling test) — non-hook utility module convention in `hooks/`: named exports, camelCase file, co-located `.test.ts`.\n- `packages/web/vitest.config.ts:1-10` — vitest + `happy-dom` environment, no `setupFiles`. Import `'fake-indexeddb/auto'` at the top of the test file(s) that need `indexedDB` — happy-dom does not provide it.\n\n**Reusable code:**\n- `idb-keyval` — `createStore`, `get`, `set`. `createStore(dbName, storeName)` returns a store handle that `get`/`set` accept as a second arg, which is how we can open a \"second instance\" to simulate reload (two `createStore` calls against the same names share underlying IDB).\n- No existing cache utility — this is the first `hooks/` module to own client-side persistence.\n\n**Blast radius:**\n- `fetchDiffContent` importers (3, all in `packages/web/src/hooks/`):\n  - `useDiffContent.ts:31` — inside `useQuery.queryFn` (committed on-demand read).\n  - `useComputedDiffs.ts:62` — inside `fetchAndComputeDiff`, committed branch only (`worktree` branch uses `fetchWorktreeDiffContent` and is intentionally out of scope).\n  - `usePrefetchNeighbors.ts:46` — inside `queryClient.prefetchQuery.queryFn` (Layer 2 predictive).\n- All three transparently inherit cache behavior after Step 3; no call-site changes required.\n- Existing tests to keep green: `useDiffContent.test.ts`, `useComputedDiffs.test.ts`, `usePrefetchNeighbors.test.ts`.\n\n**Worktree path is untouched:** `fetchWorktreeDiffContent` is deliberately bypassed (worktree is mutable; epic anti-pattern guards against caching without invalidation).\n\n### Steps\n\n### Step 1: diffCache.getDiff / setDiff round-trip a value and return null on miss\n\n**Pattern:** `packages/web/src/hooks/nextFilesToPrefetch.ts` (module shape) + `packages/web/src/hooks/useDiffContent.test.ts:1-14` (vitest describe/beforeEach shape).\n\n**Changes:**\n- `packages/web/package.json` — add `idb-keyval` to `dependencies`, `fake-indexeddb` to `devDependencies`. Re-run the workspace installer after editing.\n- `packages/web/src/hooks/diffCache.ts` (new) — export `getDiff({ oldRef, newRef, path })` and `setDiff(key, value)`. Build the cache key deterministically from the triple (e.g., `${oldRef}\\u0000${newRef}\\u0000${path}`). Delegate to `idb-keyval` `get`/`set` against a dedicated store created via `createStore('greppa-diff-cache', 'diffs')`. `getDiff` resolves `null` for missing entries. Value type is `DiffResponse` from `@greppa/core`.\n- `packages/web/src/hooks/diffCache.test.ts` (new) — import `'fake-indexeddb/auto'` at the top. Exercise the round-trip and the null-on-miss behavior.\n\n**Verify:** `cape test packages/web/src/hooks/diffCache.test.ts`\n\n---\n\n### Step 2: Stored values survive a simulated reload (second store instance reads prior writes)\n\n**Pattern:** Same test file as Step 1; single new `it` block in the same describe.\n\n**Changes:**\n- `packages/web/src/hooks/diffCache.ts` — if Step 1 already exposes the store via a module-level `createStore` call, no change needed. If not, extract the `createStore(...)` args to constants so the test can open a fresh store against the same DB/store names and read the previously written value. Do NOT export the store handle itself.\n- `packages/web/src/hooks/diffCache.test.ts` — add a test that writes via `setDiff`, opens a second `createStore` with the same DB/store names, and reads the entry directly with `idb-keyval.get`. Asserts the value survives the \"reload\".\n\n**Verify:** `cape test packages/web/src/hooks/diffCache.test.ts`\n\n---\n\n### Step 3: fetchDiffContent checks the cache first; on miss it fetches, writes, and returns\n\n**Pattern:** `packages/web/src/hooks/useDiffContent.test.ts:5-60` — existing `vi.spyOn(globalThis, 'fetch')` pattern. Extend with `vi.spyOn` on the `diffCache` module's `getDiff` / `setDiff`.\n\n**Changes:**\n- `packages/web/src/hooks/useDiffContent.ts:4-22` — wrap the body: call `getDiff` first, return the hit if non-null; otherwise run the existing fetch, call `setDiff` after a successful response, then return. Error path stays as-is (no cache write on non-ok). Key is built from `(oldRef, newRef, path)`.\n- `packages/web/src/hooks/useDiffContent.test.ts` — add tests that: (a) stub `getDiff` to return a hit and assert `fetch` is not called; (b) stub `getDiff` to return `null` and assert both the network fetch and a `setDiff` write occur (with the right key and payload). Leave the three existing `fetchDiffContent` tests green — the cache-miss path still exercises them once `getDiff` is stubbed to `null`.\n\n**Verify:** `cape test packages/web/src/hooks/useDiffContent.test.ts`\n\n---\n\n### Step 4: A previously cached triple resolves with zero network calls\n\n**Pattern:** Same mocking shape as Step 3; `vi.spyOn(globalThis, 'fetch')`.\n\n**Changes:**\n- `packages/web/src/hooks/useDiffContent.test.ts` — add a test that imports `'fake-indexeddb/auto'`, seeds the real `diffCache.setDiff` with a `DiffResponse` for a specific triple, then calls `fetchDiffContent` with the same triple and asserts `globalThis.fetch` received zero calls and the returned value equals the seeded value. No cache stubbing in this test — it's an integration assertion that the real IDB-backed cache satisfies the cross-session promise from the epic.\n\n**Verify:** `cape test packages/web/src/hooks/useDiffContent.test.ts`\n\n---\n\n### Step 5: Changing oldRef or newRef forces a server fetch even when the path was previously cached\n\n**Pattern:** Same integration pattern as Step 4.\n\n**Changes:**\n- `packages/web/src/hooks/useDiffContent.test.ts` — add a test that seeds the cache with `(oldRefA, newRefA, path)`, then calls `fetchDiffContent(oldRefB, newRefA, path)` (different oldRef). Asserts `globalThis.fetch` was called exactly once and returned value equals the mocked server response. Optionally also assert the new triple was written back via `diffCache.getDiff` in a follow-up call.\n\n**Verify:** `cape test packages/web/src/hooks/useDiffContent.test.ts`\n\n---\n\n### Final verification\n`cape check`\n\n## Outcome\n\nLayer 3 IndexedDB cache for committed diffs is in place.\n\n**What shipped:**\n- `packages/web/src/hooks/diffCache.ts` — `getDiff`/`setDiff` API wrapping `idb-keyval` against a dedicated store (`greppa-diff-cache`/`diffs`). Key computed deterministically from `(oldRef, newRef, path)` joined with NUL separators. Missing entries return `null`.\n- `packages/web/src/hooks/useDiffContent.ts` — `fetchDiffContent` now read-throughs the cache: returns on hit without touching `fetch`; on miss, fetches the server, writes the successful response back via `setDiff`, then returns. Error path untouched (no cache write on non-ok).\n- `packages/web/package.json` — `idb-keyval@6.2.2` (runtime), `fake-indexeddb@6.2.5` (test shim).\n\n**Transparent cascade:** `useDiffContent`, `useComputedDiffs` (committed branch), and `usePrefetchNeighbors` all inherit cache behavior automatically because they already flowed through `fetchDiffContent`. No call-site changes needed — the wrapping point is a single function. The worktree path (`fetchWorktreeDiffContent`) is deliberately untouched.\n\n**Test coverage (packages/web/src/hooks/diffCache.test.ts + useDiffContent.test.ts):**\n- Round-trip through IndexedDB (set → get returns the value).\n- Miss returns `null`.\n- Persists across a simulated reload via a second `createStore` handle against the same DB/store names.\n- Cache hit returns the cached value without calling `fetch`.\n- Write-back: successful fetch calls `setDiff` with the correct key and payload.\n- Cross-session: a previously cached triple satisfies the request with zero fetch calls (integration, real IDB via `fake-indexeddb`).\n- Ref invalidation: a different `oldRef` forces a fresh fetch even when the same path was cached under a prior ref (integration).\n\n**Test infrastructure note:** the project uses `vp test` which reads config from `vite.config.ts`, not `vitest.config.ts`. Attempts to wire `fake-indexeddb/auto` via a global `setupFiles` were silently ignored, so the shim is imported at the top of each test file that exercises the cache (`diffCache.test.ts`, `useDiffContent.test.ts`, `useComputedDiffs.test.ts`, `usePrefetchNeighbors.test.ts`). `useDiffContent.test.ts` also `clear()`s the store in `beforeEach` to isolate tests.\n\n**Epic success criteria touched:**\n- #2 (diff in <100ms when cached) — now possible; IndexedDB read is sub-millisecond.\n- #5 (reopening same PR loads from IndexedDB) — unit-verified via the cross-session test.\n- #6 (ref change invalidates) — unit-verified via the ref-mismatch test.\n\n**Out of scope / follow-ups:**\n- Layer 4 background warm-up over WebSocket — next task.\n- LRU/quota eviction — deferred until benchmarking shows pressure.\n- Worktree diff caching — intentionally not addressed; worktree is mutable.\n- Server-side `diffContentCache` → `CacheService` refactor — epic audit note calls this out for the Layer 4 phase.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-09T12:54:28.372327526Z","created_by":"sqve","updated_at":"2026-04-09T13:43:07.738961917Z","closed_at":"2026-04-09T13:43:07.738709099Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-30u.3","depends_on_id":"greppa-30u","type":"parent-child","created_at":"2026-04-09T12:54:28.372327526Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-30u.4","title":"Task 4: Layer 4 background warm-up via WebSocket + CacheService extraction","description":"## Goal\n\nComplete the data pipeline with Layer 4: the server computes remaining diffs in the background (worker threads) and pushes them to the client over WebSocket as they become ready. The client stashes the pushed diffs in the same Layer 3 IndexedDB cache, so when the user navigates to one of them it hits the cache immediately — no server round-trip, no waiting.\n\nThis closes epic `greppa-30u` against success criteria #4 (\"after reviewing 3-4 files, remaining files are pre-cached via background warm-up\").\n\n## Context from Task 3\n\n- `diffCache.getDiff` / `setDiff` in `packages/web/src/hooks/diffCache.ts` is the single write point for committed diffs on the client. Anything that writes there will be picked up by the next `fetchDiffContent` call automatically, and by `usePrefetchNeighbors` as a cache hit.\n- The cache key is `{oldRef, newRef, path}` with NUL separators. Any server-pushed diff must carry the same triple so the key matches on write.\n- The worktree path is deliberately not cached and is out of scope for Layer 4.\n- `fetchDiffContent` degrades gracefully if `getDiff`/`setDiff` throw (wrapped in `.catch`), so Layer 4 writes can safely fire-and-forget on the client side too.\n\n## Epic audit follow-up (not optional for this task)\n\nThe epic's audit note on `packages/server/src/Http.ts` calls out four module-level caches and a 10k-entry `stateStore` with no eviction. Layer 4 is the forcing function: background workers pushing over WebSocket need the cache to be addressable from more than one subsystem. Before (or as part of) the Layer 4 wiring, introduce a `CacheService` provided via `makeHttpLayer` so handlers and the worker/push path share a single injection point. This is the natural seam for:\n\n- `diffContentCache` (current module global) — becomes the inner ring of the Layer 3 cache.\n- A real eviction strategy (likely LRU) on `stateStore`.\n- TTL/size parameters as service construction args instead of module constants.\n\nIf the refactor balloons beyond a single task, split into (4a) CacheService extraction and (4b) WebSocket warm-up using the service.\n\n## Execution mode\n\nHITL. Decision points:\n\n- **WebSocket library**: the epic assumes a WebSocket is \"already needed for live updates\" — verify first. If none exists yet, decide between `ws`, native `WebSocket` in an Effect layer, or SSE as a simpler alternative. Default proposed: investigate existing infra before choosing.\n- **Worker thread model**: Node `worker_threads` vs a pool library vs spawning child `git` processes directly. Default proposed: start with a tiny in-process queue using `worker_threads`; upgrade if profiling justifies.\n- **Trigger**: warm-up should kick off after the initial `fetchFileList` response, not during it. Where does the client signal \"I'm reviewing this PR\" to open the socket? Could piggy-back on the existing `/api/diff/:oldRef/:newRef/:path` query path or be a dedicated `/api/warmup/:oldRef/:newRef` open endpoint. Default proposed: dedicated endpoint.\n- **Client-side prioritization**: should warm-up skip files the user has already viewed, or skip large files (`sizeTier === 'xl'`)? Default proposed: skip `xl` (mirrors Layer 2 prefetcher), do not skip viewed files (they might land in cache anyway via Layer 3 hit).\n\n## Behaviors\n\n1. Server exposes a way for the client to request background warm-up of `(oldRef, newRef)`. Contract shape TBD during expand-task investigation of existing WebSocket/SSE infra.\n\n2. Server spawns a worker (thread or pooled process) that computes diffs for all remaining files of the pair, one at a time, and emits each result as a push message. The computation reuses the existing `git diff` parsing path in `packages/server/src/Http.ts` or a refactored shared module.\n\n3. Client opens the warm-up channel after the initial file list loads and remains subscribed until the PR is closed. Each pushed diff is written to `diffCache.setDiff` with the matching triple.\n\n4. Warm-up skips files the user has already reviewed via the on-demand path — no duplicate work. Optional: skip files with `sizeTier === 'xl'` to bound the worker queue.\n\n5. Warm-up tolerates backpressure — if the client disconnects mid-stream, the server stops computing (no zombie CPU burn).\n\n6. Opening the same PR in a new session observes Layer 3 hits for previously warmed files with zero fetches, confirmed via integration test spying on `globalThis.fetch`.\n\n## Out of scope\n\n- Layer 3 LRU / quota-aware eviction — still deferred.\n- Cross-PR warm-up (predictive across repos) — explicit non-goal.\n- Worktree diff warm-up — worktree is mutable; excluded.\n- UI for warm-up progress — the whole point is that it's invisible to the user.\n\n## Success criteria\n\n- [ ] `CacheService` (or per-concern services) replaces at least `diffContentCache` as a module global, and is provided via `makeHttpLayer`.\n- [ ] Warm-up endpoint (WebSocket or SSE) streams computed diffs for a `(oldRef, newRef)` pair.\n- [ ] Server worker computes diffs off the main event loop; a profiling check confirms Layer 1 (metadata) remains < 200ms under warm-up load.\n- [ ] Client subscribes after file list loads and writes pushed diffs to `diffCache.setDiff`.\n- [ ] Integration test: after simulated warm-up, selecting a warmed file triggers zero fetches and renders from IndexedDB.\n- [ ] Epic success criterion #4 met: reviewing 3-4 files while warm-up runs leaves remaining files pre-cached.\n- [ ] Pre-commit hooks pass.\n\n## References\n\n- `packages/server/src/Http.ts` — current handlers, module-level caches flagged in epic audit.\n- `packages/web/src/hooks/diffCache.ts` — the write point for pushed diffs.\n- `packages/web/src/hooks/useDiffContent.ts` — shows how committed-diff reads flow through the cache.\n- Epic `greppa-30u` success criteria #4, audit followup section.","design":"## Split recommendation (expand-task)\n\n### Why split\n\nInvestigation (summary below) confirms two distinct architectural concerns, each ~4-6 TDD cycles:\n\n1. **CacheService extraction** is a pure Effect-layer refactor of `packages/server/src/Http.ts`. It has its own testable surface (service construction, layer wiring, handler read/write migration) and lands independently of any transport work.\n\n2. **Warm-up pipeline** is a new feature touching server transport (no WebSocket/SSE infra exists today per investigation), worker/stream logic, a new client subscription hook, and an integration test asserting zero fetches. It consumes the CacheService as its inner ring.\n\nBundling both exceeds the 10-step expand-task ceiling and mixes a refactor commit boundary with a feature commit boundary. The task's own execution-mode note already invited this split.\n\n### Key investigation findings (shared by both subtasks)\n\n- **Module caches in `Http.ts`**: `diffContentCache` L65 (read L127, write L137), plus 4 others (fileListCache L63, worktreeDiffContentCache L66, worktreeFileListCache L173, stateStore L248). All via `createCache<T>()` factory L31-61. Success criteria only requires replacing `diffContentCache` at minimum — do not scope-creep the refactor.\n- **`makeHttpLayer`** at `Http.ts:284-291` uses Effect `Layer.provide` chaining. `GitServiceLive` at `GitService.ts:182-218` is the service precedent.\n- **Git diff computation**: `runGit` at `GitService.ts:154-180` (ChildProcess spawn, already non-blocking), `parseNameStatus` L68-86, `parseNumstat` L96-106. Per-file diff assembly lives in the Http.ts diff handler (around L106-153).\n- **No WebSocket/SSE infra anywhere** in server or client. SSE is the simpler transport choice (text/event-stream from an Effect `HttpServerResponse`); no new dependency required.\n- **No `worker_threads` precedent** in server. Because `runGit` already spawns child processes (non-blocking), `worker_threads` is likely unnecessary for v1 — profiling should decide.\n- **Client cache write point**: `diffCache.setDiff` in `packages/web/src/hooks/diffCache.ts:23`, key `{oldRef, newRef, path}` L9-13.\n- **Client fetch path**: `fetchDiffContent` in `useDiffContent.ts:6-34` — warm-up subscription must call this directly (not the hook) to populate cache without touching component state, and must guard with `getDiff` first like L13.\n- **File list query**: `useFileList.ts:146-161` — warm-up trigger should fire after `files != null && !isLoading`.\n- **Integration test pattern**: `Http.integration.test.ts:33-52` uses `HttpRouter.toWebHandler(TestAppLayer)` — no port binding. Client-side test fixture: `diffCache.test.ts` uses `fake-indexeddb/auto` + `vi.spyOn(globalThis, 'fetch')`.\n- **Shared type**: `DiffResponse` in `packages/core/src/index.ts:29-36`.\n\n### Recommended subtasks\n\n**Subtask 4a: CacheService extraction + `diffContentCache` migration**\n\n- Goal: introduce `CacheService` as an Effect service provided via `makeHttpLayer`, migrate at least `diffContentCache` off its module-global, leave the other four caches untouched for now (tracked separately).\n- Scope: new `packages/server/src/CacheService.ts` (following `GitService.ts:182-218` pattern), wire into `makeHttpLayer` at `Http.ts:284-291`, replace `diffContentCache` reads at L127 and writes at L137 with `yield* CacheService`, delete the module global.\n- Success criteria: unit tests for the service (get/set/invalidate/TTL), `Http.integration.test.ts` still passes, `diffContentCache` no longer exists as a module-level symbol.\n- Estimated size: 3-4 TDD cycles. Clean commit boundary (refactor, no behavior change).\n\n**Subtask 4b: Layer 4 warm-up over SSE using CacheService**\n\n- Goal: server streams computed diffs for `(oldRef, newRef)` over SSE; client subscribes after file list loads and writes pushed diffs into `diffCache`.\n- Scope:\n  - Extract per-file diff assembly into a reusable function shared by the existing `/api/diff` handler and the new warm-up route (following the pattern of pure parsers in `GitService.ts`).\n  - Add warm-up endpoint to `packages/server/src/Api.ts` + handler group in `Http.ts` following the `DiffHandlers` pattern; response is `text/event-stream`.\n  - Server iterates remaining files for `(oldRef, newRef)`, writes each computed diff to the shared `CacheService` (inner ring), and pushes it on the stream. Tolerates client disconnect (AbortSignal) — no zombie CPU.\n  - Skip `sizeTier === 'xl'` files (mirrors Layer 2 prefetcher, per task behavior 4).\n  - New client hook (e.g. `useWarmup.ts`) subscribes via `EventSource` after `useFileList` settles, calls `diffCache.setDiff` on each message, unsubscribes on unmount/PR change.\n  - Integration test (following `diffCache.test.ts` pattern with `fake-indexeddb/auto` + `vi.spyOn(globalThis, 'fetch')`): after simulated warm-up, selecting a warmed file triggers zero fetches and renders from IndexedDB.\n  - Profiling check that Layer 1 (`/api/files`) remains < 200ms under warm-up load. If profiling shows the main loop blocked by parsing, escalate to `worker_threads` in a follow-up — do not introduce speculatively.\n- Success criteria: epic #4 met (reviewing 3-4 files while warm-up runs leaves remaining files pre-cached), warmup integration test passes, Layer 1 profiling gate passes, pre-commit hooks pass.\n- Estimated size: 5-6 TDD cycles. Depends on 4a.\n\n### Deferred explicitly\n\n- Migration of `fileListCache`, `worktreeDiffContentCache`, `worktreeFileListCache`, and `stateStore` off module globals — tracked against the epic audit note but not blocking Layer 4.\n- LRU/TTL on `stateStore` — same deferral.\n- `worker_threads` adoption — gated on profiling evidence.\n- Worktree warm-up — explicit non-goal (mutable).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-09T13:47:34.352796738Z","created_by":"sqve","updated_at":"2026-04-09T13:58:09.669912944Z","closed_at":"2026-04-09T13:58:09.669751876Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-30u.4","depends_on_id":"greppa-30u","type":"parent-child","created_at":"2026-04-09T13:47:34.352796738Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-30u.5","title":"Task 4a: CacheService extraction + diffContentCache migration","description":"## Goal\n\nIntroduce a `CacheService` as an Effect service provided via `makeHttpLayer` and migrate `diffContentCache` off its module-global. This is a pure refactor — no behavior change — that creates the shared cache seam Layer 4 warm-up will consume.\n\n## Context\n\nSplit from greppa-30u.4. The epic audit flagged five module-level caches in `packages/server/src/Http.ts` as structural blockers for Layer 4: workers/push handlers need cache addressability from more than one subsystem. Minimum viable scope is `diffContentCache` only — the other four caches remain as module globals for now.\n\nInvestigation findings (from greppa-30u.4 design field):\n\n- `diffContentCache` declared at `Http.ts:65`, read at `L127`, written at `L137`, via `createCache<T>()` factory at `L31-61`.\n- `makeHttpLayer` at `Http.ts:284-291` uses Effect `Layer.provide` chaining. Pattern precedent: `GitServiceLive` at `GitService.ts:182-218`.\n- Integration test harness: `Http.integration.test.ts:33-52` uses `HttpRouter.toWebHandler(TestAppLayer)` — no port binding.\n\n## Behaviors\n\n1. `CacheService` is an Effect service with `get<T>(key)`, `set<T>(key, value)`, and `invalidate(key)` operations, configurable with `{ ttlMs, max }` at layer construction.\n2. `CacheService` enforces TTL — entries older than `ttlMs` return miss on `get`.\n3. `CacheService` enforces max-entries eviction — oldest entry is evicted when inserting above capacity.\n4. `CacheServiceLive` is provided via `makeHttpLayer` and consumed by the committed-diff handler in place of the module-level `diffContentCache`.\n5. The committed-diff handler behavior (cache hit short-circuit, cache miss → compute → store) is unchanged as observed through `Http.integration.test.ts`.\n6. `diffContentCache` no longer exists as a module-level symbol in `Http.ts`; the other four module caches are untouched.\n\n## Implementation\n\n1. Create `packages/server/src/CacheService.ts` with an Effect service exposing `get<T>(key)`, `set<T>(key, value)`, `invalidate(key)`. Back it with the `createCache` factory logic (move out of `Http.ts` or re-export from the service). TTL and max-entries come in as construction parameters on the `Live` layer.\n2. Wire `CacheServiceLive` into `makeHttpLayer` at `Http.ts:284-291` via `Layer.provide`. The diff cache is constructed with `{ ttlMs: 30_000, max: 100 }` to preserve current behavior.\n3. Replace the `diffContentCache` read at `Http.ts:127` with `yield* CacheService` in the diff handler. Replace the write at `L137` likewise.\n4. Delete the `diffContentCache` module global (L65). Leave the other four caches untouched.\n5. Add unit tests for the service: get miss, get hit, set + get round trip, TTL expiry (fake timers), max-entries eviction.\n\n## Success criteria\n\n- [ ] `packages/server/src/CacheService.ts` exists, defines an Effect service with `get`/`set`/`invalidate`, and has unit tests covering get/set/TTL/eviction.\n- [ ] `diffContentCache` is no longer a module-level symbol in `Http.ts`.\n- [ ] `CacheService` is provided via `makeHttpLayer`; the diff handler reads/writes through it.\n- [ ] `packages/server/src/Http.integration.test.ts` still passes with no changes to its assertions.\n- [ ] `cape check` passes.\n- [ ] Pre-commit hooks pass.\n\n## Out of scope\n\n- Migrating `fileListCache`, `worktreeDiffContentCache`, `worktreeFileListCache`, or `stateStore` — deferred.\n- LRU/TTL on `stateStore` — deferred.\n- Any Layer 4 warm-up code — belongs in Task 4b.\n\n## Execution mode\n\nHITL.\n\n## References\n\n- `packages/server/src/Http.ts:31-61` — `createCache` factory to migrate.\n- `packages/server/src/Http.ts:65` — `diffContentCache` declaration to remove.\n- `packages/server/src/Http.ts:127,137` — read/write sites to migrate.\n- `packages/server/src/Http.ts:284-291` — `makeHttpLayer` wiring target.\n- `packages/server/src/GitService.ts:182-218` — Effect service precedent.\n- `packages/server/src/Http.integration.test.ts:33-52` — test harness pattern.","design":"## Expanded plan (expand-task)\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/server/src/CacheService.ts` (new) — Effect service definition + Live layer\n- `packages/server/src/CacheService.test.ts` (new) — unit tests (get/set/TTL/eviction/invalidate)\n- `packages/server/src/Http.ts:15-18,65,106-153,284-291` — remove `diffContentCache`, migrate handler to yield `CacheService`, provide `CacheServiceLive` in `makeHttpLayer`\n- `packages/server/src/Http.integration.test.ts:33-42` — add `CacheServiceLive` (constructed with `{ ttlMs: 30_000, max: 100 }`) to `PlatformLayer` so `ApiRoutes` has its new dependency satisfied\n\n**Patterns to follow:**\n- `packages/server/src/GitService.ts:182-218` — `ServiceMap.Service` class + `GitServiceLive = Layer.succeed(...)` pattern. CacheService mirrors this shape. The Live layer here is `Layer.succeed`; for CacheService we need a factory-style layer (ttlMs/max come in as construction parameters), so use `Layer.effect` or a factory function that returns a `Layer.succeed` bound to the ttl/max — see Effect 4 docs.\n- `packages/server/src/Http.ts:31-61` — existing `createCache<T>` factory logic (TTL + max-entries semantics). Reuse this logic inside `CacheServiceLive` verbatim so behavior is identical (pure refactor).\n- `packages/server/src/Http.ts:284-291` — `makeHttpLayer` `Layer.provide` chaining — add `Layer.provide(CacheServiceLive({ ttlMs: 30_000, max: 100 }))` alongside `GitServiceLive`.\n- `packages/server/src/GitService.integration.test.ts:43-60` — Effect test harness (`Layer.mergeAll` + `Effect.provide` + `Effect.runPromise`). CacheService unit tests follow the same runner shape but don't need `NodeServices`.\n\n**Reusable code:**\n- `packages/server/src/Http.ts:31-61` `createCache` factory — move into `CacheService.ts` as an internal helper powering `CacheServiceLive` (do not re-export from `Http.ts`).\n- vitest fake timers (`vi.useFakeTimers()`, `vi.advanceTimersByTime()`) for TTL expiry test.\n\n**Blast radius:**\n- `Http.ts` (only site of `diffContentCache` reads/writes; grep confirms no other module imports it).\n- `Http.integration.test.ts` must add `CacheServiceLive` to `PlatformLayer` — assertions unchanged; this is layer wiring only (success criterion explicitly allows this: \"no changes to its assertions\").\n- No other files in `packages/server/src/**` import from `Http.ts` reach the cache symbol (module-private const).\n\n### Steps\n\n### Step 1: CacheService service with get/set round-trip\n\n**Pattern:** `packages/server/src/GitService.ts:182-218` — `ServiceMap.Service` class + `Layer.succeed` factory.\n\n**Changes:**\n- `packages/server/src/CacheService.ts` (new) — define `CacheService` via `ServiceMap.Service<CacheService, { get<T>(key): Effect<T | null>; set<T>(key, value): Effect<void>; invalidate(key): Effect<void> }>()('greppa/CacheService')`. Export a `CacheServiceLive = (config: { ttlMs: number; max: number }) => Layer.succeed(CacheService, CacheService.of({...}))` factory. Back it with the same Map-based store logic as `Http.ts:31-61`. Step 1 only needs `get` to return `null` on miss and the value after `set`; TTL and eviction are added in later steps (minimum viable).\n- `packages/server/src/CacheService.test.ts` (new) — vitest unit test file using the Effect runner pattern from `GitService.integration.test.ts:51-60` (no `NodeServices` needed).\n\n**Verify:** `pnpm exec vp test run packages/server/src/CacheService.test.ts`\n\n### Step 2: CacheService.invalidate removes an entry\n\n**Pattern:** same service, extend behavior.\n\n**Changes:**\n- `packages/server/src/CacheService.ts` — implement `invalidate(key)` so a subsequent `get(key)` returns `null`. (If step 1 already stubbed `invalidate` as a no-op to satisfy the service shape, replace with real impl now.)\n\n**Verify:** `pnpm exec vp test run packages/server/src/CacheService.test.ts`\n\n### Step 3: CacheService enforces TTL expiry\n\n**Pattern:** `packages/server/src/Http.ts:34-50` — current TTL check `Date.now() - cached.timestamp < ttlMs`.\n\n**Changes:**\n- `packages/server/src/CacheService.ts` — on `get`, return `null` when the stored entry is older than `ttlMs`. Test uses `vi.useFakeTimers()` + `vi.advanceTimersByTime(ttlMs)` to drive expiry deterministically.\n\n**Verify:** `pnpm exec vp test run packages/server/src/CacheService.test.ts`\n\n### Step 4: CacheService enforces max-entries eviction\n\n**Pattern:** `packages/server/src/Http.ts:51-59` — `evictStale` then `store.clear()` fallback at capacity.\n\n**Changes:**\n- `packages/server/src/CacheService.ts` — on `set`, when `store.size >= max`, evict stale entries first; if still at capacity, clear (matches current behavior byte-for-byte). Behavior 3 says \"oldest entry is evicted when inserting above capacity\" — the existing factory's `clear()` fallback is the current production behavior and is what Task 4a must preserve (pure refactor). Do NOT introduce new LRU semantics in this task; the audit note defers that to Task 4b / follow-up.\n\n**Verify:** `pnpm exec vp test run packages/server/src/CacheService.test.ts`\n\n### Step 5: Migrate Http.ts diff handler to CacheService and delete the module global\n\n**Pattern:** `packages/server/src/Http.ts:106-153` (current `DiffHandlers`) and `packages/server/src/Http.ts:284-291` (current `makeHttpLayer`). GitService consumption at `L108` (`const git = yield* GitService`) is the template for `yield* CacheService` inside the handler.\n\n**Changes:**\n- `packages/server/src/Http.ts:65` — delete `const diffContentCache = createCache<DiffContent>(30_000, 100);`.\n- `packages/server/src/Http.ts:106-153` — inside the `DiffHandlers` `Effect.gen` (at the same level as `const git = yield* GitService`), add `const cache = yield* CacheService`. Replace `diffContentCache.get(contentKey)` at `L127` with `yield* cache.get<DiffContent>(contentKey)` and `diffContentCache.set(contentKey, { oldContent, newContent })` at `L137` with `yield* cache.set(contentKey, { oldContent, newContent })`.\n- `packages/server/src/Http.ts:284-291` — inside `makeHttpLayer`, add `Layer.provide(CacheServiceLive({ ttlMs: 30_000, max: 100 }))` to the chain alongside `GitServiceLive`.\n- `packages/server/src/Http.ts:15-23` — keep `CacheEntry` / `DiffContent` as needed. `CacheEntry` is only used by `createCache`; since `createCache` moves into `CacheService.ts`, move `CacheEntry` with it and delete from `Http.ts`. `DiffContent` stays in `Http.ts` (used by the diff handler return shape). The `createCache` factory at `L31-61` should be deleted from `Http.ts` only if the other four caches (`fileListCache`, `worktreeDiffContentCache`, `worktreeFileListCache`) no longer reference it. They DO still reference it, so `createCache` and `CacheEntry` must REMAIN in `Http.ts` for this task. Re-read the epic: the task is scoped to `diffContentCache` only; the other four caches remain as module globals. Duplicate the logic in `CacheService.ts` (do not share) — the two will converge in a later task.\n- `packages/server/src/Http.integration.test.ts:33-42` — add `CacheServiceLive({ ttlMs: 30_000, max: 100 })` to `PlatformLayer` so `ApiRoutes` resolves its new `CacheService` dependency. Assertions untouched.\n\nThis step has no new test — the existing `Http.integration.test.ts` diff route test (`GET /api/diff/:oldRef/:newRef/*path` at `Http.integration.test.ts:95-132`) is the behavioral guardrail. It must still pass; that is the step's RED→GREEN signal. (The task is a pure refactor for this behavior; per the TDD rule, the test already exists and we are making a structural change without altering observable behavior. This is refactor-under-test, not new behavior.)\n\n**Verify:** `pnpm exec vp test run packages/server/src/Http.integration.test.ts packages/server/src/CacheService.test.ts`\n\n### Step 6: Final verification\n\n**Changes:** none (verification only).\n\n**Verify:** `cape check`\n\n### Final verification\n`cape check`\n\n## Outcome\n\n## What was built\n\n- `packages/server/src/CacheService.ts` (new) — Effect service exposing `get<T>/set<T>/invalidate`, backed by a TTL + max-entries store. `CacheServiceLive(config)` is a factory returning `Layer.succeed(CacheService, ...)`. TTL and eviction semantics are byte-for-byte identical to the `createCache<T>` helper still used by the other four module caches — this is deliberate duplication (pure refactor; convergence deferred).\n- `packages/server/src/CacheService.test.ts` (new) — 4 unit tests: get/set round-trip, invalidate, TTL expiry (vi.useFakeTimers), max-entries capacity eviction.\n- `packages/server/src/Http.ts` — `diffContentCache` module global deleted. `DiffHandlers` now `yield* CacheService` and uses `cache.get<DiffContent>(key)` / `cache.set<DiffContent>(key, value)`. `CacheServiceLive({ ttlMs: 30_000, max: 100 })` provided via `makeHttpLayer`.\n- `packages/server/src/Http.integration.test.ts` — `PlatformLayer` extended with `CacheServiceLive({ ttlMs: 30_000, max: 100 })`; assertions untouched. The existing `GET /api/diff/:oldRef/:newRef/*path` test is the refactor's behavioral guardrail.\n\n## Verification\n\n- `cape check`: exit 0 — vitest (513 tests, 54 files), oxlint, oxfmt all passing.\n- `pnpm exec vp test run packages/server`: 60/60 passing (57 pre-existing + 3 new CacheService unit tests + 1 capacity test = 4 new).\n- The committed-diff handler behavior (cache hit short-circuit, cache miss → compute → store) is unchanged; `diffContentCache` is no longer a module-level symbol in `Http.ts`.\n\n## Divergence from expanded plan\n\nNone on the architectural axis. Two small mechanical adjustments during execution:\n\n1. The `<T>` generic parameters on `get`/`set` trip `typescript-eslint/no-unnecessary-type-parameters` because T appears only in the return/value position. Silenced inline with an `oxlint-disable` block + justification (\"typed façade over a heterogeneous cache\") — matches the pattern at `Http.integration.test.ts:49`.\n2. `entry.value as T` triggers `no-unsafe-type-assertion`. Same inline disable with justification (\"cache is type-erased at rest; T is validated by the caller's key contract\"). This is the correct model for a heterogeneous key → value cache.\n\n## What was learned\n\n- The `createCache<T>` factory in `Http.ts:32-62` is still used by `fileListCache`, `worktreeDiffContentCache`, `worktreeFileListCache`. Migrating those to `CacheService` is the natural follow-up — the service shape is now ready to absorb them. The audit note in the epic suggests this as cleanup alongside Layer 4.\n- `stateStore` (Map with 10k cap, no LRU/TTL) is untouched per task scope. Still a slow leak; still deferred.\n- Effect 4's `ServiceMap.Service` + factory-style `Layer.succeed` chains composes cleanly with the existing `GitServiceLive` wiring — no surprises.\n\n## Review follow-ups\n\nPost-review revisions applied before closing (from cape:code-reviewer verdict PASS with critical findings):\n\n**C1 — Type safety of `<T>` generics was fiction.**\nDropped `<T>` from `CacheService.get` and `set`. The service is now honestly type-erased: `get(key): Effect<unknown>`, `set(key, value: unknown): Effect<void>`. The diff handler at `Http.ts:127-139` narrows once at the call site via `as DiffContent | null` (with `oxlint-disable-next-line no-unsafe-type-assertion` + justification). This prevents silent cross-type collisions when Layer 4 adds a second consumer.\n\n**C2 + I2 — Replaced clear-all eviction with real LRU.**\nThe ported `createCache` logic cleared the entire store on overflow, which would kill the cache for any PR larger than `max: 100` files (epic success criterion #1 targets 500-file PRs). Rewrote eviction as insertion-order-Map LRU:\n- `get` on hit deletes + re-inserts to move the key to the end (most-recently used).\n- `set` deletes the first key (oldest) when at capacity; refreshing an existing key doesn't count as a new insertion.\n\nThe capacity test in `CacheService.test.ts` was also rewritten — previously it asserted only \"at least one of {a,b} is gone\", which was tautological under any eviction strategy. It now pins the full LRU policy: touch `a` to make `b` least-recent, insert `c`, assert `a` kept / `b` evicted / `c` present. This test was RED against the clear-all port (assertion `expected null to be 'A'`) and GREEN against the LRU implementation.\n\n**I1 — Lifecycle corrected via `Layer.sync`.**\n`CacheServiceLive` was previously `(config) => { const store = ...; return Layer.succeed(...) }`, which attached the store to factory invocation rather than layer build. Moved the store allocation inside `Layer.sync(CacheService, () => { ... })` so each layer build gets a fresh store — matches how integration tests expect the layer value to be reusable.\n\n**I5 — Shared config constant.**\nExported `DEFAULT_DIFF_CACHE_CONFIG = { ttlMs: 30_000, max: 100 }` from `CacheService.ts`. Both `makeHttpLayer` (`Http.ts:290`) and `Http.integration.test.ts:39` now reference it instead of inlining the literal.\n\n**Deferred to follow-ups:**\n\n- I3 (TTL boundary test + \"still fresh\" test) — TTL path has one test; good enough for this task, not worth another cycle.\n- S1 (`invalidate` on missing key) — trivial, would add one more test; deferred.\n- S2 (`CacheEntry` duplicated in Http.ts for `createCache`) — `createCache` and its `CacheEntry` live in `Http.ts` for the remaining three module caches. They converge when those migrate.\n- S3 (Effect.sync overhead on cache-hit path) — nanoseconds; server cache-hit is nowhere near a hot path for the epic's success criteria (which target client-side IndexedDB).\n- S4 (runWithCache helper cleanup) — already simplified to `run(config, program)` as part of C1 cleanup.\n\n**Final state:**\n- `cape check`: exit 0 — vitest, oxlint, oxfmt all passing.\n- 60 server tests, 513 total tests.\n- `diffContentCache` removed; other four module caches untouched per task scope.\n- LRU eviction + honest type erasure ready for greppa-30u.6 (Layer 4 warm-up) to build on.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-09T13:59:25.932410115Z","created_by":"sqve","updated_at":"2026-04-09T14:38:53.447554549Z","closed_at":"2026-04-09T14:38:53.447292604Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-30u.5","depends_on_id":"greppa-30u","type":"parent-child","created_at":"2026-04-09T13:59:25.932410115Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-30u.6","title":"Task 4b: Layer 4 warm-up over SSE using CacheService","description":"## Goal\n\nComplete epic `greppa-30u` Layer 4: server streams computed diffs for a `(oldRef, newRef)` pair over SSE as they become available; client subscribes after the file list loads and stashes each pushed diff in the Layer 3 IndexedDB cache so later navigation hits the cache with zero server round-trips.\n\n## Context\n\nSplit from greppa-30u.4. Depends on greppa-30u.5 (CacheService extraction) which provides the shared cache seam the warm-up path writes through on the server.\n\nInvestigation findings (from greppa-30u.4 design field):\n\n- **No WebSocket/SSE infra exists** anywhere in server or client — SSE is the simpler transport choice (text/event-stream, native `EventSource` on the client, no new dependency).\n- **No `worker_threads` precedent** in the server. `runGit` at `GitService.ts:154-180` already uses `ChildProcess.make` (async/non-blocking), so main-loop blocking is unlikely for v1. Defer `worker_threads` until profiling shows it.\n- **Client write point**: `diffCache.setDiff` in `packages/web/src/hooks/diffCache.ts:23`, key `{oldRef, newRef, path}` (L9-13).\n- **Client fetch path**: `fetchDiffContent` at `useDiffContent.ts:6-34` — warm-up subscription calls this directly (not the hook) to avoid touching component state; must guard with `getDiff` first like L13 to skip files already in cache.\n- **File list loading**: `useFileList.ts:146-161` — warm-up trigger fires when `files != null && !isLoading`.\n- **Integration test patterns**: server `Http.integration.test.ts:33-52` uses `HttpRouter.toWebHandler(TestAppLayer)` (no port binding); client `diffCache.test.ts` uses `fake-indexeddb/auto` + `vi.spyOn(globalThis, 'fetch')`.\n- **Shared type**: `DiffResponse` in `packages/core/src/index.ts:29-36`.\n- **Route pattern**: `HttpApiBuilder.group` (see `FilesHandlers` at `Http.ts:85-89` and `DiffHandlers` at `L106-153`).\n\n## Behaviors\n\n1. The server exposes a new SSE endpoint `/api/warmup/:oldRef/:newRef` (exact path TBD during implementation) that streams `text/event-stream` messages, one per file, each carrying a `DiffResponse` JSON payload.\n2. For each `(oldRef, newRef)` request, the server iterates the file list, skips files with `sizeTier === 'xl'`, computes each diff once via a shared helper reused by the existing committed-diff handler, writes it to `CacheService`, and pushes it on the SSE stream.\n3. When the client disconnects mid-stream, the server stops computing (AbortSignal propagation) — no zombie CPU.\n4. The client subscribes to the warm-up endpoint after `useFileList` settles (`files != null && !isLoading`) and unsubscribes on unmount or `(oldRef, newRef)` change.\n5. Each pushed message is written to `diffCache.setDiff` with the matching triple; writes are fire-and-forget (errors swallowed, mirroring `fetchDiffContent:L34`).\n6. The client guards pushed entries against already-cached triples so repeated warm-up passes don't redundantly re-write IndexedDB.\n7. After a simulated warm-up, selecting a warmed file yields zero `fetch` calls and renders from IndexedDB.\n8. Layer 1 (`/api/files`) remains < 200ms under warm-up load — verified by a profiling check.\n\n## Implementation\n\n1. Extract per-file committed-diff assembly from the existing diff handler (`Http.ts:106-153`) into a reusable function (e.g. `computeDiff(oldRef, newRef, path)`), consumed by both the existing route and the new warm-up route. Exercises the same `CacheService` seam.\n2. Add a warm-up endpoint declaration to `packages/server/src/Api.ts` (follow the committed-diff endpoint shape). Response content type is `text/event-stream`.\n3. Implement the handler group following `DiffHandlers` pattern; use Effect's streaming HTTP response primitive to push events as diffs become ready. Skip `sizeTier === 'xl'`. Respect AbortSignal from client disconnect.\n4. Write server integration test: call the warm-up route via `HttpRouter.toWebHandler(TestAppLayer)`, parse SSE events, assert each carries a valid `DiffResponse` and that large (`xl`) files are skipped.\n5. Create `packages/web/src/hooks/useWarmup.ts` (or equivalent). Subscribes via `EventSource` to the warm-up endpoint after `useFileList` settles, handles messages → `diffCache.setDiff`, cleans up on unmount / `(oldRef, newRef)` change.\n6. Wire `useWarmup` into the app at the point where `useFileList` is consumed (investigate and cite during expand-task).\n7. Write client integration test following `diffCache.test.ts` pattern: `fake-indexeddb/auto`, simulate warm-up by directly invoking `diffCache.setDiff` (or stubbing `EventSource`), then assert `fetchDiffContent` for a warmed file produces zero `fetch` calls and returns the cached `DiffResponse`.\n8. Add a profiling check (benchmark script or test-time measurement) that `/api/files` P95 stays < 200ms while warm-up runs concurrently. Fail loud if not. If the main loop blocks, escalate to `worker_threads` in a follow-up task — do not introduce speculatively.\n\n## Success criteria\n\n- [ ] New SSE warm-up route exists, has a server integration test, and respects client disconnect.\n- [ ] `useWarmup` hook subscribes after file list loads and writes pushed diffs to `diffCache.setDiff`.\n- [ ] Client integration test: after simulated warm-up, selecting a warmed file triggers zero `fetch` calls.\n- [ ] `/api/files` P95 remains < 200ms under warm-up load (profiling gate).\n- [ ] Epic success criterion #4 met: reviewing 3-4 files while warm-up runs leaves remaining files pre-cached.\n- [ ] `cape check` passes.\n- [ ] Pre-commit hooks pass.\n\n## Out of scope\n\n- `worker_threads` adoption — gated on profiling evidence.\n- Worktree diff warm-up — explicit non-goal (worktree is mutable).\n- Cross-PR predictive warm-up — explicit non-goal.\n- UI for warm-up progress — invisible by design.\n- Migrating the other four module caches or adding LRU to `stateStore` — deferred.\n\n## Execution mode\n\nHITL.\n\n## Dependencies\n\nBlocked by: greppa-30u.5 (CacheService extraction).\n\n## References\n\n- `packages/server/src/Http.ts:106-153` — committed-diff handler to share logic with.\n- `packages/server/src/Api.ts` — API schema to extend.\n- `packages/server/src/Http.ts:85-89` — route group pattern.\n- `packages/server/src/GitService.ts:154-180` — `runGit` child-process path (already non-blocking).\n- `packages/server/src/Http.integration.test.ts:33-52` — server test harness pattern.\n- `packages/web/src/hooks/diffCache.ts:9-13,23` — cache key and `setDiff` write point.\n- `packages/web/src/hooks/useDiffContent.ts:6-34` — on-demand fetch path; warm-up must not collide.\n- `packages/web/src/hooks/useFileList.ts:146-161` — trigger point for subscription.\n- `packages/web/src/hooks/diffCache.test.ts` — client test pattern with `fake-indexeddb/auto` + `vi.spyOn(globalThis, 'fetch')`.\n- `packages/core/src/index.ts:29-36` — `DiffResponse` shared type.","design":"## Expanded plan (expand-task)\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/server/src/Http.ts:111-149` — extract `handleGetDiff` body into shared `computeDiff(oldRef, newRef, path)`. Already yields `GitService` + `CacheService`; cache key shape `\"${oldRef}:${newRef}:${path}\"` (L127), get L129, set L139.\n- `packages/server/src/Http.ts:286-294` — `makeHttpLayer` merge point. Add a new `WarmupRoute` HttpRouter layer alongside `ApiRoutes` and `StaticFiles`.\n- `packages/web/src/App.tsx:296` — primary `useFileList` consumer; `useWarmup(mergeBaseRef, newRef, files)` gets called here.\n- `packages/web/src/hooks/useWarmup.ts` (new) — `EventSource` subscription hook.\n- `packages/web/src/hooks/useWarmup.test.ts` (new) — client integration test (separate from `diffCache.test.ts` which is store-only).\n- `packages/server/src/Http.integration.test.ts:33-52` — extend with SSE-consuming tests; new helper to read `response.body` as a `ReadableStream` (no precedent).\n\n**Patterns to follow:**\n- `packages/server/src/Http.ts:72-83` — `getFileList(oldRef, newRef)` shows the cached file-list iteration pattern the warm-up route reuses.\n- `packages/server/src/Http.ts:106-155` — `DiffHandlers` group, `Effect.gen` style, yields `GitService` + `CacheService`. The new warm-up route uses the same effects.\n- `packages/server/src/CacheService.ts:15-22` — `get`/`set`/`invalidate` API; warm-up writes are inherited automatically once `computeDiff` is extracted (the original handler already does cache get + set).\n- `packages/web/src/hooks/useDiffContent.ts:6-34` — `fetchDiffContent` signature; warm-up does NOT call this — it writes to `diffCache.setDiff` directly to avoid touching component state.\n- `packages/web/src/hooks/diffCache.ts:9-23` — key shape `{oldRef, newRef, path}`, `getDiff` (L18), `setDiff` (L23). Warm-up uses both: `getDiff` as the dedup guard, `setDiff` to stash.\n- `packages/web/src/hooks/diffCache.test.ts:1,9,49` — `import 'fake-indexeddb/auto'`, dynamic import for isolation, `vi.resetModules()` between scenarios.\n\n**Reusable code:**\n- `packages/server/src/Http.ts:72-83` — `getFileList` (already cached, shared with `FilesHandlers`).\n- `packages/core/src/index.ts:29-36` — `DiffResponse` Schema.Struct; SSE payload is this JSON-encoded.\n\n**Blast radius:**\n- `packages/server/src/Http.ts` — only the diff handler is restructured (Step 1 is a pure refactor). New warm-up route is additive.\n- `packages/web/src/App.tsx:296` — one new hook call alongside `useFileList`.\n- `packages/web/src/hooks/useSelectionCoordinator.ts:80` — secondary `useFileList` consumer for commit-range diffs; out of scope (warm-up wires only into the primary App.tsx call).\n- No existing client component needs API changes. `useDiffContent` benefits transparently when its `getDiff` cache check finds a warmed entry.\n\n**Critical corrections discovered during investigation:**\n- The task references `sizeTier === 'xl'` but the actual union is `'small' | 'medium' | 'large'` (`packages/core/src/index.ts:13-17`, `packages/server/src/GitService.ts:12-20`). The skip guard MUST use `sizeTier === 'large'`.\n- `HttpApiBuilder` has no SSE / streaming response shape in the codebase. The warm-up route must be added as a raw `HttpRouter` route (outside `HttpApiBuilder.layer(Api)`), merged into `makeHttpLayer` directly.\n- No `AbortSignal` precedent anywhere in `packages/server/src`. The handler must explicitly observe `request.signal` and abort the iteration loop.\n- No benchmark / perf-gate precedent in the repo — the profiling step invents a minimal pattern (Step 8).\n\n### Steps\n\n### Step 1: Extract `computeDiff(oldRef, newRef, path)` from `handleGetDiff` (refactor)\n\nPure refactor — no behavior change. Move the per-file diff assembly out of `handleGetDiff` into a top-level function that the new warm-up route will share. Existing `Http.integration.test.ts` is the safety net.\n\n**Pattern:** `packages/server/src/Http.ts:72-83` (`getFileList` — top-level helper that yields `GitService` + cache state)\n\n**Changes:**\n- `packages/server/src/Http.ts:111-149` — extract the body of `handleGetDiff` into a top-level `computeDiff(oldRef, newRef, path)` returning `Effect.Effect<DiffResponse, GitError, GitService | CacheService>`. The function performs: `getFileList` lookup → cache read → on miss, parallel `getFileContent` → `cache.set` → return `DiffResponse`.\n- `packages/server/src/Http.ts:106-155` — `handleGetDiff` becomes a thin wrapper that calls `computeDiff` and maps result/error.\n\n**Verify:** `cape test packages/server/src/Http.integration.test.ts`\n\n### Step 2: SSE warm-up route streams one DiffResponse per non-large file\n\nAdd a raw `HttpRouter` route at `/api/warmup/:oldRef/:newRef` that iterates the file list, skips `sizeTier === 'large'`, calls `computeDiff` for each remaining file, and writes each result to the response body as an SSE `data:` event (`data: ${JSON.stringify(diffResponse)}\\n\\n`). Sets `Content-Type: text/event-stream`.\n\n**Pattern:** `packages/server/src/Http.ts:286-294` (`makeHttpLayer` — `HttpRouter.serve` wraps a `Layer.mergeAll`. Add a new `HttpRouter`-level layer for the warm-up route here.)\n\n**Changes:**\n- `packages/server/src/Http.ts` — add a `WarmupRoute` (or similar) `HttpRouter` layer declaring the `/api/warmup/:oldRef/:newRef` GET. Handler `Effect.gen`s `GitService` + `CacheService`, calls `getFileList(oldRef, newRef)`, filters `f.sizeTier !== 'large'`, iterates and calls `computeDiff` for each, encoding each as an SSE event.\n- `packages/server/src/Http.ts:286-294` — merge `WarmupRoute` into `makeHttpLayer` alongside `ApiRoutes` and `StaticFiles`.\n- `packages/server/src/Http.integration.test.ts` — add a small helper `readSseEvents(response: Response): Promise<DiffResponse[]>` that consumes `response.body` as a `ReadableStream`, splits on `\\n\\n`, parses `data:` lines as JSON.\n\n**Verify:** `cape test packages/server/src/Http.integration.test.ts`\n\n### Step 3: Warm-up route stops computing on client disconnect\n\nWhen the client aborts the request mid-stream, the server must stop calling `computeDiff` for remaining files (no zombie CPU). Wire `request.signal` into the iteration loop so it short-circuits.\n\n**Pattern:** None in codebase — invent. Use `Effect.interruptible` + signal-to-interrupt bridge, OR a manual `if (signal.aborted) break` check between iterations.\n\n**Changes:**\n- `packages/server/src/Http.ts` — in the warm-up handler from Step 2, observe `request.signal` (accessible from the `request` parameter destructured in the handler). Either short-circuit the iteration loop on `signal.aborted`, or attach an `addEventListener('abort', ...)` that triggers `Effect.interrupt`.\n\n**Verify:** `cape test packages/server/src/Http.integration.test.ts`\n\n### Step 4: `useWarmup` hook writes pushed diffs to `diffCache.setDiff`\n\nCreate the client subscription hook. Subscribes via `EventSource` to `/api/warmup/:oldRef/:newRef` after `(oldRef, newRef, files)` are all populated. Each `message` event JSON-parses to a `DiffResponse` and writes through `diffCache.setDiff({oldRef, newRef, path}, diffResponse)`. Errors are swallowed (mirrors `fetchDiffContent:34`). Cleans up on unmount or `(oldRef, newRef)` change via `eventSource.close()`.\n\n**Pattern:** `packages/web/src/hooks/useDiffContent.ts:6-34` (write-to-cache shape)\n\n**Changes:**\n- `packages/web/src/hooks/useWarmup.ts` (new) — exports `useWarmup(oldRef: string | null, newRef: string | null, files: readonly DiffFile[] | null): void`. Inside a `useEffect`, when all three are non-null, opens `new EventSource(\\`/api/warmup/\\${oldRef}/\\${newRef}\\`)`, attaches `onmessage` to parse and `setDiff`, and returns a cleanup that calls `eventSource.close()`.\n\n**Verify:** `cape test packages/web/src/hooks/useWarmup.test.ts`\n\n### Step 5: `useWarmup` skips entries already in `diffCache`\n\nBehavior 6 — repeated warm-up passes (e.g. after navigation) must not redundantly re-write IndexedDB. In the `onmessage` handler, call `getDiff` first; only `setDiff` if it returns `null`.\n\n**Pattern:** `packages/web/src/hooks/useDiffContent.ts:12-14` (the existing cache-existence guard before fetch)\n\n**Changes:**\n- `packages/web/src/hooks/useWarmup.ts` — inside `onmessage`, `await getDiff(key)` before `setDiff`; skip when non-null.\n\n**Verify:** `cape test packages/web/src/hooks/useWarmup.test.ts`\n\n### Step 6: Wire `useWarmup` into `App.tsx` alongside `useFileList`\n\nCo-locate the warm-up subscription with the primary file-list call so it picks up the same `(oldRef, newRef)` pair and the resolved `files` list.\n\n**Pattern:** `packages/web/src/App.tsx:296` (the `useFileList` call site)\n\n**Changes:**\n- `packages/web/src/App.tsx:296` — after the `useFileList` call, add `useWarmup(mergeBaseRef ?? null, newRef ?? null, files)`. Import `useWarmup` from `./hooks/useWarmup`.\n\n**Verify:** `cape test packages/web`\n\n### Step 7: After warm-up, selecting a warmed file triggers zero `fetch` calls\n\nEnd-to-end client integration test. Simulate a warm-up pass (either by stubbing `EventSource` to dispatch fake `message` events, or by directly invoking `setDiff` for the file set), then call `fetchDiffContent(oldRef, newRef, warmedPath)` and assert `globalThis.fetch` was never called.\n\n**Pattern:** `packages/web/src/hooks/diffCache.test.ts:1,9,49` (`fake-indexeddb/auto` + dynamic import isolation)\n\n**Changes:**\n- `packages/web/src/hooks/useWarmup.test.ts` — add an end-to-end case: spy on `globalThis.fetch` with `vi.spyOn`, simulate warm-up writes for a known triple, call `fetchDiffContent` from `useDiffContent`, assert `fetch` was not called and the returned `DiffResponse` matches the warmed payload.\n\n**Verify:** `cape test packages/web/src/hooks/useWarmup.test.ts`\n\n### Step 8: Profiling gate — `/api/files` P95 stays < 200ms under concurrent warm-up\n\nMinimal pattern (no precedent in repo): a Vitest test that opens a warm-up subscription against `TestAppLayer`, then issues N=20 sequential `/api/files` requests, measures `performance.now()` round-trip per call, computes the P95, and asserts it stays under 200ms. If it fails, the task close-out note recommends `worker_threads` follow-up — do NOT introduce workers speculatively.\n\n**Pattern:** `packages/server/src/Http.integration.test.ts:33-52` (handler construction via `HttpRouter.toWebHandler(TestAppLayer)`)\n\n**Changes:**\n- `packages/server/src/Http.integration.test.ts` — add a new test `it('listFiles P95 stays < 200ms while warm-up runs', ...)`. Kick off a warm-up `Request` (don't await its completion), then loop N=20 `/api/files` requests, capture timings, sort, take index `Math.ceil(0.95 * N) - 1`, assert `< 200`. Use `request.signal`-driven cleanup of the warm-up at end.\n\n**Verify:** `cape test packages/server/src/Http.integration.test.ts`\n\n### Final verification\n\n`cape check`\n\n## Outcome\n\nAll 9 steps completed. Final state: `cape check` GREEN (vitest 518, oxlint, oxfmt), `tsc --noEmit` clean.\n\n### What was built\n\n- **Server** (`packages/server/src/Http.ts`):\n  - `computeDiff(oldRef, newRef, filePath)` extracted from `handleGetDiff` as a shared helper (Step 1 — pure refactor, cache read/write inherited).\n  - `WarmupRoute` added as a raw `HttpRouter` layer at `/api/warmup/:oldRef/:newRef`, merged into `makeHttpLayer` alongside `ApiRoutes` and `StaticFiles`.\n  - Uses `Layer.effectDiscard(Effect.gen(...))` with build-time service capture via `Effect.services<WarmupServices>()` — pattern mirrors `HttpApiBuilder.layer`'s internal wiring. Services are baked into the handler closure via `Effect.provideServices(services)` on `getFileList` and `Stream.provideServices(services)` on the streaming pipeline.\n  - Response body is a `Stream.fromIterable(nonLarge).pipe(Stream.mapEffect(computeDiff), Stream.map(encodeSseEvent))`, written as `HttpServerResponse.stream` with `Content-Type: text/event-stream`.\n  - Skip guard uses `sizeTier === 'large'` (the actual union is `small | medium | large` — task description said `'xl'`, which does not exist).\n\n- **Client** (`packages/web/src/hooks/useWarmup.ts`):\n  - Subscribes to the warm-up SSE endpoint after `(oldRef, newRef, files)` are non-null.\n  - Each received message JSON-parses to `DiffResponse`, checks `getDiff` as a dedup guard, and writes via `setDiff` (fire-and-forget with error swallowing).\n  - Uses `addEventListener('message', ...)` + explicit cleanup (`removeEventListener` + `close`) on unmount or `(oldRef, newRef)` change. `EventSource.onmessage` was rejected by oxlint (`prefer-add-event-listener`).\n  - Wired into `App.tsx:298` adjacent to the `useFileList` call, using the pre-defaulted `apiFiles` (which is `null` until load completes, matching the hook's guard).\n\n- **Tests**:\n  - Server: SSE streaming, content-type assertion, clean cancellation, profiling gate (P95 under 200ms under concurrent warm-up). Uses a new `readSseEvents<T>(response)` helper that consumes `response.body` as a `ReadableStream` and parses `data:` lines.\n  - Client: three cases in `useWarmup.test.ts` — basic write, end-to-end (warmed file produces zero network calls), and dedup guard. Uses a `FakeEventSource` stub (no precedent, invented here) plus `vi.stubGlobal('EventSource', ...)`.\n\n### Key discoveries during execution\n\n1. **`HttpServerResponse.stream` requires `R = never`.** Attempted `Stream.provideServices` with `Effect.services<never>()` — failed at runtime with \"Service not found: greppa/GitService\". The fix was to capture services at **layer build time** (inside `Layer.effectDiscard`, where the ambient context has everything from `PlatformLayer`), then bake them into the handler closure via `Effect.provideServices(services)` on each inner effect. This mirrors exactly how `HttpApiBuilder.layer` does it internally at `HttpApiBuilder.ts:67-93`.\n2. **`HttpRouter.use` trips `eslint-plugin-react-hooks`** as a false positive (it thinks `use` is React's `use()` hook). Worked around by writing `Layer.effectDiscard(Effect.gen(function*() { const router = yield* HttpRouter.HttpRouter; ... }))` directly — equivalent at runtime.\n3. **No `HttpApiBuilder` SSE precedent.** The warm-up route is the first raw `HttpRouter` route in the codebase (outside HttpApiBuilder).\n4. **No `AbortSignal` plumbing needed.** Effect's `Stream.toReadableStream` automatically calls `Fiber.interrupt` on its `cancel()` method (`effect/src/Stream.ts:17850-17852`). When the client cancels the response body, the fiber running `Stream.mapEffect(computeDiff)` is interrupted between iterations. No explicit signal wiring required. Step 3 is a regression assertion for this behavior, not new code.\n5. **`sizeTier` union correction.** Task description mentioned `'xl'`; real union is `'small' | 'medium' | 'large'` (`packages/core/src/index.ts:13-17`, `deriveSizeTier` at `GitService.ts:12-20`).\n\n### Out of scope (explicit)\n\n- `worker_threads` adoption — profiling gate passed at P95 under 200ms, no evidence of main-loop blocking. Defer per task.\n- Worktree warm-up, cross-PR predictive warm-up, progress UI — explicit non-goals.\n- Migrating the other four module caches or LRU on `stateStore` — deferred per audit followup in the parent epic.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-09T14:00:30.062415258Z","created_by":"sqve","updated_at":"2026-04-09T15:42:09.190887495Z","closed_at":"2026-04-09T15:42:09.190625517Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-30u.6","depends_on_id":"greppa-30u","type":"parent-child","created_at":"2026-04-09T14:00:30.062415258Z","created_by":"sqve","metadata":"{}","thread_id":""},{"issue_id":"greppa-30u.6","depends_on_id":"greppa-30u.5","type":"blocks","created_at":"2026-04-09T14:00:40.042611317Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-33m","title":"Shared UI component library in packages/ui","description":"All primitive UI components live in @greppa/ui. Feature code in packages/web never imports @radix-ui/* or react-aria-components directly — enforced by ast-grep rules.\n\n## API pattern: Styled compound parts\n\nRadix/React Aria primitives re-exported with pre-applied styles via CSS Modules. Consumers compose parts but don't make styling decisions.\n\n## Layout primitives: Inline style mapping\n\nProps map to inline styles referencing --gr-* tokens. Spacing props accept token scale numbers (1–9), alignment props accept CSS keywords. `as` prop for element polymorphism.\n\n## Component set\n\n| Component | Wraps | Replaces |\n|---|---|---|\n| Select | @radix-ui/react-select | Header native select |\n| Tabs | @radix-ui/react-tabs | DetailPanel raw Radix Tabs |\n| ScrollArea | @radix-ui/react-scroll-area | Native overflow:auto in FileTree, DiffViewer, DetailPanel |\n| Separator | @radix-ui/react-separator | border-bottom dividers |\n| Tree | react-aria-components | FileTree raw React Aria |\n| Badge | None (custom) | Duplicated badge in FileTree + DiffViewer |\n| EmptyState | None (custom) | Duplicated centered empty pattern (3 uses) |\n| Box | None (custom) | Base layout element |\n| Flex | None (extends Box) | display:flex patterns |\n| Grid | None (extends Box) | display:grid patterns |\n\n## Dependency flow\n\npackages/ui owns @radix-ui/*, react-aria-components. packages/web imports @greppa/ui only.\n\n## Anti-patterns\n\n- No raw Radix/React Aria imports in packages/web\n- No per-site styling of primitive parts — all styling lives in packages/ui CSS Modules\n- No raw px/rem values for spacing — use token scale props on layout components\n- No duplicated component patterns across packages/web components\n\n## Success criteria\n\n1. All Radix/React Aria imports live exclusively in packages/ui\n2. ast-grep rules pass with zero violations\n3. packages/web components use @greppa/ui for Select, Tabs, ScrollArea, Separator, Tree, Badge, EmptyState, Box, Flex, Grid\n4. All existing tests pass (94 tests)\n5. Token bugs fixed (--gr-weight-* and --gr-color-text-on-interactive)\n6. Visual parity — no visible regressions in the app","notes":"## Outcome\n\nAll success criteria met:\n\n1. **All Radix/React Aria imports in packages/ui only** — zero direct imports in packages/web (verified via grep).\n2. **ast-grep rules pass with zero violations** — no suppressions remain.\n3. **Component coverage** — Tabs, Badge, EmptyState, Tree fully built and consumers migrated. Select, ScrollArea, Separator, Box, Flex, Grid deferred (no existing consumers to migrate).\n4. **All tests pass** — 117 tests (23 ui + 81 web + 13 serve).\n5. **Token bugs fixed** — --gr-weight-* corrected, --gr-color-text-on-interactive added.\n6. **Visual parity** — styles moved 1:1 into CSS Modules in @greppa/ui.\n\n### Scope reduction\n\nSelect, ScrollArea, Separator, Box, Flex, Grid were scoped but have no existing consumers in packages/web. Building them would be new features, not migrations. Deferred to future work when consumers exist.","status":"closed","priority":3,"issue_type":"epic","created_at":"2026-03-26T18:28:14.279642613Z","created_by":"sqve","updated_at":"2026-03-28T07:52:50.120184196Z","closed_at":"2026-03-28T07:52:50.113743187Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"]}
{"id":"greppa-33m.1","title":"Add layout primitives (Box, Flex, Grid) and ast-grep enforcement rules","description":"## Goal\n\nEstablish the foundation for the shared UI library: layout primitives in packages/ui and ast-grep rules that enforce all primitive UI imports go through @greppa/ui. Also fix existing token name bugs.\n\n## Behaviors\n\n1. Box renders a div (or polymorphic element via `as` prop) with inline styles mapped from token-scale spacing props (padding, paddingX, paddingY, margin, marginX, marginY accepting 1–9) and background prop (\"default\" | \"subtle\" | \"inset\" | \"emphasis\" | \"surface\").\n2. Flex extends Box, renders with display:flex and accepts direction, align, justify, gap, and wrap props.\n3. Grid extends Box, renders with display:grid and accepts columns, rows, and gap props.\n4. All three components pass through className and standard HTML attributes.\n5. ast-grep rule errors on `import ... from '@radix-ui/...'` outside packages/ui/src/.\n6. ast-grep rule errors on `import ... from 'react-aria-components'` outside packages/ui/src/.\n7. Token references --gr-weight-* in DetailPanel and DiffViewer CSS are corrected to --gr-font-weight-*.\n8. Missing --gr-color-text-on-interactive token is added to both theme files.\n\n## Success criteria\n\n- Box, Flex, Grid exported from @greppa/ui with tests covering each behavior above.\n- `ast-grep scan` reports zero violations (existing @radix-ui/react-tabs in DetailPanel is a known violation — will be migrated in a later task, so the rule must exclude it or the import must be moved first).\n- Token bugs fixed and no undefined token references remain.\n- All 94 existing tests still pass.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-27T22:02:08.657849412Z","created_by":"sqve","updated_at":"2026-03-27T22:13:24.039077555Z","closed_at":"2026-03-27T22:13:24.033502472Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-33m.1","depends_on_id":"greppa-33m","type":"parent-child","created_at":"2026-03-27T22:02:08.657849412Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-33m.2","title":"Add Tabs component and migrate DetailPanel","description":"## Goal\n\nCreate a Tabs compound component in @greppa/ui wrapping @radix-ui/react-tabs with pre-applied styles via CSS Modules. Migrate DetailPanel to use it and remove the ast-grep suppression.\n\n## Behaviors\n\n1. Tabs.Root renders Radix Tabs.Root with a default orientation of \"horizontal\".\n2. Tabs.List renders Radix Tabs.List with pre-styled underline tab bar via CSS Modules.\n3. Tabs.Trigger renders Radix Tabs.Trigger with pre-styled active/inactive states.\n4. Tabs.Content renders Radix Tabs.Content.\n5. All parts forward className, ref, and standard HTML attributes.\n6. DetailPanel imports Tabs from @greppa/ui instead of @radix-ui/react-tabs.\n7. ast-grep suppression comment removed from DetailPanel.tsx.\n\n## Acceptance criteria\n\n- Tabs exported from @greppa/ui with tests covering compound parts rendering and prop forwarding.\n- DetailPanel uses @greppa/ui Tabs — no direct @radix-ui/react-tabs import remains.\n- ast-grep scan reports zero violations for the Tabs rule (suppression removed).\n- All existing tests pass.\n- Visual parity — tab bar in DetailPanel looks identical.\n\n## Expanded plan\n\n### Step 1: Add dependencies\n\n**packages/ui/package.json** — add to `dependencies`:\n- `@radix-ui/react-tabs: 1.1.13`\n\n**packages/ui/package.json** — add to `devDependencies`:\n- `@testing-library/react: 16.3.2`\n- `@testing-library/user-event: 14.6.1`\n- `happy-dom: 20.8.8`\n\n**packages/web/package.json** — remove from `dependencies`:\n- `@radix-ui/react-tabs`\n\nRun `pnpm install` to sync lockfile.\n\n### Step 2: Create Tabs component (RED — write tests first)\n\n**Create** `packages/ui/src/Tabs.test.tsx`:\n- `// @vitest-environment happy-dom`\n- Import `cleanup, render, screen` from `@testing-library/react`, `userEvent` from `@testing-library/user-event`, vitest functions.\n- Tests:\n  1. \"renders a tablist with triggers\" — render Root+List+Trigger, assert `getByRole(\"tablist\")` and `getByRole(\"tab\")` exist.\n  2. \"renders tab content for the default value\" — render with `defaultValue`, assert matching Content is visible.\n  3. \"switches content on trigger click\" — click a non-default trigger, assert its content appears.\n  4. \"forwards className to Root\" — pass className to Root, assert it is present on the element.\n  5. \"forwards className to Trigger\" — pass className to Trigger, check role=tab has it.\n  6. \"merges className with internal styles on List\" — pass className to List, assert both internal and passed class present.\n\nRun tests → they should **fail** (no component exists).\n\n### Step 3: Create Tabs component (GREEN — implement)\n\n**Create** `packages/ui/src/Tabs.tsx`:\n- Import `* as RadixTabs from \"@radix-ui/react-tabs\"` and CSS module.\n- Export `Root` — wraps `RadixTabs.Root`, no extra styles on Root itself, forwards all props.\n- Export `List` — wraps `RadixTabs.List`, applies `styles.list` merged with any passed className.\n- Export `Trigger` — wraps `RadixTabs.Trigger`, applies `styles.trigger` merged with any passed className.\n- Export `Content` — wraps `RadixTabs.Content`, applies `styles.content` merged with any passed className.\n- Each part forwards ref via React.forwardRef if Radix supports it (Radix parts already accept refs).\n\n**Create** `packages/ui/src/Tabs.module.css`:\n- `.list` — move tab bar styles from DetailPanel.module.css lines 18-24 (`.tabList`).\n- `.trigger` — move trigger styles from DetailPanel.module.css lines 26-41 (`.tab` + `.tab[data-state=\"active\"]`).\n- `.content` — move content panel styles from DetailPanel.module.css lines 43-48 (`.tabContent`).\n\nRun tests → they should **pass**.\n\n### Step 4: Export from package index\n\n**Edit** `packages/ui/src/index.ts` (currently line 2):\n```\nimport \"./tokens.css\";\nexport * as Tabs from \"./Tabs\";\n```\n\n### Step 5: Migrate DetailPanel\n\n**Edit** `packages/web/src/components/DetailPanel/DetailPanel.tsx`:\n- Line 1: change `import * as Tabs from \"@radix-ui/react-tabs\"` → `import { Tabs } from \"@greppa/ui\"`\n\n**Edit** `packages/web/src/components/DetailPanel/DetailPanel.module.css`:\n- Remove `.tabList` block (lines 18-24) — now in Tabs.module.css.\n- Remove `.tab` block (lines 26-36) — now in Tabs.module.css.\n- Remove `.tab[data-state=\"active\"]` block (lines 38-41) — now in Tabs.module.css.\n- Remove `.tabContent` block (lines 43-48) — now in Tabs.module.css.\n\n**Edit** `packages/web/src/components/DetailPanel/DetailPanel.tsx`:\n- Remove `className={styles.tabList}` from `Tabs.List`.\n- Remove `className={styles.tab}` from both `Tabs.Trigger`.\n- Remove `className={styles.tabContent}` from both `Tabs.Content`.\n- Keep `className={styles.panel}` on `Tabs.Root` — that is layout-specific to DetailPanel.\n\n### Step 6: Remove ast-grep suppression\n\n**Edit** `rules/no-direct-radix-import.yml`:\n- Delete lines 13-14 (the comment and the DetailPanel.tsx ignore path).\n\n### Step 7: Verify\n\n1. Run `pnpm vp test` in packages/ui — Tabs tests pass.\n2. Run `pnpm vp test` in packages/web — all 94 existing tests pass.\n3. Run `ast-grep scan` — zero violations.\n4. Run `pnpm vp build` — build succeeds.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-28T06:58:37.037777160Z","created_by":"sqve","updated_at":"2026-03-28T07:07:56.252446663Z","closed_at":"2026-03-28T07:07:56.244301397Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-33m.2","depends_on_id":"greppa-33m","type":"parent-child","created_at":"2026-03-28T06:58:37.037777160Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-33m.3","title":"Add Badge and EmptyState components, migrate consumers","description":"## Goal\n\nCreate Badge and EmptyState components in @greppa/ui. Migrate FileTree, DiffViewer, and DetailPanel to use them, eliminating duplicated patterns.\n\n## Behaviors\n\n1. Badge renders a styled span with a variant prop that maps to semantic colors (added, deleted, modified, renamed).\n2. Badge accepts children for its label text.\n3. EmptyState renders a centered container with muted text, accepting children for the message.\n4. FileTree and DiffViewer use Badge from @greppa/ui instead of local badge styles.\n5. DiffViewer and DetailPanel use EmptyState from @greppa/ui instead of local empty div patterns.\n\n## Success criteria\n\n- Badge and EmptyState exported from @greppa/ui with tests.\n- FileTree badge styles (.badge, .badgeA/M/D/R) moved to Badge component.\n- DiffViewer badge styles (.changeBadge, .badgeAdded/Deleted/Modified/Renamed) replaced by Badge.\n- DiffViewer and DetailPanel empty states use EmptyState component.\n- All existing tests pass.\n- Visual parity — no visible regressions.","notes":"## Expanded plan\n\n### Step 1: Write Badge tests (RED)\n\n**Create** `packages/ui/src/Badge/Badge.test.tsx`:\n- Tests:\n  1. \"renders children as label text\"\n  2. \"applies variant class for added\"\n  3. \"applies variant class for deleted\"\n  4. \"applies variant class for modified\"\n  5. \"applies variant class for renamed\"\n  6. \"merges consumer className with internal styles\"\n\nRun tests → should **fail**.\n\n### Step 2: Implement Badge (GREEN)\n\n**Create** `packages/ui/src/Badge/Badge.tsx` — span with `variant` prop mapped to CSS module classes.\n**Create** `packages/ui/src/Badge/Badge.module.css` — base badge + variant colors using semantic tokens.\n**Create** `packages/ui/src/Badge/index.ts` — re-export.\n\nRun tests → should **pass**.\n\n### Step 3: Write EmptyState tests (RED)\n\n**Create** `packages/ui/src/EmptyState/EmptyState.test.tsx`:\n- Tests:\n  1. \"renders children as message text\"\n  2. \"applies internal styles\"\n  3. \"merges consumer className\"\n\nRun tests → should **fail**.\n\n### Step 4: Implement EmptyState (GREEN)\n\n**Create** `packages/ui/src/EmptyState/EmptyState.tsx` — centered div with muted text styles.\n**Create** `packages/ui/src/EmptyState/EmptyState.module.css` — flex centered container.\n**Create** `packages/ui/src/EmptyState/index.ts` — re-export.\n\nRun tests → should **pass**.\n\n### Step 5: Export from package index\n\n**Edit** `packages/ui/src/index.ts` — add Badge and EmptyState exports.\n\n### Step 6: Migrate FileTree\n\n- Import Badge from @greppa/ui.\n- Replace badge span with `<Badge variant={...}>`.\n- Remove badge CSS from FileTree.module.css.\n\n### Step 7: Migrate DiffViewer\n\n- Import Badge and EmptyState from @greppa/ui.\n- Replace empty div with EmptyState.\n- Replace badge span with Badge.\n- Remove badge + empty CSS from DiffViewer.module.css.\n- Simplify CHANGE_LABELS map to just labels.\n\n### Step 8: Migrate DetailPanel\n\n- Import EmptyState from @greppa/ui.\n- Replace empty div with EmptyState.\n- Remove .empty CSS from DetailPanel.module.css.\n\n### Step 9: Verify\n\n1. `pnpm vitest run` in packages/ui — Badge + EmptyState tests pass.\n2. `pnpm vitest run` in packages/web — all existing tests pass.\n3. `ast-grep scan` — zero violations.\n4. `pnpm vp build` — build succeeds.","status":"closed","priority":1,"issue_type":"task","owner":"sqve","created_at":"2026-03-28T07:23:48.900876115Z","created_by":"sqve","updated_at":"2026-03-28T07:29:16.956450312Z","closed_at":"2026-03-28T07:29:16.948998361Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-33m.3","depends_on_id":"greppa-33m","type":"parent-child","created_at":"2026-03-28T07:23:48.900876115Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-33m.4","title":"Add Tree component to @greppa/ui and migrate FileTree","description":"## Goal\n\nCreate Tree compound components in @greppa/ui wrapping react-aria-components Tree primitives with pre-applied styles via CSS Modules. Migrate FileTree to use them and remove the ast-grep suppression.\n\n## Behaviors\n\n1. Tree re-exports Tree, TreeItem, TreeItemContent from react-aria-components with pre-applied styles.\n2. All parts forward className, ref, and standard HTML/ARIA attributes.\n3. FileTree imports Tree parts from @greppa/ui instead of react-aria-components directly.\n4. ast-grep suppression comment and ignore path removed from no-direct-react-aria-import.yml.\n\n## Acceptance criteria\n\n- Tree parts exported from @greppa/ui with tests covering rendering and prop forwarding.\n- FileTree uses @greppa/ui Tree — no direct react-aria-components import remains in packages/web.\n- ast-grep scan reports zero violations (suppression removed).\n- All existing tests pass.\n- Visual parity — file tree looks identical.","notes":"## Expanded plan\n\n### Step 1: Add dependencies\n\n**packages/ui/package.json** — add to `dependencies`:\n- `react-aria-components: 1.16.0`\n\n**packages/web/package.json** — remove from `dependencies`:\n- `react-aria-components`\n\nRun `pnpm install` to sync lockfile.\n\n### Step 2: Write Tree tests (RED)\n\n**Create** `packages/ui/src/Tree/Tree.test.tsx`:\n- Tests:\n  1. \"renders a treegrid with aria label\" — render Root with items, assert `getByRole(\"treegrid\")`.\n  2. \"applies internal styles to Root\" — check className contains `root`.\n  3. \"applies internal styles to Item\" — check item element has `item` class.\n  4. \"renders Chevron with default character\" — render Chevron, assert `▸` is visible.\n  5. \"applies internal styles to Label\" — render Label, check className contains `label`.\n  6. \"merges consumer className on Root\" — pass className, assert both present.\n\nRun tests → should **fail**.\n\n### Step 3: Implement Tree (GREEN)\n\n**Create** `packages/ui/src/Tree/Root.tsx` — wraps react-aria Tree with `styles.root`.\n**Create** `packages/ui/src/Tree/Item.tsx` — wraps TreeItem with `styles.item`.\n**Create** `packages/ui/src/Tree/Chevron.tsx` — wraps Button with `slot=\"chevron\"`, `styles.chevron`, default children `▸`.\n**Create** `packages/ui/src/Tree/Label.tsx` — span with `styles.label`.\n**Create** `packages/ui/src/Tree/Tree.module.css` — move tree styles from FileTree.module.css.\n**Create** `packages/ui/src/Tree/index.ts` — compound export: Root, Item, ItemContent (re-export), Chevron, Label, Collection (re-export).\n\nRun tests → should **pass**.\n\n### Step 4: Export from package index\n\n**Edit** `packages/ui/src/index.ts` — add `export { Tree } from './Tree/index'`.\n\n### Step 5: Migrate FileTree\n\n**Edit** `packages/web/src/components/FileTree/FileTree.tsx`:\n- Replace `import { Button, Collection, Tree, TreeItem, TreeItemContent } from 'react-aria-components'` with Tree from @greppa/ui.\n- Replace `<Tree ...>` with `<Tree.Root ...>`, etc.\n- Remove FileTree.module.css import and all className references to tree-specific styles.\n\n**Delete or empty** `packages/web/src/components/FileTree/FileTree.module.css` — all styles moved to Tree.module.css.\n\n### Step 6: Remove ast-grep suppression\n\n**Edit** `rules/no-direct-react-aria-import.yml`:\n- Delete the comment and FileTree.tsx ignore path (lines 13-14).\n\n### Step 7: Verify\n\n1. `pnpm vitest run` in packages/ui — Tree tests pass.\n2. `pnpm vitest run` in packages/web — all existing tests pass.\n3. `ast-grep scan` — zero violations.\n4. `pnpm vp build` — build succeeds.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-28T07:47:40.494457779Z","created_by":"sqve","updated_at":"2026-03-28T07:51:09.046787362Z","closed_at":"2026-03-28T07:51:09.039643134Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","ui"],"dependencies":[{"issue_id":"greppa-33m.4","depends_on_id":"greppa-33m","type":"parent-child","created_at":"2026-03-28T07:47:40.494457779Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-33q","title":"Restrict diff selection to one side","description":"## Problem\n\nBrowser text selection crosses both diff sides because left/right `RowSideCell` components share the same DOM flow in a CSS grid row. Dragging on the right side selects content from the left side too.\n\n## Design\n\nA `useDiffSelection` hook that gates selection to one side via a `data-active-side` attribute on the viewer root, combined with a `copy` event handler that extracts only the relevant side's content.\n\n### Requirements\n\n- [ ] Selection is visually restricted to the side where the drag started\n- [ ] Copy produces only raw code lines from the active side (no line numbers, no gutters)\n- [ ] Context lines included when in selection range\n- [ ] Hunk headers excluded from copy and selection\n- [ ] Empty cells skipped during copy\n- [ ] Keyboard selection (Shift+Click, Shift+Arrow) respects side constraint\n- [ ] Ctrl+A not intercepted — filtering happens at copy time\n- [ ] Works with TanStack Virtual (CSS gating on root so mounted rows inherit state)\n- [ ] Works in Chrome, Firefox, and Safari\n\n### Anti-patterns\n\n- Do not intercept Ctrl+A or change its native behavior\n- Do not use two separate virtualizers for left/right sides\n- Do not restructure the grid layout or DOM hierarchy\n- Do not store selection state in React state (use DOM attributes for performance)\n\n### Components\n\n**CSS (~6 lines in `DiffViewer.module.css`):**\n- `.viewer[data-active-side=\"left\"]` disables `user-select` on right-side cells\n- `.viewer[data-active-side=\"right\"]` disables `user-select` on left-side cells\n- `.hunkHeader` gets `user-select: none`\n\n**`useDiffSelection` hook:**\n- `mousedown`: finds closest `[data-side]`, sets `data-active-side` on viewer root\n- `mouseup` (on `document`): clears `data-active-side`\n- `copy`: walks Selection, collects `.content` text from active side, writes to clipboard\n- Shift+Click: enforces same-side constraint\n\n### Success criteria\n\n1. Selecting text on one side does not highlight text on the opposite side\n2. Copied text contains only code content (no line numbers, no hunk headers)\n3. Keyboard-driven selection stays within the originating side\n4. No regressions in diff rendering, scrolling, or virtualization","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-04-01T13:43:31.347297212Z","created_by":"sqve","updated_at":"2026-04-01T19:29:43.757334183Z","closed_at":"2026-04-01T19:29:43.756933908Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["frontend","selection"]}
{"id":"greppa-33q.1","title":"Implement useDiffSelection hook and CSS gating","description":"## Goal\n\nCreate a `useDiffSelection` hook and CSS rules that restrict text selection and clipboard output to a single diff side.\n\n## Behaviors\n\n1. mousedown on a `[data-side]` element sets `data-active-side` on the viewer root\n2. mouseup on `document` clears `data-active-side`\n3. CSS disables `user-select` on the opposite side when `data-active-side` is set\n4. `copy` event handler collects only `.content` text from the active side, excluding gutters, hunk headers, and empty cells\n5. Shift+Click enforces same-side constraint (prevents cross-side selection extension)\n6. Hunk headers have `user-select: none`\n\n## Success criteria\n\n- [ ] Selecting text on one side does not highlight text on the opposite side\n- [ ] Copied text contains only code content from the active side (no line numbers, no hunk headers)\n- [ ] Shift+Click cannot extend selection across sides\n- [ ] Hunk headers are not selectable\n- [ ] Hook is wired into `DiffViewer.tsx` on the scroll container ref\n- [ ] No regressions in diff rendering, scrolling, or virtualization\n\n## Files to touch\n\n- `packages/web/src/components/DiffViewer/useDiffSelection.ts` (new)\n- `packages/web/src/components/DiffViewer/DiffViewer.module.css` (edit)\n- `packages/web/src/components/DiffViewer/DiffViewer.tsx` (edit — wire hook)","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-01T13:43:48.851670898Z","created_by":"sqve","updated_at":"2026-04-01T14:12:54.693825242Z","closed_at":"2026-04-01T14:12:54.693602585Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["frontend","selection"],"dependencies":[{"issue_id":"greppa-33q.1","depends_on_id":"greppa-33q","type":"parent-child","created_at":"2026-04-01T13:43:48.851670898Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-35g","title":"References","description":"- packages/core/src/index.ts — existing ChangeType and FileEntry schemas","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-31T10:55:52.343452084Z","updated_at":"2026-03-31T10:56:02.036328671Z","closed_at":"2026-03-31T10:56:02.036177360Z","close_reason":"wontdo","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"]}
{"id":"greppa-3gt","title":"Add URL routing for file selection","description":"Opening different files does not change the URL. The selected file path should be reflected in the URL so that file selections are shareable and browser back/forward navigation works. Needs a routing system (e.g. react-router or URL search params).","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-26T18:19:48.185077136Z","created_by":"sqve","updated_at":"2026-03-30T06:43:40.597486590Z","closed_at":"2026-03-30T06:43:40.579895280Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["navigation","ui"]}
{"id":"greppa-3jt","title":"Replace main-thread highlighting with worker-backed hook","description":"## Goal\n\nReplace the current useSyntaxHighlighting hook (which runs Shiki on the main thread) with a new implementation that sends highlight requests to the Web Worker from greppa-fku and returns tokens progressively. First paint shows plain text; tokens patch in asynchronously.\n\n## Behaviors\n\n1. Hook spawns the highlight worker once and reuses it across renders\n2. Hook sends highlight request to worker when diff or theme changes\n3. Hook returns null token map immediately (plain text first paint)\n4. Hook updates token map when worker responds with highlighted tokens\n5. Hook returns previous tokens during file switches (no flash to unstyled)\n6. TokenSpans and RowSideCell continue to work with the new HighlightToken type\n7. DiffViewer passes worker token map to rows identically to current flow\n\n## Success criteria\n\n- [ ] useSyntaxHighlighting returns HighlightToken map from worker instead of ThemedToken map from main thread\n- [ ] First render shows plain text (null token map), tokens appear async\n- [ ] Worker is created once, not per file switch\n- [ ] Existing DiffViewer tests still pass\n- [ ] TokenSpans accepts the new HighlightToken type\n- [ ] No Shiki imports remain on the main thread","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-30T14:01:33.763810553Z","created_by":"sqve","updated_at":"2026-03-31T08:39:05.406668688Z","closed_at":"2026-03-31T08:39:05.406451867Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"]}
{"id":"greppa-3mf","title":"Developer tooling — root scripts and lint/format config","description":"## Context\n\nThe walking skeleton is complete (greppa-3nc closed). All 5 workspace packages exist, `vp test`/`vp check` work with defaults, but no package.json scripts exist and oxlint/oxfmt run with zero project-specific configuration. Before building domain logic, codify the project's code standards and add convenience scripts.\n\n## Requirements\n\n- [ ] Root `package.json` has scripts: `dev`, `build`, `test`, `lint`, `format`, `format:check`, `check`, `typecheck`\n- [ ] `.oxlintrc.json` at root with TypeScript, React, Vitest, and import plugins enabled\n- [ ] `.oxlintrc.json` sets `correctness: error`, `suspicious: warn`, `perf: warn`; test file overrides relax strict rules\n- [ ] `.oxfmtrc.json` at root with project formatting preferences (single quotes, trailing commas, 100 print width)\n- [ ] All existing code passes `vp check` cleanly after configuration\n- [ ] No new dependencies required — oxlint and oxfmt are bundled in vite-plus\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Config location | Standalone `.oxlintrc.json` + `.oxfmtrc.json` | Full config surface available; editor extensions read standalone files; vp's lint/fmt blocks are alpha with limited fields |\n| oxlint plugins | typescript, react, vitest, import | Matches the stack — Effect uses TS, web uses React, tests use vitest, import rules catch cycles |\n| oxfmt style | Single quotes, trailing commas, 100 width, semicolons | Single quotes match existing code; 100 width is oxfmt default; trailing commas reduce diff noise |\n| Script wrappers | Thin `vp` wrappers in root package.json | `pnpm test` is muscle memory; scripts document available commands; no `vp run` tasks needed yet for built-in ops |\n| typecheck script | `tsc --noEmit` directly | vp check includes type checking, but a standalone typecheck script is useful for CI and editor integration |\n| dev script | Deferred — not part of this epic | Two-process dev (server + web) needs concurrency tooling; will address when building the dev workflow |\n\n## Success criteria\n\n1. `pnpm test` runs vitest and passes\n2. `pnpm lint` runs oxlint with configured plugins/rules\n3. `pnpm format:check` verifies formatting without modifying files\n4. `pnpm check` runs lint + format + typecheck in one pass\n5. `pnpm typecheck` runs tsc independently\n6. All existing source files pass all checks cleanly","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-25T20:01:36.750620737Z","created_by":"sqve","updated_at":"2026-03-25T20:38:13.697017500Z","closed_at":"2026-03-25T20:38:13.696194466Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"greppa-3mf.1","title":"Configure oxlint and oxfmt with project rules","description":"## What\n\nCreate `.oxlintrc.json` and `.oxfmtrc.json` at the monorepo root with project-appropriate settings. Enable type-aware mode.\n\n## oxlint config (.oxlintrc.json)\n\n### Plugins\n\n```json\n[\"typescript\", \"react\", \"react_perf\", \"unicorn\", \"oxc\", \"import\", \"vitest\", \"jsx_a11y\", \"promise\", \"node\"]\n```\n\nNo jest, jsdoc, nextjs, or vue plugins.\n\n### Categories\n\n```json\n{\n  \"correctness\": \"error\",\n  \"suspicious\": \"error\",\n  \"perf\": \"warn\",\n  \"pedantic\": \"off\",\n  \"style\": \"off\",\n  \"restriction\": \"off\",\n  \"nursery\": \"off\"\n}\n```\n\n### Type-aware mode\n\n```json\n{\n  \"options\": {\n    \"typeAware\": true\n  }\n}\n```\n\n### Rules to turn OFF from enabled categories\n\n```json\n{\n  \"jest/expect-expect\": \"off\",\n  \"jest/no-conditional-expect\": \"off\",\n  \"jest/no-disabled-tests\": \"off\",\n  \"jest/no-export\": \"off\",\n  \"jest/no-focused-tests\": \"off\",\n  \"jest/no-standalone-expect\": \"off\",\n  \"jest/require-to-throw-message\": \"off\",\n  \"jest/valid-describe-callback\": \"off\",\n  \"jest/valid-expect\": \"off\",\n  \"jest/valid-title\": \"off\",\n  \"typescript/unbound-method\": \"off\",\n  \"typescript/no-extraneous-class\": \"off\",\n  \"typescript/no-unsafe-enum-comparison\": \"off\",\n  \"react/react-in-jsx-scope\": \"off\",\n  \"oxc/no-async-endpoint-handlers\": \"off\",\n  \"oxc/no-this-in-exported-function\": \"off\"\n}\n```\n\n### Cherry-picked rules — error\n\nUser preferences:\n- `eslint/func-style`: arrow functions over function declarations\n- `eslint/curly`: braces for all control flow\n- `eslint/prefer-const`: const when not reassigned\n- `eslint/no-nested-ternary`: no nested ternaries\n- `typescript/consistent-type-imports`: import type for type-only imports\n- `typescript/consistent-type-definitions`: interface for object shapes\n- `typescript/no-inferrable-types`: omit inferable types\n- `typescript/prefer-nullish-coalescing`: ?? over ||\n- `typescript/no-explicit-any`: unknown instead of any\n- `typescript/no-non-null-assertion`: explicit checks over !\n\nBug prevention:\n- `eslint/eqeqeq` (with allowNull option): === except null checks\n- `eslint/array-callback-return`: missing return in .map/.filter\n- `eslint/no-fallthrough`: accidental switch fallthrough\n- `eslint/no-param-reassign`: no parameter reassignment\n- `eslint/no-case-declarations`: scoping bugs in switch\n- `eslint/no-loop-func`: closure-over-loop bugs\n- `eslint/no-redeclare`: accidental re-declarations\n- `eslint/no-self-compare`: x === x is always a bug\n- `eslint/no-throw-literal`: always throw Error objects\n- `eslint/no-array-constructor`: use [] literal\n- `eslint/no-new-wrappers`: no new String() etc\n- `eslint/no-object-constructor`: use {} literal\n- `eslint/no-promise-executor-return`: mistakes in Promise constructors\n- `eslint/no-prototype-builtins`: use Object.hasOwn()\n- `eslint/radix`: always pass radix to parseInt\n- `typescript/no-misused-promises`: floating promises, promises in booleans\n- `typescript/switch-exhaustiveness-check`: exhaustive union handling\n- `typescript/only-throw-error`: only throw Error instances\n- `typescript/return-await`: proper async stack traces\n- `typescript/restrict-plus-operands`: no implicit string coercion\n- `typescript/ban-ts-comment`: force @ts-expect-error over @ts-ignore\n- `typescript/ban-types`: ban Object, Function, {} types\n- `typescript/no-unsafe-function-type`: ban Function type\n- `typescript/prefer-includes`: .includes() over .indexOf()\n- `typescript/prefer-promise-reject-errors`: reject with Error objects\n- `typescript/prefer-ts-expect-error`: @ts-expect-error over @ts-ignore\n- `import/no-cycle`: circular dependencies\n- `react/rules-of-hooks`: hook call order\n- `react/checked-requires-onchange-or-readonly`: controlled input bugs\n- `react/jsx-no-target-blank`: security\n- `react/no-unescaped-entities`: raw entities in JSX\n- `unicorn/no-negation-in-equality-check`: !a === b bugs\n- `unicorn/no-immediate-mutation`: mutation on temporaries\n- `unicorn/no-instanceof-array`: use Array.isArray()\n- `unicorn/no-this-assignment`: no const self = this\n- `unicorn/no-unreadable-iife`: unreadable IIFEs\n- `unicorn/no-useless-promise-resolve-reject`: redundant resolves\n- `unicorn/new-for-builtins`: new Map(), new Set()\n- `unicorn/consistent-empty-array-spread`: [...[], x] anti-pattern\n- `unicorn/escape-case`: uppercase escape sequences\n\nESM enforcement:\n- `eslint/no-var`: ban var\n- `import/no-commonjs`: ban require/module.exports\n- `import/no-amd`: ban AMD\n- `import/no-dynamic-require`: ban dynamic require\n- `import/no-webpack-loader-syntax`: ban webpack loader syntax\n- `typescript/no-namespace`: ban namespaces\n- `typescript/no-require-imports`: ban require() imports\n- `typescript/no-var-requires`: ban const x = require()\n- `unicorn/prefer-node-protocol`: node: prefix required\n- `unicorn/prefer-module`: ban __dirname, __filename\n- `oxc/no-const-enum`: breaks isolatedModules\n\nCode quality:\n- `eslint/no-implicit-coercion`: explicit Boolean(), Number()\n- `eslint/prefer-template`: template literals\n- `eslint/prefer-object-has-own`: Object.hasOwn()\n- `eslint/prefer-rest-params`: ...args over arguments\n- `eslint/prefer-spread`: spread over .apply()\n- `eslint/no-multi-assign`: no a = b = c\n- `eslint/no-multi-str`: no backslash continuation\n- `eslint/no-new-func`: no new Function()\n- `eslint/no-return-assign`: no assignments in return\n- `eslint/no-script-url`: no javascript: URLs\n- `eslint/no-labels`: disallow labels\n- `eslint/no-label-var`: labels must not shadow vars\n- `eslint/default-case-last`: default case last in switch\n- `eslint/default-param-last`: default params after required\n- `eslint/no-duplicate-imports`: merge imports from same module\n- `eslint/yoda`: no \"red\" === color\n- `import/consistent-type-specifier-style`: consistent import type\n- `import/first`: imports before other statements\n- `import/no-duplicates`: merge duplicate imports\n- `import/no-mutable-exports`: exported bindings must be const\n- `import/no-anonymous-default-export`: named exports\n- `typescript/consistent-generic-constructors`: generics on constructor (warn→error)\n- `typescript/no-empty-interface`: disallow empty interfaces\n- `typescript/array-type`: consistent T[] syntax\n- `typescript/ban-tslint-comment`: remove tslint directives\n- `typescript/adjacent-overload-signatures`: keep overloads together\n- `typescript/no-empty-object-type`: {} means any non-nullish\n- `typescript/no-import-type-side-effects`: verbatimModuleSyntax alignment\n- `typescript/no-non-null-asserted-nullish-coalescing`: x! ?? y is contradictory\n- `typescript/use-unknown-in-catch-callback-variable`: err as unknown\n- `unicorn/catch-error-name`: name catch var error\n- `unicorn/error-message`: always pass message to Error()\n- `unicorn/throw-new-error`: throw new Error()\n- `unicorn/prefer-structured-clone`: over JSON.parse(JSON.stringify())\n- `unicorn/prefer-date-now`: Date.now() over new Date().getTime()\n- `unicorn/prefer-type-error`: throw TypeError for type checks\n- `unicorn/no-new-buffer`: Buffer.from()/Buffer.alloc()\n- `unicorn/no-length-as-slice-end`: redundant slice end\n- `unicorn/no-anonymous-default-export`: named exports\n- `unicorn/no-abusive-eslint-disable`: bare eslint-disable banned\n- `unicorn/no-document-cookie`: use proper cookie library\n- `unicorn/no-useless-error-capture-stack-trace`: extending Error already captures stack\n- `unicorn/prefer-modern-math-apis`: Math.trunc() etc\n- `unicorn/prefer-number-properties`: Number.parseInt()\n- `unicorn/switch-case-braces`: braces in switch cases\n- `react/jsx-pascal-case`: PascalCase components\n- `react/button-has-type`: explicit button type\n- `react/no-clone-element`: deprecated API\n- `react/no-react-children`: legacy API\n- `react/jsx-no-constructed-context-values`: unmemoized context values\n- `unicorn/prefer-array-find`: .find() over .filter()[0]\n- `unicorn/prefer-array-flat-map`: .flatMap() over .map().flat()\n- `eslint/no-useless-call`: dead weight .call(undefined)\n- `oxc/no-accumulating-spread`: O(n²) spread in reduce\n- `eslint/no-alert`: no alert/confirm/prompt\n- `eslint/no-empty`: empty blocks\n- `eslint/no-sequences`: comma operator\n- `eslint/no-proto`: deprecated __proto__\n- `eslint/unicode-bom`: no BOM in UTF-8\n- `oxc/bad-bitwise-operator`: | vs || typos\n- `node/no-new-require`: ESM only\n- `node/no-path-concat`: use path.join()\n- `import/no-namespace`: no import * as\n- `node/no-exports-assign`: ESM only\n- `eslint/getter-return`: getters must return (nursery)\n- `eslint/no-unreachable`: dead code (nursery)\n- `import/export`: duplicate exports (nursery)\n- `promise/no-return-in-finally`: swallows errors (nursery)\n- `react/require-render-return`: components must return (nursery)\n\n### Cherry-picked rules — warn\n\n- `eslint/no-console`: Effect handles logging\n- `eslint/complexity`: cyclomatic complexity > 15\n- `eslint/arrow-body-style`: concise arrow bodies\n- `eslint/max-params`: too many params\n- `eslint/max-depth`: nesting > 4\n- `eslint/max-nested-callbacks`: callback nesting > 3\n- `eslint/no-else-return`: prefer early returns\n- `eslint/no-lonely-if`: use else if\n- `eslint/no-negated-condition`: prefer positive conditions\n- `eslint/no-useless-return`: redundant returns\n- `eslint/no-template-curly-in-string`: \"${x}\" when template literal intended\n- `eslint/no-useless-computed-key`: ['key'] when not needed\n- `eslint/operator-assignment`: x += 1 over x = x + 1\n- `eslint/prefer-exponentiation-operator`: ** over Math.pow()\n- `eslint/prefer-numeric-literals`: 0b, 0o, 0x over parseInt\n- `eslint/prefer-object-spread`: {...obj} over Object.assign\n- `eslint/grouped-accessor-pairs`: keep getters/setters together\n- `eslint/guard-for-in`: hasOwnProperty in for...in\n- `eslint/no-lone-blocks`: remove unnecessary blocks\n- `eslint/symbol-description`: always name Symbols\n- `eslint/accessor-pairs`: getter-without-setter bugs\n- `eslint/no-await-in-loop`: flag raw await in loops\n- `eslint/no-empty-function`: usually a mistake\n- `typescript/no-deprecated`: flag deprecated APIs\n- `typescript/require-await`: async without await\n- `typescript/no-confusing-void-expression`: void in arrow bodies\n- `typescript/related-getter-setter-pairs`: mismatched types\n- `typescript/consistent-indexed-object-style`: Record<K,V>\n- `typescript/consistent-type-assertions`: as T over <T>\n- `typescript/prefer-for-of`: for...of when index not needed\n- `typescript/prefer-function-type`: type Fn = () => void\n- `typescript/prefer-reduce-type-parameter`: type param on reduce<T>()\n- `typescript/unified-signatures`: merge overloads\n- `typescript/non-nullable-type-assertion-style`: simpler assertions\n- `typescript/no-dynamic-delete`: flag delete obj[key]\n- `typescript/no-invalid-void-type`: void in unions/intersections\n- `typescript/consistent-type-exports`: export type (nursery)\n- `typescript/no-unnecessary-condition`: always-true/false (nursery)\n- `typescript/no-unnecessary-type-conversion`: redundant conversions (nursery)\n- `typescript/no-unnecessary-type-parameters`: single-use type params (nursery)\n- `typescript/no-useless-default-assignment`: param = undefined (nursery)\n- `typescript/prefer-find`: .filter()[0] pattern (nursery)\n- `typescript/prefer-optional-chain`: x?.y over x && x.y (nursery)\n- `typescript/prefer-readonly`: fields never reassigned (nursery)\n- `typescript/prefer-string-starts-ends-with`: nursery\n- `import/no-named-as-default`: importing named via default\n- `import/no-named-as-default-member`: accessing named as default property\n- `import/no-unassigned-import`: side-effect imports\n- `import/no-named-default`: don't import { default as x }\n- `promise/no-nesting`: nested .then() chains\n- `promise/no-return-wrap`: no Promise.resolve(x) in .then()\n- `promise/param-names`: name params resolve/reject\n- `promise/prefer-await-to-then`: async/await over .then()\n- `react/jsx-boolean-value`: <C disabled /> not <C disabled={true} />\n- `react/jsx-curly-brace-presence`: remove unnecessary curlies\n- `react/jsx-fragments`: <> shorthand\n- `react/self-closing-comp`: <Component /> not <Component></Component>\n- `react/jsx-no-useless-fragment`: remove unnecessary fragments\n- `react/no-array-index-key`: index keys break reconciliation\n- `react/no-danger`: dangerouslySetInnerHTML should be rare\n- `react_perf/jsx-no-new-array-as-prop`: inline array literals\n- `react_perf/jsx-no-new-object-as-prop`: inline object literals\n- `jsx_a11y/anchor-ambiguous-text`: \"click here\" links\n- `unicorn/consistent-function-scoping`: hoist out of closures\n- `unicorn/no-array-for-each`: for...of over .forEach()\n- `unicorn/no-object-as-default-parameter`: mutable default objects\n- `unicorn/explicit-length-check`: .length > 0 not .length\n- `unicorn/no-hex-escape`: prefer unicode escapes\n- `unicorn/no-typeof-undefined`: use === undefined\n- `unicorn/no-unnecessary-array-flat-depth`: remove default .flat(1)\n- `unicorn/no-unnecessary-array-splice-count`: redundant count\n- `unicorn/no-unnecessary-slice-end`: redundant slice end\n- `unicorn/no-useless-switch-case`: fallthrough to default\n- `unicorn/prefer-array-flat`: .flat() over concat\n- `unicorn/prefer-array-some`: .some() over .find() !== undefined\n- `unicorn/prefer-at`: .at(-1) for last element\n- `unicorn/prefer-code-point`: codePointAt over charCodeAt\n- `unicorn/prefer-event-target`: EventTarget over EventEmitter\n- `unicorn/prefer-math-min-max`: Math.min/max over ternary\n- `unicorn/prefer-math-trunc`: Math.trunc() over | 0\n- `unicorn/prefer-native-coercion-functions`: Number not x => Number(x)\n- `unicorn/prefer-regexp-test`: .test() over .match() for booleans\n- `unicorn/prefer-string-replace-all`: .replaceAll()\n- `unicorn/prefer-string-slice`: .slice() over .substring()\n- `unicorn/prefer-top-level-await`: ESM supports it\n- `unicorn/require-number-to-fixed-digits-argument`: pass digits to .toFixed()\n- `unicorn/prefer-prototype-methods`: Array.prototype.slice.call\n- `unicorn/consistent-date-clone`: consistent new Date(date)\n- `unicorn/consistent-existence-index-check`: consistent !== -1\n- `unicorn/custom-error-definition`: proper custom error patterns\n- `unicorn/filename-case`: camelCase (PascalCase for React)\n- `unicorn/no-array-method-this-argument`: arrow over thisArg\n- `unicorn/no-await-expression-member`: assign await result first\n- `unicorn/no-unreadable-array-destructuring`: no const [,,, x]\n- `unicorn/no-useless-collection-argument`: redundant Map/Set args\n- `unicorn/no-zero-fractions`: 1 not 1.0\n- `unicorn/number-literal-case`: uppercase 0xFF\n- `unicorn/numeric-separators-style`: 1_000_000\n- `unicorn/prefer-array-index-of`: .indexOf() for equality\n- `unicorn/prefer-default-parameters`: default params over ?? default\n- `unicorn/prefer-global-this`: globalThis over window/global\n- `unicorn/prefer-includes`: .includes() over .indexOf()\n- `unicorn/prefer-logical-operator-over-ternary`: a ?? b over a ? a : b\n- `unicorn/prefer-negative-index`: .slice(-2)\n- `unicorn/prefer-object-from-entries`: Object.fromEntries()\n- `unicorn/prefer-optional-catch-binding`: omit unused catch param\n- `unicorn/prefer-spread`: spread over .concat()\n- `unicorn/prefer-string-trim-start-end`: .trimStart()\n- `unicorn/prefer-response-static-json`: Response.json()\n- `unicorn/relative-url-style`: consistent relative URLs\n- `unicorn/require-array-join-separator`: pass separator to .join()\n- `unicorn/text-encoding-identifier-case`: 'utf-8'\n- `unicorn/no-lonely-if`: combine else { if }\n- `unicorn/prefer-blob-reading-methods`: .text() over decode\n- `unicorn/prefer-set-has`: .has() over .includes() for large sets\n- `unicorn/no-instanceof-builtins`: instanceof fails across realms\n- `oxc/no-barrel-file`: discourage barrel files\n- `oxc/no-map-spread`: prefer .set()\n- `oxc/branches-sharing-code`: duplicated if/else code (nursery)\n- `node/no-process-env`: centralize env access\n- `node/no-process-exit` (via unicorn): warn outside CLI\n\n### Overrides — test files\n\nFor `**/*.test.{ts,tsx}`:\n- `typescript/no-explicit-any`: \"off\"\n- `typescript/no-non-null-assertion`: \"off\"\n- `typescript/no-unsafe-type-assertion`: \"off\"\n- `eslint/no-empty-function`: \"off\"\n- `eslint/max-nested-callbacks`: \"off\"\n\n### Explicitly skipped (do NOT enable)\n\n- `typescript/no-unsafe-argument`: too noisy with Effect generics\n- `typescript/no-unsafe-assignment`: too noisy with Effect generics\n- `typescript/no-unsafe-call`: too noisy with Effect generics\n- `typescript/no-unsafe-member-access`: too noisy with Effect generics\n- `typescript/no-unsafe-return`: too noisy with Effect generics\n- `typescript/strict-boolean-expressions`: too noisy with Effect\n- `typescript/explicit-function-return-type`: user prefers inference\n- `typescript/explicit-module-boundary-types`: user prefers inference\n- `unicorn/no-null`: project uses null for domain models\n- `unicorn/no-static-only-class`: Effect ServiceMap.Service pattern\n- `unicorn/no-useless-undefined`: project uses undefined for optional params\n- `eslint/no-warning-comments`: TODOs useful during development\n- `eslint/sort-keys`: kills logical grouping\n- `eslint/no-magic-numbers`: too noisy\n- `eslint/no-ternary`: ternaries are fine, just not nested\n\n## oxfmt config (.oxfmtrc.json)\n\n- `printWidth: 100` (oxfmt default)\n- `singleQuote: true`\n- `trailingComma: \"all\"`\n- `semi: true`\n- `tabWidth: 2`\n- `useTabs: false`\n- `endOfLine: \"lf\"`\n- `bracketSpacing: true`\n- `arrowParens: \"always\"`\n- Ignore: `dist/**`, `coverage/**`\n\n## Acceptance\n\n- `vp lint --type-aware` passes with the new config\n- `vp fmt --check` passes (reformat existing code if needed)\n- `vp check` passes cleanly\n- No false positives on existing Effect code patterns","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-25T20:01:46.294114174Z","created_by":"sqve","updated_at":"2026-03-25T20:36:05.555789840Z","closed_at":"2026-03-25T20:36:05.554310852Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3mf.1","depends_on_id":"greppa-3mf","type":"parent-child","created_at":"2026-03-25T20:01:46.294114174Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3mf.2","title":"Add root package.json scripts for dev workflow","description":"## What\n\nAdd scripts to root `package.json` wrapping `vp` commands. These provide `pnpm test`, `pnpm lint`, etc. as muscle-memory aliases.\n\n## Scripts\n\n- `test`: `vp test`\n- `lint`: `vp lint`\n- `format`: `vp fmt`\n- `format:check`: `vp fmt --check`\n- `check`: `vp check`\n- `typecheck`: `tsc --noEmit`\n- `build`: `vp build`\n- `dev`: deferred (placeholder echo) — two-process dev needs concurrency tooling\n\n## Acceptance\n\n- `pnpm test` runs vitest and passes\n- `pnpm lint` runs oxlint with configured plugins/rules\n- `pnpm format:check` verifies formatting without modifying files\n- `pnpm check` runs lint + format + typecheck in one pass\n- `pnpm typecheck` runs tsc independently\n- `pnpm build` runs vp build","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-25T20:36:23.448439712Z","created_by":"sqve","updated_at":"2026-03-25T20:37:55.378840188Z","closed_at":"2026-03-25T20:37:55.377455934Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3mf.2","depends_on_id":"greppa-3mf","type":"parent-child","created_at":"2026-03-25T20:36:23.448439712Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc","title":"Walking skeleton — monorepo setup","description":"## Context\n\nGreppa has zero implementation — only vision and stack docs. Before building domain logic, prove the full toolchain and architecture works end-to-end: CLI starts an Effect HTTP server, browser connects, everything builds/tests/lints.\n\n## Requirements\n\n- [ ] pnpm workspace with 5 packages (core, server, cli, web, ui)\n- [ ] Vite+ (`vp`) as unified toolchain — build, test, lint, format\n- [ ] Effect v4 beta.40 (pinned) for backend (server, cli, core)\n- [ ] React 19 frontend (web) with Vite dev server proxying `/api` to `:4400`\n- [ ] `greppa serve --port 4400` starts Effect HttpServer via @effect/cli\n- [ ] `GET /api/health` returns `{ status: \"ok\" }`\n- [ ] Web app fetches and renders health status\n- [ ] All deps pinned to exact versions (save-exact=true)\n- [ ] `vp test` passes, `vp lint` clean, `vp fmt` clean\n- [ ] STACK.md updated with v4 and Vite+ corrections (done)\n\n## Design decisions\n\n| Decision | Choice |\n|---|---|\n| Setup depth | Walking skeleton (scaffold + health endpoint vertical slice) |\n| Toolchain | Vite+ alpha, CLI is `vp`, MIT license |\n| Effect | v4 beta.40 pinned to avoid unstable churn |\n| Dev workflow | Two processes — Vite :5173 + Effect :4400 |\n| Packages | All 5: core, server, cli, web, ui |\n\n## Effect v4 patterns\n\n- `ServiceMap.Service` (not `Effect.Service`)\n- `effect/unstable/httpapi` for HttpApi/Builder/Group/Endpoint\n- `effect/unstable/cli` with `Flag`/`Argument` (not Options/Args)\n- `Schema.TaggedErrorClass` (not `Schema.TaggedError`)\n- `@effect/vitest` `it.effect()` unchanged\n- `@effect/sql-sqlite-node` unchanged\n\n## Success criteria\n\n1. `pnpm install` resolves all workspace deps\n2. `vp test` runs and passes across all packages\n3. `vp lint` and `vp fmt` report clean\n4. `greppa serve` starts server on :4400\n5. `curl localhost:4400/api/health` returns `{ status: \"ok\" }`\n6. `pnpm --filter @greppa/web dev` proxies to server and renders health status","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-03-25T15:24:55.146570107Z","created_by":"sqve","updated_at":"2026-03-25T20:00:49.358701241Z","closed_at":"2026-03-25T20:00:49.357758725Z","close_reason":"All 7 tasks complete. Walking skeleton operational: CLI, server, web, tests, lint all working.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"greppa-3nc.1","title":"Root monorepo scaffold","description":"## What\n\nSet up the root monorepo config that all packages build on.\n\n## Deliverables\n\n- `package.json` — `\"type\": \"module\"`, `\"name\": \"greppa\"`, `engines.node >= 22`, `packageManager` field for pnpm\n- `pnpm-workspace.yaml` — `packages: ['packages/*']`\n- `.npmrc` — `save-exact=true`, `strict-peer-dependencies=true`\n- `tsconfig.base.json` — shared compiler options (ESM target, strict, moduleResolution bundler, paths)\n- `vite.config.ts` — root Vite+ config (`defineConfig` from `vite-plus`) with lint/fmt settings\n- Install `vite-plus` as root devDependency\n\n## Verification\n\n- `pnpm install` succeeds\n- `vp --version` runs\n- `vp check` runs (may warn about no source files — that's fine)\n\n## Notes\n\n- All deps pinned to exact versions (enforced by `.npmrc save-exact=true`)\n- ESM throughout (`\"type\": \"module\"`)\n- This task unblocks all package tasks","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-25T15:25:07.061490499Z","created_by":"sqve","updated_at":"2026-03-25T16:38:38.507976660Z","closed_at":"2026-03-25T16:38:38.507523714Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.1","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T15:25:07.061490499Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc.2","title":"Package stubs — core, server, cli, web, ui","description":"## What\n\nCreate the 5 workspace packages with minimal package.json and tsconfig.json so pnpm resolves all internal deps and vp discovers them.\n\n## Deliverables\n\n- `packages/core/package.json` — `@greppa/core`, depends on `effect@4.0.0-beta.40`\n- `packages/server/package.json` — `@greppa/server`, depends on `@greppa/core`, `effect`, `@effect/platform-node`\n- `packages/cli/package.json` — `@greppa/cli`, depends on `@greppa/core`, `@greppa/server`, `effect`\n- `packages/web/package.json` — `@greppa/web`, depends on `react`, `react-dom`\n- `packages/ui/package.json` — `@greppa/ui`, depends on `react`, `react-dom`\n- Each package gets a `tsconfig.json` extending `../../tsconfig.base.json`\n- Each backend package (core, server, cli) gets a `src/index.ts` placeholder export\n- web gets a Vite React entry point (minimal)\n- ui gets a `src/index.ts` placeholder\n\n## Verification\n\n- `pnpm install` resolves all workspace deps\n- `vp check` runs clean across all packages\n- TypeScript project references resolve (no red squiggles on imports)\n\n## Notes\n\n- All deps pinned exact (enforced by .npmrc)\n- ESM throughout (type: module in all package.json)\n- Use workspace protocol for internal deps: `\"@greppa/core\": \"workspace:*\"`","status":"closed","priority":1,"issue_type":"task","owner":"sqve","created_at":"2026-03-25T16:38:55.638118986Z","created_by":"sqve","updated_at":"2026-03-25T16:49:13.728460193Z","closed_at":"2026-03-25T16:49:13.727820407Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.2","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T16:38:55.638118986Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc.3","title":"Health endpoint — Effect HttpServer + CLI serve command","description":"## What\n\nWire the vertical slice: `greppa serve --port 4400` starts an Effect HttpServer that responds to `GET /api/health` with `{ status: \\\"ok\\\" }`.\n\n## Deliverables\n\n- `packages/server/src/HealthApi.ts` — HttpApiGroup with GET /api/health endpoint\n- `packages/server/src/Api.ts` — HttpApi composing HealthApi\n- `packages/server/src/Http.ts` — HttpApiBuilder setup binding handlers\n- `packages/server/src/index.ts` — exports server layer (HttpServer.listen)\n- `packages/cli/src/commands/serve.ts` — `serve` command with `--port` flag (default 4400)\n- `packages/cli/src/main.ts` — root Command wiring serve subcommand\n- `packages/cli/src/index.ts` — entry point calling NodeRuntime.runMain\n- `packages/cli/package.json` — add bin field pointing to src/index.ts\n\n## Verification\n\n- `pnpm --filter @greppa/cli exec tsx src/index.ts serve --port 4400` starts the server\n- `curl localhost:4400/api/health` returns `{ \"status\": \"ok\" }`\n- `vp check` passes\n\n## Notes\n\n- Use Effect v4 patterns: `effect/unstable/httpapi`, `effect/unstable/cli`\n- `ServiceMap.Service` for any services, `Flag` for CLI flags\n- Keep it minimal — no middleware, no database, just the health endpoint","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-25T16:49:31.303737102Z","created_by":"sqve","updated_at":"2026-03-25T17:07:48.172126140Z","closed_at":"2026-03-25T17:07:48.171680815Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.3","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T16:49:31.303737102Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc.4","title":"Set up test infrastructure — vp test, @effect/vitest","description":"## Context\n\nZero test files and no test runner config exist. Vite+ bundles vitest but nothing is wired.\n\n## Requirements\n\n- [ ] Add `@effect/vitest` to server and cli packages\n- [ ] Verify `vp test` discovers and runs `.test.ts` files\n- [ ] Add a `test` script to root `package.json` (or confirm `vp test` works without one)\n\n## Why first\n\nBlocks all other test tasks — no point writing tests until the runner works.","status":"closed","priority":1,"issue_type":"task","assignee":"sqve","created_at":"2026-03-25T19:26:33.230563432Z","created_by":"sqve","updated_at":"2026-03-25T19:31:55.225592176Z","closed_at":"2026-03-25T19:31:55.223925007Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.4","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T19:26:33.230563432Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc.5","title":"Test health endpoint — GET /api/health returns 200 ok","description":"## Context\n\n`packages/server/src/Http.ts` defines `HealthHandlers` and `makeHttpLayer`. Zero tests exist.\n\n## Requirements\n\n- [ ] Integration test: spin up server on ephemeral port, `GET /api/health`, assert 200 + `{ status: \"ok\" }`\n- [ ] Use `Effect.runPromise` pattern (`@effect/vitest` is incompatible with Vite+)\n- [ ] Test file: `packages/server/src/Http.test.ts` (replace smoke test)\n\n## Scope\n\nFiles under test:\n- `packages/server/src/Http.ts` (handler + layer)\n- `packages/server/src/Api.ts` (composition)\n- `packages/server/src/HealthApi.ts` (schema + route)","status":"closed","priority":1,"issue_type":"task","assignee":"sqve","created_at":"2026-03-25T19:26:38.987551501Z","created_by":"sqve","updated_at":"2026-03-25T19:36:51.612184136Z","closed_at":"2026-03-25T19:36:51.610762238Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.5","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T19:26:38.987551501Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc.6","title":"Test CLI command wiring — serve subcommand and port flag","description":"## Context\n\n`packages/cli/src/commands/serve.ts` defines the `serve` command with a `--port` flag (default 4400). `main.ts` wires it as a subcommand. Zero tests exist.\n\n## Requirements\n\n- [ ] Test port flag defaults to 4400\n- [ ] Test port flag accepts integer input\n- [ ] Test `main` command includes `serve` as subcommand\n- [ ] Test file: `packages/cli/src/commands/serve.test.ts`\n\n## Blocked by\n\ngreppa-3nc.4 (test infrastructure)","status":"closed","priority":2,"issue_type":"task","assignee":"sqve","created_at":"2026-03-25T19:26:44.089560782Z","created_by":"sqve","updated_at":"2026-03-25T19:42:24.485021626Z","closed_at":"2026-03-25T19:42:24.483526364Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.6","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T19:26:44.089560782Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nc.7","title":"Web proxy + health fetch — Vite proxy /api to :4400, render health status","description":"## Context\n\nThe web package has a placeholder App component. It needs to proxy API calls to the Effect server and render the health status.\n\n## Requirements\n\n- [ ] Configure Vite dev server proxy: `/api` → `http://localhost:4400`\n- [ ] Fetch `GET /api/health` in the web app\n- [ ] Render the health status response in the App component\n- [ ] Verify with `pnpm --filter @greppa/web dev` + running server\n\n## Files\n\n- `packages/web/vite.config.ts` — add proxy config\n- `packages/web/src/App.tsx` — fetch and render health status","status":"closed","priority":1,"issue_type":"task","assignee":"sqve","created_at":"2026-03-25T19:42:56.689811451Z","created_by":"sqve","updated_at":"2026-03-25T19:43:43.998722023Z","closed_at":"2026-03-25T19:43:43.997178104Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-3nc.7","depends_on_id":"greppa-3nc","type":"parent-child","created_at":"2026-03-25T19:42:56.689811451Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-3nt","title":"Long diff lines overflow horizontally instead of scrolling within panel","description":"When a diff contains lines longer than the panel width, the content overflows and a horizontal scrollbar appears on the individual line rather than being contained within the DiffViewer panel. The table layout with white-space: pre on .content cells causes this — the overflow should be handled at the viewer level.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-03-26T18:19:46.832189004Z","created_by":"sqve","updated_at":"2026-03-26T19:03:50.280064104Z","closed_at":"2026-03-26T19:03:50.275357031Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["diff-viewer","ui"]}
{"id":"greppa-744","title":"CI foundation: GitHub Actions, CodeRabbit, and Changesets","description":"## Problem\n\nNo CI exists. Code quality checks only run locally via pre-commit hooks. PRs can be merged without passing lint, typecheck, tests, or build. No automated code review. No release management.\n\n## Requirements\n\n1. CI workflow runs format check, lint, typecheck, test, and build as separate parallel jobs on PRs and pushes to main\n2. All GitHub Actions are SHA-pinned with version comments\n3. Cancel-in-progress enabled for superseded PR runs\n4. Dependency review workflow blocks PRs introducing vulnerable dependencies\n5. Changesets configured with GitHub changelog integration — release workflow creates Version Packages PR and publishes on merge\n6. CodeRabbit configured with assertive profile, path filters for generated/lock files, knowledge base pointing to CLAUDE.md\n7. Branch protection rules: require PRs (0 approvals), require status checks, linear history, block force pushes\n\n## Success criteria\n\n1. Pushing to a PR branch triggers CI that runs format:check, lint, typecheck, test, and build as separate parallel jobs — each visible as its own check in the GitHub UI\n2. All actions use SHA-pinned references with version comments (e.g., actions/checkout@<sha> # v4.2.2)\n3. Pushing a new commit to a PR while CI is running cancels the previous run\n4. A PR adding a dependency with a known vulnerability fails the dependency-review check\n5. Running pnpm changeset creates a changeset file; merging to main with changesets triggers a \"Version Packages\" PR via changesets/action\n6. CodeRabbit posts a review comment on new PRs with assertive profile, skipping dist/lockfile/changeset paths\n7. Direct pushes to main are blocked; PRs require passing status checks before merge","design":"## Problem\n\nNo CI exists. Code quality checks only run locally via pre-commit hooks. PRs can be merged without passing lint, typecheck, tests, or build. No automated code review. No release management.\n\n## Requirements\n\n1. CI workflow runs format check, lint, typecheck, test, and build as separate parallel jobs on PRs and pushes to main\n2. All GitHub Actions are SHA-pinned with version comments\n3. Cancel-in-progress enabled for superseded PR runs\n4. Dependency review workflow blocks PRs introducing vulnerable dependencies\n5. Changesets configured with GitHub changelog integration — release workflow creates Version Packages PR and publishes on merge\n6. CodeRabbit configured with assertive profile, path filters for generated/lock files, knowledge base pointing to CLAUDE.md\n7. Branch protection rules: require PRs (0 approvals), require status checks, linear history, block force pushes\n\n## Success criteria\n\n1. Pushing to a PR branch triggers CI that runs format:check, lint, typecheck, test, and build as separate parallel jobs — each visible as its own check in the GitHub UI\n2. All actions use SHA-pinned references with version comments (e.g., actions/checkout@<sha> # v4.2.2)\n3. Pushing a new commit to a PR while CI is running cancels the previous run\n4. A PR adding a dependency with a known vulnerability fails the dependency-review check\n5. Running pnpm changeset creates a changeset file; merging to main with changesets triggers a \"Version Packages\" PR via changesets/action\n6. CodeRabbit posts a review comment on new PRs with assertive profile, skipping dist/lockfile/changeset paths\n7. Direct pushes to main are blocked; PRs require passing status checks before merge\n\n## Solution\n\nThree GitHub Actions workflows (ci, dependency-review, release), a CodeRabbit config, and Changesets for release management.\n\n## Architecture\n\n### CI workflow (.github/workflows/ci.yml)\n- Triggers: push to main, pull_request to main\n- Concurrency: ci-${{ github.ref }} with cancel-in-progress\n- Permissions: contents: read\n- 5 parallel jobs: format, lint, typecheck, test, build\n- Each job: checkout → pnpm/action-setup → setup-node (node 22, cache pnpm) → pnpm install --frozen-lockfile → run command\n- Commands: pnpm format:check, pnpm lint, pnpm typecheck, pnpm test, pnpm build\n\n### Dependency review workflow (.github/workflows/dependency-review.yml)\n- Triggers: pull_request only\n- Single job using actions/dependency-review-action with fail-on-severity: moderate\n\n### Release workflow (.github/workflows/release.yml)\n- Triggers: push to main only\n- Concurrency: release-${{ github.ref }}\n- Uses changesets/action with commitMode: github-api\n- Publish command: pnpm ci:publish (pnpm publish -r --access public --no-git-checks)\n- Creates GitHub Releases automatically\n- Permissions: contents: write, pull-requests: write\n\n### CodeRabbit config (.coderabbit.yaml)\n- Profile: assertive\n- Tone: concise and direct, flag real issues, skip style enforced by oxlint/oxfmt\n- Path filters: exclude dist/, node_modules/, lockfiles, .beads/, .changeset/*.md\n- Knowledge base: CLAUDE.md\n- Auto-review: enabled, skip drafts and WIP titles\n\n### Changesets config (.changeset/config.json)\n- Changelog: @changesets/changelog-github with repo sQVe/greppa\n- Access: public\n- Base branch: main\n- updateInternalDependencies: patch\n\n### Branch protection (manual via GitHub UI)\n- Require PRs (0 approvals for solo)\n- Required status checks: format, lint, typecheck, test, build, dependency-review\n- Require linear history\n- Block force pushes and deletions\n- Require conversation resolution\n\n## Key decisions\n- Single ci.yml with parallel jobs (not per-package workflows) — project is small, 5 packages\n- Separate steps for format/lint/typecheck — better GitHub UI error visibility than bundled pnpm check\n- SHA-pinned actions — security best practice after tj-actions supply chain attack\n- Corepack enabled in setup-node to enforce pnpm version from packageManager field\n- Node 22 LTS pinned in CI\n- changesets/action with commitMode: github-api — compatible with branch protection signed commit requirements\n- release.yml uses pnpm install WITHOUT --frozen-lockfile — changesets bumps versions so lockfile needs updating\n- ci:publish uses --access public --no-git-checks — required for scoped packages and detached CI state","status":"closed","priority":2,"issue_type":"epic","owner":"sqve","created_at":"2026-03-26T08:53:54.652617278Z","created_by":"sqve","updated_at":"2026-03-26T13:27:08.331987556Z","closed_at":"2026-03-26T13:27:08.329460495Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"comments":[{"id":1,"issue_id":"greppa-744","author":"Oskar Grunning","text":"## Outcome (2026-03-26)\n\nAll 6 tasks completed and PR merged to main.\n\n### Success criteria results\n\n1. CI parallel jobs — PASS (adjusted). Format/lint/typecheck consolidated into single Check job; Test and Build as separate parallel jobs.\n2. SHA-pinned actions — PASS. All actions use sha # version format.\n3. Cancel-in-progress — PASS. Concurrency group with cancel-in-progress.\n4. Dependency review — PASS. fail-on-severity: moderate on PRs.\n5. Changesets — PASS. changelog-github, commitMode: github-api.\n6. CodeRabbit — PASS. Assertive profile, path filters, CLAUDE.md KB.\n7. Branch protection — PASS. Ruleset active: PRs required, status checks, linear history, no force push.\n\nAll local checks pass: format, lint, typecheck, 13 tests, build.","created_at":"2026-03-26T13:27:04Z"}]}
{"id":"greppa-744.1","title":"Create CI workflow with parallel quality check jobs","description":"## Goal\n\nCreate .github/workflows/ci.yml with 5 parallel jobs (format, lint, typecheck, test, build) that run on PRs and pushes to main. SHA-pin all actions. Include cancel-in-progress concurrency.\n\n## Behaviors\n\n1. Workflow triggers on push to main and pull_request to main\n2. Concurrency group ci-${{ github.ref }} cancels in-progress runs on same ref\n3. Workflow-level permissions set to contents: read\n4. Each of 5 jobs (format, lint, typecheck, test, build) runs independently in parallel\n5. Each job: checkout → pnpm/action-setup → setup-node (node 22, cache pnpm) → pnpm install --frozen-lockfile → run command\n6. All action references use full SHA with version comment\n7. Corepack enabled via setup-node for pnpm version enforcement\n\n## Success criteria\n\n- .github/workflows/ci.yml exists with valid YAML syntax\n- 5 jobs visible: format, lint, typecheck, test, build\n- All actions SHA-pinned (no @v4 tag references)\n- Concurrency group with cancel-in-progress: true present\n- permissions: contents: read set at workflow level\n- Each job uses node 22 and pnpm cache","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-03-26T08:54:52.171572336Z","created_by":"sqve","updated_at":"2026-03-26T09:18:48.582547121Z","closed_at":"2026-03-26T09:18:48.580786566Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"dependencies":[{"issue_id":"greppa-744.1","depends_on_id":"greppa-744","type":"parent-child","created_at":"2026-03-26T08:54:52.171572336Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-744.2","title":"Create dependency review workflow","description":"## Goal\n\nCreate .github/workflows/dependency-review.yml that blocks PRs introducing vulnerable dependencies.\n\n## Behaviors\n\n1. Triggers on pull_request only\n2. Uses actions/dependency-review-action with fail-on-severity: moderate\n3. All actions SHA-pinned with version comments\n4. Permissions: contents: read\n\n## Success criteria\n\n- .github/workflows/dependency-review.yml exists with valid YAML\n- Only triggers on pull_request\n- fail-on-severity set to moderate\n- All actions SHA-pinned","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-03-26T09:19:00.324985323Z","created_by":"sqve","updated_at":"2026-03-26T09:19:48.388127238Z","closed_at":"2026-03-26T09:19:48.386129429Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"dependencies":[{"issue_id":"greppa-744.2","depends_on_id":"greppa-744","type":"parent-child","created_at":"2026-03-26T09:19:00.324985323Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-744.3","title":"Set up Changesets with release workflow","description":"## Goal\n\nInitialize @changesets/cli, configure it for the monorepo, add ci:publish script, and create the release workflow.\n\n## Behaviors\n\n1. @changesets/cli and @changesets/changelog-github installed as devDependencies\n2. .changeset/config.json configured: changelog-github with repo sQVe/greppa, access public, baseBranch main, updateInternalDependencies patch\n3. Root package.json has ci:publish script: pnpm publish -r --access public --no-git-checks\n4. .github/workflows/release.yml triggers on push to main only\n5. Release workflow uses changesets/action with commitMode: github-api\n6. Release workflow creates GitHub Releases automatically\n7. Permissions: contents: write, pull-requests: write\n8. All actions SHA-pinned\n\n## Success criteria\n\n- .changeset/config.json exists with correct settings\n- @changesets/cli and @changesets/changelog-github in devDependencies\n- ci:publish script in root package.json\n- .github/workflows/release.yml exists with valid YAML\n- changesets/action configured with commitMode: github-api","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-03-26T09:20:01.423662320Z","created_by":"sqve","updated_at":"2026-03-26T09:21:41.261524486Z","closed_at":"2026-03-26T09:21:41.259382020Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"dependencies":[{"issue_id":"greppa-744.3","depends_on_id":"greppa-744","type":"parent-child","created_at":"2026-03-26T09:20:01.423662320Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-744.4","title":"Configure CodeRabbit for AI code reviews","description":"## Goal\n\nCreate .coderabbit.yaml with assertive profile, adapted from nit.nvim config for this TypeScript monorepo.\n\n## Behaviors\n\n1. Assertive review profile\n2. Tone: concise and direct, flag real issues, skip style enforced by oxlint/oxfmt\n3. Path filters exclude dist/, node_modules/, lockfiles, .beads/, .changeset/*.md\n4. Knowledge base: CLAUDE.md\n5. Auto-review enabled, skip drafts and WIP titles\n\n## Success criteria\n\n- .coderabbit.yaml exists at repo root with valid YAML\n- Profile set to assertive\n- Path filters exclude generated/lock files\n- Knowledge base references CLAUDE.md","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-03-26T09:21:52.957435727Z","created_by":"sqve","updated_at":"2026-03-26T09:22:24.266943782Z","closed_at":"2026-03-26T09:22:24.264758851Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"dependencies":[{"issue_id":"greppa-744.4","depends_on_id":"greppa-744","type":"parent-child","created_at":"2026-03-26T09:21:52.957435727Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-744.5","title":"Configure branch protection rules on GitHub","description":"## Goal\n\nApply branch protection rules for the main branch via GitHub rulesets.\n\n## Behaviors\n\n1. Direct pushes to main are blocked — all changes require a PR\n2. PRs require passing status checks (Format, Lint, Typecheck, Test, Build, Dependency review) before merge\n3. Only linear history allowed (rebase merges, no merge commits)\n4. Force pushes to main are blocked\n5. Branch deletion of main is blocked\n6. Conversations must be resolved before merge\n\n## Note\n\nThis is a manual step — configure via GitHub Settings > Rules > Rulesets, or via gh CLI. Cannot be automated in a workflow file.\n\n## Success criteria\n\n- Ruleset active on main branch\n- Direct pushes blocked\n- PRs require passing status checks before merge\n- Linear history enforced","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-03-26T09:22:50.503275679Z","created_by":"sqve","updated_at":"2026-03-26T13:23:35.267200524Z","closed_at":"2026-03-26T13:23:35.264837891Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"dependencies":[{"issue_id":"greppa-744.5","depends_on_id":"greppa-744","type":"parent-child","created_at":"2026-03-26T09:22:50.503275679Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-744.6","title":"Configure Renovate for automated dependency updates","description":"## Goal\n\nCreate renovate.json config at repo root for automated dependency updates. Install Renovate GitHub App on the sQVe/greppa repo.\n\n## Behaviors\n\n1. Renovate opens grouped PRs for dependency updates (not one PR per dep)\n2. GitHub Actions SHA pins are updated with version comments preserved\n3. Patch and minor updates for devDependencies are automerged after CI passes\n4. Major updates open separate PRs for manual review\n5. pnpm lockfile maintenance runs on a weekly schedule\n6. Renovate respects the monorepo workspace structure\n7. PRs for lockfile-only or pin-only changes skip changeset requirement\n\n## Success criteria\n\n- renovate.json exists at repo root with valid JSON\n- Group presets configured: actions, devDependencies, production dependencies\n- Automerge enabled for non-major devDependency updates\n- Schedule set to avoid noise (e.g., weekly or weekday-only)\n- SHA pin updating configured for GitHub Actions workflows","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-03-26T09:39:27.147714833Z","created_by":"sqve","updated_at":"2026-03-26T09:44:35.502516384Z","closed_at":"2026-03-26T09:44:35.500167960Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["ci","dx"],"dependencies":[{"issue_id":"greppa-744.6","depends_on_id":"greppa-744","type":"parent-child","created_at":"2026-03-26T09:39:27.147714833Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-7hw","title":"Extract logic from JSX into custom hooks and testable modules","description":"Many component files are too large and mix rendering with business logic. JSX files should hold minimal logic — extract into custom hooks (or use official React hooks) so logic is independently testable and components stay thin.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-03-26T18:19:45.527559396Z","created_by":"sqve","updated_at":"2026-03-27T15:59:59.647852342Z","closed_at":"2026-03-27T15:59:59.642560196Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","refactor"]}
{"id":"greppa-7hw.1","title":"Extract syntax highlighting logic from DiffViewer into useSyntaxHighlighting hook","description":"## What\n\nExtract the syntax highlighting pipeline from `DiffViewer.tsx` into a dedicated `useSyntaxHighlighting` hook in `packages/web/src/hooks/`.\n\n### Logic to extract\n\n- `getOrCreateHighlighter` singleton\n- `useHighlighter` hook\n- `buildTokenEntries` pure function\n- `useTokenMap` hook\n- `diffLineKey` helper (shared by both `buildRows` and token logic)\n\n### Target shape\n\nA single `useSyntaxHighlighting(diff, theme)` hook that returns a `tokenMap: Map<string, ThemedToken[]> | null`. Internal helpers (`buildTokenEntries`, `getOrCreateHighlighter`, `diffLineKey`) become module-private or exported for testing.\n\n### Success criteria\n\n- `DiffViewer.tsx` imports and calls `useSyntaxHighlighting` instead of the inline hooks\n- All existing DiffViewer tests pass unchanged\n- New test file for the hook covering: null diff, null highlighter, token map population, multi-hunk entries\n- `buildRows` still works (it uses `diffLineKey` which moves to the new module)","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T19:57:02.644318600Z","created_by":"sqve","updated_at":"2026-03-26T19:59:58.924619629Z","closed_at":"2026-03-26T19:59:58.919822345Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","refactor"],"dependencies":[{"issue_id":"greppa-7hw.1","depends_on_id":"greppa-7hw","type":"parent-child","created_at":"2026-03-26T19:57:02.644318600Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-7hw.2","title":"Extract file selection and review tracking from App into useFileSelection hook","description":"## What\n\nExtract file selection state, review tracking, and derived data from `App.tsx` into a `useFileSelection` hook in `packages/web/src/hooks/`.\n\n### Logic to extract\n\n- `collectFiles` pure function (flattens FileNode tree to file list)\n- `initialReviewed` computation (pre-reviewed set from fixture data)\n- `selectedFilePath` / `setSelectedFilePath` state\n- `reviewedPaths` / `setReviewedPaths` state\n- `handleSelectFile` callback (selects file + marks as reviewed)\n- Derived data: filtered comments for selected file, fileInfo lookup for selected file\n\n### Target shape\n\nA `useFileSelection(files, diffs, comments, fileInfoMap)` hook returning:\n- `selectedFilePath`\n- `selectFile` (the handler)\n- `reviewedCount`\n- `totalCount`\n- `selectedDiff`\n- `selectedThreads`\n- `selectedFileInfo`\n\n### Success criteria\n\n- `App.tsx` becomes a pure layout shell with no business logic\n- All existing App tests pass unchanged\n- New test file for the hook covering: initial state, selecting a file, marking as reviewed, re-selecting already-reviewed file, derived data filtering\n- `collectFiles` is independently testable","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T20:02:43.521509985Z","created_by":"sqve","updated_at":"2026-03-26T20:05:07.160775784Z","closed_at":"2026-03-26T20:05:07.155703618Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","refactor"],"dependencies":[{"issue_id":"greppa-7hw.2","depends_on_id":"greppa-7hw","type":"parent-child","created_at":"2026-03-26T20:02:43.521509985Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-7hw.3","title":"Extract buildRows and row types from DiffViewer into dedicated module","description":"## What\n\nExtract `buildRows`, `RowSide`, and `DiffRow` from `DiffViewer.tsx` into `packages/web/src/lib/buildRows.ts`.\n\n### Logic to extract\n\n- `RowSide` interface\n- `DiffRow` interface\n- `buildRows(hunk)` pure function (lines 21-83)\n\n### Success criteria\n\n- `DiffViewer.tsx` imports `buildRows` and types from the new module\n- All existing DiffViewer tests pass unchanged\n- New test file with direct unit tests for `buildRows` covering: context lines, removed/added pairing, unmatched lines produce null sides, flush at end of hunk","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-26T20:06:00.758452957Z","created_by":"sqve","updated_at":"2026-03-26T20:07:14.564340783Z","closed_at":"2026-03-26T20:07:14.558860974Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["dx","refactor"],"dependencies":[{"issue_id":"greppa-7hw.3","depends_on_id":"greppa-7hw","type":"parent-child","created_at":"2026-03-26T20:06:00.758452957Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-9hy","title":"Epic: DiffViewer and StackedDiffViewer refactor into hooks and subcomponents","description":"## Requirements (IMMUTABLE)\n- All planned extractions complete: useDiffVirtualItems, useFlatItemIndex, useActiveFile, SplitDiffRow, CollapsedDiffNotice, FilePath.\n- Each extraction preserves existing component behavior — no change to rendered diff output, virtualization, scroll behavior, active-file tracking, or token rendering.\n- Extracted hooks live in packages/web/src/hooks/ with co-located .test.ts.\n- Extracted subcomponents stay co-located with their parent directory (SplitDiffRow in DiffViewer/, CollapsedDiffNotice in DiffViewer/, FilePath in StackedDiffViewer/).\n- Host component public props stay unchanged.\n- Each extraction lands as its own atomic commit.\n\n## Success criteria\n- [ ] useDiffVirtualItems hook extracted; DiffViewer consumes it.\n- [ ] useFlatItemIndex hook extracted; StackedDiffViewer consumes it.\n- [ ] useActiveFile hook extracted; StackedDiffViewer consumes it.\n- [ ] SplitDiffRow subcomponent extracted; VirtualRow and DiffRowRenderer consume it.\n- [ ] CollapsedDiffNotice subcomponent extracted; DiffViewer renders it for collapsed state.\n- [ ] FilePath subcomponent extracted; FileHeader renders it.\n- [ ] Every extracted unit has dedicated test coverage.\n- [ ] No public prop signature changed on DiffViewer, StackedDiffViewer, FileHeader, VirtualRow, or DiffRowRenderer.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.\n\n## Anti-patterns (FORBIDDEN)\n- NO behavior changes in the same commit as an extraction (reason: keeps the commit reviewable as a pure move; behavior tweaks belong in a separate follow-up).\n- NO temporary parallel names like DiffViewerV2 (reason: extraction is atomic; rename callers in the same commit).\n- NO expanding test coverage beyond what exercises the new unit (reason: scope creep; coverage gaps are separate tasks).\n- NO public prop changes on the host component (reason: downstream callers and tests should not need simultaneous edits).\n- NO drive-by cleanup of unrelated code (reason: pollutes the diff; split into its own commit/task if worth doing).\n- NO bundling multiple extractions into one commit (reason: each extraction must be independently revertable).\n- NO replacing @tanstack/react-virtual with a custom virtualizer (reason: explicit out of scope; virtualization behavior must be preserved byte-for-byte).\n\n## Durable decisions\n- Hooks directory: packages/web/src/hooks/ (matches existing project convention).\n- Subcomponents: co-located with parent directory (matches FileHeader.tsx alongside StackedDiffViewer.tsx).\n- Naming: useX for hooks (camelCase), PascalCase.tsx for subcomponents.\n- Test co-location: .test.ts/.test.tsx next to source.\n- Virtualization library: @tanstack/react-virtual (unchanged).\n\n## Approach\nSix independent extractions, each landing as its own atomic commit. Existing component tests pin behavior; new unit tests exercise the extracted units in isolation. Work order favors useFlatItemIndex as a pathfinder (smallest self-contained extraction), then the two larger hooks (useDiffVirtualItems, useActiveFile), then the three subcomponents.\n\nVirtualization internals are preserved unchanged. Hook extractions pass scrollRef/flatItems/containerRef as parameters rather than trying to abstract the virtualizer itself.\n\n## Architecture\n- packages/web/src/hooks/useDiffVirtualItems.ts (new) — hunks + items + tier/isCollapsed/shouldDeferHighlighting pipeline from DiffViewer.tsx:59-71.\n- packages/web/src/hooks/useFlatItemIndex.ts (new) — fileHeaderIndices + filePathByIndex + visibleDiffs filter from StackedDiffViewer.tsx:52-99.\n- packages/web/src/hooks/useActiveFile.ts (new) — activeFileIndex state + updateActiveFile callback + effects from StackedDiffViewer.tsx:136-170.\n- packages/web/src/components/DiffViewer/SplitDiffRow.tsx (new) — split-row render shared by VirtualRow and DiffRowRenderer.\n- packages/web/src/components/DiffViewer/CollapsedDiffNotice.tsx (new) — collapsed-state branch from DiffViewer.tsx:93-103.\n- packages/web/src/components/StackedDiffViewer/FilePath.tsx (new) — path-segment rendering from FileHeader.tsx:116-124.\n\n## Design rationale\n\n### Problem\nDiffViewer and StackedDiffViewer concentrate virtualization, scroll tracking, and row rendering inline. The audit surfaced six sites where cohesive pipelines or duplicated render blocks can be extracted without changing output. Two components share an identical split-row wrapper (VirtualRow, DiffRowRenderer); duplication must be removed.\n\n### Research findings\n\n**Codebase audit (3-agent sweep, April 2026):**\n- packages/web/src/components/DiffViewer/DiffViewer.tsx:59-71 — three useMemos forming a single diff→virtual-items pipeline.\n- packages/web/src/components/DiffViewer/DiffViewer.tsx:93-103 — collapsed-state conditional with derived totalLines.\n- packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx:52-99 — three memos deriving index maps + visibleDiffs filter.\n- packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx:136-170 — 35 lines of activeFileIndex state + effects for scroll-position tracking.\n- packages/web/src/components/StackedDiffViewer/FileHeader.tsx:116-124 — path-segment rendering (~10 lines of segments.map).\n- packages/web/src/components/DiffViewer/VirtualRow.tsx:38-53 + packages/web/src/components/DiffViewer/DiffRowRenderer.tsx:26-29 — duplicated split-row wrapper (two RowSideCell instances inside styled row div).\n\n**Existing patterns:**\n- packages/web/src/hooks/useDiffComputation.ts — reference for diff-pipeline hook.\n- packages/web/src/components/StackedDiffViewer/FileHeader.tsx — reference for subcomponent co-located with parent.\n\n### Approaches considered\n\n#### 1. Six independent atomic commits (selected)\n\n**What:** Each extraction lands as its own commit; pathfinder is useFlatItemIndex.\n**Pros:** Independently revertable, each commit a pure move, test pinning incremental.\n**Cons:** Six commits.\n**Chosen because:** Anti-pattern rules require atomic extractions; units have no interdependencies.\n\n#### 2. Combined StackedDiffViewer-only hook `useStackedDiffState` (rejected)\n\n**What:** Fold useFlatItemIndex + useActiveFile into one hook that owns all StackedDiffViewer state.\n**Why explored:** They feed each other (useActiveFile consumes fileHeaderIndices).\n**Why rejected:** The two concerns are individually reusable — useFlatItemIndex is pure data derivation, useActiveFile drives scroll tracking. Combining them hides boundaries.\n**DO NOT REVISIT UNLESS:** A third consumer emerges that needs both simultaneously.\n\n### Scope boundaries\n\n**In scope:** Six extractions listed in Success criteria.\n**Out of scope:** CommitList and FileTree refactors (Epic A), StatusBar/ActivityRail/DetailPanel refactors (Epic C), virtualization library swap, behavior changes, prop signature changes.\n\n### Open questions\n- useActiveFile hook signature: take (containerRef, fileHeaderIndices, flatItems, onActiveFileChange) or narrow further? Defer to first task touching it.\n- SplitDiffRow prop shape (left/right cells) — confirmed in the first task touching it.\n\n## Design discovery\n\n### Key decisions made\n\n| Question | Answer | Implication |\n|----------|--------|-------------|\n| First task? | useFlatItemIndex | Smallest self-contained extraction, good pathfinder |\n| Combine useFlatItemIndex + useActiveFile? | No | Each is individually reusable |\n| Touch virtualizer internals? | No | @tanstack/react-virtual behavior preserved byte-for-byte |\n\n### Dead-end paths\n\n#### Combined useStackedDiffState hook\n**Why explored:** useActiveFile consumes fileHeaderIndices.\n**What found:** Hides boundaries between pure derivation and side-effecting scroll tracking.\n**Why abandoned:** Split wins on reusability and testability.\n\n### Open concerns raised\n- Behavior preservation for virtualization -> enforced by anti-pattern (NO virtualizer replacement) and by existing snapshot/integration tests.\n- Active-file tracking timing (scroll effects) -> useActiveFile preserves existing effect dependencies exactly.","status":"open","priority":3,"issue_type":"epic","created_at":"2026-04-22T11:59:10.500659066Z","created_by":"sqve","updated_at":"2026-04-22T11:59:10.500659066Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["refactor","write-plan"]}
{"id":"greppa-9hy.1","title":"Task 1: Extract useFlatItemIndex hook from StackedDiffViewer","description":"## Goal\nExtract the flat-item index maps (fileHeaderIndices, filePathByIndex) and visibleDiffs filter from StackedDiffViewer.tsx:52-99 into a useFlatItemIndex hook in packages/web/src/hooks/. StackedDiffViewer consumes the hook; existing tests continue to pass unchanged.\n\n## Execution mode\nAFK — behavior-preserving extraction. Existing StackedDiffViewer tests pin the behavior; the hook is a pure data-derivation refactor.\n\n## Behaviors\n1. Hook useFlatItemIndex(flatItems, diffs) returns { fileHeaderIndices, filePathByIndex, visibleDiffs } — a factory for visibleDiffs(visibleVirtualItems) — with semantics identical to the current inline useMemos.\n2. fileHeaderIndices maps header virtual-item index → file path, matching current derivation.\n3. filePathByIndex maps every virtual-item index → its owning file path.\n4. visibleDiffs factory filters diffs to those whose headers appear in the supplied visibleVirtualItems, matching StackedDiffViewer.tsx:90-99 exactly.\n5. StackedDiffViewer.tsx imports and consumes the hook; inline useMemos for these three derivations are removed; scroll-handle, active-file tracking, and token-map lookup call sites still work unchanged.\n6. Existing packages/web/src/components/StackedDiffViewer/StackedDiffViewer.test.tsx continues to pass without modification.\n\n## References\n- packages/web/src/hooks/useCommitSelection.ts — hook file structure + test co-location pattern.\n- packages/web/src/hooks/useComputedDiffs.ts — hook deriving from diff data.\n- packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx:52-99 — source of the extraction.\n- packages/web/src/components/StackedDiffViewer/StackedDiffViewer.test.tsx — existing behavior pins.\n\n## Success criteria\n- [ ] packages/web/src/hooks/useFlatItemIndex.ts exists and exports the hook.\n- [ ] packages/web/src/hooks/useFlatItemIndex.test.ts covers the four behavioral semantics above.\n- [ ] StackedDiffViewer.tsx delegates the three memos to the hook; no inline useMemo for these derivations remains.\n- [ ] All existing tests pass (StackedDiffViewer.test.tsx unchanged).\n- [ ] Pre-commit hooks passing.\n- [ ] Commit is atomic — only the hook extraction and its StackedDiffViewer call-site swap (plus new tests).","status":"open","priority":3,"issue_type":"task","created_at":"2026-04-22T12:00:39.151174272Z","created_by":"sqve","updated_at":"2026-04-22T12:00:39.151174272Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["afk","refactor","write-plan"],"dependencies":[{"issue_id":"greppa-9hy.1","depends_on_id":"greppa-9hy","type":"parent-child","created_at":"2026-04-22T12:00:39.151174272Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-c4a","title":"Epic: Expandable commit groups with per-commit file review","description":"## Requirements (IMMUTABLE)\n- Commits section rows gain a left-side chevron; chevron toggles expansion, row click unchanged (toggles SHA in `commits` URL param, driving the Changes aggregate).\n- Expanded commit shows a flat list of files touched by that commit; no directory grouping inside an expanded commit.\n- Clicking a file inside an expanded commit toggles a `\"sha:path\"` entry in a new URL param `commitFile`; does not modify `commits`.\n- Diff viewer renders entries from `commitFile` as per-commit patches (same path across two commits = two distinct entries, never merged).\n- `useReviewState` gains `reviewedCommitFiles: string[]` keyed `\"sha:path\"`, independent of `reviewedPaths`.\n- `CommitEntry` schema (packages/core/src/index.ts:62) gains `files: readonly string[]`; server populates via `git log --name-only` over the existing ref range — no extra endpoint.\n- Three sidebar sections (Changes, Working tree, Commits) remain; only the Commits section changes.\n\n## Success criteria\n- [ ] Commit rows render with a chevron; chevron expands/collapses without altering `commits` selection.\n- [ ] Expanded commit lists files from that commit as flat children.\n- [ ] Clicking a file in an expanded commit adds `\"sha:path\"` to `commitFile` and opens a per-commit diff entry in the viewer.\n- [ ] Same path in two different commits renders as two independent diff entries.\n- [ ] `reviewedCommitFiles` toggles independently of `reviewedPaths` and persists to localStorage.\n- [ ] `CommitEntry.files` populated by the server with no additional round trip.\n- [ ] Tests cover: URL param round-trip for `commitFile`, independence of reviewed-state tracks, chevron-vs-row click separation, server `files` population.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.\n\n## Anti-patterns (FORBIDDEN)\n- NO merging same-path entries from different commits in the diff viewer (reason: each commit's changes to file X are a distinct review unit per user decision).\n- NO hunk-level selection (reason: out of scope; user explicitly deferred).\n- NO separate endpoint for per-commit files (reason: ref range already scans commits; adding `--name-only` is free and avoids a round trip).\n- NO coupling `commitFile` selection to `commits` aggregate (reason: viewing a commit's patch for one file must not change the Changes aggregate).\n- NO shared reviewed-state between path-level and commit-scoped (reason: marking file X reviewed at commit A must not mark X reviewed globally).\n- NO directory grouping inside an expanded commit (reason: per-commit file counts are small; flat list keeps visual distinction from the Changes tree).\n- NO auto-expanding commits on load (reason: expand is a deliberate user action; default view stays compact).\n\n## Durable decisions\n- URL param shape: `commitFile: string[]`, entries formatted `\"<sha>:<path>\"`. Parallel to existing `file`/`wt` params.\n- Reviewed-state shape in localStorage: `reviewedCommitFiles: string[]` alongside existing `reviewedPaths: string[]`.\n- `CommitEntry.files` is ordered by git's natural output; no client-side sorting contract.\n- Diff scope for commit-scoped entries: the patch introduced by that single commit for that path (`sha^..sha -- path`), not cumulative.\n- Commits API endpoint remains singular — files ship inline with commit list.\n\n## Approach\nExtend the existing commits response with per-commit file names, reuse the RAC Tree primitive already used by `FileTree` to render an expandable `CommitList`, and introduce a new URL param + reviewed-state slice that parallels the path-based ones. The diff viewer already iterates active selections; `commitFile` becomes a third source alongside `file`/`wt`.\n\nNo data migration (new param is additive), no new endpoints, no reshaping of `commits`/Changes behavior. The Changes aggregate stays authoritative for whole-commit review; `commitFile` is a strictly additive drill-down channel.\n\n## Architecture\n- packages/core/src/index.ts:62 — extend `CommitEntry` with `files: readonly string[]`.\n- Server commits endpoint — populate `files` via `git log --name-only` over the existing ref range.\n- packages/web/src/components/CommitList/CommitList.tsx — migrate from flat button list to RAC `Tree` (see packages/ui/src/components/Tree/Tree.tsx); chevron expansion via local `expandedKeysRef` (mirror packages/web/src/components/FileTree/FileTree.tsx).\n- packages/web/src/hooks/useCommitFileSelection.ts (new) — URL param helpers for `commitFile`, mirroring useFileSelection.\n- packages/web/src/hooks/useReviewState.ts — add `reviewedCommitFiles` track.\n- Diff viewer — consume `commitFile` entries, fetch per-commit patches scoped to `sha^..sha -- path`.\n\n## Design rationale\n\n### Problem\nPR review in greppa today collapses every selected commit into a flat aggregate file list. Reviewers lose the author's commit-level chunking and cannot drill into a single commit's changes to a single file. This matters for PRs where commits carry distinct intent (refactor vs feature vs tests).\n\n### Research findings\n\n**Codebase:**\n- packages/web/src/components/CommitList/CommitList.tsx — current flat button list; shift/cmd toggle SHAs in `commits`.\n- packages/web/src/components/FileTree/FileTree.tsx — RAC Tree with chevron expansion via `expandedKeysRef`; direct model for CommitList.\n- packages/web/src/components/FileTree/FileTreePanel.tsx — three collapsible sections; unchanged by this epic.\n- packages/web/src/hooks/useReviewState.ts — localStorage-backed `{collapsedPaths, reviewedPaths}`; extended with `reviewedCommitFiles`.\n- packages/core/src/index.ts:62 — `CommitEntry` schema; gains `files`.\n- packages/ui/src/components/Tree/Tree.tsx — shared RAC Tree primitive; reused.\n\n**External:**\n- git `log --name-only` / `--name-status` — per-commit file list in the same pass.\n\n### Approaches considered\n\n#### 1. Path-set model with `commitFile` URL param (selected)\n\n**What:** Add `commitFile: string[]` of `\"sha:path\"` entries. `commits` unchanged. Files ship inline on `CommitEntry`.\n**Pros:** Additive; zero migration; backward-compatible; one round trip.\n**Cons:** URL can grow with heavy selection.\n**Chosen because:** Preserves existing Changes-aggregate semantics, minimal surface area, matches user decisions (row click unchanged, chevron expands, cross-commit mix allowed).\n\n#### 2. Unified `selection: Array<{sha, path?}>` model (rejected)\n\n**What:** Replace `commits` with a single list where entries may optionally carry a path.\n**Why explored:** Uniform leaves — every selection identified the same way.\n**Why rejected:** Churns every existing `commits` consumer for no user-visible gain; diff aggregator needs rewrite.\n**DO NOT REVISIT UNLESS:** A future feature demands ordered/heterogeneous selection lists.\n\n#### 3. Lazy per-commit files endpoint (rejected)\n\n**What:** Fetch a commit's files on first expand via a new endpoint.\n**Why explored:** Keeps commit-list payload small for PRs with many commits.\n**Why rejected:** Adds latency to expand, forces prefetch, premature until commit counts actually hurt.\n**DO NOT REVISIT UNLESS:** A PR with 100+ commits measurably bloats the commits response.\n\n### Scope boundaries\n\n**In scope:** Commits section expansion, per-commit file click, `commitFile` URL param, per-commit reviewed-state, schema + server `files` population, diff viewer consumption of `commitFile`.\n**Out of scope:** Hunk-level selection; auto-grouping commits by area (separate notebox task); directory grouping inside a commit; prefetch/lazy loading of per-commit files; cross-commit blame.\n\n### Open questions\n- Keyboard shortcut for expand/collapse of a commit row (RAC Tree defaults likely suffice).\n- Exact ordering of entries in the diff viewer when `file` + `commitFile` are both active.\n\n## Design discovery\n\n### Key decisions made\n\n| Question | Answer | Implication |\n|----------|--------|-------------|\n| Keep Changes / Working tree / Commits sections? | Yes | Scope shrinks to Commits section only |\n| Diff scope for a file inside a commit? | Per-commit patch | Aggregator must accept a commit-scoped mode |\n| Cross-commit file mixing allowed? | Yes | Selection is an additive list of `\"sha:path\"` |\n| Default when a commit row is clicked? | Whole-commit select (existing) | Chevron separate from row click |\n| Click semantics: row vs chevron? | Chevron expands, row selects | No merged affordance |\n| Same file across two expanded commits? | Two distinct entries | Never merged in diff viewer |\n| Reviewed-state for commit-scoped files? | Separate from path-level | New `reviewedCommitFiles` track |\n| Hunk-level selection? | No | Explicitly out of scope |\n\n### Dead-end paths\n\n#### Full sidebar reframe (commits become the primary axis)\n**Why explored:** Initial brainstorm pushed toward treating commits as containers and killing the flat Changes section.\n**What found:** User rejected — Changes/Working tree/Commits sections stay intact.\n**Why abandoned:** Over-scoped. True task is narrower: make commit rows expandable for drill-down.\n\n### Open concerns raised\n- URL length with many `\"sha:path\"` entries -> accept for now; revisit if real users hit limits.\n- Commit row with zero files (empty commit) -> chevron disabled; no expansion affordance.","design":"test","status":"open","priority":2,"issue_type":"epic","created_at":"2026-04-15T13:43:18.252395492Z","created_by":"sqve","updated_at":"2026-04-16T19:54:02.169594234Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["expandable-commits"]}
{"id":"greppa-c4a.1","title":"Task 1: Expand one commit, click one file, render its per-commit diff","description":"## Goal\nDeliver a thin end-to-end slice: a commit row in the Commits section can be expanded via a chevron, clicking one file inside it adds a `\"sha:path\"` entry to a new `commitFile` URL param, and the diff viewer renders the patch introduced by that commit for that path. No reviewed-state, no multi-select semantics, no shift/cmd handling — those come in later tasks.\n\n## Execution mode\nHITL — UI interaction decisions (chevron placement, empty-commit affordance, child-row styling) benefit from human review mid-implementation.\n\n## Behaviors\nEach is a single TDD cycle. Stop between them; one test first, then the minimum code to pass.\n\n1. `CommitEntry` schema accepts `files: readonly string[]` (decode test in packages/core).\n2. Server commits endpoint populates `files` via `git log --name-only` over the existing ref range (integration test against a fixture repo).\n3. `commitFile` URL param encodes/decodes a `string[]` with `\"<sha>:<path>\"` entries (round-trip test; mirror `useFileSelection` test).\n4. New hook `useCommitFileSelection` exposes `entries`, `toggle(sha, path)`, `isSelected(sha, path)` (unit test).\n5. `CommitList` renders with a chevron on each row; chevron click toggles local expansion state; does NOT modify `commits` (component test).\n6. Expanding a commit renders its `files` as flat child rows in the tree (component test).\n7. Clicking a child file row calls `useCommitFileSelection.toggle(sha, path)`; does NOT modify `commits` (component test).\n8. Diff viewer, given a `commitFile` entry, fetches and renders the patch for `sha^..sha -- path` (integration test with a fixture repo; same file appearing in two expanded commits renders as two distinct entries).\n\nAfter behavior 8 lands, the feature is demonstrable end-to-end for a single file in a single commit, with cross-commit duplication proven. Reviewed-state, keyboard, and multi-select are separate follow-up tasks.\n\n## References\n- packages/web/src/components/FileTree/FileTree.tsx — RAC Tree + chevron expansion via `expandedKeysRef`; primary reference for CommitList restructure.\n- packages/web/src/hooks/useFileSelection.ts — URL param helper pattern to mirror in useCommitFileSelection.\n- packages/web/src/hooks/useReviewState.ts — localStorage-backed state pattern (consulted but not modified in this task).\n- packages/ui/src/components/Tree/Tree.tsx — shared RAC Tree primitive.\n- packages/core/src/index.ts:62 — CommitEntry schema to extend.\n\n## Success criteria\n- [ ] `CommitEntry.files` decodes from server and surfaces in the commit list data.\n- [ ] Chevron on a commit row toggles expansion; row click still toggles `commits` (regression-free).\n- [ ] Expanded commit shows flat file list; clicking a file writes a `\"sha:path\"` entry to `commitFile`; `commits` unchanged.\n- [ ] Diff viewer renders a per-commit patch for a single `commitFile` entry.\n- [ ] Same file under two different commits renders as two independent diff entries.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.","design":"test","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-15T13:43:48.646587618Z","created_by":"sqve","updated_at":"2026-04-21T11:43:06.681277Z","closed_at":"2026-04-21T11:43:06.681011930Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-c4a.1","depends_on_id":"greppa-c4a","type":"parent-child","created_at":"2026-04-15T13:43:48.646587618Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-c4a.2","title":"Task 2: Fix commitFile URL param dropped when state id is set","description":"## Goal\nFix a silent data-loss bug: when the router encodes state into `?s=<id>`, any subsequent write of `commitFile` is discarded by `stringifySearch`, because `StatePayload` does not include `commitFile`. After commit #1 is selected, clicking a file inside any expanded commit no longer writes to the URL, so the diff viewer never receives a `commitFile` entry. This breaks the epic success criterion \"Same path in two different commits renders as two independent diff entries\" in practice.\n\n## Behaviors\nOne TDD cycle per item.\n\n1. `StatePayload` (`packages/web/src/stateCache.ts`) and `StateData` (`packages/core/src/index.ts`) gain `commitFile: Schema.Array(Schema.String)`. `stateKey` includes commitFile so cache keys stay unique.\n2. `useCommitSelection.buildState`/`navigateWithState` carry forward current `commitFile` into every new state (read via `useRouterState`). Regression test: click commit A → URL encodes current `commitFile` in the new state id.\n3. `useCommitFileSelection.toggle` writes via `getOrCreateStateId` with `{ file, wt, commits, commitFile: next }`, not raw `navigate({ search })`. Regression test: toggle with empty commits writes plain `?commitFile=` as before; toggle with non-empty commits writes to the state-id payload.\n4. `commitsRoute.beforeLoad` redirect includes `commitFile: state.commitFile` so reload from a shared URL restores selection.\n5. Integration test: click commit, click another commit, click a file → `useSelectionCoordinator.commitFileDiffs.diffs.length === 1`; repeated clicks toggle (length → 0).\n\n## Success criteria\n- [ ] Clicking a file inside an expanded commit after selecting one or more commits writes the entry to the URL-encoded state and surfaces it in the diff viewer.\n- [ ] Reload preserves `commitFile` selection.\n- [ ] Existing single-file tests (no commits selected) unchanged.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.\n\n## References\n- packages/web/src/router.ts:49-66 — `stringifySearch` short-circuit to be worked around via state payload.\n- packages/web/src/stateCache.ts:3-18 — `StatePayload`, `stateKey`.\n- packages/web/src/hooks/useCommitSelection.ts:10-14,29-35 — `buildState`/`navigateWithState`.\n- packages/web/src/hooks/useCommitFileSelection.ts:42-55 — `toggle` to rewrite.\n- packages/core/src/index.ts:44-49 — `StateData` schema.\n- packages/server/src/StateApi.ts — no code change needed; payload shape comes from core schema.","design":"## Expanded plan (expand-task)\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/core/src/index.ts:44-49` — `StateData` schema needs `commitFile: Schema.Array(Schema.String)`. `StateSaveRequest`/`StateSaveResponse` inherit via `...StateData.fields`.\n- `packages/web/src/stateCache.ts:3-18` — `StatePayload` interface gains `commitFile: string[]`; `stateKey` must include it so cache keys stay unique across selections that differ only by `commitFile`.\n- `packages/server/src/Http.ts:29-33,272` — `StoredState` interface and the `saveState` handler's store write must thread `commitFile` through.\n- `packages/web/src/router.ts:23` — `commitsSearch` already accepts `commitFile` (no change). `commitsRoute.beforeLoad` redirect at L158 must add `commitFile: state.commitFile`. L116 and L136 cross-section redirects already spread `...state`, so they pick up `commitFile` once `StatePayload` gains the field.\n- `packages/web/src/hooks/useCommitSelection.ts:10-14,16-17,20,29-35` — `buildState` currently returns `{file:[], wt:[], commits:shas}`. It must carry forward the current `commitFile` from router state. `navigateWithState` must include `commitFile` in the final route search so the URL round-trips.\n- `packages/web/src/hooks/useCommitFileSelection.ts:42-55` — `toggle` uses `navigate({ search: (prev) => ({ ...prev, commitFile: next }) })`. When `?s=<id>` is active, `prev` only contains `{s, commits}` because `commitFile` is not in `StatePayload`. Fix: read `file`/`wt`/`commits` from router state, call `getOrCreateStateId({file, wt, commits, commitFile: next})`, navigate with `{ s: id, commits, commitFile: next }`.\n- `packages/web/src/hooks/useMultiSelect.ts:28-31` — `buildState` must include `commitFile: []` to satisfy the new `StatePayload` shape.\n\n**Patterns to follow:**\n- `packages/web/src/hooks/useCommitSelection.test.ts:49-54` — `commitsUrl` helper seeds `?s=<id>` via `cacheState` and renders hook. Reuse pattern for the `?s=` round-trip regression test.\n- `packages/web/src/hooks/useCommitSelection.ts:16-17,20` — `useRouterState({ select: ... })` selector pattern.\n- `packages/web/src/hooks/useMultiSelect.ts:68-78` — `navigateWithState` composing `getOrCreateStateId` + `navigate`.\n\n**Reusable code:**\n- `packages/web/src/toStringArray.ts` — already used by both hooks.\n- `packages/web/src/stateCache.ts:38-47` `getOrCreateStateId()` — mint-id entry point.\n- `cacheState(id, state)` — seed state in tests.\n\n**Blast radius:**\n- `StatePayload`/`StateData` construction sites: `useCommitSelection.buildState`, `useMultiSelect.buildState` (must add `commitFile: []`). No other sites.\n- `getOrCreateStateId` callsites: `useCommitSelection:31`, `useMultiSelect:70`.\n- Test helpers: `packages/web/src/hooks/useCommitSelection.test.ts:50` (`commitsUrl`) constructs a literal; needs `commitFile: []`.\n- Server `StoredState` must stay in lockstep or `saveState` drops the field.\n- `commitsRoute.beforeLoad` redirect — the only redirect that specifically needs `commitFile`.\n\n**Pre-existing issues (out of scope, surface only):**\n- `packages/web/src/hooks/useCommitSelection.test.ts:27-33` and `packages/web/src/hooks/useCommitList.test.ts:28` construct `CommitEntry` without `files` (added in commit 0359288). Pre-existing typecheck mismatches; do not fix.\n\n### Steps\n\n### Step 1: StateData / StatePayload / StoredState carry commitFile\n\n**Pattern:** `packages/core/src/index.ts:44-49` — another `Schema.Array(Schema.String)` field alongside `file`/`wt`/`commits`.\n\n**Changes:**\n- `packages/core/src/index.ts:44-49` — add `commitFile: Schema.Array(Schema.String)` to `StateData`.\n- `packages/web/src/stateCache.ts:3-7` — add `commitFile: string[]` to `StatePayload`.\n- `packages/web/src/stateCache.ts:17-18` — include `state.commitFile` in `stateKey` tuple.\n- `packages/server/src/Http.ts:29-33` — add `commitFile: string[]` to `StoredState`.\n- `packages/server/src/Http.ts:272` — spread `commitFile: [...payload.commitFile]` in store write.\n- `packages/web/src/hooks/useCommitSelection.ts:10-14` — `buildState` returns `commitFile: []` (Step 2 makes it carry forward).\n- `packages/web/src/hooks/useMultiSelect.ts:28-31` — both `buildState` branches return `commitFile: []`.\n- `packages/web/src/hooks/useCommitSelection.test.ts:50` — `commitsUrl` helper literal gets `commitFile: []`.\n\n**Verify:** `pnpm --filter @greppa/core test && pnpm --filter @greppa/web test -- stateCache useCommitSelection useCommitFileSelection && pnpm --filter @greppa/server test`\n\n### Step 2: useCommitSelection carries commitFile through every new state\n\n**Pattern:** `packages/web/src/hooks/useCommitSelection.ts:16-17,20` — existing selector pattern.\n\n**Changes:**\n- `packages/web/src/hooks/useCommitSelection.ts` — add `selectCommitFileParams` via `toStringArray(s.location.search.commitFile)`. Read via `useRouterState`. Rework call sites so `navigateWithState` receives a payload with current `commitFile`. In `navigateWithState`, add `commitFile: state.commitFile` to the navigate `search`.\n- `clear()` at L37-40 — include `commitFile: []` in the search reset.\n\n**Verify:** `pnpm --filter @greppa/web test -- useCommitSelection`\n\n### Step 3: useCommitFileSelection.toggle writes via getOrCreateStateId\n\n**Pattern:** `packages/web/src/hooks/useMultiSelect.ts:68-78`.\n\n**Changes:**\n- `packages/web/src/hooks/useCommitFileSelection.ts:23-25` — extend `selectParams` to read `file`, `wt`, `commits`.\n- `packages/web/src/hooks/useCommitFileSelection.ts:27-29` — destructure all four arrays.\n- `packages/web/src/hooks/useCommitFileSelection.ts:42-55` — rewrite `toggle`:\n  1. compute `next` commitFile list.\n  2. `getOrCreateStateId({ file, wt, commits, commitFile: next })`.\n  3. `navigate({ to: '/commits', search: { s: id, commits, commitFile: next }, replace: true })`.\n- Dep array at L54 includes `file`, `wt`, `commits`.\n\n**Verify:** `pnpm --filter @greppa/web test -- useCommitFileSelection`\n\n### Step 4: commitsRoute.beforeLoad redirect preserves commitFile\n\n**Pattern:** `packages/web/src/router.ts:116,136` — cross-section redirects already spread `...state`; only same-section branch needs explicit threading.\n\n**Changes:**\n- `packages/web/src/router.ts:158` — `throw redirect({ to: '/commits', search: { s: search.s, commits: state.commits, commitFile: state.commitFile } });`\n\n**Verify:** `pnpm --filter @greppa/web test`\n\n### Step 5: Round-trip regression — commitFile survives a state-id URL\n\n**Pattern:** `packages/web/src/hooks/useCommitFileSelection.test.ts:118-130` \"toggle does not modify commits param\", but seeded with a `?s=<id>` URL. Assert on the resolved payload from `stateCache.get(newId)`, not the visible URL params.\n\n**Changes:**\n- `packages/web/src/hooks/useCommitFileSelection.test.ts` — add a new `it` case: `cacheState` an id with `{file:[], wt:[], commits:['aaa'], commitFile:[]}`, render `/commits?s=<id>`, act → `toggle('aaa', 'src/a.ts')`, await, read `router.state.location.search.s` (non-empty string), look up `stateCache.get(newId)`, assert payload `commitFile === ['aaa:src/a.ts']` and `commits === ['aaa']`.\n\n**Verify:** `pnpm --filter @greppa/web test -- useCommitFileSelection`\n\n### Final verification\n`cape check`\n\n\n## Divergence\n\nStep 5 (round-trip regression test) merged with Step 3 during implementation.\n\nThe test I added under Step 3 — \"toggle survives a state-id URL by minting a new state id\" in\nuseCommitFileSelection.test.ts — already performs the exact assertion Step 5 described: seed\n?s=<id>, toggle, assert new stateCache payload contains both commits and commitFile.\n\nAdditional coverage already exists: useSelectionCoordinator.test.ts L112-132 verifies\ncross-SHA entries flow through to useCommitFileDiffs (covering task behavior #5's\n\"same path → two distinct diff entries\" intent).\n\nNo new Step 5 test is necessary. Verification proceeds with final cape check.\n\n## Outcome\n\nFix landed across schema, state cache, navigation hooks, and router redirect.\n\nRoot cause was a schema-level omission: `StatePayload`/`StateData` lacked `commitFile`. When\nany selection wrote via `?s=<id>`, the router's `stringifySearch` short-circuit dropped every\nother param, but `stateKey` only hashed `[file, wt, commits]` and `toggle` spread `prev` which\nnever contained `commitFile`. Adding `commitFile` to the payload shape + threading it through\n`useCommitSelection.navigateWithState`, `useCommitFileSelection.toggle` (now via\n`getOrCreateStateId`), and the `commitsRoute.beforeLoad` redirect closes the loop.\n\n### Shape changes\n- `StateData.fields` (core) and `StatePayload` (web) gained `commitFile: string[]`.\n- `stateKey` includes `commitFile` so identical commit selections with different per-commit\n  file selections mint distinct ids.\n- Server's `StoredState` tracks `commitFile` so persisted state round-trips.\n- `useMultiSelect.buildState` zero-inits `commitFile` to satisfy the shape (no behavior change\n  — path selection never populates commit files).\n\n### Behavior changes\n- `useCommitSelection` reads current `commitFile` via `useRouterState` and carries it forward\n  into every `navigateWithState` call (plain click, meta+click, shift+range, clear).\n- `useCommitFileSelection.toggle` now reads full `{file, wt, commits}` alongside `commitFile`,\n  mints a state id via `getOrCreateStateId`, and navigates to `/commits` with\n  `{ s, commits, commitFile }`. Works with or without other selections active.\n- `commitsRoute.beforeLoad` same-section redirect uses the new\n  `buildCommitsRedirectSearch(s, state)` helper to include `commitFile: state.commitFile` so\n  reload from a shared URL restores the per-commit file selection.\n\n### Tests added\n- `packages/core/src/index.test.ts` — `StateData` decodes with/without `commitFile`; rejects\n  payloads missing the field.\n- `packages/web/src/stateCache.test.ts` (new) — `stateKey` uniqueness under `commitFile`\n  variation; identical payloads collapse to one id.\n- `packages/web/src/hooks/useCommitSelection.test.ts` — \"should carry forward commitFile into\n  the new state\" covers the buildState → stateCache round-trip.\n- `packages/web/src/hooks/useCommitFileSelection.test.ts` — \"toggle survives a state-id URL\n  by minting a new state id\" covers the primary bug (toggle after `?s=` was active).\n- `packages/web/src/router.test.ts` (new) — `buildCommitsRedirectSearch` shape test.\n\n### Verification\n- Step 1-5 gates passed individually.\n- `cape check`: all 563 web + core + server tests pass (vitest ✓, oxlint ✓, oxfmt ✓).\n- One pre-existing flaky test (`diffCache.test.ts: propagates getDiff rejections ...`) also\n  fails intermittently on the base branch without my changes — surfaced, not addressed.\n\n### Surfaced but out of scope\n- `useCommitSelection.test.ts:27-33` and `useCommitList.test.ts:28` construct `CommitEntry`\n  without `files` (added in commit 0359288); typecheck mismatch pre-existed this task.\n\n### DB note (unrelated to this task's code)\nThe beads SQLite database was corrupt for the greppa-c4a subtree throughout this session.\n`br doctor --repair` and full JSONL→DB rebuild both produced new corruption at Rowid 84+/138+.\nWorked around by editing `.beads/issues.jsonl` directly and re-importing. Eventually\n`br update` started accepting writes to greppa-c4a.2. Worth an investigation outside this task.\n\n## Revert note\n\nInitial fix (commit 93795ea) caused a render loop / tab hang when interacting\nwith commits or expanded-commit file rows in the dev server. Reverted in\n74a2dfd without a reproduction. The revert restores pre-fix behavior; the\noriginal bug (commitFile dropped when ?s= is active) is still present.\n\nSuspected cause — not verified:\n\n- `useCommitSelection` now subscribes to `commitFile` via useRouterState\n  and carries it forward in every `navigateWithState`. This adds a second\n  subscription that mutates `selectCommit`'s dep array (`commitFileParams`)\n  every nav, cascading ref changes through CommitList and into the\n  selectedDiffs -> StackedDiffViewer chain.\n- Alternatively: `useCommitFileSelection.toggle` now mints a new state id\n  via `getOrCreateStateId` on every toggle when any selection is active.\n  Each toggle writes a new URL, which re-renders the entire selection\n  coordinator, which recomputes entries and diffs. If `useCommitFileDiffs`\n  returns a new `{diffs, failedPaths}` object each combine() call, the\n  App.tsx `selectedDiffs` useMemo invalidates every render even without\n  new data.\n\nPlan for a re-do:\n\n1. Reproduce in the dev server first (chrome-extension was down during the\n   original attempt).\n2. Narrow the fix — only touch `useCommitFileSelection.toggle` and the\n   state schema. Do NOT thread commitFile through `useCommitSelection` as a\n   first pass. Accept that selecting a new commit clears commitFile\n   (matches historical behavior); revisit later if needed.\n3. If clearing on commit-select is unacceptable, add carry-forward as a\n   second commit with a manual browser test gate BEFORE committing.\n4. Verify `commitFileDiffs` returns stable references across renders when\n   data hasn't changed (memoize `{diffs, failedPaths}` via useMemo in the\n   combine output, or verify useQueries already does this).\n\nTask stays open; verify.json cleared so next attempt starts from step 1.\n\n## Expanded plan v2 (narrow, post-revert)\n\n# Re-attempt greppa-c4a.2: narrow fix for commitFile dropped by ?s= URLs\n\n## Context\n\nWhen the router encodes URL search into `?s=<id>` (`packages/web/src/router.ts:49-52`), every non-`s` param is dropped from the URL. `StatePayload` (`packages/web/src/stateCache.ts:3-7`) and `StateData` (`packages/core/src/index.ts:44-49`) never included `commitFile`, so toggling a file inside an expanded commit after selecting any commit silently lost the write. The diff viewer never received the entry. This defeats the epic success criterion \"Same path in two different commits renders as two independent diff entries.\"\n\nThe first fix (commit `93795ea`) was reverted in `74a2dfd` because the dev tab hung during interaction — unreproducible in tests, probably from the combined effect of also threading `commitFile` through `useCommitSelection` (its `selectCommit` useCallback got `commitFileParams` as a dep, re-memoizing on every navigation and cascading prop changes through `CommitList`).\n\nThis attempt is narrower: schema + `useCommitFileSelection.toggle` + redirect only. No changes to `useCommitSelection`. Accept that selecting a new commit will reset `commitFile` — this matches pre-fix behavior and is NOT a success criterion on the task.\n\n## Approach\n\nFour files change behavior; two packages get new test coverage. No changes to `useCommitSelection` or `useMultiSelect` beyond zero-initing the new field.\n\n**Validation gates:** automated tests at every step, plus a manual browser check in the dev server after the final step (before commit) — clicking a commit, then a file inside it, and confirming (a) the file renders in the diff viewer, (b) the tab does not hang, (c) reload preserves the selection.\n\n## Critical files\n\n- `packages/core/src/index.ts:44-49` — `StateData` struct, add `commitFile: Schema.Array(Schema.String)`.\n- `packages/web/src/stateCache.ts:3-7,17-18` — `StatePayload` adds `commitFile: string[]`; `stateKey` includes it.\n- `packages/server/src/Http.ts:29-33,272` — `StoredState` adds `commitFile: string[]`; save handler threads it.\n- `packages/web/src/hooks/useCommitFileSelection.ts:23-55` — `toggle` branches: plain navigate when no selection, `getOrCreateStateId` when any of file/wt/commits is active.\n- `packages/web/src/router.ts:142-160` — `commitsRoute.beforeLoad` same-section redirect includes `commitFile`.\n- `packages/web/src/hooks/useCommitSelection.ts:10-14` — `buildState` zero-inits `commitFile: []` (shape only; NO threading).\n- `packages/web/src/hooks/useMultiSelect.ts:28-31` — `buildState` branches zero-init `commitFile: []` (shape only).\n- `packages/web/src/hooks/useCommitSelection.test.ts:50` — `commitsUrl` helper literal gets `commitFile: []`.\n\n## Existing patterns to reuse\n\n- `packages/web/src/hooks/useMultiSelect.ts:68-78` — `navigateWithState` using `getOrCreateStateId` + `navigate`. Template for the toggle rewrite when any selection is active.\n- `packages/web/src/stateCache.ts:38-47` `getOrCreateStateId()` — returns an existing id for matching payload (via `findExistingId`), so repeated toggles with the same result don't flood `postState`.\n- `packages/web/src/toStringArray.ts` — already used for search param coercion.\n- `cacheState(id, state)` from `stateCache.ts` — for seeding a `?s=<id>` URL in round-trip tests.\n\n## Steps (TDD, verify after each)\n\n1. **Extend schema shape.** Add `commitFile: Schema.Array(Schema.String)` to `StateData`; `commitFile: string[]` to `StatePayload` and `StoredState`; include `commitFile` in `stateKey`; update `saveState` handler to thread `commitFile`; zero-init `commitFile: []` in both `useCommitSelection.buildState` and `useMultiSelect.buildState`; update `commitsUrl` helper in `useCommitSelection.test.ts`. Add schema decode tests in `packages/core/src/index.test.ts`. Add `packages/web/src/stateCache.test.ts` with a `getOrCreateStateId` uniqueness test (two payloads differing only in `commitFile` get distinct ids). **Verify:** `node_modules/.bin/vp test packages/core packages/web packages/server`.\n\n2. **Rewrite `useCommitFileSelection.toggle`.** Extend selector to read `file`, `wt`, `commits`, `commitFile`. Toggle:\n   - If `file.length === 0 && wt.length === 0 && commits.length === 0`: plain navigate to `/commits` with `{s: '', commits: [], commitFile: next}` (matches pre-fix plain-param behavior).\n   - Else: `getOrCreateStateId({file, wt, commits, commitFile: next})`, navigate with `{s: id, commits, commitFile: next}`.\n   Add two tests in `useCommitFileSelection.test.ts`: (a) \"toggle with no other selection writes plain commitFile params, not a state id\"; (b) \"toggle survives a state-id URL by minting a new state id\" that seeds `cacheState` with `{file:['src/x.ts'], wt:['src/y.ts'], commits:['aaa'], commitFile:[]}` and asserts `file`/`wt`/`commits` all carry forward in the new payload. **Verify:** `node_modules/.bin/vp test packages/web -- useCommitFileSelection`.\n\n3. **Fix the `/commits` same-section redirect.** Extract `buildCommitsRedirectSearch(s, state)` in `router.ts` returning `{s, commits: state.commits, commitFile: state.commitFile}`. Wire into `commitsRoute.beforeLoad` at line 158. Export the helper for testing. Add `packages/web/src/router.test.ts` asserting the helper's output shape. **Verify:** `node_modules/.bin/vp test packages/web`.\n\n4. **Manual browser validation.** Before committing: start the dev servers (`pnpm --filter @greppa/server run dev & pnpm --filter @greppa/web run dev`). In the browser tab:\n   - Click a commit to select it. Confirm no hang. Check DevTools for unbounded renders.\n   - Expand the commit (chevron). Click a file row. Confirm the diff appears in the viewer and the URL updates.\n   - Reload the tab. Confirm the file stays selected and the diff still renders.\n   - Click a different commit. Note: `commitFile` clears (expected with narrow fix). Confirm no hang.\n   Only if all four pass: proceed to `cape check` then `cape:commit`.\n\n## Out of scope (deferred)\n\n- Threading `commitFile` through `useCommitSelection.navigateWithState` so selecting a new commit carries `commitFile` forward. This was the suspected cause of the render loop. Not required by any success criterion on greppa-c4a.2. File as a follow-up task only if user-visible feedback demands it.\n- Hunk-level selection, auto-grouping, reviewed-state — all explicitly deferred by the epic.\n\n## Verification\n\n- **Automated:** `cape check` passes (vitest, oxlint, oxfmt).\n- **Manual:** the browser checks above. If the tab hangs at any step, STOP, do not commit, and bisect toggle vs redirect by partially reverting.\n- **Success criteria (task):**\n  - Clicking a file after selecting commits writes to URL-encoded state and surfaces in the diff viewer (Step 2 test + manual).\n  - Reload preserves `commitFile` selection (Step 3 redirect + manual reload).\n  - Existing single-file tests unchanged (Step 2 keeps plain-param path for no-selection).\n  - All tests passing (`cape check`).\n  - Pre-commit hooks passing (committed via `cape commit`).\n\n## Outcome (v2 narrow)\n\nRe-attempted after the v1 revert. The narrow plan landed cleanly.\n\n### Root cause of prior hang\nv1 threaded `commitFile` through `useCommitSelection.navigateWithState`, adding\n`commitFileParams` as a `selectCommit` useCallback dep. That change alone was\nnot the hang trigger — browser-traced the real issue: RAC `Tree.Collection`\nmemoizes its items-render output by items reference. Since `commits` is stable\nacross renders (react-query), child `Tree.Item` onPointerDown handlers are NOT\nrebound when the parent re-renders. The first-render `toggle` closure (with\nempty `commits`) stays bound to the DOM. When the user clicks a file after\nselecting a commit, toggle sees stale `commits: []` and takes the empty-branch,\nwriting plain `?commitFile=` and dropping the state-id.\n\nv1 likely hung because it also dropped the `(prev) => ...` navigate form, so\nevery toggle call fired a nav to `/commits?s=<newid>` even when the closure\nmade the new state identical to the old — triggering a tight re-render loop\nthrough CommitList → selectedDiffs → StackedDiffViewer.\n\n### v2 fix (this attempt)\nNarrow scope + stable-callback pattern. `useCommitFileSelection.toggle` reads\ncurrent state via a `stateRef` synced on each render, so the callback's closure\nis stable (only deps: `navigate`) but reads fresh values at call time. No\nchanges to `useCommitSelection`.\n\n### Shape changes\n- `StateData` (core), `StatePayload` (web), `StoredState` (server) gain\n  `commitFile: string[]`.\n- `stateKey` includes `commitFile` — distinct ids per commit-file selection.\n- Server `saveState` threads `commitFile`.\n- `useCommitSelection.buildState` and `useMultiSelect.buildState` zero-init\n  `commitFile: []` (shape only).\n\n### Behavior changes\n- `useCommitFileSelection.toggle`:\n  - When file/wt/commits all empty: plain navigate `?commitFile=`.\n  - Else: mint state id via `getOrCreateStateId({file, wt, commits, commitFile: next})`, navigate `{s, commits, commitFile}`.\n- `commitsRoute.beforeLoad` uses new `buildCommitsRedirectSearch(s, state)`\n  helper so same-section reload preserves commitFile.\n\n### Tests\n- `packages/core/src/index.test.ts` — `StateData` decode tests.\n- `packages/web/src/stateCache.test.ts` (new) — `stateKey` uniqueness under `commitFile`.\n- `packages/web/src/hooks/useCommitFileSelection.test.ts` — plain-param path and state-id round-trip carrying file/wt/commits forward.\n- `packages/web/src/router.test.ts` (new) — redirect-target shape.\n\n### Browser validation (against narrow-plan Step 4)\n1. Click commit: `?s=<id>` minted, no hang.\n2. Expand commit + click file: `?s=<new-id>` minted, state payload has both\n   `commits` and `commitFile`, diff viewer shows 13 entries (12 commit files +\n   1 commitFile entry for the same path).\n3. Reload: router state preserves `commits` AND `commitFile` via the\n   `buildCommitsRedirectSearch` redirect.\n4. Click a different commit: new state id, `commitFile` cleared to `[]`,\n   no hang. Matches narrow-fix expected behavior.\n\n### Known out-of-scope (per plan)\n- Commit-select does not carry forward `commitFile`. Not a task success\n  criterion.\n- Server dev requires manual restart on `@greppa/core` schema changes (tsx\n  doesn't HMR).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-16T19:37:12.975267177Z","created_by":"sqve","updated_at":"2026-04-17T08:31:36.002447451Z","closed_at":"2026-04-17T08:31:36.000651212Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-c4a.2","depends_on_id":"greppa-c4a","type":"parent-child","created_at":"2026-04-16T19:37:12.975267177Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-c4a.3","title":"Task 3: Visible selected-state on commit file rows","description":"## Goal\nChild file rows in an expanded commit currently render with no visual difference when they are the active `commitFile` entry. Clicking a file already updates the URL and diff viewer, but the row itself gives no feedback — users believe the click did nothing. Wire `useCommitFileSelection.isSelected` into the child `Tree.Item` and style the selected state.\n\n## Behaviors\nOne TDD cycle per item.\n\n1. `CommitList` accepts `selectedCommitFiles: Set<string>` (encoded `\"sha:path\"` strings) as a prop; passes `aria-selected` to each child Tree.Item.\n2. Selected child rows receive a visible CSS treatment (background tint + text color), mirroring the existing committed `FileTree` selected-row pattern in `packages/web/src/components/FileTree/FileTree.module.css`.\n3. `App.tsx` threads `new Set(commitFileSelection.entries.map((e) => \\`${e.sha}:${e.path}\\`))` into CommitList via `FileTreePanel`.\n4. Component test: render with `selectedCommitFiles` containing one entry → matching child row has `aria-selected=\"true\"`, other rows have `aria-selected=\"false\"`.\n\n## Success criteria\n- [ ] Clicking a file in an expanded commit visually marks that row as selected.\n- [ ] Clicking the same file again deselects it and removes the visual state.\n- [ ] The selected indicator is distinct from the hover and focus states.\n- [ ] Tests passing; pre-commit hooks passing.\n\n## Dependencies\nMust land after greppa-c4a.2 — the selected state depends on `commitFile` surviving selection transitions.\n\n## References\n- packages/web/src/components/CommitList/CommitList.tsx — child `Tree.Item` render.\n- packages/web/src/components/FileTree/FileTree.module.css — existing selected-row CSS pattern to mirror.\n- packages/web/src/hooks/useCommitFileSelection.ts — `entries`, `isSelected`.\n- packages/web/src/App.tsx — wiring site.","design":"test","status":"closed","priority":3,"issue_type":"task","created_at":"2026-04-16T19:37:26.977616901Z","created_by":"sqve","updated_at":"2026-04-18T12:00:54.001815598Z","closed_at":"2026-04-18T12:00:54.000233799Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-c4a.3","depends_on_id":"greppa-c4a","type":"parent-child","created_at":"2026-04-16T19:37:26.977616901Z","created_by":"sqve","metadata":"{}","thread_id":""},{"issue_id":"greppa-c4a.3","depends_on_id":"greppa-c4a.2","type":"blocks","created_at":"2026-04-16T19:37:48.953828176Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-c4a.4","title":"Task 4: Visual polish pass on commit list rows","description":"## Goal\nThree small visual issues on the Commits section make it feel unfinished:\n1. Time (\"9h ago\", \"1d ago\") wraps to two lines because the meta column is too narrow — every commit row is ~2x tall.\n2. Child file rows render as bare text with no file-type icon, making them indistinguishable from free-form labels.\n3. No visible indent guide for level-2 (child) rows under an expanded commit, so the tree structure is not obvious.\n\nPure CSS + small JSX in `CommitList.tsx`; no behavior change, no new tests beyond presence checks.\n\n## Behaviors\nBundle into one verification loop (visual).\n\n1. Meta column rendered inline, not stacked. `abbrevSha` + relative time fit on one line at default `file-tree` panel width (240px). Component test: commit row height stays within single-line bounds via `getBoundingClientRect`.\n2. Child file rows render a file-type icon (reuse whatever icon `FileTree` child files use, e.g. `IconFile`). Component test: expanded commit's child row contains an `svg` element.\n3. Tree indent guide visible on level-2 rows (CSS on `[aria-level=\"2\"]` via `Tree.Indent`). Visual verification only — no automated test for pixel-level guide rendering.\n\n## Success criteria\n- [ ] Commit row meta (\"9h ago\", \"aaa111\") fits on one line at 240px sidebar width.\n- [ ] Child file rows display a file icon.\n- [ ] Indent guide visible under expanded commits.\n- [ ] All tests passing; pre-commit hooks passing.\n\n## Dependencies\nIndependent of greppa-c4a.2 and .3 — ordering is flexible.\n\n## References\n- packages/web/src/components/CommitList/CommitList.tsx and CommitList.module.css — primary edits.\n- packages/web/src/components/FileTree/FileTree.tsx — file icon pattern to mirror.\n- packages/ui/src/components/Tree/Tree.tsx — `Tree.Indent` primitive.","design":"## Expanded plan (expand-task)\n\n### Investigation findings\n\n**Files to modify:**\n- `packages/web/src/components/CommitList/CommitList.tsx` (1–143) — JSX for the RAC Tree rendering commit rows + child file rows. Needs a file icon on child rows.\n- `packages/web/src/components/CommitList/CommitList.module.css` (1–72) — styles for `.subject`, `.meta`, `.hash`, `.time`. `.item`/`.list` blocks are dead (no `className` reference in the tsx); leave alone or delete if clearly dead. The meta-wrap and indent-guide fixes live here.\n\n**Patterns to follow:**\n- `packages/web/src/components/FileTree/FileTree.tsx:90-95` — the `FileIcon` usage pattern. Import `FileIcon` from `@greppa/ui` (index.ts:7), pass `name={path.split('/').pop()}` and `baseUrl='/material-icons'`. FileIcon renders an `<img>` (see `packages/ui/src/components/FileIcon/FileIcon.tsx:27`), not an `<svg>` — the task's \"svg element\" wording is a description error; TDD should assert on `img` per the established pattern at `FileTree.test.tsx:122-128`.\n- `packages/ui/src/components/Tree/Tree.module.css:22-38` — the shared Tree primitive already draws a per-level guide via `.item::before` + `--tree-item-level`. It is scoped to the UI primitive and affects every consumer (FileTree too). Do NOT modify that global rule. For a Commits-section-local guide, layer CSS via `CommitList.module.css` on the Tree root (e.g. class on `Tree.Root` targeting `[aria-level=\"2\"]`).\n\n**Reusable code:**\n- `@greppa/ui` → `FileIcon` — already exported; no new dependency.\n- RAC sets `aria-level` on `[role=\"row\"]` automatically (confirmed in `react-aria-components/dist/Tree.main.js:497,725`), so level-based selectors work without extra wiring.\n\n**Blast radius:**\n- `packages/web/src/components/CommitList/CommitList.test.tsx` — existing tests assert on `screen.getByText('src/a.ts')` inside a child row; adding an icon sibling to `Tree.Label` should not break these, but adding a new assertion for icon presence is in-scope.\n- No other importer of `CommitList` — only `FileTreePanel.tsx` passes props; untouched by this task.\n\n### Steps\n\n### Step 1: Commit row meta renders on a single line at 240px panel width\n\n**Pattern:** `packages/web/src/components/CommitList/CommitList.tsx:109-116` — current `Tree.ItemContent` markup with `styles.subject` and `styles.meta` siblings.\n\n**Changes:**\n- `packages/web/src/components/CommitList/CommitList.module.css:49-72` — update `.subject` to `flex: 1; min-width: 0;` so it absorbs available width and ellipsis-truncates. Update `.meta` to `flex-shrink: 0; white-space: nowrap; gap: var(--gr-space-sm);`. Drop `.time { margin-left: auto }` — meta is no longer the row-level positioner; its children flow with the new gap. Keep `.hash` untouched.\n\n**Verify:** `pnpm --filter @greppa/web test -- CommitList`\n\n### Step 2: Expanded commit's child file rows render a file-type icon\n\n**Pattern:** `packages/web/src/components/FileTree/FileTree.tsx:88-96` — `FileIcon` placed between `Tree.Indent` and `Tree.Label` inside a non-directory `Tree.ItemContent`. `ICONS_BASE_URL = '/material-icons'` (FileTree.tsx:21). Test pattern at `packages/web/src/components/FileTree/FileTree.test.tsx:122-128` — count `container.querySelectorAll('img')`.\n\n**Changes:**\n- `packages/web/src/components/CommitList/CommitList.tsx:1` — add `FileIcon` to the `@greppa/ui` import alongside `Tree`.\n- `packages/web/src/components/CommitList/CommitList.tsx:117-137` — inside the child `Tree.ItemContent`, add `<FileIcon name={basename(child.path)} baseUrl=\"/material-icons\" />` between `<Tree.Indent />` and `<Tree.Label>`. `basename` = `child.path.split('/').pop() ?? child.path`; inline it, no helper module.\n\n**Verify:** `pnpm --filter @greppa/web test -- CommitList`\n\n### Step 3: Visible indent guide on level-2 rows inside the Commits Tree\n\n**Pattern:** `packages/ui/src/components/Tree/Tree.module.css:22-38` — the shared primitive's `::before` guide. The CommitList change mirrors the intent locally so we don't alter FileTree styling. RAC-applied `aria-level=\"2\"` on child rows is the selector hook.\n\n**Changes:**\n- `packages/web/src/components/CommitList/CommitList.tsx:78-85` — add `className={styles.tree}` to `<Tree.Root>` so the local CSS can scope to this tree only.\n- `packages/web/src/components/CommitList/CommitList.module.css` — add a `.tree [role=\"row\"][aria-level=\"2\"]::before` rule that paints a 1px vertical guide at the left edge of the child-row indent (use `--gr-color-border-muted` with `opacity: 0.4` to match the primitive). Position absolute inside the relatively-positioned Tree `.item` (already set by `Tree.module.css:11-13`). This is visual-only — no automated test per the task's success criteria.\n\n**Verify:** `pnpm --filter @greppa/web test -- CommitList && pnpm --filter @greppa/web typecheck`\n\n### Final verification\n\n`pnpm check`\n\n## Outcome\n\n### What changed\n- `packages/web/src/components/CommitList/CommitList.module.css` — `.subject` now flex:1 / min-width:0; `.meta` now flex-shrink:0 with white-space:nowrap and a gap; `.time` rule and className removed (replaced by meta's gap). New `.tree [role='row'][aria-level='2']::after` draws a 1px vertical guide at `calc(var(--gr-space-md) + 8px)` from the row's left edge.\n- `packages/web/src/components/CommitList/CommitList.tsx` — imports `FileIcon` from `@greppa/ui`; renders `<FileIcon name={basename(path)} baseUrl=\"/material-icons\" />` on each child file row; adds `className={styles.tree}` to `Tree.Root`; inlines the time span without a CSS class.\n- `packages/web/src/components/CommitList/CommitList.test.tsx` — new assertion that every expanded commit's child file row contains an `img` element (TDD RED → GREEN).\n\n### What was learned\n- The task text's \"svg element\" assertion was imprecise; FileTree's file icons are `<img>` via `@greppa/ui`'s `FileIcon`, which is the established pattern in this codebase. The new test asserts on `img`, matching `FileTree.test.tsx:122-128`.\n- The shared Tree primitive (`packages/ui/src/components/Tree/Tree.module.css:22-38`) already paints a per-level guide via `::before` + `--tree-item-level`, but it's subtle at 0.4 opacity and, at level 2, only a single hairline gradient stop. The task's feedback (\"no visible indent guide\") reflects that subtlety at 240px width. A locally-scoped `::after` rule keeps the primitive unchanged (so FileTree styling is preserved) and produces a visible 0.6-opacity guide on CommitList children only.\n- Step 1's CSS-only behavior is not observable in happy-dom (no layout engine), so it was implemented without a new failing test; the existing 16-test suite guards against structural regressions. Step 2's icon presence is testable (RED/GREEN cycle recorded). Step 3 is visual-only per task success criteria.\n\n### Verification\n- `cape check`: vitest 575 pass / 1 skipped, oxlint pass, oxfmt pass.\n- `pnpm run typecheck`: clean.\n- Manual in-browser check deferred to HITL checkpoint (three small, easily-reasoned CSS/JSX changes; no behavior change).","status":"closed","priority":3,"issue_type":"task","created_at":"2026-04-16T19:37:43.847062939Z","created_by":"sqve","updated_at":"2026-04-21T12:03:24.121071674Z","closed_at":"2026-04-21T12:03:24.120868541Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["hitl"],"dependencies":[{"issue_id":"greppa-c4a.4","depends_on_id":"greppa-c4a","type":"parent-child","created_at":"2026-04-16T19:37:43.847062939Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-ci4","title":"Tree styling: row height, selection bar, indent guides","description":"## Goal\n\nRestyle the generic Tree component (packages/ui) to match editor-quality density and selection behavior: 22px rows, full-width selection bar, and indent guides.\n\n## Behaviors\n\n- Tree rows are 22px tall (min-height: 22px, tight padding)\n- Selected items use a full-width background fill with no border-radius — background spans the entire sidebar width\n- Focused items use a subtle background instead of an outline\n- Hovered items keep current behavior (bg-surface-hover)\n- Indent guides render as subtle vertical lines at each nesting depth via ::before pseudo-element\n- Indentation uses --tree-item-level CSS variable on the chevron's padding-left, not on the row\n- A new Tree.Indent sub-component provides a spacer for leaf items (no chevron) to align with folder rows\n- --gr-tree-indent custom property (20px) controls indent step size\n\n## Success criteria\n\n- [ ] .item has min-height: 22px with reduced vertical padding\n- [ ] .item has no border-radius\n- [ ] .item[data-focused] uses background instead of outline\n- [ ] .item::before draws vertical guide lines using repeating-linear-gradient\n- [ ] .chevron padding-left uses calc((var(--tree-item-level, 1) - 1) * var(--gr-tree-indent))\n- [ ] Tree.Indent renders a span sized by --tree-item-level for leaf item alignment\n- [ ] Existing Tree.test.tsx tests pass\n- [ ] New tests verify indent and styling classes\n- [ ] Tree.preview.tsx visually confirms all changes","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-01T13:03:15.326266708Z","created_by":"sqve","updated_at":"2026-04-01T13:11:51.804062017Z","closed_at":"2026-04-01T13:11:51.803906955Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["file-tree","ui"],"dependencies":[{"issue_id":"greppa-ci4","depends_on_id":"greppa-cqt","type":"parent-child","created_at":"2026-04-01T13:03:31.534586855Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-cqt","title":"Editor-quality file tree sidebar","description":"## Context\n\nThe file tree sidebar is functional but visually basic compared to VSCode/JetBrains. It lacks file icons, indent guides, compact folders, and uses an outline-based selection instead of a full-width background fill. This epic brings it to editor-quality through 7 visual improvements in 3 phases.\n\n## Requirements\n\n- [ ] Tree rows use 22px height matching VSCode density\n- [ ] Selected/focused items use full-width background fill instead of outline — no border-radius, background spans edge-to-edge\n- [ ] Indent guides render as subtle vertical lines at each nesting depth using --tree-item-level CSS variable\n- [ ] Indentation lives on the chevron/spacer (inner element), never on the TreeItem row itself\n- [ ] File type icons render next to each filename via vscode-material-icons (910 icons, <img> rendering)\n- [ ] Folder icons show open/closed state matching expand/collapse\n- [ ] Leaf items align with folder items via an Indent spacer where the chevron would be\n- [ ] Filename text color reflects change type: green (added), red (deleted), yellow (modified), muted (renamed)\n- [ ] Single-child directory chains flatten into one row (compact folders), using deepest dir path as React Aria id\n- [ ] Directories inherit the highest-priority changeType from descendants (added > deleted > modified > renamed)\n- [ ] Directory rows show colored text but no letter badge\n\n## Success criteria\n\n1. All 7 visual improvements render correctly in both Catppuccin Latte and Mocha themes\n2. Existing Tree and FileTree tests pass without modification (new tests added alongside)\n3. File icons load via <img> tags from static assets — no dangerouslySetInnerHTML\n4. Compact folders produce correct tree shape: `src/events` as one row when `events` has a single child directory\n5. Directory status propagation colors parent folders based on child file statuses\n6. pnpm test, pnpm typecheck, pnpm lint all pass\n\n## Anti-patterns\n\n- Do NOT put level-based padding on the TreeItem row — only on inner elements (chevron, spacer). This breaks full-width selection highlight.\n- Do NOT use dangerouslySetInnerHTML for icons — use <img> with vscode-material-icons URL API\n- Do NOT compute compact folders or status propagation at render time — do it in the data layer (buildFileTree)\n- Do NOT show letter badges (M, A, D, R) on directory rows — only colored text\n- Do NOT add keyboard shortcuts in this epic — UI visual quality only\n- Do NOT modify the Tree UI component's public API surface beyond adding Indent sub-component\n\n## Approach\n\nThree phases, each a separate PR:\n\n**Phase 1 — Tree styling (CSS-only):** Tighter row height, full-width selection bar, indent guides + chevron indentation. All in packages/ui Tree component.\n\n**Phase 2 — File icons + colors:** New FileIcon component in packages/ui using vscode-material-icons. Integrate into FileTree with TreeItemContent render function for isExpanded access. Add change type color classes to filename text.\n\n**Phase 3 — Data transforms:** compactTree() and propagateChangeType() functions in useFileList.ts. Pure data-layer transforms called inside buildFileTree after sortNodes.\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Icon library | vscode-material-icons | Folder open/closed support, 910 icons, <img> rendering vs dangerouslySetInnerHTML |\n| Indentation | Chevron padding-left | React Aria pattern — row stays full-width for selection highlight |\n| Compact folder identity | Deepest dir path | Stable React Aria id for selection/expansion |\n| Status propagation | Data layer (buildFileTree) | Pure function, testable, no render-time computation |\n| Row height | 22px (VSCode standard) | Editor-density familiarity |\n| Deleted propagation | Yes (unlike VSCode) | Code review tool — deleted files matter for review visibility |","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-04-01T13:03:00.835039246Z","created_by":"sqve","updated_at":"2026-04-01T19:29:29.620108253Z","closed_at":"2026-04-01T19:29:29.619956001Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["file-tree","ui"],"dependencies":[{"issue_id":"greppa-cqt","depends_on_id":"greppa-n2s","type":"blocks","created_at":"2026-04-01T15:45:42.998833552Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-dgm","title":"Serve built web app from greppa serve","description":"## Requirements (IMMUTABLE)\n\n- [ ] `greppa serve` serves both API endpoints and the built web frontend on a single port\n- [ ] Web assets are bundled into the CLI dist at build time — no runtime path guessing\n- [ ] SPA fallback: non-API routes serve `index.html` for client-side routing\n- [ ] API routes (`/api/*`) continue working unchanged\n- [ ] Build order enforced: web builds before CLI\n- [ ] `greppa serve` opens to a working web app in the browser at `http://localhost:4400`\n\n## Success criteria\n\n- [ ] `pnpm run build` from root builds web then CLI, with web dist copied into CLI dist\n- [ ] `greppa serve` starts, browser at `http://localhost:4400/` loads the web app\n- [ ] `GET /api/health` returns `{ status: \"ok\" }` (API still works)\n- [ ] `GET /api/refs` returns refs (API still works)\n- [ ] Navigating to `/file/some/path` in browser works (SPA fallback serves index.html)\n- [ ] All tests passing, pre-commit hooks passing\n\n## Anti-patterns (FORBIDDEN)\n\n- NO runtime path resolution to find web assets outside the CLI dist (reason: fragile, breaks if directory structure changes)\n- NO separate static file server or second port (reason: must be single command, single port)\n- NO new dependencies — use Effect's built-in `HttpStaticServer` from `effect/unstable/http/HttpStaticServer`\n- NO changes to the dev flow — `pnpm dev` continues using Caddy + Vite dev server\n\n## Durable decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Asset location | Bundled in CLI dist at build time | Fail-proof, no runtime path guessing |\n| Static server | `HttpStaticServer.layer({ root, spa: true })` | Built into Effect, composes with existing HttpRouter |\n| Build order | `@greppa/web` as devDependency of `@greppa/cli` | `vp run -r build` respects dependency order |\n| Copy mechanism | `cp -r ../web/dist dist/web` in CLI build script | Boring, explicit, zero dependencies |\n| Path resolution | `resolve(import.meta.dirname, 'web')` | Always correct since assets ship alongside JS |\n\n## Approach\n\nThree changes:\n\n1. **CLI build** — update `packages/cli/package.json`: add `@greppa/web` devDependency, change build script to `\"vp pack && cp -r ../web/dist dist/web\"`\n2. **Server** — update `packages/server/src/Http.ts`: `makeHttpLayer` gains `webDistPath` parameter, merges `HttpStaticServer.layer({ root: webDistPath, spa: true })` with `ApiRoutes` inside `HttpRouter.serve`\n3. **CLI serve** — update `packages/cli/src/commands/serve.ts`: resolve `webDistPath` via `import.meta.dirname` and pass to `makeHttpLayer`\n\n## Architecture\n\n- `HttpStaticServer.layer` provides routes to the same `HttpRouter` as `ApiRoutes` — they compose via the layer system\n- API routes match first (explicit paths), static files are the fallback\n- `spa: true` serves `index.html` for paths that don't match a file — enables client-side routing\n- `FileSystem`, `Path`, `HttpPlatform` dependencies are already provided by `NodeHttpServer.layer`","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-04-03T18:21:53.387880380Z","created_by":"sqve","updated_at":"2026-04-03T19:40:09.932458529Z","closed_at":"2026-04-03T19:40:09.932208184Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cli","server"]}
{"id":"greppa-dgm.1","title":"Bundle web assets and serve from single port","description":"## Goal\n\nMake `greppa serve` serve the built web frontend alongside the API on a single port using Effect's `HttpStaticServer`.\n\n## Behaviors\n\n1. `packages/cli/package.json` gains `@greppa/web` as devDependency. Build script changes to `\"vp pack && cp -r ../web/dist dist/web\"`.\n2. `packages/server/src/Http.ts`: `makeHttpLayer` gains a `webDistPath: string` parameter. Inside, `HttpStaticServer.layer({ root: webDistPath, spa: true })` is merged with `ApiRoutes` so the static file layer provides routes to the same `HttpRouter`.\n3. `packages/cli/src/commands/serve.ts`: resolves `webDistPath` as `resolve(import.meta.dirname, 'web')` and passes to `makeHttpLayer`.\n4. `packages/server/src/Http.integration.test.ts`: update `makeHttpLayer` call or `TestAppLayer` to accommodate the new parameter.\n\n## References\n\n- packages/server/src/Http.ts:126-138 — `ApiRoutes` and `makeHttpLayer`\n- packages/cli/src/commands/serve.ts:45-52 — serve command handler calling `makeHttpLayer`\n- packages/cli/package.json — build script and dependencies\n- effect/unstable/http/HttpStaticServer — `layer({ root, spa, prefix })` API\n- packages/web/dist/ — built web assets (index.html, assets/, material-icons/)\n\n## Success criteria\n\n- [ ] `pnpm run build` from root builds web then CLI, CLI dist contains `web/` subdirectory with index.html\n- [ ] `greppa serve` starts, `http://localhost:4400/` serves the web app\n- [ ] `GET /api/health` still returns `{ status: \"ok\" }`\n- [ ] Browser navigation to `/file/some/path` serves index.html (SPA fallback)\n- [ ] All tests passing, pre-commit hooks passing","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T18:22:06.791883918Z","created_by":"sqve","updated_at":"2026-04-03T18:59:27.758103024Z","closed_at":"2026-04-03T18:59:27.757929292Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cli","server"],"dependencies":[{"issue_id":"greppa-dgm.1","depends_on_id":"greppa-dgm","type":"parent-child","created_at":"2026-04-03T18:22:06.791883918Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-fku","title":"Create Shiki Web Worker with message protocol","description":"## Goal\n\nCreate a dedicated Web Worker that owns the Shiki highlighter instance and responds to highlight requests via postMessage. Define the message protocol types shared between main thread and worker.\n\n## Behaviors\n\n1. Worker initializes Shiki highlighter lazily on first highlight request\n2. Worker loads language grammars on demand per request, not all at init\n3. Worker receives lines with keys and returns ThemedToken arrays keyed by line key\n4. Worker caches highlighted results by {filePath, lineKey} to skip re-work\n5. Worker falls back to plaintext tokens for unknown languages\n6. Shared TypeScript types define the request/response message protocol\n\n## Success criteria\n\n- [ ] Worker file uses Vite's native `new Worker(new URL(...), { type: 'module' })` pattern\n- [ ] Message protocol types are importable from both worker and main thread code\n- [ ] Language grammars are loaded lazily, not bundled at init\n- [ ] Cache prevents re-highlighting the same line for the same file\n- [ ] Unknown languages produce plain text tokens instead of errors\n- [ ] No integration with DiffViewer yet (next task)","design":"## Expanded plan\n\n### New files\n\n1. `packages/web/src/workers/highlightProtocol.ts` — Shared message types\n2. `packages/web/src/workers/highlightEngine.ts` — Core highlighting logic (testable without worker)\n3. `packages/web/src/workers/highlightEngine.test.ts` — Tests for engine\n4. `packages/web/src/workers/highlightWorker.ts` — Thin worker entry point wiring postMessage to engine\n\n### Message protocol (highlightProtocol.ts)\n\n- HighlightRequest: { type, filePath, language, theme, lines: {key, content}[] }\n- HighlightResponse: { type, filePath, tokens: Record<string, {content, color?}[]> }\n- WorkerRequest = HighlightRequest; WorkerResponse = HighlightResponse\n\n### Engine logic (highlightEngine.ts)\n\nExports handleHighlightRequest(request): Promise<HighlightResponse>\n\nState: lazy highlighterPromise, loadedLanguages Set, cache Map<filePath:lineKey, token[]>\n\nFlow:\n1. Init highlighter lazily on first call (themes only, no langs)\n2. Load language on demand via highlighter.loadLanguage(). On error, fall back to plaintext\n3. Filter out cached lines. Join uncached lines, call codeToTokens once (preserves grammar state). Split back by index. Serialize ThemedToken to {content, color}. Store in cache\n4. Return response with all tokens (cached + fresh)\n\n### Worker entry (highlightWorker.ts)\n\nThin: imports handleHighlightRequest, wires self.onmessage to it, posts response back\n\n### TDD cycles\n\n1. Protocol types compile — import and use types in test\n2. Returns tokens for lines — mock shiki, 2 lines in, token map with both keys out\n3. Lazy init — createHighlighter not called until first request\n4. Language on demand — request with python triggers loadLanguage\n5. Cache hit — same request twice, codeToTokens called once\n6. Unknown language fallback — fakeLang rejected, plaintext tokens returned","status":"closed","priority":1,"issue_type":"task","assignee":"sqve","created_at":"2026-03-30T13:54:30.241570995Z","created_by":"sqve","updated_at":"2026-03-30T14:01:17.396461892Z","closed_at":"2026-03-30T14:01:17.376050076Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-fku","depends_on_id":"greppa-2oq","type":"parent-child","created_at":"2026-03-30T13:54:47.235380543Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-g0r","title":"URL-driven routing for review state","description":"## Context\n\nAll selection state (multi-select files, commit ranges, scroll target) lives in React useState hooks with no URL representation. Refreshing the page or sharing a link loses multi-select and commit range state. The current router has three splat routes (`/`, `/file/$`, `/wt/$`) that only track single file selection.\n\n## Requirements\n\n- [ ] Single `/review` route with validated search params replaces all existing routes\n- [ ] `/` redirects to `/review`\n- [ ] `file` search param: array of committed file paths (single or multi-select)\n- [ ] `wt` search param: array of worktree file paths (single or multi-select)\n- [ ] `commits` search param: array of commit SHAs (discrete cmd+click) or single `sha1..sha2` (continuous shift+click range)\n- [ ] `file`, `wt`, and `commits` are mutually exclusive — switching source clears the other params\n- [ ] `*` sentinel for select-all (`?file=*`, `?wt=*`) — resolves dynamically against current ref pair\n- [ ] Hash fragment encodes scroll target file within stacked diffs (`#src/App.tsx`)\n- [ ] Hash with line anchor (`#src/App.tsx:L42`) scrolls to specific line\n- [ ] File tree expanded section derived from which param is present (no extra URL param)\n- [ ] pushState for new intents (first file click, source switch, first commit click)\n- [ ] replaceState for refinements (cmd+click toggle, shift+click range, select-all, minimap click)\n- [ ] Scrolling through stacked diffs does NOT update the URL\n- [ ] Minimap clicks update hash via replaceState\n- [ ] Page load with hash fragment triggers scrollToFile after diffs render\n- [ ] Old routes (`/file/$`, `/wt/$`) redirect to `/review?file=...` / `/review?wt=...`\n- [ ] All existing selection behavior (single click, cmd+click, shift+click, directory click, section header click) preserved\n\n## Approach\n\nFour phases:\n\n1. **Router + search param schema** — Replace `router.ts` with single `/review` route using `validateSearch`. Install `@tanstack/zod-adapter` and `zod`. Add redirect routes for `/`, `/file/$`, `/wt/$`. No behavior changes yet — just wire the route, existing state hooks still work.\n\n2. **File selection via URL** — Replace `useFileSelection`'s navigate-to-splat approach with search param updates. Wire `useMultiSelect` and `useFileSelectionHandlers` to read/write `file`/`wt` search params. Single click = pushState, modifier clicks = replaceState. Source switching clears other params. `*` sentinel for select-all.\n\n3. **Commit selection via URL** — Wire `useCommitSelection` to read/write `commits` search param. Discrete SHAs for cmd+click, `sha1..sha2` for shift+click range. Commit selection clears `file`/`wt` params and vice versa.\n\n4. **Scroll target via hash** — Add hash fragment management for scroll-to-file. Minimap clicks and file tree clicks in multi-select update hash via replaceState. Page load reads hash and calls `scrollToFile` after render. Line anchor support (`#path:L42`).\n\n### Key design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| URL shape | Search params on single route | Multi-select needs arrays; path segments can't encode multiple files unambiguously |\n| Validation | Zod via @tanstack/zod-adapter | Type-safe search params, fallback defaults, array handling |\n| History model | pushState = new intent, replaceState = refinement | Keeps back stack clean — ~3-5 entries per session, not 30+ |\n| Scroll target | Hash fragment, not search param | Hash changes don't trigger router navigation or re-renders |\n| Select-all | `*` sentinel, dynamic resolution | Avoids URL length blowup; resolves at load time against current diff |\n| Panel section | Derived from params | No extra URL state; section follows selection source |\n\n## Anti-patterns\n\n- Do NOT create bidirectional URL ↔ scroll sync — URL drives scroll, never the reverse\n- Do NOT push history on every cmd+click toggle — floods back stack\n- Do NOT encode scroll pixel offset in URL — use logical file paths\n- Do NOT allow `file` + `wt` + `commits` to coexist in the same URL\n- Do NOT read search params and also maintain parallel useState for the same data — single source of truth is the URL\n\n## Success criteria\n\n1. Refreshing the page preserves full selection state (multi-select files, commit ranges)\n2. Sharing a URL reproduces the exact view (correct files selected, correct section expanded, scrolled to correct file)\n3. Browser back/forward navigates between intents (not between individual multi-select toggles)\n4. Old bookmarked URLs (`/file/...`, `/wt/...`) redirect correctly to new scheme\n5. No infinite render loops from URL ↔ state sync\n6. All existing tests pass (with adaptation for the new routing)","design":"## docs/ROUTING.md\n\n","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-04-04T21:05:20.187490213Z","created_by":"sqve","updated_at":"2026-04-08T19:43:52.192528044Z","closed_at":"2026-04-08T19:43:52.192278529Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["routing"]}
{"id":"greppa-g0r.1","title":"Replace router with single /review route and search param schema","description":"## Goal\n\nSet up the new `/review` route with validated search params and redirects from old routes. No selection behavior changes — just the routing foundation.\n\n## Behaviors\n\n- App renders at `/review` with the same UI as before\n- `/` redirects to `/review`\n- `/file/{path}` redirects to `/review?file={path}`\n- `/wt/{path}` redirects to `/review?wt={path}`\n- Single file click navigates to `/review?file={path}` (committed) or `/review?wt={path}` (worktree)\n- Search params validated via Zod — invalid params fall back to empty arrays\n- `useFileSelection` reads file/wt from search params instead of splat route matches\n\n## Success criteria\n\n- [ ] App loads at `/review` with identical UI\n- [ ] All three redirect routes work (`/`, `/file/$`, `/wt/$`)\n- [ ] `router.ts` has single `/review` route with `validateSearch`\n- [ ] `useFileSelection` reads from search params, not `useMatch`\n- [ ] All existing tests pass (with route path updates)","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T21:05:50.921924828Z","created_by":"sqve","updated_at":"2026-04-08T11:06:02.109669060Z","closed_at":"2026-04-08T11:06:02.109518423Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["routing"],"dependencies":[{"issue_id":"greppa-g0r.1","depends_on_id":"greppa-g0r","type":"parent-child","created_at":"2026-04-04T21:05:50.921924828Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-g0r.2","title":"Wire file selection to URL search params","description":"## Goal\n\nMake URL search params the single source of truth for file selection. Replace useMultiSelect's useState with URL-backed state. All selection actions (click, meta+click, shift+click, directory click, select-all) write to file/wt search params.\n\n## Behaviors\n\n- Plain click: pushState with `file=[path]` or `wt=[path]` (clears other params)\n- Meta+click (toggle): replaceState toggling the path in the array\n- Shift+click (range): replaceState with range of paths\n- Directory click: replaceState with `selectAll` of descendant paths\n- Select-all: replaceState with `file=*` or `wt=*` sentinel\n- Source switch: clears other source params\n- `*` sentinel resolves dynamically against current file list\n- Reading back: useMultiSelect derives selectedPaths/activeSource from URL params\n- anchorPath stays as local useState (ephemeral, not in URL)\n\n## Success criteria\n\n- [ ] useMultiSelect reads from URL, writes via navigate\n- [ ] Plain click = pushState, modifier clicks = replaceState\n- [ ] `*` sentinel for select-all resolves at read time\n- [ ] Source switching clears other params\n- [ ] useFileSelection simplified (no longer owns select/deselect navigation)\n- [ ] All existing tests pass (adapted for URL-backed behavior)\n- [ ] No parallel useState for selection data","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-08T11:49:54.689235119Z","created_by":"sqve","updated_at":"2026-04-08T17:38:15.128215493Z","closed_at":"2026-04-08T17:38:15.128064375Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["routing"],"dependencies":[{"issue_id":"greppa-g0r.2","depends_on_id":"greppa-g0r","type":"parent-child","created_at":"2026-04-08T11:49:54.689235119Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-g0r.3","title":"Wire scroll target to URL hash fragment","description":"## Goal\n\nWire URL hash fragments as the scroll target for stacked diffs. File tree clicks in multi-select mode update the hash via replaceState. Page load with a hash fragment triggers scrollToFile after diffs render. Line anchor support (`#path:L42`).\n\n## Behaviors\n\n- File tree click in multi-select mode: replaceState with `#filepath` (single-select already navigates, no hash needed)\n- Page load with `#filepath`: call `StackedDiffViewer.scrollToFile(path)` after diffs render\n- Page load with `#filepath:L42`: scroll to file, then scroll to line 42\n- Scrolling through stacked diffs does NOT update the hash (one-directional: URL drives scroll)\n- Hash managed outside TanStack Router via `window.history.replaceState`\n\n## Success criteria\n\n- [ ] Page load with `#filepath` scrolls to the correct file after diffs render\n- [ ] Page load with `#filepath:L42` scrolls to the correct line\n- [ ] File tree click in multi-select updates hash via replaceState\n- [ ] Scrolling through diffs does NOT write to the hash\n- [ ] Hash uses `window.history.replaceState`, not router navigate\n- [ ] All existing tests pass","design":"## Expanded plan\n\n## Implementation plan\n\n### 1. `parseHash` utility (new file)\n`packages/web/src/hooks/parseHash.ts`\n- Input: hash string (`#src/App.tsx`, `#src/App.tsx:L42`, empty)\n- Output: `{ path: string; line: number | null } | null`\n- Pure function, strip leading `#`, split on `:L`\n\n### 2. Extend `StackedDiffViewerHandle.scrollToLine`\n`packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx`\n- Add `scrollToLine: (path: string, line: number) => void` to handle interface (L23-25)\n- Implementation: find file-header index, then scan forward for diff-row where `row.right?.lineNumber === line` (or `row.left?.lineNumber`), call `scrollToIndex`\n- Wire into `useImperativeHandle` (L102-111)\n\n### 3. `useHashScroll` hook (new file)\n`packages/web/src/hooks/useHashScroll.ts`\n- Args: `ref: RefObject<StackedDiffViewerHandle>`, `diffs: DiffFile[]`\n- On mount, read `window.location.hash`, parse with `parseHash`\n- When diffs become non-empty AND hash is present, call `ref.current.scrollToFile(path)` or `ref.current.scrollToLine(path, line)`\n- Use a `hasScrolled` ref to fire only once\n\n### 4. Wire `useHashScroll` in App.tsx\n`packages/web/src/App.tsx`\n- After L170: call `useHashScroll(stackedDiffRef, selectedDiffs)`\n\n### 5. Hash writing from file tree clicks\n`packages/web/src/hooks/useFileSelectionHandlers.ts`\n- After modifier clicks (meta, shift, directory), call:\n  `window.history.replaceState(null, '', `${window.location.pathname}${window.location.search}#${path}`)`\n- Only when already in multi-select (multiSelect.isMultiSelect) or when the action creates multi-select\n\n### TDD order\n1. parseHash: test → implement\n2. scrollToLine: test → implement\n3. useHashScroll: test → implement\n4. App.tsx wiring (integration)\n5. Hash writing in useFileSelectionHandlers: test → implement","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-08T17:42:53.551764720Z","created_by":"sqve","updated_at":"2026-04-08T17:58:13.411343930Z","closed_at":"2026-04-08T17:58:13.410974137Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["routing"],"dependencies":[{"issue_id":"greppa-g0r.3","depends_on_id":"greppa-g0r","type":"parent-child","created_at":"2026-04-08T17:42:53.551764720Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-h1w","title":"Editor shell polish","description":"## Context\n\nGreppa's UI should feel like a code editor, not a web app. The current shell has multiple headers (app header, diff header, detail panel tabs), 1px borders everywhere, and a status bar that only shows review progress. This epic consolidates the shell into a cohesive editor-like experience.\n\n## Requirements\n\n1. **Single unified header** — merge the app header (logo, theme picker) and diff file header (file path, change badge, branch indicator) into one header bar. The detail panel tab bar remains as panel-internal navigation.\n2. **Tonal separation over borders** — remove all 1px solid borders (header, status bar, detail panel tabs/threads/info rows, diff divider, panel separators). Use background shade differences (--gr-color-bg-subtle vs --gr-color-bg-surface vs --gr-color-bg-default) to distinguish regions.\n3. **Editor-style status bar** — left side: review progress, branch. Right side: file-local metadata (language, encoding, line/col position). Requires adding file metadata to StatusBar props.\n4. **Document no-border principle in VISION.md** — add a design principle about preferring tonal layering over structural borders.\n\n## Anti-patterns\n\n- Do not add new borders to replace removed ones.\n- Do not introduce Tailwind — the project uses CSS modules with --gr-* custom properties.\n- Do not change component behavior or data flow — this is purely visual/structural.\n\n## Success criteria\n\n- Zero 1px solid borders in the app shell (header, status bar, panel separators). Exception: the active tab indicator in DetailPanel may keep its bottom accent.\n- One header bar visible at the top of the app.\n- StatusBar shows file metadata on the right when a file is selected.\n- All panels visually distinct through background shade differences alone.\n- VISION.md contains the no-border design principle.\n- All existing tests pass.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-03-26T18:58:07.671000710Z","created_by":"sqve","updated_at":"2026-03-29T20:31:39.456915100Z","closed_at":"2026-03-29T20:31:39.439919222Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["design","ui"]}
{"id":"greppa-h1w.1","title":"Add no-border design principle to VISION.md","description":"## Goal\n\nAdd a design principle to docs/VISION.md under \"Design principles\" stating that the UI prefers tonal layering (background shade shifts) over structural borders (1px solid lines) to separate regions.\n\n## Why\n\nSets the ground rule before the visual changes in subsequent tasks. Other tasks in this epic reference this principle.\n\n## Behaviors\n\n- VISION.md design principles section contains a new bullet about tonal separation over borders.\n\n## Success criteria\n\n- New bullet point in the Design principles section of VISION.md.\n- Principle explains tonal layering: use --gr-color-bg-subtle, --gr-color-bg-surface, --gr-color-bg-default shade differences rather than borders.\n- Concise — one bullet, 2-3 sentences max.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-26T18:58:33.446416990Z","created_by":"sqve","updated_at":"2026-03-29T20:22:52.982953353Z","closed_at":"2026-03-29T20:22:52.951364092Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["design","ui"],"dependencies":[{"issue_id":"greppa-h1w.1","depends_on_id":"greppa-h1w","type":"parent-child","created_at":"2026-03-26T18:58:33.446416990Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-h1w.2","title":"Remove structural borders and use tonal separation","description":"## Goal\n\nReplace all 1px solid borders in the app shell with background shade differences, implementing the tonal separation principle from VISION.md.\n\n## Why\n\nThe no-border design principle (task .1) is documented. This task applies it across the UI so all subsequent tasks (unified header, status bar) build on a border-free shell.\n\n## Scope\n\nEvery `border` declaration in the app shell CSS modules except:\n- The active tab indicator (`border-bottom: 2px solid` on `Tabs .trigger[data-state='active']`) — this is an accent, not a structural border.\n- `border-radius` — not structural.\n- `border: none` / `border-bottom: none` resets — harmless.\n- The `Separator` in App.module.css — this is a resize handle, uses `background` not `border`, keep as-is.\n\n### Borders to remove\n\n| File | Selector | Declaration | Replacement |\n|------|----------|-------------|-------------|\n| Header.module.css:6 | `.header` | `border-bottom: 1px solid var(--gr-color-border-default)` | Remove. Header already has `background: var(--gr-color-bg-subtle)` which contrasts with body `--gr-color-bg-default`. |\n| Header.module.css:19 | `.themePicker` | `border: 1px solid var(--gr-color-border-default)` | Remove. Use `background: var(--gr-color-bg-surface)` contrast against header's `--gr-color-bg-subtle`. |\n| StatusBar.module.css:5 | `.statusBar` | `border-top: 1px solid var(--gr-color-border-default)` | Remove. StatusBar already has `background: var(--gr-color-bg-subtle)`. |\n| DiffViewer.module.css:18 | `.fileHeader` | `border-bottom: 1px solid var(--gr-color-border-muted)` | Remove. fileHeader has `background: var(--gr-color-bg-subtle)`, diff content uses default bg. |\n| DiffViewer.module.css:36 | `.side[data-side='right']` | `border-left: 1px solid var(--gr-color-border-default)` | Remove. Left/right sides are already visually distinct via diff coloring. |\n| DetailPanel.module.css:10 | `.thread` | `border-bottom: 1px solid var(--gr-color-border-muted)` | Remove border-bottom. Add `margin-bottom: var(--gr-space-sm)` gap + thread items already have `background: var(--gr-color-bg-surface)` on `.comment`. |\n| DetailPanel.module.css:71 | `.infoRow` | `border-bottom: 1px solid var(--gr-color-border-muted)` | Remove border-bottom. Rely on row padding for visual separation. |\n| Tabs.module.css:3 (packages/ui) | `.list` | `border-bottom: 1px solid var(--gr-color-border-default)` | Remove. Tab list has `background: var(--gr-color-bg-subtle)` contrasting with content area. |\n\n## Behaviors\n\n- No `1px solid` borders remain in app shell CSS (Header, StatusBar, DiffViewer, DetailPanel, Tabs) except the active tab accent.\n- Regions remain visually distinct through background shade layering.\n- Thread and infoRow separators use spacing instead of lines.\n\n## Success criteria\n\n- Zero `1px solid` border declarations in the listed CSS modules (excluding tab accent and border-radius).\n- Visual separation achieved through existing `--gr-color-bg-*` variables.\n- All existing tests pass.\n- No new CSS custom properties introduced.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-29T20:15:00Z","created_by":"sqve","updated_at":"2026-03-29T20:23:53.034831910Z","closed_at":"2026-03-29T20:23:53.020383358Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["design","ui"],"dependencies":[{"issue_id":"greppa-h1w.2","depends_on_id":"greppa-h1w","type":"parent-child","created_at":"2026-03-29T20:15:00Z","created_by":"sqve","metadata":"{}","thread_id":""},{"issue_id":"greppa-h1w.2","depends_on_id":"greppa-h1w.1","type":"blocked-by","created_at":"2026-03-29T20:15:00Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-h1w.3","title":"Merge app header and file header into single unified header","description":"## Goal\n\nMerge the app Header (logo + theme picker) and the DiffViewer fileHeader (change badge + file path + old path) into a single top-level header bar.\n\n## Why\n\nEpic requirement 1: \"Single unified header.\" Currently there are two header bars — the app header at the very top and the sticky file header inside DiffViewer. Merging them reduces visual clutter and matches the editor metaphor (one title bar).\n\n## Scope\n\n### Header component changes\n- Accept optional props for the currently selected file: filePath, oldPath, changeType (from DiffFile).\n- Render left: logo. Center/left-of-center: change badge + file path + old path (when a file is selected). Right: theme picker.\n- When no file is selected, center area is empty or shows a subtle placeholder.\n\n### DiffViewer changes\n- Remove the .fileHeader div and its CSS entirely.\n- DiffViewer no longer renders file path or change badge — that moves to Header.\n\n### App.tsx changes\n- Pass selectedDiff metadata (path, oldPath, changeType) to Header.\n- No change to DiffViewer props — it still receives the full diff for rendering hunks.\n\n### CSS changes\n- Header.module.css: add styles for the file path area (use --gr-font-mono for paths).\n- DiffViewer.module.css: remove .fileHeader rule.\n- No borders. Tonal separation only.\n\n## Behaviors\n\n- Single header bar visible at top containing logo, file info, and theme picker.\n- File info updates when a different file is selected.\n- No file info shown when no file is selected.\n- DiffViewer starts directly with hunk content (no sticky file header).\n\n## Success criteria\n\n- One header element in the DOM at the top of the app.\n- No .fileHeader in DiffViewer.\n- Header shows file path and change badge when a file is selected.\n- All existing tests pass (update Header and DiffViewer tests as needed).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-29T20:25:05.627895338Z","created_by":"sqve","updated_at":"2026-03-29T20:29:10.547752528Z","closed_at":"2026-03-29T20:29:10.533066261Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["design","ui"],"dependencies":[{"issue_id":"greppa-h1w.3","depends_on_id":"greppa-h1w","type":"parent-child","created_at":"2026-03-29T20:25:05.627895338Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-h1w.4","title":"Add file metadata to editor-style status bar","description":"## Goal\n\nExtend StatusBar to show file-local metadata on the right side when a file is selected: language and encoding.\n\n## Why\n\nEpic requirement 3: \"Editor-style status bar — left side: review progress, branch. Right side: file-local metadata (language, encoding, line/col position).\" This makes the status bar match the editor metaphor (VSCode shows language, encoding, line info in its status bar).\n\n## Scope\n\n### FileInfo type changes (fixtures/types.ts)\n- Add language: string (e.g. \"TypeScript\", \"CSS\") to FileInfo interface.\n- Add encoding: string (e.g. \"UTF-8\") to FileInfo interface.\n- Update fixture data (fileInfo.json) with language and encoding values.\n\n### StatusBar component changes\n- Add optional props: language (string | null), encoding (string | null).\n- Left side: review progress (existing).\n- Right side: language + encoding, shown only when values are non-null.\n- Use justify-content: space-between to push left/right apart.\n\n### StatusBar.module.css changes\n- Add right-side segment styles matching the existing progress style (same font, size, color).\n\n### App.tsx changes\n- Pass selectedFileInfo?.language and selectedFileInfo?.encoding to StatusBar.\n\n### Test updates\n- Update StatusBar tests to cover file metadata rendering.\n- Update fixture data references if needed.\n\n## Behaviors\n\n- When a file is selected, status bar right side shows \"TypeScript  UTF-8\" (language and encoding separated by spacing).\n- When no file is selected, right side is empty.\n- Left side continues showing review progress unchanged.\n\n## Success criteria\n\n- StatusBar renders file language and encoding on the right when a file is selected.\n- StatusBar right side is empty when no file is selected.\n- FileInfo type includes language and encoding fields.\n- All existing tests pass.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-03-29T20:25:28.918750904Z","created_by":"sqve","updated_at":"2026-03-29T20:31:25.078863551Z","closed_at":"2026-03-29T20:31:25.062793564Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["design","ui"],"dependencies":[{"issue_id":"greppa-h1w.4","depends_on_id":"greppa-h1w","type":"parent-child","created_at":"2026-03-29T20:25:28.918750904Z","created_by":"sqve","metadata":"{}","thread_id":""},{"issue_id":"greppa-h1w.4","depends_on_id":"greppa-h1w.3","type":"blocks","created_at":"2026-03-29T20:25:28.918750904Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-isi","title":"Diff viewer scroll performance","description":"## Context\n\nScrolling quickly through large multi-file diffs causes visible lag. Three layers of problems compound:\n\n1. **DOM accumulation**: `LazyDiffViewer` uses a one-way IntersectionObserver latch — once a file becomes visible it stays mounted forever. After scrolling through 20 files, all 20 are fully rendered with every DOM node alive (~60k–90k nodes for a 50-file diff). The inner `DiffViewer` row virtualizer (`@tanstack/react-virtual`) is dead in stacked mode because the outer container scrolls, not the inner `.viewer` divs.\n\n2. **Cascading re-renders**: `activeFilePath` state lives in `App` — every scroll intersection triggers `setActiveFilePath` → full App re-render. `selectedDiffs` is an unstable array reference. `clearCommitsOnFileSelect` factory produces 6 new function refs at JSX callsites. No `React.memo` on any heavy component.\n\n3. **Per-element style cost**: `TokenSpans` applies inline `style={{ color }}` on every syntax token span (~2,400 live inline-style elements in the viewport), forcing unique computed style objects per element.\n\n## Requirements\n\n- [ ] File-level virtualization: only 3–5 files mounted at a time via `useVirtualizer`, with overscan of 1–2 files\n- [ ] Sticky file headers work within the virtualized list\n- [ ] `scrollToFile` imperative handle works with virtualized scrolling\n- [ ] Active file detection works with virtualized items\n- [ ] `DiffViewer` supports an `embedded` mode (no own scroll container, overflow visible) for stacked use\n- [ ] `LazyDiffViewer` component deleted — replaced by virtualizer\n- [ ] `activeFilePath` state isolated from App — scroll events don't re-render the full tree\n- [ ] `selectedDiffs` memoized — stable reference when inputs unchanged\n- [ ] Heavy components (`StackedDiffViewer`, `FileMinimap`, `FileTreePanel`) wrapped in `React.memo`\n- [ ] `clearCommitsOnFileSelect` callbacks stabilized — no new refs at JSX callsites\n- [ ] Token colors applied via CSS classes instead of inline styles\n- [ ] `content-visibility: auto` with `contain-intrinsic-size` on file wrapper divs\n- [ ] Server: parallel `git show` calls via `Effect.all`\n- [ ] Server: diff content cache (TTL 30s, keyed by `oldRef:newRef:path`)\n\n## Approach\n\nFive phases, each building on the previous:\n\n1. **State management fixes** (E, F, G, H) — isolated, low-risk, reduce noise for measuring virtualization impact\n2. **File-level virtualization** (A) — biggest change, replaces LazyDiffViewer with useVirtualizer, adds embedded mode to DiffViewer\n3. **CSS token class caching** (D) — replace inline style={{ color }} with injected CSS classes\n4. **content-visibility** (B) — CSS-only optimization on file wrapper divs within the virtualized list\n5. **Server parallelization + caching** (J, K) — parallelize git show calls, add diff content cache\n\n### Key design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Virtualization level | File-level in StackedDiffViewer | Avoids flattening all rows into one list (breaks sticky headers, per-file syntax highlighting). Inner row virtualizer stays for standalone mode |\n| Virtualizer library | `@tanstack/react-virtual` | Already installed at 3.13.23. `estimateHeight()` already exists |\n| Sticky headers | Kept via CSS `position: sticky` within each virtualized file section | User confirmed sticky headers must remain |\n| DiffViewer scroll mode | `embedded` prop: when true, no `overflow-y: auto`, height determined by content | Eliminates the nested-scroll-container conflict in stacked mode |\n| Token styling | CSS class per unique color, injected stylesheet | Eliminates ~2,400 inline style objects from viewport. Classes are stable across renders |\n| activeFilePath isolation | Separate wrapper component or useRef+callback | Only FileMinimap consumes this value — no reason to re-render App |\n| Server diff format | Keep old+new content (not unified diff) | Client needs full content for char-level highlighting, side-by-side alignment, syntax tokenization |\n\n## Anti-patterns\n\n- Do NOT flatten all files/hunks/rows into a single virtualizer item list — breaks sticky headers and per-file syntax highlighting\n- Do NOT remove the inner DiffViewer row virtualizer — still needed for standalone single-file mode\n- Do NOT switch to server-side unified diffs — client needs full file contents for char-level highlighting\n- Do NOT use `useRef` for activeFilePath without a mechanism to update FileMinimap — it must still react to changes\n- Do NOT add React.memo without first stabilizing the props — memo with unstable props is worse than no memo (false sense of safety)\n\n## Success criteria\n\n1. Scrolling through a 50-file diff maintains < 10k live DOM nodes at any point\n2. Fast scrolling shows no visible jank or blank gaps (overscan covers scroll speed)\n3. `scrollToFile` navigates to any file in the virtualized list\n4. Sticky file headers remain visible while scrolling within a file's diff\n5. No cascading App re-renders from scroll-driven state changes (verify with React DevTools profiler)\n6. All existing tests pass without modification (or with minimal adaptation for the embedded prop)","notes":"## Revised approach (2026-04-07)\n\nFile-level virtualization (task .2) revealed that `position: absolute` + `transform: translateY()` breaks `position: sticky` headers and causes sub-pixel rendering artifacts. IntersectionObserver on dynamically mounted headers is unreliable.\n\nNew plan: replace two-level architecture with a **single flat virtualizer** (task .6). See REFACTOR.md for full design. Task .4 (content-visibility) closed as superseded — no file wrapper divs in the flat model.\n\nAnti-pattern revision: the original \"do NOT flatten\" anti-pattern was wrong — flattening works when using `rangeExtractor` for sticky headers instead of CSS `position: sticky`.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-04-04T20:59:32.130186073Z","created_by":"sqve","updated_at":"2026-04-07T09:06:28.483444920Z","closed_at":"2026-04-07T09:06:28.483191996Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"]}
{"id":"greppa-isi.1","title":"Stabilize props and memoize heavy components","description":"## Goal\n\nStop cascading re-renders from scroll events and file selection before adding virtualization. Isolate `activeFilePath` state, memoize derived data, stabilize callback references, and wrap heavy components in `React.memo`.\n\n## Context\n\nFour state management problems in App.tsx cause the entire component tree to re-render on every scroll intersection event:\n\n1. `activeFilePath` state (App.tsx:167) lives in App — every scroll intersection triggers setActiveFilePath → full App re-render including FileTreePanel, DetailPanel, etc. Only FileMinimap needs this value.\n2. `selectedDiffs` (App.tsx:261) is an unmemoized ternary — new array reference every render propagates to StackedDiffViewer and FileMinimap.\n3. `clearCommitsOnFileSelect` (App.tsx:228-235) is a factory that produces 6 new function refs at JSX callsites (lines 294-299), forcing FileTreePanel to re-render.\n4. No `React.memo` on StackedDiffViewer, FileMinimap, or FileTreePanel — every App re-render cascades through all heavy components.\n\n## Behaviors\n\n- Extract `activeFilePath` state into a wrapper component around FileMinimap + StackedDiffViewer (or use useRef+callback that only re-renders FileMinimap). Scroll events must not trigger App re-renders.\n- Wrap `selectedDiffs` in `useMemo(() => commitSelection.isActive ? commitDiffs : fileDiffs, [commitSelection.isActive, commitDiffs, fileDiffs])`.\n- Pre-bind all 6 `clearCommitsOnFileSelect` handlers via `useMemo` so no factory calls happen at JSX callsites. The resulting callbacks must be referentially stable across renders when `commitSelection` hasn't changed.\n- Wrap `StackedDiffViewer`, `FileMinimap`, and `FileTreePanel` in `React.memo`. Ensure all props passed to them are stable (callbacks via useCallback, objects via useMemo).\n\n## Success criteria\n\n1. Scrolling through files does NOT re-render FileTreePanel or DetailPanel (verifiable via React DevTools profiler)\n2. Changing file selection does NOT re-render unaffected siblings\n3. All existing tests pass\n4. No behavioral changes — the app works identically from the user's perspective\n\n## Files\n\n- `packages/web/src/App.tsx`\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx`\n- `packages/web/src/components/StackedDiffViewer/FileMinimap.tsx`\n- `packages/web/src/components/FileTree/FileTreePanel.tsx`","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T21:00:04.225405036Z","created_by":"sqve","updated_at":"2026-04-06T19:09:51.484366704Z","closed_at":"2026-04-06T19:09:51.484110512Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-isi.1","depends_on_id":"greppa-isi","type":"parent-child","created_at":"2026-04-04T21:00:04.225405036Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-isi.2","title":"File-level virtualization in StackedDiffViewer","description":"## Goal\n\nReplace the one-way `LazyDiffViewer` IntersectionObserver latch with file-level `useVirtualizer` so only 3–5 files are mounted at a time. Add an `embedded` prop to `DiffViewer` so it renders without its own scroll container in stacked mode.\n\n## Context\n\n`LazyDiffViewer` (StackedDiffViewer.tsx:17-46) mounts files when they scroll into view but never unmounts them. After scrolling through 20 files, all 20 are fully rendered. The inner `DiffViewer` row virtualizer is also dead in stacked mode because the outer container scrolls, not the inner `.viewer` divs — so all rows render unvirtualized.\n\nThe fix: virtualize at the file level in `StackedDiffViewer` using the already-installed `@tanstack/react-virtual`. `estimateHeight()` at line 12-15 already provides size estimates. Each file section includes its sticky header + DiffViewer. The virtualizer mounts/unmounts entire file sections as they enter/leave the viewport.\n\n`DiffViewer` needs a new `embedded` prop. When `embedded=true`: no `overflow-y: auto` on `.viewer`, no fixed height constraint, the inner row virtualizer is disabled (rows render in full since the outer virtualizer controls visibility). When `embedded=false` (default): current behavior preserved for standalone use.\n\n## Behaviors\n\n- `StackedDiffViewer` uses `useVirtualizer` with `count: diffs.length`, `estimateSize` from `estimateHeight`, `measureElement` enabled, and `overscan: 2`.\n- Delete `LazyDiffViewer` component entirely.\n- Each virtual item renders: sticky file header + `<DiffViewer diff={diff} embedded />`.\n- `scrollToFile(path)` uses `virtualizer.scrollToIndex(index)` where index is found from `diffs.findIndex`.\n- Active file detection: use the virtualizer's visible range or an IntersectionObserver on the rendered file headers.\n- `DiffViewer` with `embedded` prop: removes `overflow-y: auto` and `height: 100%` from `.viewer`, disables the inner `useVirtualizer` (renders all items directly), removes padding-top/padding-bottom virtual spacers.\n- Sticky headers (`position: sticky; top: 0`) work because each file section is a direct child of the virtualizer's offset container.\n\n## Success criteria\n\n1. Scrolling through a 50-file diff maintains < 10k live DOM nodes at any point\n2. Fast scrolling shows no visible blank gaps (overscan of 2 covers normal scroll speed)\n3. `scrollToFile` navigates correctly to any file\n4. Sticky file headers remain visible while scrolling within a file's diff\n5. All existing StackedDiffViewer and DiffViewer tests pass (or are adapted for the embedded prop)\n6. Standalone (non-embedded) DiffViewer behavior unchanged\n\n## Files\n\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx` — replace LazyDiffViewer with useVirtualizer\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.module.css` — adjust container styles if needed\n- `packages/web/src/components/DiffViewer/DiffViewer.tsx` — add embedded prop\n- `packages/web/src/components/DiffViewer/DiffViewer.module.css` — conditional overflow/height styles\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.test.tsx` — adapt tests\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.test.tsx` — adapt tests","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T21:00:26.978339677Z","created_by":"sqve","updated_at":"2026-04-06T19:22:38.445289357Z","closed_at":"2026-04-06T19:22:38.445019507Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-isi.2","depends_on_id":"greppa-isi","type":"parent-child","created_at":"2026-04-04T21:00:26.978339677Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-isi.3","title":"CSS class caching for syntax token colors","description":"## Goal\n\nReplace inline `style={{ color }}` on every syntax token span with CSS classes generated from the theme. Eliminates ~2,400 unique inline style objects from the visible viewport.\n\n## Context\n\n`TokenSpans.tsx:46-51` renders one `<span>` per syntax token with `style={{ color: token.color }}`. With ~30 tokens/line × ~40 visible rows × 2 sides = ~2,400 inline-style spans. Each creates a unique computed style object, forcing per-element style recalc on every paint.\n\nThe fix: build a stylesheet mapping unique color hex values to CSS classes (e.g., `.tk-a1b2c3 { color: #a1b2c3 }`), inject it once via a `<style>` element or `CSSStyleSheet`, and apply classNames instead of inline styles.\n\n## Behaviors\n\n- Create a utility that maps color hex strings to stable CSS class names (e.g., `tk-` + hex without `#`).\n- When the highlight worker returns token maps, collect all unique colors and inject/update a `<style>` element with the class rules.\n- `TokenSpans` applies `className={tokenClassName(token.color)}` instead of `style={{ color: token.color }}`.\n- The stylesheet updates when the theme changes (new color set).\n- For char-highlight spans that combine a highlight class with a color, apply both the token color class and the highlight class.\n\n## Success criteria\n\n1. No inline `style={{ color }}` on token spans in the rendered DOM\n2. A single `<style>` element (or `CSSStyleSheet`) contains all token color rules\n3. Theme switching updates the stylesheet correctly\n4. Syntax highlighting appearance is identical to before\n5. All existing DiffViewer and TokenSpans tests pass\n\n## Files\n\n- `packages/web/src/components/DiffViewer/TokenSpans.tsx` — replace inline styles with classNames\n- `packages/web/src/components/DiffViewer/tokenStylesheet.ts` — new utility for class name generation and stylesheet injection\n- `packages/web/src/components/DiffViewer/DiffViewer.tsx` — wire stylesheet lifecycle if needed","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-04T21:00:44.858060441Z","created_by":"sqve","updated_at":"2026-04-07T09:02:31.132250364Z","closed_at":"2026-04-07T09:02:31.131740636Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-isi.3","depends_on_id":"greppa-isi","type":"parent-child","created_at":"2026-04-04T21:00:44.858060441Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-isi.4","title":"Add content-visibility auto to file wrapper divs","description":"## Goal\n\nAdd `content-visibility: auto` with `contain-intrinsic-size` on file wrapper divs in the virtualized StackedDiffViewer. Supplementary CSS optimization that lets the browser skip layout and paint for off-screen files in the overscan range.\n\n## Context\n\nAfter file-level virtualization (task greppa-isi.2), the overscan range renders 1-2 extra files above and below the viewport. These files are mounted in the DOM but not visible. `content-visibility: auto` tells the browser to skip their layout and paint entirely until they scroll into view.\n\n`estimateHeight()` already exists and provides the value for `contain-intrinsic-size`.\n\n## Behaviors\n\n- Each file section div in the virtualized list gets `content-visibility: auto` and `contain-intrinsic-size: auto <height>px` where height comes from `estimateHeight(diff)`.\n- The file header is excluded from `content-visibility` (it needs to remain visible for sticky behavior).\n- Only the DiffViewer content area within each file section gets the containment.\n\n## Success criteria\n\n1. Off-screen file diffs in the overscan range have `content-visibility: auto` in the computed styles\n2. No visual difference when scrolling — files appear correctly as they enter the viewport\n3. Sticky file headers still work correctly\n4. All existing tests pass\n\n## Files\n\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx` — apply content-visibility style to DiffViewer wrapper\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.module.css` — add content-visibility class","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-04T21:01:01.070543879Z","created_by":"sqve","updated_at":"2026-04-07T06:15:32.927824775Z","closed_at":"2026-04-07T06:15:32.927588877Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-isi.4","depends_on_id":"greppa-isi","type":"parent-child","created_at":"2026-04-04T21:01:01.070543879Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-isi.5","title":"Server: parallelize git show and add diff content cache","description":"## Goal\n\nParallelize the two sequential `git show` calls in the diff endpoint and add a diff content cache to prevent redundant subprocess spawns.\n\n## Context\n\n`Http.ts:99-102`: the diff handler fetches `oldContent` and `newContent` sequentially via two `yield*` calls. These are independent `git show` commands that can run in parallel.\n\nThe file list is cached (TTL 30s, max 50 entries) but individual file diff content is fetched fresh on every request. When navigating between commits or scrolling back to previously viewed files, the same `git show` commands run again unnecessarily.\n\n## Behaviors\n\n- Replace sequential `yield*` for `oldContent`/`newContent` with `Effect.all([git.getFileContent(oldRef, path), git.getFileContent(newRef, path)])` for parallel execution.\n- Add a diff content cache alongside the existing file list cache:\n  - Key: `${oldRef}:${newRef}:${filePath}`\n  - TTL: 30 seconds (same as file list cache)\n  - Max entries: 100 (higher than file list since individual files are smaller metadata)\n  - Same eviction strategy as `fileListCache`\n- Apply the same parallel + cache pattern to the worktree diff handler (Http.ts:164-199) where applicable (worktree content may change, so shorter TTL of 3s matching `WORKTREE_CACHE_TTL_MS`).\n\n## Success criteria\n\n1. Diff endpoint response time reduced (two git show calls run in parallel instead of sequentially)\n2. Repeated requests for the same file diff within 30s return cached content without spawning git processes\n3. Cache eviction works correctly — stale entries are cleaned up\n4. All existing server behavior preserved — responses are identical\n5. Worktree diff handler also parallelized\n\n## Files\n\n- `packages/server/src/Http.ts` — parallelize git show, add diff content cache","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-04T21:01:17.574530708Z","created_by":"sqve","updated_at":"2026-04-07T09:05:23.373840019Z","closed_at":"2026-04-07T09:05:23.372828629Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-isi.5","depends_on_id":"greppa-isi","type":"parent-child","created_at":"2026-04-04T21:01:17.574530708Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-isi.6","title":"Single flat virtualizer for StackedDiffViewer","description":"## Goal\n\nReplace the two-level virtualizer architecture (file-level virtualizer + embedded DiffViewer) with a single flat virtualizer that treats all items across all files as one virtual list. Fixes sticky headers, active file tracking, and sub-pixel rendering artifacts.\n\n## Context\n\nTask .2 implemented file-level virtualization using `position: absolute` + `transform: translateY()` for each file block. This breaks:\n\n- **Sticky file headers** — `position: sticky` doesn't work inside transformed containers\n- **FileMinimap active tracking** — IntersectionObserver on dynamically mounted headers is unreliable\n- **Sub-pixel rendering** — transform-based positioning causes grid-line artifacts between rows\n\nThe fix is described in REFACTOR.md: flatten all files into a single `FlatVirtualItem[]` array and virtualize that with one `useVirtualizer`. Use `rangeExtractor` to pin the current file's header (sticky behavior without CSS sticky). Derive active file from the virtualizer's visible items (no IntersectionObserver).\n\n## Behaviors\n\n**Build phase** — extend `buildVirtualItems` (or create `buildFlatItems`) to produce a flat array across all files:\n- For each file: one `file-header` item, then its hunk-headers and diff-rows, then a `file-separator`\n- Type: `{ kind: 'file-header'; diff: DiffFile } | { kind: 'hunk-header'; header: string } | { kind: 'diff-row'; row: DiffRow } | { kind: 'file-separator' }`\n\n**Single virtualizer** in `StackedDiffViewer`:\n- `useVirtualizer({ count: flatItems.length, estimateSize })` where estimateSize switches on item kind: `ROW_HEIGHT_ESTIMATE (36)` for diff-rows and hunk-headers, `40` for file-headers, `24` for separators\n- One scroll container, standard virtualizer rendering (no `position: absolute` + `transform`)\n\n**Sticky file headers** via `rangeExtractor`:\n- Custom `rangeExtractor` that always includes the nearest file-header index above the current scroll position\n- The pinned header renders with `position: sticky; top: 0; z-index: 1`\n\n**Active file tracking** — derived from virtualizer state:\n- First visible `file-header` item in `getVirtualItems()` determines active file\n- Replaces IntersectionObserver entirely\n- `onActiveFileChange` callback fires when active file changes\n\n**scrollToFile** — find target file-header index in flat array, call `virtualizer.scrollToIndex(index)`\n\n**Syntax highlighting** — `useSyntaxHighlighting` called once per visible file (deduplicate by file path from visible items)\n\n**Remove**:\n- `DiffViewer` `embedded` prop and embedded rendering branch (`DiffViewer.tsx:108-132`)\n- `DiffPane` component (`DiffPane.tsx`, `DiffPane.module.css`) — App.tsx imports StackedDiffViewer directly\n- `FileMinimap` component (`FileMinimap.tsx`, `FileMinimap.module.css`, `FileMinimap.test.tsx`) — sticky headers + file tree sidebar replace its function\n- `DiffRowRenderer` optional props — `measureRef` and `dataIndex` become required (no embedded mode)\n- `countFileItems` / `estimateFileHeight` / `fileMetrics` in StackedDiffViewer — replaced by flat item array\n- IntersectionObserver logic in StackedDiffViewer (headerRefs, observerRef, observer effect)\n\n**Update App.tsx**:\n- Import `StackedDiffViewer` directly instead of `DiffPane`\n- `activeFilePath` state stays in a lightweight wrapper or StackedDiffViewer itself (no App re-renders)\n- `scrollToFile` handle still exposed via `StackedDiffViewerHandle`\n\n## Success criteria\n\n1. Scrolling through a 50-file diff maintains < 10k live DOM nodes\n2. File headers stick to the top of the viewport while scrolling within that file's diff\n3. Active file in FileTreePanel updates correctly during scroll (derived from virtualizer, no IntersectionObserver)\n4. `scrollToFile` navigates to any file in the flat list\n5. Fast scrolling shows no blank gaps (appropriate overscan)\n6. No sub-pixel rendering artifacts between rows\n7. Standalone DiffViewer (single-file, non-stacked) still works with its own row virtualizer\n8. All existing tests pass or are adapted\n\n## Files\n\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx` — rewrite with flat virtualizer\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.module.css` — update styles\n- `packages/web/src/components/StackedDiffViewer/StackedDiffViewer.test.tsx` — adapt tests\n- `packages/web/src/components/DiffViewer/DiffViewer.tsx` — remove embedded prop/branch\n- `packages/web/src/components/DiffViewer/DiffViewer.module.css` — remove embedded class\n- `packages/web/src/components/DiffViewer/DiffRowRenderer.tsx` — make measureRef/dataIndex required\n- `packages/web/src/components/DiffViewer/buildVirtualItems.ts` — extend with flat item types or create buildFlatItems\n- `packages/web/src/App.tsx` — replace DiffPane import with StackedDiffViewer\n- `packages/web/src/components/StackedDiffViewer/DiffPane.tsx` — DELETE\n- `packages/web/src/components/StackedDiffViewer/DiffPane.module.css` — DELETE\n- `packages/web/src/components/StackedDiffViewer/FileMinimap.tsx` — DELETE\n- `packages/web/src/components/StackedDiffViewer/FileMinimap.module.css` — DELETE\n- `packages/web/src/components/StackedDiffViewer/FileMinimap.test.tsx` — DELETE","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-07T06:16:39.206560124Z","created_by":"sqve","updated_at":"2026-04-07T06:35:25.585654578Z","closed_at":"2026-04-07T06:35:25.585498110Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["performance"],"dependencies":[{"issue_id":"greppa-isi.6","depends_on_id":"greppa-isi","type":"parent-child","created_at":"2026-04-07T06:16:39.206560124Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-jv4","title":"Success criteria","description":"- [ ] @greppa/core exports DiffResponse Effect Schema with derived TS type","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-31T10:55:52.343452084Z","updated_at":"2026-03-31T10:56:02.040065934Z","closed_at":"2026-03-31T10:56:02.039912502Z","close_reason":"wontdo","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"]}
{"id":"greppa-kgz","title":"Editor-style UI shell","description":"Refactor the greppa web UI to feel like a dense, polished code editor. Remove the top header, add an activity rail, redesign the status bar, and tighten all spacing/typography to match design-v1.html mockup.\n\n## Requirements\n1. Remove the Header component. Replace with a 36px ActivityRail on the left edge (outside react-resizable-panels).\n2. ActivityRail has two icons: Files (toggles file-tree panel visibility) and Settings (bottom, opens theme popover via Radix Popover).\n3. Active rail icon shows a 2px left accent bar in interactive color.\n4. Redesign StatusBar to be context-aware with segmented layout: left segments (review progress, comment count, commit SHA, working-tree label) vary by mode; right segments show mocked keyboard hint badges.\n5. StatusBar modes: file-review, commit-review, working-tree, composer-open, review-complete — each renders different left/right segments per mockup.\n6. Keyboard hint badges are styled kbd elements (mono font, surface bg, subtle box-shadow). Static/mocked for now.\n7. Visual polish pass across all components to match mockup density: sidebar section headers (32px height, uppercase xs text, muted color), file rows (24px height, mono sm text), diff file header (36px), status bar (24px).\n8. Font consistency: mono font for all data (file names, line counts, commit hashes, stats), UI font for labels and section names.\n9. Color hierarchy: muted for secondary info, secondary for file names, primary for active/selected, interactive for accents, success/warning/danger for status badges only.\n10. Panel stability: diff-viewer is the only panel that flexes on window resize. Side panels preserve pixel size.\n\n## Success criteria\n- Header component deleted, ActivityRail renders in its place.\n- Theme picker works from settings popover in rail.\n- StatusBar shows correct segments for each mode.\n- All spacing, font sizes, heights match design-v1.html mockup values.\n- File-tree panel collapses/expands via rail Files icon.\n- No visual regressions in diff viewer or detail panel.\n\n## Anti-patterns\n- No placeholder/disabled icons in the rail — only render what works.\n- No top-level header or logo — the rail IS the chrome.\n- No color on file/directory names in the tree — only change badges carry color.\n- Don't add keyboard shortcut handlers — hints are visual-only for now.\n- Don't change any behavior/logic — this is purely visual/structural.\n\n## Approach\nFive sequential tasks:\n1. Remove Header, restructure App shell layout to rail + panels + status bar.\n2. Build ActivityRail component with Files icon (toggles file-tree panel) and Settings icon (opens theme popover).\n3. Redesign StatusBar with segmented, mode-aware layout and mocked keyboard hints.\n4. Visual polish pass — tighten spacing, font sizes, heights, colors across FileTreePanel, StackedDiffViewer file headers, DetailPanel, and section headers to match mockup density.\n5. Panel resize stability — ensure side panels preserve pixel size and only diff-viewer flexes.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-04-07T14:40:53.088123623Z","created_by":"sqve","updated_at":"2026-04-07T16:28:47.762558641Z","closed_at":"2026-04-07T16:28:47.762403036Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"]}
{"id":"greppa-kgz.1","title":"Remove Header and restructure App shell layout","description":"## Goal\nDelete the Header component and restructure App.tsx so the layout is: ActivityRail (left) | resizable panels | StatusBar (bottom). This sets up the shell for subsequent tasks.\n\n## Behaviors\n- Delete `packages/web/src/components/Header/Header.tsx` and `Header.module.css`.\n- Remove the `<Header />` import and render from `App.tsx`.\n- Change the App layout from vertical (header → panels → statusbar) to: a horizontal flex row containing a future ActivityRail slot (empty 36px div for now) and the existing panel Group, with the StatusBar below.\n- The ActivityRail slot sits outside react-resizable-panels, to the left of the Group.\n- App.module.css: the `.app` remains `flex-direction: column` with `.body` as a horizontal flex row. Add a `.rail` placeholder class (36px wide, bg-inset, flex-shrink: 0).\n- The rail placeholder div renders in the `.body` row, before the panel Group.\n\n## Success criteria\n- Header component files deleted.\n- App renders without a top header bar.\n- A 36px-wide placeholder div appears on the left edge (bg-inset color).\n- Panel Group and StatusBar remain functional.\n- No changes to any other component.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-07T14:41:09.143035279Z","created_by":"sqve","updated_at":"2026-04-07T14:44:54.147202474Z","closed_at":"2026-04-07T14:44:54.147048833Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-kgz.1","depends_on_id":"greppa-kgz","type":"parent-child","created_at":"2026-04-07T14:41:09.143035279Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-kgz.2","title":"Build ActivityRail component with Files and Settings icons","description":"## Goal\nReplace the empty rail placeholder div with an ActivityRail component containing two functional icons: Files (toggles file-tree panel visibility) and Settings (opens a theme picker popover via Radix Popover).\n\n## Behaviors\n- Create packages/web/src/components/ActivityRail/ActivityRail.tsx and ActivityRail.module.css.\n- Install @radix-ui/react-popover as a dependency.\n- ActivityRail renders a 36px-wide column with rail-top (Files icon) and rail-bottom (Settings icon) groups, matching the mockup layout.\n- Files icon: uses IconFile from @tabler/icons-react (size 15, stroke 1.5). Clicking toggles the file-tree panel collapsed/expanded via react-resizable-panels imperative API. Active when file-tree panel is expanded — shows 2px left accent bar in interactive color.\n- Settings icon: uses IconSettings from @tabler/icons-react (size 15, stroke 1.5). Wrapped in a Radix Popover trigger. Popover content shows the existing theme options from usePreferences as clickable items.\n- Icon buttons: 36x32px, centered, relative position. Muted color default, primary color when active. Active state includes a ::before pseudo-element (2px wide, interactive color, left-aligned accent bar).\n- Update App.tsx: replace the placeholder div with ActivityRail, pass a ref or callback to control the file-tree panel collapsed state.\n- For panel toggle: use a ref to the file-tree Panel and call collapse()/expand() from react-resizable-panels API.\n\n## Success criteria\n- Rail shows Files icon (top) and Settings icon (bottom).\n- Clicking Files icon toggles file-tree panel visibility.\n- Active Files icon has 2px left accent bar.\n- Clicking Settings icon opens a popover with theme options.\n- Selecting a theme applies it immediately.\n- Rail matches mockup dimensions and colors exactly.\n- Existing tests still pass.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-07T15:19:14.696935332Z","created_by":"sqve","updated_at":"2026-04-07T15:24:47.765053038Z","closed_at":"2026-04-07T15:24:47.764733435Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-kgz.2","depends_on_id":"greppa-kgz","type":"parent-child","created_at":"2026-04-07T15:19:14.696935332Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-kgz.3","title":"Redesign StatusBar with segmented mode-aware layout","description":"## Goal\nRedesign StatusBar with a segmented, mode-aware layout and mocked keyboard hint badges matching the design-v1.html mockup.\n\n## Behaviors\n- Rewrite StatusBar.tsx and StatusBar.module.css to use a segmented layout: left segments, a flexible spacer, and right segments.\n- StatusBar accepts a `mode` prop with values: file-review, commit-review, working-tree, review-complete, composer-open.\n- Additional props vary by mode: reviewedCount, totalCount, commentCount, commitSha, modifiedCount, selectedFileCount.\n- Left segments per mode:\n  - file-review: reviewed count (success color) | comment count (warning color, only if > 0)\n  - commit-review: commit SHA (interactive color) | reviewed count\n  - working-tree: 'working tree' label (warning color) | 'N modified'\n  - review-complete: reviewed count (success color, all reviewed)\n  - composer-open: reviewed count (success color) | comment count (warning color)\n- Right segments (kbd hints) per mode:\n  - file-review: Space reviewed, Tab next file\n  - commit-review: Space reviewed, Tab next file\n  - working-tree: Tab next file\n  - review-complete: Enter submit\n  - composer-open: Cmd+Enter submit, Esc cancel\n- Kbd elements styled as: mono font, xs size, surface bg, sm radius, 1px box-shadow.\n- StatusBar height: 24px, bg-subtle, mono font, xs text.\n- Remove language/encoding metadata segments (not in mockup).\n- Update App.tsx to pass the new mode and relevant props to StatusBar.\n- Update StatusBar tests and App tests accordingly.\n\n## Success criteria\n- StatusBar renders correct segments for each mode.\n- Kbd badges match mockup styling (surface bg, box-shadow, mono font).\n- 24px height, mono font, xs text size.\n- All tests pass.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-07T15:26:36.382268337Z","created_by":"sqve","updated_at":"2026-04-07T15:28:59.914151802Z","closed_at":"2026-04-07T15:28:59.913999423Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-kgz.3","depends_on_id":"greppa-kgz","type":"parent-child","created_at":"2026-04-07T15:26:36.382268337Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-kgz.4","title":"Visual polish pass matching mockup density","description":"## Goal\nTighten spacing, font sizes, heights, and colors across FileTreePanel, StackedDiffViewer file headers, Tree component, and section headers to match design-v1.html mockup density.\n\n## Behaviors\n- FileTreePanel section headers: height 32px, padding 0 var(--gr-space-lg), gap var(--gr-space-md). Title color to muted, letter-spacing 0.06em. Count font to mono.\n- Tree items: min-height 24px, font-family mono, font-size sm, label color secondary.\n- Remove change-type coloring from file/directory names in FileTree (only badges carry color).\n- StackedDiffViewer file header: height 36px, padding 0 var(--gr-space-xl), gap var(--gr-space-lg). File path font to mono, font-size sm.\n- Font consistency: mono for all data (file names, line counts, commit hashes, stats), UI font for labels/section names.\n- Color hierarchy: muted for secondary info, secondary for file names, primary for active/selected, interactive for accents.\n\n## Success criteria\n- Section headers 32px, uppercase xs text, muted color, mono count.\n- File rows 24px, mono sm text, secondary color.\n- Diff file header 36px with mono file path.\n- No color on file/directory names — only change badges carry color.\n- All tests pass.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-07T15:34:12.802530294Z","created_by":"sqve","updated_at":"2026-04-07T15:36:46.485449914Z","closed_at":"2026-04-07T15:36:46.485291374Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-kgz.4","depends_on_id":"greppa-kgz","type":"parent-child","created_at":"2026-04-07T15:34:12.802530294Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-kgz.5","title":"Panel resize stability — side panels preserve pixel size, diff-viewer flexes","description":"## Goal\nEnsure side panels preserve their pixel size on window resize and only the diff-viewer panel flexes.\n\n## Behaviors\n- In App.tsx, set the file-tree panel and any detail panel to use fixed pixel sizes (not percentages) via react-resizable-panels props.\n- The diff-viewer (center) panel should be the only panel that grows/shrinks on window resize.\n- Use `defaultSize` in pixels (or minSize/maxSize constraints) so side panels don't shift when the window is resized.\n- Verify that collapsing/expanding the file-tree panel via the ActivityRail icon still works correctly after these changes.\n\n## Success criteria\n- Resizing the browser window only changes the diff-viewer panel width.\n- Side panels maintain their pixel width on window resize.\n- File-tree panel collapse/expand via rail icon still works.\n- All tests pass.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-07T16:21:54.079086249Z","created_by":"sqve","updated_at":"2026-04-07T16:22:53.042530788Z","closed_at":"2026-04-07T16:22:53.042374400Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-kgz.5","depends_on_id":"greppa-kgz","type":"parent-child","created_at":"2026-04-07T16:21:54.079086249Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-kye","title":"Epic: DetailPanel ActivityRail and StatusBar refactor into subcomponents and helpers","description":"## Requirements (IMMUTABLE)\n- All planned extractions complete: LeftSegments, RightSegments, ThemePicker, calcPercent.\n- Each extraction preserves existing component behavior — no change to rendered UI, theme switching, status display, or segment ordering.\n- Extracted subcomponents stay co-located with their parent directory.\n- calcPercent helper is module-scoped (not exported beyond its source file unless needed).\n- Host component public props stay unchanged.\n- Each extraction lands as its own atomic commit.\n\n## Success criteria\n- [ ] LeftSegments subcomponent extracted; StatusBar renders it as JSX.\n- [ ] RightSegments subcomponent extracted; StatusBar renders it as JSX.\n- [ ] ThemePicker subcomponent extracted; ActivityRail's popover renders it.\n- [ ] calcPercent helper extracted; renderProgressBar and renderReviewed consume it.\n- [ ] Every extracted unit has dedicated test coverage (calcPercent's guard behavior pinned by unit tests).\n- [ ] No public prop signature changed on StatusBar or ActivityRail.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.\n\n## Anti-patterns (FORBIDDEN)\n- NO behavior changes in the same commit as an extraction (reason: keeps the commit reviewable as a pure move).\n- NO temporary parallel names like StatusBarV2 (reason: extraction is atomic).\n- NO expanding test coverage beyond what exercises the new unit (reason: scope creep).\n- NO public prop changes on the host component (reason: downstream callers should not need simultaneous edits).\n- NO drive-by cleanup of unrelated code (reason: pollutes the diff).\n- NO bundling multiple extractions into one commit (reason: each extraction must be independently revertable).\n- NO refactoring DetailPanel, FileInfoContent, or CommentsContent (reason: audit found no warranted extractions there; leave them alone).\n\n## Durable decisions\n- Subcomponents: co-located with parent directory (ThemePicker.tsx inside ActivityRail/, LeftSegments.tsx and RightSegments.tsx inside StatusBar/).\n- Helpers: co-located with consumer file unless reused across modules.\n- Naming: useX for hooks (camelCase), PascalCase.tsx for subcomponents, camelCase.ts for helpers.\n- Test co-location: .test.ts/.test.tsx next to source.\n\n## Approach\nFour independent extractions, each landing as its own atomic commit. Existing component tests pin behavior; new tests exercise the extracted units. Work order starts with LeftSegments/RightSegments (largest, most characteristic win — converts plain render functions to real components that participate in React reconciliation), then the easier ThemePicker and calcPercent wins.\n\ncalcPercent is tiny but its duplicated guard (`total > 0 ? (reviewed / total) * 100 : 0`) is the only logic in StatusBar and warrants its own named unit.\n\n## Architecture\n- packages/web/src/components/StatusBar/LeftSegments.tsx (new) — left-side segment render from StatusBar.tsx:73-...\n- packages/web/src/components/StatusBar/RightSegments.tsx (new) — right-side segment render from StatusBar.tsx:...-144.\n- packages/web/src/components/StatusBar/calcPercent.ts (new) — percent-with-zero-guard helper.\n- packages/web/src/components/ActivityRail/ThemePicker.tsx (new) — popover theme-picker block from ActivityRail.tsx:42-53.\n\n## Design rationale\n\n### Problem\nStatusBar contains two plain render functions (LeftSegments/RightSegments) that bypass React reconciliation — they cannot be memoized or keyed cleanly. ActivityRail embeds a theme-picker popover inline, making the rail component carry preference-specific data. Audit also flagged a duplicated percent-with-zero-guard in StatusBar worth naming.\n\n### Research findings\n\n**Codebase audit (3-agent sweep, April 2026):**\n- packages/web/src/components/StatusBar/StatusBar.tsx:73-144 — renderLeftSegments / renderRightSegments plain functions (~40+ combined lines) that should be components.\n- packages/web/src/components/StatusBar/StatusBar.tsx:47-65 — `total > 0 ? (reviewed / total) * 100 : 0` duplicated across renderProgressBar and renderReviewed.\n- packages/web/src/components/ActivityRail/ActivityRail.tsx:42-53 — 12-line inline theme-picker block inside Popover.Content.\n- packages/web/src/components/DetailPanel/* — no findings; leave as-is.\n\n**Existing patterns:**\n- packages/web/src/components/StackedDiffViewer/FileHeader.tsx — reference for subcomponent co-located with parent.\n\n### Approaches considered\n\n#### 1. Four independent atomic commits (selected)\n\n**What:** Each extraction lands as its own commit; start with LeftSegments/RightSegments.\n**Pros:** Independently revertable, each commit a pure move.\n**Cons:** Four commits for a small epic.\n**Chosen because:** Matches the atomic-commit convention used in Epic A and Epic B.\n\n#### 2. Leave plain render functions alone (rejected)\n\n**What:** Keep StatusBar.tsx:73-144 as-is; only extract ThemePicker and calcPercent.\n**Why explored:** Plain render functions are valid React idioms.\n**Why rejected:** Audit specifically flagged them as bypassing reconciliation at 40+ combined lines. Converting to components enables memoization and keys, and the pattern will grow with future segments.\n**DO NOT REVISIT UNLESS:** Performance profiling shows the component-based version is slower.\n\n### Scope boundaries\n\n**In scope:** Four extractions listed in Success criteria.\n**Out of scope:** CommitList/FileTree refactors (Epic A), DiffViewer refactors (Epic B), DetailPanel/FileInfoContent/CommentsContent (no findings), theme data model changes, StatusBar segment set changes.\n\n### Open questions\n- Should LeftSegments/RightSegments accept `props` and `mode` as-is, or narrow to the specific fields they read? Narrow in the first task — pass only what's consumed.\n- Does calcPercent warrant a dedicated test file or can it ride alongside StatusBar.test.tsx? Decide in the first extracting task; default to dedicated (match convention).\n\n## Design discovery\n\n### Key decisions made\n\n| Question | Answer | Implication |\n|----------|--------|-------------|\n| First task? | LeftSegments/RightSegments conversion | Largest, most characteristic win — once components, the remaining extractions are trivial |\n| Touch DetailPanel? | No | Audit found no warranted extractions; enforced via anti-pattern |\n| calcPercent scope? | Named helper + tests | Tiny but its guard is the only logic in StatusBar |\n\n### Dead-end paths\n\n#### Leaving plain render functions as-is\n**Why explored:** They are valid React idioms.\n**What found:** Bypass reconciliation at 40+ combined lines; audit flagged them for correctness, not cosmetics.\n**Why abandoned:** Converting to components enables memoization and keys.\n\n### Open concerns raised\n- Prop narrowing for LeftSegments/RightSegments -> resolve in first task; default to passing only consumed fields.\n- calcPercent test location -> dedicated .test.ts file to match project convention.","status":"open","priority":3,"issue_type":"epic","created_at":"2026-04-22T11:59:56.147995574Z","created_by":"sqve","updated_at":"2026-04-22T11:59:56.147995574Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["refactor","write-plan"]}
{"id":"greppa-kye.1","title":"Task 1: Convert StatusBar render functions to LeftSegments and RightSegments components","description":"## Goal\nConvert the renderLeftSegments and renderRightSegments plain functions at StatusBar.tsx:73-144 into proper React subcomponents (LeftSegments, RightSegments) that participate in reconciliation. StatusBar renders them as JSX; existing tests continue to pass unchanged.\n\n## Execution mode\nAFK — behavior-preserving extraction. The render output must match the current plain-function implementation byte-for-byte.\n\n## Behaviors\n1. LeftSegments subcomponent accepts only the fields renderLeftSegments currently reads from props (narrow, not the whole props object); returns identical JSX.\n2. RightSegments subcomponent accepts only the fields renderRightSegments currently reads (incl. mode); returns identical JSX.\n3. StatusBar.tsx renders <LeftSegments ... /> and <RightSegments ... /> with stable keys where appropriate; inline render-function calls are removed.\n4. Existing packages/web/src/components/StatusBar/StatusBar.test.tsx continues to pass without modification.\n\n## References\n- packages/web/src/components/StackedDiffViewer/FileHeader.tsx — subcomponent co-located with parent pattern.\n- packages/web/src/components/StatusBar/StatusBar.tsx:73-144 — source of the conversion.\n- packages/web/src/components/StatusBar/StatusBar.test.tsx — existing behavior pins.\n\n## Success criteria\n- [ ] packages/web/src/components/StatusBar/LeftSegments.tsx exists.\n- [ ] packages/web/src/components/StatusBar/RightSegments.tsx exists.\n- [ ] Both files have co-located .test.tsx files that render the segments with representative inputs and assert the expected segment set appears.\n- [ ] StatusBar.tsx consumes them via JSX; no renderLeftSegments/renderRightSegments plain functions remain.\n- [ ] Both subcomponents receive only the specific fields they read (narrowed props).\n- [ ] All existing tests pass (StatusBar.test.tsx unchanged).\n- [ ] Pre-commit hooks passing.\n- [ ] Commit is atomic — only the two subcomponent extractions and their StatusBar call-site swap (plus new tests).","status":"open","priority":3,"issue_type":"task","created_at":"2026-04-22T12:00:58.689539276Z","created_by":"sqve","updated_at":"2026-04-22T12:00:58.689539276Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["afk","refactor","write-plan"],"dependencies":[{"issue_id":"greppa-kye.1","depends_on_id":"greppa-kye","type":"parent-child","created_at":"2026-04-22T12:00:58.689539276Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-lif","title":"Multi-diff review workflow","description":"## Overview\n\nTransform the diff viewer from single-file to multi-file review. Three capabilities:\n\n1. **Multi-file diff scrolling** — stack multiple diffs vertically, scroll through continuously\n2. **Multi-select** — click = single select, shift+click = add to selection, click directory = select all children\n3. **Commits section** — third sidebar section showing commits between oldRef..newRef, click to scope review\n\n## Requirements\n\n### Multi-file diff stacking\n- When multiple files are selected, render their diffs vertically in a single scrollable pane\n- Sticky file header per diff showing: file path, change badge (M/A/D/R), review button\n- Diagonal stripe separator between file diffs\n- File navigation bar: \"File X of Y\" with prev/next arrows\n- File minimap: thin vertical strip between sidebar and diff pane, segments sized proportionally to diff length, blue highlight for current scroll position\n- Bidirectional sync: scrolling diff highlights current file in tree; clicking file in tree scrolls to its diff\n\n### Multi-select interaction model\n- Click = new selection (implicitly clears previous)\n- Shift+click = add to selection\n- Click directory = select all files under it\n- Click section header = select all files in that section\n- Selection resets when switching between sections (no cross-section multi-select)\n- Single file click (no shift) returns to single-file view\n\n### Commits section\n- Third collapsible accordion section alongside Changes and Working tree\n- Lists commits between oldRef and newRef (commits in the review)\n- Each commit shows: short hash, commit message, relative time\n- Flat list (not tree) — commits are linear\n- Click commit = show that commit's changed files as stacked diffs (parent..commit)\n- Shift+click commits = select range, show combined changes across selected commits\n\n### Visual design\n- File/directory names in tree use neutral text color — only change badges carry color\n- Spring animations for accordion sections (existing pattern)\n- Mockups at .mockups/multi-diff-and-commits.html and .mockups/multi-select.html\n\n## Approach\n\nBuild front-to-back in three phases:\n\n**Phase 1: Multi-file diff stacking.** Create a StackedDiffViewer wrapper that renders multiple DiffViewer instances vertically with sticky headers and separators. Add IntersectionObserver for scroll-to-tree sync. Keep existing single-file view as the default — stacked view activates only when multiple files are selected.\n\n**Phase 2: Multi-select.** Extend Tree.Root to support selectionMode=\"multiple\" with shift-click. Update useFileSelection to manage a Set of selected paths instead of a single path. Directory click collects all descendant file paths. Section header click selects all files in section. URL encoding for multi-select uses comma-separated paths or falls back to state-only (no URL for multi).\n\n**Phase 3: Commits section.** Add listCommits to GitService (git log --format). New API endpoints: /api/commits for commit list, /api/commit/:sha/files for per-commit file list, /api/commit/:sha/diff/* for per-commit diff. Add CommitList component to FileTreePanel accordion. Commit selection reuses the same multi-select interaction model.\n\nEach phase delivers independently usable value. Phase 1 works with directory clicks even before shift-click exists. Phase 3 builds on the multi-select from Phase 2.\n\n## Anti-patterns\n- Do NOT add checkboxes to tree items or commit items\n- Do NOT add a \"clear selection\" button — clicking without shift is the clear\n- Do NOT allow cross-section multi-select (e.g., files from Changes + commits)\n- Do NOT merge all diffs into one virtual list — keep per-file DiffViewer instances for isolation\n- Do NOT break single-file view — it must remain the default and feel identical to today\n\n## Technical constraints\n- DiffViewer takes single DiffFile — needs wrapper for stacking, not rewrite\n- useFileSelection is single-select via URL — needs multi-select state\n- Tree.Root uses selectionMode=\"single\" — needs \"multiple\" mode with shift-click\n- GitService has no listCommits method — must add git log wrapper\n- Diff endpoint oldRef is overridden server-side to mergeBaseRef — per-commit diffs need new endpoint or explicit ref passthrough\n- API returns one diff per request — multi-file view fetches in parallel, not batch\n- DiffResponse contains raw strings (oldContent/newContent) — hunks computed client-side via worker\n\n## Success criteria\n- [ ] Can click a directory and scroll through all its files' diffs continuously\n- [ ] Can shift+click to build up a custom multi-file selection\n- [ ] Can click section header to view all files in that section\n- [ ] File minimap shows proportional segments with current position\n- [ ] Scrolling diff pane syncs highlighted file in tree\n- [ ] Commits section lists commits between oldRef..newRef\n- [ ] Can click a commit to view its changes as stacked diffs\n- [ ] Can shift+click commits to select a range\n- [ ] Single file click still works as today (no regression)\n- [ ] Review state (reviewed/unreviewed) works per-file in multi-diff view","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-04-03T22:01:26.191949598Z","created_by":"sqve","updated_at":"2026-04-04T19:25:15.275989250Z","closed_at":"2026-04-04T19:25:15.275571654Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"]}
{"id":"greppa-lif.1","title":"Create StackedDiffViewer component","description":"## Goal\n\nCreate a StackedDiffViewer component that renders multiple DiffViewer instances vertically with sticky file headers and separators. Foundation for multi-file review.\n\n## Behaviors\n\n- When given multiple DiffFiles, renders them vertically in a single scrollable pane\n- Each file diff has a sticky header: file path, change badge (M/A/D/R), review button\n- Diagonal stripe separator between file diffs\n- File navigation bar at top: \"File X of Y\" with prev/next arrows\n- IntersectionObserver on file headers tracks which file is currently in view\n- Clicking prev/next scrolls to the adjacent file's header\n- When given a single DiffFile, renders identically to current single-file DiffViewer (no visual regression)\n- Component wraps existing DiffViewer — does not modify its internals\n\n## Success criteria\n\n- [x] StackedDiffViewer renders multiple diffs vertically with sticky headers\n- [x] Diagonal stripe separator visible between file diffs\n- [x] Navigation bar shows \"File X of Y\" and prev/next scrolls correctly\n- [x] Single-file rendering is visually identical to current behavior\n- [x] Tests cover: multi-file rendering, navigation, single-file fallback","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T22:01:51.724866107Z","created_by":"sqve","updated_at":"2026-04-03T22:09:48.847565042Z","closed_at":"2026-04-03T22:09:48.847414347Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-lif.1","depends_on_id":"greppa-lif","type":"parent-child","created_at":"2026-04-03T22:01:51.724866107Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-lif.2","title":"Multi-select file tree and wire to StackedDiffViewer","description":"## Goal\n\nEnable multi-select in the file tree so multiple files can be selected and their diffs shown in StackedDiffViewer.\n\n## Behaviors\n\n- Extend `useFileSelection` to manage a `Set<string>` of selected paths instead of a single path\n- Click (no shift) = single select, clears previous selection, navigates URL to that file\n- Shift+click = toggle file in/out of selection set, does not navigate URL\n- Click directory = select all descendant file paths under it\n- Click section header (Changes / Working tree) = select all files in that section\n- Selection resets when switching between sections (no cross-section multi-select)\n- Single file selected = single DiffViewer (current behavior); multiple = StackedDiffViewer\n- Update `Tree.Root` usage in `FileTree` to support `selectionMode=\"multiple\"`\n- Wire `App.tsx` to pass all selected diffs to `StackedDiffViewer`\n\n## References\n\n- `packages/web/src/useFileSelection.ts` — current single-select hook\n- `packages/web/src/components/FileTree/FileTree.tsx` — tree with `selectionMode=\"single\"`\n- `packages/web/src/components/FileTree/FileTreePanel.tsx` — section callbacks\n- `packages/web/src/App.tsx:165` — where selected diff is passed to viewer\n- `packages/ui/src/components/Tree/Tree.tsx` — UI tree wrapper\n- Epic anti-patterns: no checkboxes, no \"clear selection\" button, no cross-section multi-select\n\n## Success criteria\n\n- [ ] Click selects single file, shift+click adds/removes from selection\n- [ ] Click directory selects all children\n- [ ] Click section header selects all files in section\n- [ ] Multiple selected files render in StackedDiffViewer\n- [ ] Single file click still works as today (no regression)\n- [ ] Switching sections clears previous selection\n- [ ] Tests cover: single select, shift+click toggle, directory select, section select, cross-section reset","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T22:33:36.579295514Z","created_by":"sqve","updated_at":"2026-04-03T22:44:02.979374578Z","closed_at":"2026-04-03T22:44:02.979222393Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-lif.2","depends_on_id":"greppa-lif","type":"parent-child","created_at":"2026-04-03T22:33:36.579295514Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-lif.3","title":"Double-click directory to select, single-click to expand/collapse","description":"## Goal\n\nChange directory click semantics so single-click expands/collapses (standard tree behavior) and double-click selects all children for stacked diff view.\n\n## Context\n\nCurrent behavior: clicking a directory row fires `onPointerDown` → `selectAll(children)`, hijacking the click that would normally expand/collapse. The only collapse target is the 16px chevron — frustrating to hit. This change restores IDE-standard tree navigation.\n\n## Behaviors\n\n- Single-click on directory row → expand/collapse (let React Aria handle natively)\n- Double-click on directory row → select all descendant files for multi-diff (`selectAll`)\n- Shift+click on directory → toggle all children in selection (unchanged)\n- Single-click on file → select file, open single diff (unchanged)\n- Shift+click on file → toggle in multi-select (unchanged)\n- Section header click → select all files in section (unchanged)\n\n## Implementation\n\n### 1. Split directory interaction in App.tsx selection handlers\n\nIn `handleSelectCommittedFile` and `handleSelectWorktreeFile` (lines 127-173):\n- Remove the directory branch from the `onPointerDown`/click handler — directories should no longer trigger `selectAll` on single click\n- Create new `handleDoubleClickCommittedFile` and `handleDoubleClickWorktreeFile` handlers that contain the directory `selectAll` logic\n\n### 2. Add onDoubleClick prop to FileTree\n\nIn `packages/web/src/components/FileTree/FileTree.tsx`:\n- Add `onDoubleClickFile?: (path: string) => void` prop to `FileTreeProps`\n- Attach `onDoubleClick` to `Tree.Item` for directory nodes only\n- Keep `onPointerDown` for file selection (single click on files unchanged)\n\n### 3. Wire double-click in FileTreePanel\n\nIn `packages/web/src/components/FileTree/FileTreePanel.tsx`:\n- Pass the new double-click handlers through to `FileTree`\n\n### 4. Let React Aria handle directory expand/collapse natively\n\nWith `onPointerDown` no longer calling `selectAll` for directories, React Aria's built-in expand/collapse on `TreeItem` click takes over. The `expandedKeys` and `onExpandedChange` props already handle this — no changes needed to the expand/collapse plumbing.\n\n## Anti-patterns\n\n- Do NOT add a click delay to distinguish single from double click — React Aria handles expand/collapse on first click, double-click is a separate event\n- Do NOT change shift+click behavior — it stays as toggle\n- Do NOT modify the `useMultiSelect` hook — selection logic is unchanged, only the trigger gesture changes\n- Do NOT touch section header behavior — it stays as single-click select-all\n\n## Success criteria\n\n- [ ] Single-click on directory expands/collapses it\n- [ ] Double-click on directory selects all children → stacked diff\n- [ ] Shift+click on directory toggles all children (unchanged)\n- [ ] Single-click on file opens single diff (unchanged)\n- [ ] Chevron still works for expand/collapse\n- [ ] No regression in multi-select file behavior","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T11:42:35.357472310Z","created_by":"sqve","updated_at":"2026-04-04T11:52:15.857999478Z","closed_at":"2026-04-04T11:52:15.857844155Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-lif.3","depends_on_id":"greppa-lif","type":"parent-child","created_at":"2026-04-04T11:42:35.357472310Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-lif.4","title":"File minimap, scroll-to-tree sync, and review button wiring","description":"## Goal\n\nComplete Phase 1 of the multi-diff review workflow by adding three connected features to StackedDiffViewer: a file minimap between sidebar and diff pane, bidirectional scroll-to-tree synchronization, and wiring the existing \"Mark reviewed\" button to persisted review state.\n\n## Behaviors\n\n### File minimap\n- Thin vertical strip (4px wide) positioned between the sidebar and the diff pane\n- One segment per file, sized proportionally to diff length (hunk count as proxy)\n- Blue highlight on the segment corresponding to the currently visible file\n- Clicking a minimap segment scrolls to that file's diff\n\n### Scroll-to-tree sync\n- IntersectionObserver on each file header in StackedDiffViewer tracks which file is in view\n- When the visible file changes, StackedDiffViewer calls an onActiveFileChange(path) callback\n- App.tsx passes this callback and uses it to highlight the active file in the tree (distinct from selection)\n- Clicking a file in the tree while in multi-select scrolls to that file's diff in the stacked viewer\n\n### Review button wiring\n- Mark reviewed button in each file header toggles the file path in reviewedPaths (from useReviewState)\n- Reviewed files show checkmark + Reviewed text with green accent styling\n- StackedDiffViewer receives reviewedPaths: Set<string> and onToggleReviewed: (path: string) => void as props\n- App.tsx wires these to useReviewState\n\n## References\n\n- packages/web/src/components/StackedDiffViewer/StackedDiffViewer.tsx - current component, button has no handler\n- packages/web/src/components/StackedDiffViewer/StackedDiffViewer.module.css - styles\n- packages/web/src/hooks/useReviewState.ts - persisted review state with reviewedPaths\n- packages/web/src/App.tsx - wiring point\n- .mockups/multi-diff-and-commits.html - mockup C shows minimap design\n\n## Success criteria\n\n- [ ] File minimap renders proportional segments with blue highlight for current file\n- [ ] Scrolling diff pane updates minimap highlight and syncs active file to tree\n- [ ] Clicking minimap segment scrolls to that file\n- [ ] Clicking tree file in multi-select scrolls to that file's diff\n- [ ] Mark reviewed toggles persisted state with visual feedback\n- [ ] Tests cover: minimap rendering, scroll sync callback, review toggle","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T18:35:04.636564746Z","created_by":"sqve","updated_at":"2026-04-04T18:49:54.681647586Z","closed_at":"2026-04-04T18:49:54.681495780Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-lif.4","depends_on_id":"greppa-lif","type":"parent-child","created_at":"2026-04-04T18:35:04.636564746Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-lif.5","title":"Add listCommits to GitService and commits API endpoint","description":"## Goal\n\nAdd backend support for listing commits between two refs. Foundation for the Commits section UI.\n\n## Behaviors\n\n- `GitService.listCommits(oldRef, newRef)` returns commits between oldRef..newRef using `git log --format`\n- Each commit includes: abbreviated SHA, full SHA, subject line, author name, ISO date\n- `CommitsApi` group with endpoint: `GET /api/commits?oldRef=<ref>&newRef=<ref>` returns `CommitEntry[]`\n- Per-commit diff reuses existing DiffApi by passing `parentSha..commitSha` as refs (no new endpoint needed)\n- Per-commit file list reuses existing FilesApi by passing `parentSha..commitSha` as refs\n- `CommitEntry` schema defined in `@greppa/core`\n- Commits ordered newest-first (git log default)\n\n## References\n\n- `packages/server/src/GitService.ts` — add listCommits method\n- `packages/server/src/Api.ts` — add CommitsApi group\n- `packages/server/src/Http.ts` — add CommitsHandlers\n- `packages/core/src/index.ts` — add CommitEntry schema\n- `packages/server/src/FilesApi.ts` — pattern for query-param endpoint\n\n## Success criteria\n\n- [ ] CommitEntry schema in @greppa/core with sha, abbrevSha, subject, author, date\n- [ ] GitService.listCommits returns Effect<CommitEntry[], GitError>\n- [ ] GET /api/commits?oldRef=X&newRef=Y returns commit list\n- [ ] Tests cover: listCommits parsing, API endpoint response","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T18:54:37.793460567Z","created_by":"sqve","updated_at":"2026-04-04T19:00:10.285198493Z","closed_at":"2026-04-04T19:00:10.285030215Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-lif.5","depends_on_id":"greppa-lif","type":"parent-child","created_at":"2026-04-04T18:54:37.793460567Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-lif.6","title":"Commits section UI with click and range selection","description":"## Goal\n\nAdd a Commits accordion section to FileTreePanel that lists commits between oldRef..newRef, supports click-to-view and shift+click range selection.\n\n## Behaviors\n\n- Third collapsible accordion section in FileTreePanel alongside Changes and Working tree\n- Lists commits between oldRef and newRef (from GET /api/commits)\n- Each commit shows: abbreviated hash (blue mono), subject (truncated), relative time (faint)\n- Flat list, not a tree — commits are linear\n- Click commit = select it, fetch its changed files (via existing FilesApi with parent..commit refs), show as stacked diffs\n- Shift+click = select range between last-clicked and current, show combined changes across selected commits\n- Section header shows commit count badge\n- Spring animation matching existing sections\n- Selection resets when switching between sections (no cross-section multi-select)\n- Single-open accordion: expanding Commits collapses Changes/Working tree and vice versa\n\n## References\n\n- packages/web/src/components/FileTree/FileTreePanel.tsx — accordion pattern to extend\n- packages/web/src/components/FileTree/FileTreePanel.module.css — section styles to reuse\n- packages/web/src/hooks/useFileList.ts — React Query pattern for useCommitList\n- packages/web/src/hooks/useMultiSelect.ts — selection state pattern\n- packages/web/src/App.tsx — wiring point\n- .mockups/multi-diff-and-commits.html — mockup D visual design\n- @greppa/core CommitEntry — schema for commit data\n\n## Success criteria\n\n- [ ] Commits section lists commits between oldRef..newRef with hash, subject, time\n- [ ] Click commit shows that commit's changed files as stacked diffs\n- [ ] Shift+click selects commit range, shows combined changes\n- [ ] Section uses spring accordion animation matching existing sections\n- [ ] Switching sections clears commit selection\n- [ ] Tests cover: commit list rendering, click selection, shift+click range","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-04T19:06:33.452279185Z","created_by":"sqve","updated_at":"2026-04-04T19:14:29.598733644Z","closed_at":"2026-04-04T19:14:29.598530184Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["cape"],"dependencies":[{"issue_id":"greppa-lif.6","depends_on_id":"greppa-lif","type":"parent-child","created_at":"2026-04-04T19:06:33.452279185Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-mkq","title":"Review any diff — CLI ref selection with merge-base semantics","description":"## Requirements (IMMUTABLE)\n\n- [ ] CLI accepts two optional positional args: `greppa serve [old-ref] [new-ref]`\n- [ ] No args: auto-detect default branch via `git symbolic-ref refs/remotes/origin/HEAD` with fallback to `main` then `master`; new ref defaults to `HEAD`\n- [ ] One arg: use as old ref; new ref defaults to `HEAD`\n- [ ] Two args: use as old ref and new ref\n- [ ] Always resolve diff base via `git merge-base oldRef newRef` — no two-dot semantics\n- [ ] Both refs validated at startup with `git rev-parse --verify`; server refuses to start if either ref is invalid\n- [ ] `RefsConfig` as `ServiceMap.Reference` threads resolved refs from CLI through Effect Layer to HTTP handlers (matches existing `RepoPath` pattern)\n- [ ] `GET /api/refs` endpoint returns `{ oldRef, newRef }` — the original user-provided names (not SHAs)\n- [ ] `useRefs` TanStack Query hook fetches once at startup with `staleTime: Infinity`\n- [ ] `App.tsx` replaces hardcoded `'HEAD~1'`/`'HEAD'` with values from `useRefs`; shows loading state while refs fetch\n- [ ] Ref resolution is a single clear step in the pipeline so a future `--pr 123` flag can slot in without structural changes\n\n## Success criteria\n\n- [ ] `greppa serve` with no args starts server, `GET /api/refs` returns default branch and HEAD\n- [ ] `greppa serve main` starts server, `GET /api/refs` returns `{ oldRef: \"main\", newRef: \"HEAD\" }`\n- [ ] `greppa serve main feature-branch` returns `{ oldRef: \"main\", newRef: \"feature-branch\" }`\n- [ ] `greppa serve nonexistent` fails fast with clear error before server starts\n- [ ] Browser loads refs from API and renders file list and diffs using those refs\n- [ ] No hardcoded ref strings remain in client code\n- [ ] All tests passing, pre-commit hooks passing\n\n## Anti-patterns (FORBIDDEN)\n\n- NO `..` or `...` delimiter parsing in ref-spec (reason: two positional args enable shell completion and are cleaner)\n- NO two-dot diff semantics (reason: merge-base is always correct for review; two-dot adds noise when branches diverge)\n- NO storing resolved SHAs in the API response (reason: downstream git commands accept branch names; display stays readable)\n- NO lazy ref validation at API call time (reason: fail fast — no point starting a server with invalid refs)\n- NO scattered `enabled` guards in hooks for ref loading (reason: single loading gate in App.tsx is cleaner)\n- NO in-browser ref switching in this slice (reason: that's slice 3; restart server to change refs)\n\n## Durable decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Ref format | Two positional args | Shell completion, cleaner than delimiter parsing |\n| Diff semantics | Always merge-base | Two-dot adds noise for review; three-dot matches GitHub PR semantics |\n| Ref threading | `RefsConfig` as `ServiceMap.Reference` | Matches existing `RepoPath` pattern; clean Layer composition |\n| Default branch detection | `git symbolic-ref refs/remotes/origin/HEAD` → `main` → `master` | Standard git convention with sensible fallbacks |\n| Ref validation | Fail fast at startup | No point serving invalid refs |\n| API response | Original ref names, not SHAs | Readable display; git commands accept names |\n| Client loading | Loading gate in App.tsx | Single loading state, not scattered enabled checks |\n| Future PR support | `--pr` flag as additive input path | `RefsConfig` is the universal abstraction; no structural changes needed |\n\n## Approach\n\nThread ref-spec from CLI positional args through Effect's Layer system to an HTTP endpoint the client fetches at startup. Always resolve the actual diff base via `git merge-base` so the diff shows only what the branch introduced (matching GitHub PR \"Files changed\" semantics).\n\nThe server is authoritative for ref resolution — the client just asks \"what refs am I reviewing?\" and uses the answer everywhere. This keeps ref logic in one place and makes future input methods (PR number, URL) a backend concern.\n\n## Architecture\n\n- @greppa/server: `GitService` gains `resolveRef` (rev-parse --verify), `detectDefaultBranch` (symbolic-ref + fallbacks), and `mergeBase` methods. `RefsConfig` as new `ServiceMap.Reference`. `RefsApi` HttpApiGroup with single `GET /api/refs` endpoint.\n- @greppa/cli: `serve` command gains two optional positional args, resolves refs, provides `RefsConfig` to Layer.\n- @greppa/core: `RefsResponse` Effect Schema for `{ oldRef, newRef }`.\n- @greppa/web: `useRefs` hook, `App.tsx` wiring with loading gate.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-04-03T13:08:52.635111522Z","created_by":"sqve","updated_at":"2026-04-03T19:40:11.108322066Z","closed_at":"2026-04-03T19:40:11.108072312Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","cli","frontend"]}
{"id":"greppa-mkq.1","title":"Add resolveRef, detectDefaultBranch, and mergeBase to GitService","description":"## Goal\n\nAdd ref resolution methods to GitService: validate refs, detect default branch, and compute merge-base. These are the git primitives that the ref-spec pipeline depends on.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. `GitService.resolveRef(ref)` runs `git rev-parse --verify <ref>` and returns the ref string (not the SHA). Rejects refs starting with `-`. Fails with `ResolveRefError` if the ref does not exist.\n2. `GitService.detectDefaultBranch()` tries `git symbolic-ref refs/remotes/origin/HEAD` and strips the `refs/remotes/origin/` prefix. Falls back to checking if `main` ref exists (via resolveRef), then `master`. Fails with `DetectDefaultBranchError` if none found.\n3. `GitService.mergeBase(ref1, ref2)` runs `git merge-base <ref1> <ref2>` and returns the SHA string. Fails with `MergeBaseError` if no common ancestor exists.\n4. All three methods use `ChildProcess` from `effect/unstable/process`, matching existing `listFiles` and `getFileContent` patterns.\n5. Typed error classes follow existing `GitError` pattern in `GitService.ts`.\n\n## References\n\n- packages/server/src/GitService.ts:1-113 — existing GitService with listFiles, getFileContent, ChildProcess usage, input validation, RepoPath reference\n- packages/server/src/GitService.integration.test.ts:1-80 — existing integration test patterns with real git repo, TestLayer, guard-gated describes\n\n## Success criteria\n\n- [ ] `resolveRef` succeeds for valid branch names, tags, SHAs, and `HEAD~N` syntax\n- [ ] `resolveRef` fails with `ResolveRefError` for nonexistent refs\n- [ ] `resolveRef` rejects refs starting with `-`\n- [ ] `detectDefaultBranch` returns the default branch name from the current repo\n- [ ] `mergeBase` returns the common ancestor SHA for two refs\n- [ ] `mergeBase` fails with `MergeBaseError` when no common ancestor exists\n- [ ] Integration tests pass against the real greppa git repo\n- [ ] Pre-commit hooks passing","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T13:09:27.944964536Z","created_by":"sqve","updated_at":"2026-04-03T13:19:47.401542735Z","closed_at":"2026-04-03T13:19:47.401373739Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend"],"dependencies":[{"issue_id":"greppa-mkq.1","depends_on_id":"greppa-mkq","type":"parent-child","created_at":"2026-04-03T13:09:27.944964536Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-mkq.2","title":"Add RefsConfig, RefsResponse schema, and GET /api/refs endpoint","description":"## Goal\n\nWire ref config from a ServiceMap.Reference through an HTTP endpoint so the client can discover which refs it is reviewing. This is the plumbing layer between GitService (task .1) and the CLI (next task).\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. `RefsConfig` defined as `ServiceMap.Reference` in `packages/server/src/GitService.ts` with shape `{ oldRef: string, newRef: string, mergeBaseRef: string }`. No default value — must be provided. Follows existing `RepoPath` pattern.\n2. `RefsResponse` schema defined in `packages/core/src/index.ts` as `Schema.Struct({ oldRef: Schema.String, newRef: Schema.String })`. Only exposes user-facing ref names (not merge-base SHA).\n3. `RefsApi` defined as `HttpApiGroup.make('refs')` in `packages/server/src/RefsApi.ts` with single `HttpApiEndpoint.get('getRefs', '/api/refs')` endpoint. Success response is `RefsResponse`.\n4. `RefsHandlers` in `packages/server/src/Http.ts` reads `RefsConfig` from context and returns `{ oldRef, newRef }`.\n5. `Api` in `packages/server/src/Api.ts` gains `.add(RefsApi)`.\n6. `ApiRoutes` in `packages/server/src/Http.ts` gains `Layer.provide(RefsHandlers)`.\n7. Integration test verifies `GET /api/refs` returns correct JSON when `RefsConfig` is provided in the test layer.\n\n## References\n\n- packages/server/src/GitService.ts:22-24 — existing RepoPath pattern for ServiceMap.Reference\n- packages/server/src/HealthApi.ts:1-8 — simplest HttpApiGroup example\n- packages/server/src/Api.ts:1-7 — API composition with .add()\n- packages/server/src/Http.ts:44-46 — handler binding pattern\n- packages/server/src/Http.ts:116-126 — ApiRoutes and makeHttpLayer composition\n- packages/core/src/index.ts:1-27 — existing schema definitions\n- packages/server/src/Http.integration.test.ts:1-80 — integration test patterns\n\n## Success criteria\n\n- [ ] `RefsConfig` exists as `ServiceMap.Reference` with `{ oldRef, newRef, mergeBaseRef }` shape\n- [ ] `RefsResponse` schema in core has `{ oldRef, newRef }` shape\n- [ ] `GET /api/refs` returns `{ oldRef, newRef }` from the provided `RefsConfig`\n- [ ] Integration test provides `RefsConfig` and verifies the endpoint response\n- [ ] All tests passing, pre-commit hooks passing","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T13:24:43.678546403Z","created_by":"sqve","updated_at":"2026-04-03T13:27:08.313572063Z","closed_at":"2026-04-03T13:27:08.313416311Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","core"],"dependencies":[{"issue_id":"greppa-mkq.2","depends_on_id":"greppa-mkq","type":"parent-child","created_at":"2026-04-03T13:24:43.678546403Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-mkq.3","title":"Wire CLI positional args through ref resolution to RefsConfig","description":"## Goal\n\nAdd two optional positional args to `greppa serve` that flow through ref resolution into `RefsConfig`, which the HTTP layer already consumes via the refs endpoint.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. `serve` command accepts two optional positional args: `greppa serve [old-ref] [new-ref]`. Uses `Argument.string` with `Argument.optional` from Effect CLI.\n2. No args: calls `GitService.detectDefaultBranch()` for oldRef, defaults newRef to `\"HEAD\"`.\n3. One arg: uses it as oldRef, defaults newRef to `\"HEAD\"`.\n4. Two args: uses as oldRef and newRef.\n5. Both refs validated via `GitService.resolveRef()` — server refuses to start if either is invalid.\n6. Merge-base computed via `GitService.mergeBase(oldRef, newRef)`.\n7. `makeHttpLayer` gains a `refsConfig` parameter and provides `Layer.succeed(RefsConfig, refsConfig)` internally.\n8. The serve command handler resolves refs, builds refsConfig, and passes it to `makeHttpLayer`.\n\n## References\n\n- packages/cli/src/commands/serve.ts:1-17 — current serve command\n- packages/cli/src/index.ts:1-19 — CLI bootstrap with NodeServices.layer\n- packages/server/src/Http.ts:122-126 — makeHttpLayer\n- packages/server/src/GitService.ts:1-175 — GitService with resolveRef, detectDefaultBranch, mergeBase, RefsConfig\n- Argument API: Argument.string, Argument.optional, Argument.withDescription from effect/unstable/cli\n\n## Success criteria\n\n- [ ] `greppa serve` with no args resolves default branch + HEAD + merge-base, starts server\n- [ ] `greppa serve main` uses main as oldRef, HEAD as newRef\n- [ ] `greppa serve main feature-branch` uses both args\n- [ ] `greppa serve nonexistent` fails fast with clear error before server starts\n- [ ] `makeHttpLayer` accepts and provides `RefsConfig`\n- [ ] All tests passing, pre-commit hooks passing","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T13:29:54.187824255Z","created_by":"sqve","updated_at":"2026-04-03T13:34:53.841583697Z","closed_at":"2026-04-03T13:34:53.841428019Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","cli"],"dependencies":[{"issue_id":"greppa-mkq.3","depends_on_id":"greppa-mkq","type":"parent-child","created_at":"2026-04-03T13:29:54.187824255Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-mkq.4","title":"Add useRefs hook and wire App.tsx to fetch refs from API","description":"## Goal\n\nCreate a `useRefs` TanStack Query hook that fetches ref names from `GET /api/refs` at startup, then replace all hardcoded `'HEAD~1'` and `'HEAD'` strings in `App.tsx` with values from the hook. Show a loading state while refs are fetching.\n\n## Execution mode\n\nAFK\n\n## Behaviors\n\n1. `useRefs` hook in `packages/web/src/hooks/useRefs.ts` calls `GET /api/refs`, returns `{ oldRef, newRef, isLoading }`. Uses `staleTime: Infinity` (refs don't change during a session).\n2. `App.tsx` calls `useRefs()` at the top, passes `oldRef`/`newRef` to `useFileList` and `useDiffContent` instead of hardcoded strings.\n3. While refs are loading, App shows a simple loading state (not the full panel layout).\n4. No `enabled` guards scattered in individual hooks — the loading gate lives in App.tsx.\n5. Unit test for `useRefs` hook verifying fetch call and return shape.\n\n## References\n\n- packages/web/src/App.tsx:27,57 — hardcoded 'HEAD~1' and 'HEAD'\n- packages/web/src/hooks/useFileList.ts:144-158 — existing hook pattern with TanStack Query\n- packages/web/src/hooks/useDiffContent.ts:24-39 — existing hook pattern\n- packages/core/src/index.ts:29-33 — RefsResponse schema\n- packages/server/src/RefsApi.ts:1-8 — GET /api/refs endpoint\n\n## Success criteria\n\n- [ ] `useRefs` hook fetches from `/api/refs` with `staleTime: Infinity`\n- [ ] App.tsx uses refs from hook, no hardcoded ref strings remain in client code\n- [ ] Loading state shown while refs are fetching\n- [ ] Unit test for useRefs hook\n- [ ] All tests passing, pre-commit hooks passing","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-03T14:15:31.509398473Z","created_by":"sqve","updated_at":"2026-04-03T14:18:14.589982781Z","closed_at":"2026-04-03T14:18:14.589830046Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["frontend"],"dependencies":[{"issue_id":"greppa-mkq.4","depends_on_id":"greppa-mkq","type":"parent-child","created_at":"2026-04-03T14:15:31.509398473Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-mpv","title":"Editor-quality file tree sidebar","description":"## Context\n\nThe file tree sidebar is functional but visually basic compared to VSCode/JetBrains. It lacks file icons, indent guides, compact folders, and uses an outline-based selection instead of a full-width background fill.\n\n## Design summary\n\nBring the file tree to editor-quality through 7 visual improvements in 3 phases:\n\n### Phase 1: Tree styling (CSS-only)\n1. **Tighter row height** — 22px rows matching VSCode density\n2. **Full-width selection bar** — replace orange outline with background fill spanning the entire sidebar width. Key: never put level-based padding on TreeItem — only on inner elements (chevron/spacer)\n3. **Indent guides** — subtle vertical lines via ::before pseudo-element using --tree-item-level CSS variable and repeating-linear-gradient\n\n### Phase 2: File icons + change type colors\n4. **File/folder icons** — vscode-material-icons (910 icons, folder open/closed pairs, <img> rendering). New FileIcon component in packages/ui\n5. **Change type colors on filename text** — color the label itself per status (green=added, red=deleted, yellow=modified, muted=renamed) using existing design tokens\n\n### Phase 3: Data transforms\n6. **Compact folders** — flatten single-child directory chains (VSCode's algorithm: loop-based merge, deepest dir path as identity)\n7. **Directory status propagation** — folders inherit highest-priority changeType from descendants (added > deleted > modified > renamed)\n\n## Key decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Icon library | vscode-material-icons | Folder open/closed support, 910 icons, img rendering vs dangerouslySetInnerHTML |\n| Indentation | Chevron padding-left | React Aria pattern — row stays full-width for selection highlight |\n| Compact folder identity | Deepest dir path | Stable React Aria id for selection/expansion |\n| Status propagation | Data layer (buildFileTree) | Pure function, testable, no render-time computation |\n| Row height | 22px (VSCode standard) | Editor-density familiarity |\n\n## Constraints\n\n- TreeItemContent render props provide: hasChildItems, isExpanded, level\n- Tree UI component is generic (packages/ui), FileTree is domain-specific (packages/web)\n- Must follow TDD, CSS modules, existing design token system\n- Deleted files DO propagate status (unlike VSCode — review tool needs this)","design":"## Context\n\nThe file tree sidebar is functional but visually basic compared to VSCode/JetBrains. It lacks file icons, indent guides, compact folders, and uses an outline-based selection instead of a full-width background fill.\n\n## Design summary\n\nBring the file tree to editor-quality through 7 visual improvements in 3 phases:\n\n### Phase 1: Tree styling (CSS-only)\n1. **Tighter row height** — 22px rows matching VSCode density\n2. **Full-width selection bar** — replace orange outline with background fill spanning the entire sidebar width. Key: never put level-based padding on TreeItem — only on inner elements (chevron/spacer)\n3. **Indent guides** — subtle vertical lines via ::before pseudo-element using --tree-item-level CSS variable and repeating-linear-gradient\n\n### Phase 2: File icons + change type colors\n4. **File/folder icons** — vscode-material-icons (910 icons, folder open/closed pairs, img rendering). New FileIcon component in packages/ui\n5. **Change type colors on filename text** — color the label itself per status (green=added, red=deleted, yellow=modified, muted=renamed) using existing design tokens\n\n### Phase 3: Data transforms\n6. **Compact folders** — flatten single-child directory chains (VSCode algorithm: loop-based merge, deepest dir path as identity)\n7. **Directory status propagation** — folders inherit highest-priority changeType from descendants (added > deleted > modified > renamed)\n\n## Key decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Icon library | vscode-material-icons | Folder open/closed support, 910 icons, img rendering vs dangerouslySetInnerHTML |\n| Indentation | Chevron padding-left | React Aria pattern — row stays full-width for selection highlight |\n| Compact folder identity | Deepest dir path | Stable React Aria id for selection/expansion |\n| Status propagation | Data layer (buildFileTree) | Pure function, testable, no render-time computation |\n| Row height | 22px (VSCode standard) | Editor-density familiarity |\n\n## Constraints\n\n- TreeItemContent render props provide: hasChildItems, isExpanded, level\n- Tree UI component is generic (packages/ui), FileTree is domain-specific (packages/web)\n- Must follow TDD, CSS modules, existing design token system\n- Deleted files DO propagate status (unlike VSCode — review tool needs this)","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-04-01T12:20:11.747410930Z","created_by":"sqve","updated_at":"2026-04-01T13:03:39.107108035Z","closed_at":"2026-04-01T13:03:39.106702141Z","close_reason":"Formalized into epic greppa-cqt","source_repo":".","compaction_level":0,"original_size":0,"labels":["brainstorm","file-tree","ui"]}
{"id":"greppa-n2s","title":"Compact folders and change type propagation","description":"## Goal\n\nAdd two pure data-layer transforms to buildFileTree: compactTree() flattens single-child directory chains into one row, and propagateChangeType() bubbles the highest-priority changeType from descendants up to parent directories.\n\n## Behaviors\n\n- compactTree: when a directory has exactly one child and that child is also a directory, merge them into a single row with a joined name (e.g. \"src/events\"). Use the deepest dir's path as the node path (React Aria id). Recurse through the entire tree.\n- propagateChangeType: walk the tree bottom-up. Each directory inherits the highest-priority changeType from its descendants. Priority order: added > deleted > modified > renamed.\n- Both transforms are called inside buildFileTree after sortNodes.\n- FileNode type gets a displayName field for compact path labels.\n- FileTree renders displayName ?? name in the label.\n\n## Success criteria\n\n- [ ] compactTree merges single-child directory chains into one row\n- [ ] Compact row uses deepest dir path as id, joined names as displayName\n- [ ] propagateChangeType sets changeType on directories from descendants\n- [ ] Priority order: added > deleted > modified > renamed\n- [ ] Both transforms are pure functions called in buildFileTree\n- [ ] FileTree renders displayName when present\n- [ ] New tests cover both transforms and rendering\n- [ ] pnpm test, pnpm typecheck, pnpm lint all pass","status":"closed","priority":2,"issue_type":"task","owner":"sqve","created_at":"2026-04-01T15:39:51.607749226Z","created_by":"sqve","updated_at":"2026-04-01T15:45:47.373320460Z","closed_at":"2026-04-01T15:45:47.373002400Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["file-tree","ui"]}
{"id":"greppa-pw3","title":"Persisted UI state via createPersistedStore factory","description":"## Context\n\ngreppa has no systematic state persistence. Theme uses a one-off `useTheme` hook with `useSyncExternalStore` + localStorage. Panel sizes use react-resizable-panels' built-in persistence. Everything else (reviewed files, tree expansion, filters, sort order, view mode, scroll positions) is lost on page refresh.\n\n## Requirements\n\n- [ ] Generic `createPersistedStore(key, schema, defaults)` factory that returns a React hook\n- [ ] Uses `useSyncExternalStore` with localStorage (same pub/sub pattern as existing `useTheme`)\n- [ ] Validates reads with Effect `Schema`, falls back to defaults on decode failure\n- [ ] `usePreferences()` store — global UI preferences, single localStorage key `gr-preferences`\n- [ ] Theme migrated from standalone `useTheme` into `usePreferences`\n- [ ] `useReviewState(sessionId)` store — per-review UI state, key `gr-review:<sessionId>`\n- [ ] Per-review state: collapsed tree paths, file verdicts, active filters, scroll positions\n- [ ] FileTree expansion state persisted via `useReviewState`\n- [ ] Reviewed files persisted via `useReviewState`\n\n## Design decisions\n\n| Decision | Choice | Rationale |\n|---|---|---|\n| Storage backend | localStorage | Per-browser persistence, no server roundtrip. Vision doc reserves SQLite for review data (comments, sync state) |\n| Validation | Effect Schema | Already in the stack (via @greppa/core). Handles schema evolution (new fields get defaults). STACK.md designates Schema as shared across all layers |\n| React integration | useSyncExternalStore | Proven pattern in useTheme. No new dependencies needed |\n| Architecture | Generic factory + per-concern stores | createPersistedStore(key, schema, defaults) generalizes useTheme pattern. Each concern calls factory with its own key |\n| Global vs per-review | Two namespaces | Global prefs (gr-preferences) for theme/view-mode/sort. Per-review state (gr-review:sessionId) for tree expansion, verdicts, scroll positions |\n| Review state key | Server-assigned session ID | Decoupled from git refs (which are mutable). Session ID comes from SQLite reviews table. Client stores gr-review:sessionId |\n| Panel sizes | Leave with react-resizable-panels | Already persisted by the library. No reason to absorb |\n| New dependencies | None | useSyncExternalStore + Schema + localStorage is sufficient |\n\n## Anti-patterns\n\n- Do not add Zustand, Jotai, or any state management library.\n- Do not persist review data (comments, verdicts synced to GitHub) client-side — that belongs in server-side SQLite.\n- Do not absorb panel size persistence from react-resizable-panels.\n- Do not create a React Context provider — the factory uses module-level singletons like useTheme.\n\n## Success criteria\n\n1. createPersistedStore factory exists with full test coverage\n2. Schema validation rejects malformed localStorage data and falls back to defaults\n3. usePreferences() hook manages theme (and future global prefs) with localStorage persistence\n4. useTheme hook is removed; all consumers use usePreferences().theme\n5. useReviewState(sessionId) hook persists tree expansion and reviewed files per session\n6. Different session IDs produce isolated state (no cross-contamination)\n7. FileTree reads collapsed/expanded state from useReviewState\n8. Reviewed files in useFileSelection persist across page refreshes via useReviewState\n9. No new runtime dependencies added to packages/web\n10. All existing tests pass","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-04-01T19:50:03.587469665Z","created_by":"sqve","updated_at":"2026-04-02T10:16:56.818715464Z","closed_at":"2026-04-02T10:16:56.818563055Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["state","web"]}
{"id":"greppa-pw3.1","title":"Build createPersistedStore factory","description":"## Goal\n\nCreate a generic `createPersistedStore(key, schema, defaults)` factory function that returns a React hook backed by `useSyncExternalStore` + localStorage. This generalizes the existing `useTheme` pattern into a reusable primitive.\n\nAdd `effect` (`4.0.0-beta.40`) as a direct dependency to `packages/web`.\n\n## Behaviors\n\n1. Returns defaults when localStorage key is missing\n2. Returns defaults when localStorage contains malformed JSON\n3. Returns defaults when localStorage data fails Schema validation\n4. Returns valid stored data when localStorage contains conforming JSON\n5. `set()` writes partial updates to localStorage and notifies listeners\n6. `set()` merges into existing state (does not replace)\n7. Multiple stores with different keys are independent (no cross-contamination)\n8. `getServerSnapshot` returns defaults\n\n## API shape\n\n```typescript\nconst useStore = createPersistedStore({\n  key: \"gr-example\",\n  schema: ExampleSchema,\n  defaults: { theme: \"dark\", fontSize: 14 },\n});\n\n// In a component:\nconst { state, set } = useStore();\n// state is fully typed from Schema\n// set merges partial updates: set({ fontSize: 16 })\n```\n\n## Files to create\n\n- `packages/web/src/hooks/createPersistedStore.ts`\n- `packages/web/src/hooks/createPersistedStore.test.ts`\n\n## Files to modify\n\n- `packages/web/package.json` — add `effect: 4.0.0-beta.40` to dependencies\n\n## Success criteria\n\n1. All 8 behaviors have passing tests\n2. `pnpm --filter @greppa/web test` passes with no regressions\n3. Factory uses Effect Schema for validation, not hand-rolled guards\n4. Same `useSyncExternalStore` + listener pub/sub pattern as `useTheme`","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-01T19:50:28.360938674Z","created_by":"sqve","updated_at":"2026-04-01T20:00:42.019458071Z","closed_at":"2026-04-01T20:00:42.019301115Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["state","web"],"dependencies":[{"issue_id":"greppa-pw3.1","depends_on_id":"greppa-pw3","type":"parent-child","created_at":"2026-04-01T19:50:28.360938674Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-pw3.2","title":"Build usePreferences and migrate theme from useTheme","description":"## Goal\n\nCreate `usePreferences()` hook using `createPersistedStore` for global UI preferences (starting with theme), then migrate all `useTheme` consumers and delete the old hook.\n\n## Behaviors\n\n1. usePreferences returns `{ state, set }` with typed preferences including theme\n2. Theme defaults to OS preference (dark/light detection via matchMedia)\n3. setTheme via set({ theme }) updates localStorage key `gr-preferences`\n4. set({ theme }) also sets `data-theme` attribute on document.documentElement\n5. All existing useTheme consumers work identically after migration\n6. useTheme.ts and useTheme.test.ts are deleted\n7. usePreferences has full test coverage\n\n## Files to create\n- packages/web/src/hooks/usePreferences.ts\n- packages/web/src/hooks/usePreferences.test.ts\n\n## Files to modify\n- packages/web/src/components/Header/Header.tsx — use usePreferences instead of useTheme\n- packages/web/src/components/Header/Header.test.tsx — update mock\n- packages/web/src/components/DiffViewer/DiffViewer.tsx — use usePreferences instead of useTheme\n- packages/web/src/components/DiffViewer/DiffViewer.test.tsx — update mock\n- packages/web/src/App.test.tsx — update mock\n\n## Files to delete\n- packages/web/src/hooks/useTheme.ts\n- packages/web/src/hooks/useTheme.test.ts\n\n## Success criteria\n1. usePreferences() hook manages theme with localStorage persistence under `gr-preferences` key\n2. useTheme is fully removed — no imports remain\n3. data-theme attribute on documentElement still works\n4. All existing tests pass (with updated mocks)\n5. No behavioral changes visible to users","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-01T20:01:55.670919859Z","created_by":"sqve","updated_at":"2026-04-01T20:07:19.904234688Z","closed_at":"2026-04-01T20:07:19.904074835Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["state","web"],"dependencies":[{"issue_id":"greppa-pw3.2","depends_on_id":"greppa-pw3","type":"parent-child","created_at":"2026-04-01T20:01:55.670919859Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-pw3.3","title":"Build useReviewState hook with dynamic session key","description":"## Goal\n\nCreate `useReviewState(sessionId)` hook for per-review persisted state. Unlike `usePreferences` (singleton), this hook manages multiple independent stores keyed by `gr-review:<sessionId>`.\n\n## Design\n\nCannot reuse `createPersistedStore` directly (designed for singletons). Build a standalone hook using `useSyncExternalStore` + Effect Schema + localStorage with per-key caching. Shared subscriber list across all sessions — any mutation notifies all listeners, each re-reads its own key.\n\n## Schema\n\n```typescript\nSchema.Struct({\n  collapsedPaths: Schema.Array(Schema.String),\n  reviewedPaths: Schema.Array(Schema.String),\n})\n```\n\nDefaults: `{ collapsedPaths: [], reviewedPaths: [] }`\n\n## Behaviors\n\n1. Returns defaults when localStorage key is missing\n2. Returns defaults when localStorage data fails Schema validation\n3. Returns valid stored data when localStorage contains conforming JSON\n4. set() merges partial updates and notifies listeners\n5. Different session IDs produce isolated state (no cross-contamination)\n6. Same session ID from multiple components shares state\n\n## Files to create\n\n- packages/web/src/hooks/useReviewState.ts\n- packages/web/src/hooks/useReviewState.test.ts\n\n## Success criteria\n\n1. All 6 behaviors have passing tests\n2. pnpm --filter @greppa/web test passes\n3. No integration with FileTree or useFileSelection yet — that is a separate task","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-01T20:09:44.761295243Z","created_by":"sqve","updated_at":"2026-04-01T20:10:51.416448647Z","closed_at":"2026-04-01T20:10:51.416294963Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["state","web"],"dependencies":[{"issue_id":"greppa-pw3.3","depends_on_id":"greppa-pw3","type":"parent-child","created_at":"2026-04-01T20:09:44.761295243Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-pw3.4","title":"Integrate FileTree expansion and reviewed files with useReviewState","description":"## Goal\n\nWire `useReviewState` into the component tree so FileTree expansion and reviewed files persist across page refreshes.\n\n## Changes\n\n### FileTree expansion (controlled mode)\n- FileTree: accept `expandedKeys` and `onExpandedKeysChange` props (Tree.Root already supports controlled mode via react-aria-components)\n- App.tsx: use `useReviewState('default')` to read/write collapsedPaths, derive expandedKeys = allDirectoryIds - collapsedPaths\n- On expansion change: compute collapsedPaths = allDirectoryIds - expandedKeys, persist via set()\n\n### Reviewed files persistence\n- Extract reviewedPaths from useFileSelection local state into useReviewState\n- useFileSelection receives reviewedPaths from caller (or reads from useReviewState directly)\n- Auto-mark-as-reviewed on file select still works, but writes to useReviewState\n\n### Session ID\n- Use hardcoded `'default'` session ID until server sessions exist\n\n## Success criteria\n1. FileTree expansion state survives page refresh\n2. Reviewed files survive page refresh\n3. All existing tests pass with updated mocks/props\n4. No behavioral changes visible to users beyond persistence","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-01T20:12:15.805306717Z","created_by":"sqve","updated_at":"2026-04-02T09:07:54.081795881Z","closed_at":"2026-04-02T09:07:54.081642167Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["state","web"],"dependencies":[{"issue_id":"greppa-pw3.4","depends_on_id":"greppa-pw3","type":"parent-child","created_at":"2026-04-01T20:12:15.805306717Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-sxj","title":"File icons and change type colors","description":"## Goal\n\nAdd file type icons via vscode-material-icons and change type color classes to filenames in the FileTree.\n\n## Behaviors\n\n- FileIcon component in packages/ui renders <img> tags from self-hosted vscode-material-icons SVGs\n- FileIcon accepts name (filename/dirname), isDirectory, and isExpanded props\n- Files show type-specific icons (typescript.svg, react_ts.svg, etc.) resolved via getIconForFilePath\n- Folders show open/closed icons: getIconForDirectoryPath returns e.g. folder-src, appending -open for expanded state\n- Filename text color reflects changeType: green (added), red (deleted), yellow (modified), muted (renamed)\n- Directories show colored text but no letter badge when they have a changeType\n- FileTree integrates FileIcon, passing isExpanded from TreeItemContent render props\n- Vite config copies vscode-material-icons SVGs to /material-icons/ in public output\n\n## Success criteria\n\n- [ ] FileIcon component renders <img> with correct src for files and directories\n- [ ] Folder icons toggle between closed and open variants based on isExpanded\n- [ ] Filename text uses change type color CSS classes\n- [ ] Directories with changeType show colored text, no Badge\n- [ ] Files with changeType show both colored text and Badge\n- [ ] <img> tags load from static assets, no dangerouslySetInnerHTML\n- [ ] Existing FileTree tests pass\n- [ ] New tests cover FileIcon and color class rendering\n- [ ] pnpm test, pnpm typecheck, pnpm lint all pass","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-01T13:48:27.878258534Z","created_by":"sqve","updated_at":"2026-04-01T13:57:33.583682221Z","closed_at":"2026-04-01T13:57:33.583519247Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["file-tree","ui"],"dependencies":[{"issue_id":"greppa-sxj","depends_on_id":"greppa-cqt","type":"blocks","created_at":"2026-04-01T13:48:27.878258534Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-td3","title":"Execution mode","description":"AFK","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-31T10:55:52.343452084Z","updated_at":"2026-03-31T10:56:02.028733391Z","closed_at":"2026-03-31T10:56:02.028581675Z","close_reason":"wontdo","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"]}
{"id":"greppa-tuh","title":"Goal","description":"Serve raw old/new file content from git for a given file path and refs. Client fetches via TanStack Query hook. This enables the client-side diff computation that follows.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-31T10:55:52.343452084Z","updated_at":"2026-03-31T10:56:02.024281735Z","closed_at":"2026-03-31T10:56:02.024131069Z","close_reason":"wontdo","source_repo":".","compaction_level":0,"original_size":0,"labels":["backend","frontend"]}
{"id":"greppa-x3s","title":"Caddy reverse proxy for local dev","description":"Set up Caddy as a reverse proxy so each worktree gets predictable *.greppa.localhost domains with zero manual DNS config.","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-03-28T20:50:33.472620246Z","created_by":"sqve","updated_at":"2026-03-29T09:46:11.849006404Z","closed_at":"2026-03-29T09:46:11.840486142Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"comments":[{"id":2,"issue_id":"greppa-x3s","author":"Oskar Grunning","text":"EPIC CONTRACT\n\n## Requirements\n\n1. Each worktree gets three domains resolved via *.localhost (RFC 6761, no /etc/hosts needed):\n   - <name>.greppa.localhost -> web app (DEV_PORT)\n   - <name>.greppa.localhost/api/* -> API server (API_PORT)\n   - playground.<name>.greppa.localhost -> UI playground (PLAYGROUND_PORT)\n   - Exception: main worktree uses greppa.localhost (no prefix)\n\n2. worktreeEnv.ts writes a per-worktree Caddy snippet to ~/.config/caddy/greppa/<name>.caddy and runs caddy reload -- fully automated, no manual steps.\n\n3. A root Caddyfile in the repo uses import ~/.config/caddy/greppa/*.caddy to aggregate all active worktrees.\n\n4. Vite dev servers print the Caddy-mapped URL (e.g. https://greppa.localhost) alongside the normal localhost:port output.\n\n5. Remove the Vite /api proxy from packages/web/vite.config.ts -- Caddy handles this now.\n\n6. STACK.md updated to document the Caddy setup.\n\n## Anti-patterns\n\n- No dnsmasq, systemd-resolved, or /etc/hosts modifications\n- No manual steps per worktree -- agents must be able to create worktrees and get working URLs automatically\n- No hardcoded ports in the Caddyfile -- everything derives from .env.local / worktreeEnv.ts\n\n## Success criteria\n\n- pnpm dev in any worktree writes the Caddy snippet, reloads Caddy, and prints the mapped URLs\n- Navigating to greppa.localhost in a browser reaches the web app\n- Navigating to greppa.localhost/api/... reaches the API server\n- Navigating to playground.greppa.localhost reaches the UI playground\n- A second worktree gets its own subdomain without conflicting with the first\n- No DNS configuration required on the host machine","created_at":"2026-03-28T20:50:58Z"}]}
{"id":"greppa-x3s.1","title":"Extend worktreeEnv to generate Caddy snippet and reload","description":"Extend scripts/worktreeEnv.ts to:\n\n1. Add a buildCaddySnippet function that generates a Caddy config snippet for a single worktree. Takes worktree name, apiPort, devPort, playgroundPort. Produces the three domain blocks (web, /api handle, playground).\n\n2. Write the snippet to ~/.config/caddy/greppa/<name>.caddy (mkdir -p the directory).\n\n3. Write a root Caddyfile to ~/.config/caddy/greppa/Caddyfile that does import ./*.caddy (only if it does not exist yet).\n\n4. Run caddy reload --config ~/.config/caddy/greppa/Caddyfile after writing the snippet. Fail gracefully if caddy is not installed or not running.\n\n5. Add a CADDY_DOMAIN env var to .env.local output (e.g. CADDY_DOMAIN=greppa.localhost or CADDY_DOMAIN=feat-foo.greppa.localhost).\n\n6. Export buildCaddySnippet and add tests for it in worktreeEnv.test.ts.\n\nDomain scheme:\n- main -> greppa.localhost, playground.greppa.localhost\n- <other> -> <name>.greppa.localhost, playground.<name>.greppa.localhost","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-28T20:51:10.289250393Z","created_by":"sqve","updated_at":"2026-03-29T09:42:36.772044838Z","closed_at":"2026-03-29T09:42:36.764454683Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"greppa-x3s.1","depends_on_id":"greppa-x3s","type":"parent-child","created_at":"2026-03-28T20:51:10.289250393Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-x3s.2","title":"Remove Vite /api proxy and print Caddy-mapped URL","description":"1. Remove the proxy config from packages/web/vite.config.ts — Caddy handles /api routing now.\n\n2. Add Caddy-mapped URL to Vite server output. Read CADDY_DOMAIN from env and print it alongside the normal localhost:port URL.\n\nCovers epic requirements 4 and 5.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-29T09:43:35.840485511Z","created_by":"sqve","updated_at":"2026-03-29T09:45:16.661745685Z","closed_at":"2026-03-29T09:45:16.653652907Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["caddy","vite"],"dependencies":[{"issue_id":"greppa-x3s.2","depends_on_id":"greppa-x3s","type":"parent-child","created_at":"2026-03-29T09:43:35.840485511Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-x3s.3","title":"Document Caddy reverse proxy setup in STACK.md","description":"Add a section to docs/STACK.md documenting:\n- The Caddy reverse proxy architecture (*.greppa.localhost domains)\n- How worktreeEnv.ts auto-generates snippets\n- The root Caddyfile import pattern\n- How to install and start Caddy for local dev\n\nCovers epic requirement 6.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-03-29T09:43:43.106376378Z","created_by":"sqve","updated_at":"2026-03-29T09:45:46.035019412Z","closed_at":"2026-03-29T09:45:46.025731620Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["caddy","docs"],"dependencies":[{"issue_id":"greppa-x3s.3","depends_on_id":"greppa-x3s","type":"parent-child","created_at":"2026-03-29T09:43:43.106376378Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
{"id":"greppa-ysf","title":"Epic: CommitList and FileTree refactor into hooks and subcomponents","description":"## Requirements (IMMUTABLE)\n- All planned extractions complete: useCommitSelectionKeys, useSectionScrollLock, PanelSection, FileTreeItem, handleTreeItemPointerDown helper, commit-row pointer-down helper.\n- Each extraction preserves existing component behavior — no change to rendered UI, selection state, keyboard nav, or event contracts.\n- Extracted hooks live in packages/web/src/hooks/ with co-located .test.ts (match useCommitSelection.ts / useCommitSelection.test.ts).\n- Extracted subcomponents stay co-located with their parent (FileTreeItem.tsx inside FileTree/, PanelSection.tsx inside FileTree/).\n- Host component public props stay unchanged.\n- Each extraction lands as its own atomic commit.\n\n## Success criteria\n- [ ] useCommitSelectionKeys hook extracted; CommitList consumes it.\n- [ ] useSectionScrollLock hook extracted; FileTreePanel's three sections consume it.\n- [ ] PanelSection subcomponent extracted; FileTreePanel renders three instances.\n- [ ] FileTreeItem subcomponent extracted; FileTree renders via it recursively.\n- [ ] handleTreeItemPointerDown helper extracted.\n- [ ] Commit-row pointer-down helper extracted.\n- [ ] Every extracted unit has dedicated test coverage.\n- [ ] No public prop signature changed on CommitList, FileTree, or FileTreePanel.\n- [ ] All tests passing.\n- [ ] Pre-commit hooks passing.\n\n## Anti-patterns (FORBIDDEN)\n- NO behavior changes in the same commit as an extraction (reason: keeps the commit reviewable as a pure move; behavior tweaks belong in a separate follow-up).\n- NO temporary parallel names like CommitListV2 or FileTreeNew (reason: extraction is atomic; rename callers in the same commit).\n- NO expanding test coverage beyond what exercises the new unit (reason: scope creep; coverage gaps are separate tasks).\n- NO public prop changes on the host component (reason: downstream callers and tests should not need simultaneous edits).\n- NO drive-by cleanup of unrelated code (reason: pollutes the diff; split into its own commit/task if worth doing).\n- NO bundling multiple extractions into one commit (reason: each extraction must be independently revertable).\n\n## Durable decisions\n- Hooks directory: packages/web/src/hooks/ (matches useCommitSelection, useCommitFileSelection, useCommitList).\n- Subcomponents: co-located with parent (matches FileHeader.tsx alongside StackedDiffViewer.tsx).\n- Naming: useX for hooks (camelCase), PascalCase.tsx for subcomponents.\n- Test co-location: .test.ts/.test.tsx next to source (matches project convention).\n\n## Approach\nSix independent extractions, each landing as its own atomic commit. Existing component tests pin behavior; new unit tests exercise the extracted units in isolation. Work order favors the pathfinder slice (useCommitSelectionKeys — familiar territory, already-pinned behavior) and then ships the highest-ratio cleanups (useSectionScrollLock + PanelSection, which together remove ~60 lines of duplication in FileTreePanel).\n\nNo runtime behavior changes, no prop changes, no restructuring beyond the listed extractions. The refactor is defensive: tests prove each move is a no-op.\n\n## Architecture\n- packages/web/src/hooks/useCommitSelectionKeys.ts (new) — selectedKeys memo + expandedKeys state from CommitList.tsx:59-110.\n- packages/web/src/hooks/useSectionScrollLock.ts (new) — bodyRefs + lockScroll + handleExpandComplete from FileTreePanel.tsx:90-126.\n- packages/web/src/components/FileTree/PanelSection.tsx (new) — section button + motion.div + content block from FileTreePanel.tsx:130-256.\n- packages/web/src/components/FileTree/FileTreeItem.tsx (new) — recursive renderItem from FileTree.tsx:47-108.\n- handleTreeItemPointerDown — helper fn, co-located with FileTreeItem or as separate module under FileTree/.\n- Commit-row pointer-down helper — helper fn, co-located with CommitList.tsx.\n\n## Design rationale\n\n### Problem\nThree components carry state/logic clusters that are hard to test in isolation and harder to reason about inline. The audit surfaced six concrete sites where cohesive behavior can be named and extracted without changing what the UI does. Status quo: testable only through full render trees; duplicated scroll-lock logic copied across three sections of FileTreePanel.\n\n### Research findings\n\n**Codebase audit (3-agent sweep, April 2026):**\n- packages/web/src/components/CommitList/CommitList.tsx:59-110 — expanded-aware selection state, coherent behavior, currently untestable in isolation.\n- packages/web/src/components/CommitList/CommitList.tsx:128-143 — commit-row onPointerDown closes over expandedKeysRef and branches across onSelectCommit/onSelectAllFilesInCommit.\n- packages/web/src/components/FileTree/FileTree.tsx:58-84 — tree-item onPointerDown with three behavioral branches.\n- packages/web/src/components/FileTree/FileTree.tsx:47-108 — recursive renderItem inside component body; blocks React.memo.\n- packages/web/src/components/FileTree/FileTreePanel.tsx:90-126 — triple bodyRef + lockScroll + handleExpandComplete concern repeated three times.\n- packages/web/src/components/FileTree/FileTreePanel.tsx:130-256 — three ~30-line section blocks with identical structure.\n\n**Existing patterns:**\n- packages/web/src/hooks/useCommitSelection.ts — reference for hook + test co-location.\n- packages/web/src/components/StackedDiffViewer/FileHeader.tsx — reference for subcomponent co-located with parent.\n\n### Approaches considered\n\n#### 1. Six independent atomic commits (selected)\n\n**What:** Each extraction lands as its own commit, starting with useCommitSelectionKeys as a pathfinder.\n**Pros:** Independently revertable, each commit is a pure move, test pinning is incremental.\n**Cons:** Six commits for one logical concern.\n**Chosen because:** Anti-pattern rules require atomic extractions, and the units have no interdependencies.\n\n#### 2. One refactor mega-commit (rejected)\n\n**What:** Move everything in one commit.\n**Why explored:** Fewer commits, single reviewable unit.\n**Why rejected:** Violates the NO bundling multiple extractions anti-pattern. A mistake in one extraction would force reverting five unrelated ones.\n**DO NOT REVISIT UNLESS:** Future tooling lets us revert sub-hunks of a commit cleanly.\n\n### Scope boundaries\n\n**In scope:** Six extractions listed in Success criteria.\n**Out of scope:** DiffViewer and StackedDiffViewer refactors (Epic B), StatusBar/ActivityRail/DetailPanel refactors (Epic C), behavior changes, prop signature changes.\n\n### Open questions\n- Should handleTreeItemPointerDown live in FileTree/ as a separate file or co-located inside FileTreeItem.tsx? Defer to first task touching it.\n\n## Design discovery\n\n### Key decisions made\n\n| Question | Answer | Implication |\n|----------|--------|-------------|\n| Scope split? | Three epics by component area (not one mega-epic) | This epic owns CommitList + FileTree only |\n| Include medium/low-value items? | Yes | Pointer-down helpers included even though lower-ratio |\n| First task? | useCommitSelectionKeys | Pathfinder — code recently touched, tests already pin behavior |\n\n### Dead-end paths\n\n#### Mega-refactor across all components\n**Why explored:** Fewer commits, one review.\n**What found:** Conflicts with atomic-commit anti-pattern; risk amplifies.\n**Why abandoned:** Independent extractions have no ordering constraint — splitting wins on risk and reviewability.\n\n### Open concerns raised\n- Component prop stability across the epic -> enforced via success criterion + anti-pattern.\n- Test coverage scope creep -> enforced via anti-pattern (NO coverage expansion beyond new-unit tests).","status":"open","priority":3,"issue_type":"epic","created_at":"2026-04-22T11:58:16.392428555Z","created_by":"sqve","updated_at":"2026-04-22T11:58:16.392428555Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["refactor","write-plan"]}
{"id":"greppa-ysf.1","title":"Task 1: Extract useCommitSelectionKeys hook from CommitList","description":"## Goal\nExtract the expanded-aware selection-key derivation from CommitList.tsx:59-110 into a reusable, independently testable useCommitSelectionKeys hook in packages/web/src/hooks/. CommitList consumes the hook; existing CommitList tests continue to pass unchanged.\n\n## Execution mode\nAFK — behavior-preserving extraction. Existing CommitList.test.tsx pins the behavior; the hook is a pure refactor.\n\n## Behaviors\n1. Hook useCommitSelectionKeys({ commits, selectedShas, selectedCommitFiles }) returns { selectedKeys, expandedKeys, setExpandedKeys } with identical semantics to the current inline useMemo + useState.\n2. Hook's selectedKeys matches the current implementation for: no selection, single-commit selectedShas collapsed, single-commit selectedShas expanded (all files highlighted), file-level selection drops commit highlight, all-files-selected implies commit selection, file-selection suppresses stale selectedShas from other commits.\n3. Hook exposes expandedKeys/setExpandedKeys for the Tree.Root consumer (preserves current behavior of the inline useState).\n4. CommitList.tsx imports and consumes the hook; inline selectedKeys memo and expandedKeys useState are removed.\n5. Existing packages/web/src/components/CommitList/CommitList.test.tsx continues to pass without modification to assertions.\n\n## References\n- packages/web/src/hooks/useCommitSelection.ts — hook file structure + test co-location pattern.\n- packages/web/src/hooks/useCommitFileSelection.ts — hook with URL param + Set-based state pattern.\n- packages/web/src/components/CommitList/CommitList.tsx:59-110 — source of the extraction.\n- packages/web/src/components/CommitList/CommitList.test.tsx — existing behavior pins.\n\n## Success criteria\n- [ ] packages/web/src/hooks/useCommitSelectionKeys.ts exists and exports the hook.\n- [ ] packages/web/src/hooks/useCommitSelectionKeys.test.ts covers the six behaviors above.\n- [ ] CommitList.tsx delegates selectedKeys/expandedKeys to the hook; no inline useMemo for selectedKeys remains.\n- [ ] All existing tests pass (CommitList.test.tsx unchanged).\n- [ ] Pre-commit hooks passing.\n- [ ] Commit is atomic — only the hook extraction and its CommitList call-site swap (plus new tests).","status":"open","priority":3,"issue_type":"task","created_at":"2026-04-22T12:00:19.990935133Z","created_by":"sqve","updated_at":"2026-04-22T12:00:19.990935133Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["afk","refactor","write-plan"],"dependencies":[{"issue_id":"greppa-ysf.1","depends_on_id":"greppa-ysf","type":"parent-child","created_at":"2026-04-22T12:00:19.990935133Z","created_by":"sqve","metadata":"{}","thread_id":""}]}
